# Friction log

What slowed the work down, written the moment it did, one line each -- and
within three days either an issue that owns the fix or the guard that now
catches it. `tools/ceremony-check` reads this file: an entry older than three
days that still says `issue: none` refuses the next push of `next`. The
mini-retro's second question ("what was harder than expected?") reads it too,
so a retro starts from what the days recorded rather than from what a session
remembers (#254; Pongogo's `retrospective_triggers`, adapted).

Format, one entry per line so a tool can read it:

    - <yyyy-mm-dd> <HH:MM> UTC -- <what slowed us, in one sentence> -- guard: <what would have caught it, or none> -- issue: #N | guard | none

`issue: guard` means the guard named on the line exists in the tree and no
issue is needed; `issue: none` is the state that expires.

## Entries

- 2026-09-23 05:20 UTC -- #250's proof compared the arrow against the fifth cut, which already carried the defect, and read a correct fix as a regression for an hour -- guard: the acceptance box names the baseline from before the series' first change; `tools/frame-diff` against an accepted cut -- issue: #252
- 2026-09-23 16:50 UTC -- the first frame-diff run failed on 156 boxes my own fixture had caused (the backdrop, the systems page) and on the hub's state differing between a full run and a walks-only run -- guard: the walks fix the state they frame (`default-pre`) and reset the QA cloud before seeding -- issue: #252
- 2026-09-23 17:40 UTC -- vm-qa run 19 died after 22 minutes because `tools/vm-qa` was edited while it ran; bash reads a script by offset -- guard: `.claude/rules/engineering-practices.md` ("Never edit a shell tool while a run of it is in flight") -- issue: guard
- 2026-09-23 18:50 UTC -- four "pending decisions" were put to the maintainer that the issues' comments, the commits and the register had settled -- guard: `tools/archaeology` before anything is called pending; a register row the day a decision is made -- issue: #253
- 2026-09-23 19:00 UTC -- `StartupSystem` does not land the carousel on `imageviewer`, so the SCREENSHOTS list and the viewer have no walk -- guard: none yet -- issue: #252
- 2026-09-23 20:30 UTC -- the ceremonies (retro, futro, audit) stopped when the work became a stream of RC cuts: fifteen cuts and six blindspots since 09-14 with none of them run, because each was tied to a phase boundary that never came -- guard: `tools/ceremony-check` in the push guard and the fork CI -- issue: #254
- 2026-09-23 23:48 UTC -- the save-mode proof took three runs because the walk counted presses to reach SLOT 1 and the manager orders its tiles by slot under DO NOT INCREMENT and by date under INCREMENT PER SAVE, so the same presses landed on AUTO SAVE the second time -- guard: `.claude/rules/generic-x64-vm-testing.md` (arrange the files so the tile is last, or read the selection from a frame) -- issue: guard
- 2026-09-24 01:18 UTC -- the first stem measurement of the 14 px toast used a span that cut the text mid-word and reported no solid stroke; the same row over the full span reported 16 of 44, and the chain had already been stopped on the first number -- guard: none in the tree yet; the measurement becomes a tool under #255 that takes the text span as an argument and prints it -- issue: #255
- 2026-09-24 02:16 UTC -- the seventeenth cut's staging script carried the sixteenth's tar date in its file name, so the copy the maintainer had said yes to failed on a missing file before touching the device -- guard: the staging becomes a tool in the tree that finds the tar by pattern -- issue: #257
- 2026-09-24 03:05 UTC -- this session ran all day with the conflict-resolution worktree's rules loaded, eleven files behind next (ceremonies, change-log, es-code-traps, es-player-text, es-ui-style-guide, handheld-evidence, least-surprise, player-language, time-to-play, vm-first, working-principles); the auditor noticed, not the session -- guard: the worktree is merged up to next now; a check that a worktree's rules match next belongs in tools/ceremony-check (held while the audit's scope is frozen) -- issue: #254
- 2026-09-24 05:50 UTC -- `tools/lint-audit-artifacts` tripped twice on its own new rules while closing #258: its gate slice began at an item heading that mentioned "Phase 7" (PL-015's title), and the "last cell is the outcome" rule broke five older folders whose tables put the word in a middle cell -- caught only because every folder was re-linted before the commit -- guard: `tools/lint-audit-artifacts` matches the gate's own heading, reads the outcome from whichever cell carries the word, and treats a missing YAML index as the two pre-index audits rather than a failure; all six folders PASS -- issue: guard
- 2026-09-24: a RetroArch patch (0017) was committed on `patch --dry-run` alone and the H700 build found the two callers it missed twenty minutes later; a syntax check with the build's own compiler, as ES has, would have caught it at the desk. issue: #264
- 2026-09-25 01:20 UTC -- #266 put #161's hotspot drop and #174's Tailscale restart to the maintainer as device facts, though D-QA-036 had folded the hotspot into a network drop and #174 was proven on the VM with two device data points; the re-derivation read the issues' boxes, not the record, and `tools/archaeology --issue` was not run per issue -- guard: the release catalog and the box-vs-record check of #267, the box lint of #268 -- issue: #267
- 2026-09-26 17:50 UTC -- the #292 proof's first run pressed `ret`/`esc` for the interface's A/B (the image maps `x`/`z`) and wrote the exit-sync toggle after the interface had loaded its settings; both are written in `generic-x64-vm-testing.md` (§ Driving EmulationStation from the monitor, § A settings change made from the shell is invisible to the running interface) and neither section was read before the proof was written -- three phases of a ten-minute run graded the harness. A shared proof helper with `press_a`/`press_b` and a seed-then-reboot step, so a proof never spells a key or writes a setting after boot, is what would have caught it -- issue: #278 (the proof scripts' promotion into `tools/`).
- 2026-09-26 22:45 UTC -- #295 spent two RetroArch cuts (x64 runs 64 and 65, H700 runs 42 and 43, about twenty minutes of builds) on a placement ratio whose reference, "the unfloored size, 10.9 px on a 640x480 handheld", was read from patch 0016's prose; the running code's number on the guest was 4.4 px, and the size the queue had before the floor was RetroArch's own 9 px floor. The two RARCH_LOG lines that settled it in one frame were added in the second cut; added in the first, one build would have done. guard: patch 0019's log lines (`msg_queue font: scaled ... before the fork's floor ... drawn`, `msg_queue place: ...`) print the values every layout, so the next placement change is checked against the running code's numbers; the general rule -- instrument a derived value before the first build when its reference comes from prose -- is a memory for now.
