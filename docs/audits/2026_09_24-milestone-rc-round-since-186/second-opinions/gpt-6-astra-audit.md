## 1. New findings

These are separate from F-01–F-31 and PL-001–PL-030. A proof gap below is not a claim that a runtime failure was observed.

| ID | Severity | Packet evidence | What fails, and when |
|---|---|---|---|
| G-01 | Medium | `02-forward-audit.md:304-305`, AC-A25’s read of patch `013-validate-a-cached-image-before-publishing-it.patch` | **The stated PNG validation does not establish “malformed bodies are never published.”** Correct length, PNG magic and IEND do not establish valid image data. A body with those boundaries but corrupt IDAT contents passes the listed checks. The PASS exceeds the validation shown. |
| G-02 | Medium | `02-forward-audit.md:286-287`, AC-A19’s read of patch `012-offline-image-miss.patch`; `OfflineProxyUrl.h:29` | **The store-only image request depends on header capitalization.** The packet explicitly says urllib’s `X-Ra-Store-Only` takes the fetch path. A caller using that capitalization loses the no-network guarantee; ES’s exact spelling hides the defect. |
| G-03 | Medium | `rocknix-corekeep:76-80,123-128,167-177`; `02-forward-audit.md:434` | **The enabled core keeper does not preserve its free-space floor.** It admits a dump after checking for 512 MB free, while permitting up to 1 GiB of raw input. With free space between those amounts, sufficiently incompressible output exhausts storage. |
| G-04 | Medium | `DisplayAspect.cpp:44-50`; the `forScreenshotPath` read at `02-forward-audit.md:413` | **First-time screenshot lookup scales with the entire game library on the interface thread.** Opening S previously unseen screenshots requires S full-library searches. The cache helps subsequent views, not the first large collection; this is separate from F-19’s stale-cache defect. |
| G-05 | Low | `GuiWifi.cpp:205`; `ApiSystem.cpp:655`; `02-forward-audit.md:509`; `last-good-scripts-test` section u PASS | **The failed-join proof stops at the shell backend.** `GuiWifi` separately writes `wifi.key`, but the cited failed-activation test exercises `wifictl`. It does not establish that a failed GUI join preserves the previous key. The packet does not show the GUI write’s ordering or rollback. |
| G-06 | Low | `rocknix-corekeep:167-177`; `02-forward-audit.md:434` | **The CUT diagnosis cannot distinguish truncation from an exactly-at-cap core.** `head -c N` followed by the retained raw-byte count gives the same result for an N-byte input and a longer input. Reporting “N raw bytes kept of more” is false for the exact-boundary case. |
| G-07 | Low | `GuiSaveState.cpp:515-531` | **The “file that arrived while the page was open” comment promises more than the refresh condition does.** Disk contents are checked only when `completed()` changes. A file arriving without a bookkeeping-job completion does not trigger that check. |
| G-08 | Low | `CloudText.h:442`; `raofflineproxy-ctl:685-689,1208,1517`; `02-forward-audit.md:262-263` | **A dead run’s well-formed progress file has no stale-file cleanup.** ES ignores it after 900 seconds, but an interval-skipped top-up leaves it behind. This is the missing “removed” half of #189 box 3, not F-13’s malformed records or missing TERM traps. |
| G-09 | Low | `journalctl -b \| grep -c WARNING` → `165`; `grep -c WARNING /var/log/es_log.txt` → `165` | **Equal WARNING counts do not prove delivery of every ES warning.** The journal command also counts other services. One missing ES warning plus one unrelated warning still passes this comparison; neither message identity nor delivery time is checked. |
| G-10 | Low | `ls /usr/lib/libcairo.so.2.*` → one versioned file; `tools/vm-upgrade-rehearsal:92`; `02-forward-audit.md:117-118` | **The cited cairo check does not prove the required symlink.** A single versioned file satisfies the quoted listing/count even if `libcairo.so.2` is absent or points elsewhere. The target required by #226 box 1 needs its own read. |
| G-11 | Low | `02-forward-audit.md:565-572` | **The audit’s totals do not add up.** Its printed PASS subtotals sum to 188, not 184; its UNTESTABLE subtotals sum to 27, not 32. The printed component totals sum to 346, not 347. The scorecard needs a recount before its headline totals or pass rate are used. |

## 2. Refuting the first reader

“Survives” below means supported at the frozen build, not that a dated closure proves the fix. The supplied acceptance lines are tests to perform, not post-fix results (`05-punch-list.md:11`).

| Finding / punch item | My verdict | What would make it false, and whether the packet shows that | Acceptance criterion: sufficient or able to pass with the problem remaining |
|---|---|---|---|
| **F-01 / PL-006** — reversed design | **Survives, Medium; narrow the dead-code claim.** | False if a normal player route still leaves the transfer running, or the comments clearly describe history. Neither is shown: `GuiCloudTransfer.cpp:215-218` permits cancellation, while `GuiCloudTransfer.h:25` describes the opposite design. Retaining post-run outcomes remains valid; not all job machinery is dead (`GuiMenu.cpp:5025-5110`). | The grep can accept a still-false sentence merely because it mentions D-UI-078. A register reference can justify retaining a path, but does not make its comments true. Read the listed comments for meaning and preserve the live post-run behavior (`05-punch-list.md:77-81`). |
| **F-02 / PL-001** — clean-install copy | **Survives, but Low rather than Medium.** | A fresh symlink would refute the discrepancy; guest d shows a regular file. But the stronger stale-update story is not established: `cmp` reports equal bytes, and `post-update:29-34` already replaces the copy with a link. The normal first update repairs it (`userconfig-setup:13,51-56`). | The fresh-boot `readlink` test addresses the ordinary clean-install defect. A passing byte-preservation rehearsal does not establish save-state launch behavior. Check the old regular-copy upgrade case explicitly rather than treating “rehearsal passes” as that proof (`05-punch-list.md:26`). |
| **F-03 / PL-002** — unlink after refusal | **Survives, Medium.** | False if unlinking were restricted to paths whose retirement was successfully recorded. Instead, `cloud_capture:237-244` unlinks all handed paths when `rc != 2`, including the refusal at `:1308-1316`. The repository-path-only caller limits exposure; it does not repair the command’s contract. | The outside-path case is necessary. It does not test mixed accepted/refused paths or failure to commit the manifest. Filling `RETIRED_OK` before durable recording succeeds would pass the scratch-file test while retaining an unrecorded-deletion path (`05-punch-list.md:33-37`). |
| **F-04 / PL-003** — crash-handler hang | **Survives, Medium.** | False if the handler avoided the held mutex or guaranteed termination without waiting on it. `main.cpp:355-392` calls the logger; `Log.cpp:70,104,122,187` supplies the non-recursive mutex. No such escape is shown. | Grepping out `LOG` and `flush` does not establish that called helpers or the unwinder avoid locks and allocation. A process-directed `kill -SEGV` during a log flood does not prove interruption of the mutex-owning thread. Require a deterministic owner-thread fault; the permitted `try_lock` approach also conflicts with the acceptance’s “no call that takes a lock” wording (`05-punch-list.md:44-48`). |
| **F-05 / PL-004** — push-gate reach | **Survives, Medium.** | False if the hook used a mandatory checker independent of the pushing worktree. The worktree-root lookup and executable-file condition explicitly skip it when absent (`.githooks/pre-push:55,229-236`). | The proposed fix and test name different copies: running the hook-location checker does **not** imply refusal when only the pushing worktree’s copy is removed. Test a missing canonical checker and a violating pushed ref. Also, an existing guard file need not be an executed guard (`05-punch-list.md:55-59`). |
| **F-06 / PL-005** — syntax-check false failures | **Survives, Medium.** | A genuine error in the same shipped translation units would refute the false-positive diagnosis. The packet instead records 52/55, byte-identical headers, and built objects at the same pin; the include-root problem survives those checks (`tools/es-syntax-check:71-106`). | Passing three unchanged files and rejecting `int oops = ;` does not prove that worktree headers are used. A checker still reading stale build headers can pass both tests. Add a header-dependent constructed error, including the supported plain and `--tree` modes (`05-punch-list.md:66-70`). |
| **F-07 / PL-007** — offline-achievements draft | **Survives, but Low rather than Medium.** | False if B still backgrounded the scan or the paragraph were explicitly historical. The current instruction at `docs/ra-offline/rocknix-org-offline-achievements.md:25` contradicts `GuiOfflineScan.cpp:111-112`. The packet establishes a wrong draft, not publication of that instruction to players. | Removing “background” and inserting “CANCEL” can pass while still describing cancellation incorrectly. Check the confirmation, stopping behavior and preservation of already cached work. The acceptance also omits the requested #241 body amendment (`05-punch-list.md:88-92`). |
| **F-08 / PL-008** — checklist discipline | **Survives, Medium; narrow it to false or stale tracking.** | False if the bodies matched the current criteria and their evidence. They do not. But honestly unticked observations are not defects: #193 lacks requested frames, and #177/#198 lack guest observations (`02-forward-audit.md:275,479,506`). Those boxes must not be ticked merely because mechanisms shipped. | Rewording an unperformed behavior test into code existence can make this gate green without obtaining the observation. Preserve outstanding work in an explicit follow-up. PL-020 does not specify the closed-issue checker extension that this acceptance invokes (`05-punch-list.md:99-103,180-181`). |
| **F-09 / PL-009** — stale comments | **Survives, Low; narrow the hash sweep.** | The renamed row, quoted-password path and raw-byte cap contradict their old comments (`GuiMenu.cpp:9041,3162,6003`; `StringUtil.cpp:459-461`; `rocknix-corekeep:32-34,167-177`). An explicitly historical schema-source hash would not be stale merely because the package pin moved; the full wording at `raofflineproxy-ctl:193` is not quoted. | Blanket removal of `64d03d30` proves neither schema compatibility nor comment truth and can erase useful provenance. Update claims of the **current** pin; retain clearly labelled historical sources where accurate (`05-punch-list.md:110-112`). |
| **F-10 / PL-010** — architecture-guard explanations | **Survives, Low.** | False if all three packages were in every device’s base package set. `virtual/emulators/package.mk:11-12` and the described device arms contradict that premise; the guards themselves are not shown broken. | Adequate if the comments are actually compared with the device arms. `pkgcheck` alone cannot establish the truth of the replacement explanation (`05-punch-list.md:117-118`). |
| **F-11 / PL-011** — placeholder ID and scanner scope | **Survives, Low.** | A real ID and scanner coverage would refute it. The packet instead supplies `D-RA-0xx` at `raofflineproxy-cache-images:6` and the omitted directory at `tools/register-check:48-58`. | Planting a bad ID in `sources/` proves that directory’s check, not the separately requested `patches/` coverage. Exercise both, including malformed IDs rather than only valid-shaped nonexistent IDs (`05-punch-list.md:123-124`). |
| **F-12 / PL-012** — successful usage error | **Survives, Low.** | False if `EX_USAGE` were defined or both branches returned a nonzero usage status. The installed-file grep reports zero definitions; `raofflineproxy-ctl:1297,1300` uses the undefined variable. | The proposed shared constant fixes both branches. The acceptance alone tests only `--bogus`; a branch-local repair could pass while leaving the other argument-error branch unchanged. Test both (`05-punch-list.md:129-131`). |
| **F-13 / PL-013** — helper lifecycle and progress | **Survives; raise to Medium.** | False if `images` and `refresh` both had parseable progress and a helper-termination path. Neither has the scan/top-up PID and TERM handling (`raofflineproxy-ctl:1269-1315`). Continuing helper work after `disable` is a behavior failure, not just a display blemish. | The acceptance tests **images only**. It can pass with `refresh` still malformed and its helper still running. Both verbs need progress, disable, process-exit and deadline checks (`05-punch-list.md:136-137`). |
| **F-14 / PL-014** — freshness red | **Survives, Low; narrow to the policy annotation.** | False if the pin were current or carried the required approved exception. The command reports `BEHIND: 16 behind …`, exit 1. That proves neither a defective proxy nor absence of a commit pin; it proves the freshness policy is unsatisfied (`raofflineproxy/package.mk:5-13`). | A truthful, authorized PINNED exception is a legitimate resolution, not an upgrade. Do not report a zero exit achieved by annotation as evidence that newer proxy code was built or tested (`05-punch-list.md:142-144`). |
| **F-15 / PL-015** — index/table disagreement | **Survives, Low.** | Agreement would refute it. The packet reports 32 `open` index entries while the table passes the lint; `tools/lint-audit-artifacts:66-87` reads only the latter. | Making both sources say “resolved” closes the consistency defect but does not prove delivery. Two matching false records pass. Outcomes still need their underlying evidence, particularly the known reversed and incomplete items (`05-punch-list.md:149-151`). |
| **F-16 / PL-016** — success over no-op adoption | **Survives, Low.** | False if rc 0 guaranteed a recorded copy or the worker inspected a recorded count. `cloud_capture:549-556` returns 0 for the refusal, while `SaveStateBookkeeper.cpp:81-85` logs success from that status. | The acceptance is sound only when it observes the **worker’s** ES log. A direct script test proving a warning or zero count does not test what the caller says (`05-punch-list.md:156-157`). |
| **F-17 / PL-017** — undecorated loop tiles | **Survives, Low.** | A decorator call on the loop path would refute it. `ImageGridComponent.h:413-417` lacks the calls present at `:305-309,377-381`. | Adequate if the frame visibly exercises loop copies, not merely ordinary grid tiles. Syntax-check success does not prove the runtime decorator was applied (`05-punch-list.md:162-163`). |
| **F-18 / PL-018** — empty generated tables | **Survives, Low; this is a missing guard, not a demonstrated bad table.** | A recipe failure on successful-but-empty generation would refute the guard finding. `fbneo-lr/package.mk:35-38` only prints a count; the installed tables are nonempty. The duplicate generator is independently reported byte-identical. | The empty-output build test and single generator address the stated defects. Preserved row counts do not prove correct rotation values or complete coverage; those are separate claims (`05-punch-list.md:168-169`). |
| **F-19 / PL-019** — three static caches | **Survives, Low, for the screenshot Transform cache; the title is too broad.** | Writer-driven invalidation would refute the live staleness. `DisplayAspect.cpp:44-50` has none. But `CaptureRotation::sKnown` is updated by `recordAfterSession`, and the packet says tables change with an image; “three caches are never invalidated” overstates the evidence (`CaptureRotation.cpp:19,26-53`; `03-retrospective.md:23`). | Force a **different** turn through the real writer for the same already-cached path. If the table already supplies the eventual turn, before/after views can pass with a stale cache. A direct file write also does not test a writer callback (`05-punch-list.md:174-175`). |
| **F-20 / PL-020** — five unproven guards | **Survives, Low; split missing execution from bad expectations.** | Recorded adverse runs would refute the missing-proof claims. The packet lacks those runs; `grep -c retroarch-wrapper-test tools/vm-qa` is 0. But archaeology’s search expectation is itself wrong, not proof that its search is broken (`02-forward-audit.md:208,211,223,296`). | A reason in a documentation table explicitly permits leaving the wrapper test unwired. That closes a documentation decision, not regression protection. Appearance in `--list` also does not establish execution and consumption of a failing return code (`05-punch-list.md:180-181`). |
| **F-21 / PL-021** — prose counts as mapping | **Survives, Low.** | Structured title matching would refute it. `tools/es-menu-map-check:153` instead searches the whole document by substring. | The prose-only TOOLS case correctly targets the reported defect. Acceptance should demonstrate that an explicit map entry or justified exception—not incidental text—is what changes the result to PASS (`05-punch-list.md:186-187`). |
| **F-22 / PL-022** — fixed translation base | **Survives only as a Low rebase risk.** | A maintained, intentional base or a derived base would defeat the stale-default concern. `tools/es-untranslated:89` contains a constant, but the packet does not show that it is already wrong. Nor does it establish that every future mismatch fails closed. | Printing the base can pass while printing the same stale constant. Exercise a changed upstream base and show that the selected base and string set follow the intended shipped history (`05-punch-list.md:192-193`). |
| **F-23 / PL-023** — missing tool-table entries | **Survives, Low.** | Four proper table rows would refute it. The reported grep finds none of the four tools in `instruction-files.md`. | A total grep count of four can come from prose or repeated mentions. Require four distinct table entries with the correct owning rules (`05-punch-list.md:198-199`). |
| **F-24 / PL-024** — change-log dates | **Survives, Low.** | Correct daily headings would refute it. Later changes sit below the 2026-09-22 heading (`docs/cloud-sync-changelog.md:2171` onward). | The daily-date acceptance is sufficient when checked against landing dates. The “or date the heading as a range” option in What does not meet that acceptance literally; `register-check` does not settle the dates (`05-punch-list.md:204-205`). |
| **F-25 / PL-025** — missing filed frames | **Survives, Low; distinguish filing from absent execution.** | Filed frames for the specified cases would refute it. The searches show the omissions, while the GB walk itself ran; that particular gap is filing, not an absent walk (`02-forward-audit.md:199,275,488,503,521`). | File existence, README rows and issue citations can all pass with the wrong language, resolution, build or screen contents. Inspect the pixels and provenance before ticking the behavior boxes (`05-punch-list.md:210-211`). |
| **F-26 / PL-026** — non-ASCII comments | **Survives only as Low rule noncompliance.** | ASCII text would refute it. The two middle dots exist at `GuiScraperRun.cpp:177` and `GuiScraperRun.h:38`. The packet also says their position excludes them from extraction and the image built; no xgettext failure is established. | The grep addresses these two line-comment sites. It is not a general check of block comments or gettext extraction. Do not describe passing it as repair of an observed translation failure (`05-punch-list.md:216-217`). |
| **F-27 / PL-027** — missing adjacent reasons | **Survives, Low; narrow the count and scope.** | Truthful adjacent reasons would refute it. The distinct cases here are cairo’s override and ruby’s series policy (`cairo/package.mk:1-13`; `tools/fork-package-freshness:73-75`). The proxy annotation is already F-14. | A truthful comment fixes the comment defect. The acceptance explicitly permits merely documenting that the tool does not read ruby’s recipe; that leaves two places to maintain the policy rather than establishing a single source (`05-punch-list.md:222-223`). |
| **F-28 / PL-028** — unbuilt row and online fallbacks | **Narrow; raise the fallback defect to Medium.** | The open #224 interface work is not itself a shipped-code defect. The fallback finding would be false if offline summary failure stayed local and missing state did not select the online path. `RetroAchievements.cpp:602-611` and `OfflineAchievements.cpp:161-163` show otherwise. | “Or the comment names the trade” permits closure without fixing either fallback. Cutting the link also misses the missing-state case **with an address still present**. Test that case and assert no web request, not merely a quick answer (`05-punch-list.md:228-229`). |
| **F-29 / PL-029** — manifest-schema debt | **Does not survive as a verified finding.** | An already-correct schema would make it false. The packet explicitly says the schema content was not checked; it relies on a register row saying a correction is owed (`03-retrospective.md:201`). The writer at `cloud_capture:1231-1272` does not establish what the document currently says. | Read the schema and compare its fields and rules with the merge code. `register-check` passing does not verify that comparison. The proposed acceptance is usable, but no supplied result establishes either the original defect or its correction (`05-punch-list.md:234-235`). |
| **F-31 / PL-030** — rehearsal assumptions | **Survives, Low, for transport assumptions; narrow the cairo criticism.** | Using the configured pair would refute the hard-coding; `tools/vm-upgrade-rehearsal:73,76` shows the fixed endpoint and wait. Keeping a one-cairo regression assertion at `:92` is not inherently wrong in a generic rehearsal merely because #226 introduced it. | Run with changed connection settings and the default endpoint unavailable, so contacting the wrong guest cannot pass unnoticed. A fast alternate-pair run does not test the fixed 90-second readiness assumption. Labelling the cairo check is documentation, not a runtime fix (`05-punch-list.md:240-241`). |

## 3. What I could not judge

| Claim or area | Where the packet stops | Evidence the orchestrator needs |
|---|---|---|
| **Every item was fixed on 2026-09-24** | The supplied items contain proposed changes and acceptance criteria, not post-fix diffs and execution results. The main mechanical results belong to the frozen build (`02-forward-audit.md:19`; `05-punch-list.md:3,11,26-241`). | Fix commits, resulting distribution/ES pins, and the actual acceptance outputs. A resolution date alone does not verify any fix. |
| **F-30** | No F-30 description is supplied. The punch list proceeds from source F-29 to source F-31 (`05-punch-list.md:232,238`). | The missing finding, or confirmation that the number was unused. It cannot be reconstructed from the numbering. |
| **H700 packaging and handheld behavior** | The packet does not reopen the H700 squashfs or relevant build logs, and it supplies no current handheld round for the many device boxes (`02-forward-audit.md:121,124,142,585-587`). | SYSTEM contents and build logs at the audited pin; device observations for alerts, performance, display behavior and the current launcher. x64 success does not supply those observations. |
| **Actual caller handling of Wi-Fi failures and password changes** | Backend failure cases pass, but the complete status-handling paths and ordering around `GuiWifi.cpp:205` are absent. `ApiSystem.cpp:625,638,655,667` adds timeouts. The requested SSH login, PSK test and debug-log grep are not supplied (`02-forward-audit.md:506,509`). | Complete caller reads plus injected backend errors/timeouts; before/after persisted settings and UI outcomes; actual authentication and secret-log checks. |
| **Real save-state compatibility and effective launcher configuration** | The upgrade proof preserves four junk fixtures, not launchable states. START NEW GAME’s quit behavior and the AUTO SAVE hotkey behavior lack the stated VM observation. The harness sources launcher functions separately (`02-forward-audit.md:350,359,362,407,440`; `SaveState.cpp:118-142`). | Launch real pre-upgrade states through each tile; inspect the final appendconfig from the complete launcher; exercise save hotkeys, AUTO SAVE, START NEW GAME and unset maximum-retention settings. |
| **Offline startup, reconnect and aged-store behavior** | Both indexing proofs took the startup path, not `NetworkThread.cpp:232`’s link-up rerun. The two-hour refresh observation and aged-store offline launch were not performed (`main.cpp:788-795`; `02-forward-audit.md:29-30,245`). | Boot disconnected, add a game, restore connectivity and observe indexing/cache completion; perform the aged-store launch and bounded-refresh observation. |
| **Image validation and helper completion on the shipped Python** | Patch 013’s proof used a host fixture. The indexed helper accesses a private executor after DONE, and the refresh helper mixes diagnostic prose into stdout; the relevant consuming behavior is not shown (`raofflineproxy-cache-indexed:69-83`; `02-forward-audit.md:254,305,317`). | The complete validator and consumer code; malformed-image tests on the image; post-DONE exception tests with resulting exit codes/stamps; confirmation that refresh consumers safely handle its output. |
| **Rotation learning and all generated mappings** | The rotation-learning proof appended `SET_ROTATION` by hand. The fixtures use the same mapping assumptions as the generators; the maintainer’s Ms. Pac-Man observation supplies only one independent case (`CaptureRotation.cpp:26-53`; `02-forward-audit.md:422,425,437`). | Real rotating-core sessions tied to their launch logs, plus independent orientation checks for the different table families and turn directions. |
| **Crash-handler safety, shutdown and log durability** | There is no deterministic fault while the logger-owning thread holds its mutex, no complete safety proof for the remaining handler calls, and no ten-reboot durability run (`main.cpp:355-392,642-646`; `Log.cpp:104-130`; `02-forward-audit.md:485,587`). | Deterministic signal tests, observed restart and SIGINT cleanup behavior, per-message latency checks, and the requested reboot/rotation evidence. |
| **“No secrets in logs or backups” as a blanket claim** | The exhaustive `LOG(` sweep was not repeated. The bare credential-token redaction is deliberately context-scoped, and the password change’s guest-level log test is absent (`001-functions:82`; `Platform.cpp:830`; `02-forward-audit.md:242,479,482,585`). | Complete current logging-path review and artifact checks using planted credentials across the actual UI/backend paths. Selected masks and clean example archives do not establish the universal negative. |
| **Manifest durability, path containment and sync races beyond the shown branches** | The packet shows the refusal/unlink defect and the UI’s running-sync checks, but not enough surrounding code to establish canonical path containment, final-write failure handling, or ordering against a sync starting after the UI check (`cloud_capture:237-244,549-556,1231-1272,1308-1316`; `GuiSaveState.cpp:409,460`). | Full path-normalization and commit paths, failed-write tests, mixed-path retirement tests, and an ordered test of bookkeeping work against automatic sync startup. |
| **Visual correctness from filed-frame counts and frame-diff PASS** | Most PNGs were checked by existence/name, not pixels. The tip’s `frame-diff` reports 78 screens and zero differences, which establishes agreement with its baseline—not independent correctness (`02-forward-audit.md:196,585`). | Inspect the specified frames at their actual resolution and locale, with provenance. For the French help row, supply the widths needed to judge the criterion’s “while the row fits” condition (`HelpComponent.cpp:127-148`; `02-forward-audit.md:533`). |
| **Other documentary and guard claims** | The packet omits #168 and PL-19’s body reads, #79’s row, the exact unit-test recipe run, the ES hook, and the harness `--old` reruns. It also does not establish a committed-history violation for D-RA-025 merely by saying it was corrected “in the same action” (`02-forward-audit.md:585`; `03-retrospective.md:52`). | Those originals and outputs; the relevant register-history diff; adverse guard runs from the intended worktree/ref and CI paths. The comment-quality method was also sampled, not an exhaustive truth check (`03-retrospective.md:128`). |