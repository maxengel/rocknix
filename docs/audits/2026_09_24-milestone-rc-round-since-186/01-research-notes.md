# Research Notes — The release-candidate round (work since #186)

**Auditor:** Code Auditor skill v1.10.0 (forward phases: a fresh Claude Fable 5.1 agent, `claude-fable-5-1`, that did not implement the range)
**Date:** 2026-09-24
**Subject:** Milestone-tier audit of the fork's work between the #186 audit (RC-6 pin, 2026-09-14) and the seventeenth cut `443028ff7a`
**Spec:** issue bodies on `maxengel/rocknix` — RC round #236 and every issue opened or closed since 2026-09-14; `docs/decision-register.md` rows dated in the window; `docs/blindspot-register.md` 41–52

---

## Running Notes

### 1.0 Freeze and grounding (02:15)

- Primary checkout `/workspace/repos/rocknix` is on `next` at `a9f503f4`; frozen scope tip is `443028ff7a`. `git log --oneline 443028ff7a..HEAD` = 5 commits (`a9f503f466`, `74f597ec72`, `cce581bc5f`, `5f8d16489b`, `2985bb6bba`), `git diff --stat 443028ff7a..HEAD` = 7 files, **all under `docs/`** (the audit running log, `docs/friction-log.md`, `docs/qa-frames/2026-09-24/*`, `docs/vm-qa-log.md`, `docs/work-logs/...`). No code path moved after the freeze, so the primary working tree is byte-identical to `443028ff7a` on `projects/`, `packages/`, `tools/`, `.githooks/`, `.github/workflows/`; mechanical checks run there stand for the frozen tip.
- Skill copies (`SKILL.md`, `references/*.md`) identical between the worktree, the primary and `next:` (`diff -q`, all SAME).
- The auditing agent's cwd is the stale `conflict-resolution` worktree: 8 rule files absent there, 11 differ, `CLAUDE.md` differs. All doctrine is read from the primary.
- Rules in scope by `paths:` glob (read from the primary): everything with `**` (adversarial-council, decision-register, device-builds, documentation-accuracy, engineering-practices, es-code-traps, es-native-ui, es-player-text, es-ui-style-guide, fork-workflow, instruction-files, issue-tracking, learning-capture, upgrade-and-install, working-principles, worktrees) plus the globbed ones that match changed paths: `change-log` (`projects/ROCKNIX/packages/**`), `generic-x64-vm-testing` (`projects/ROCKNIX/packages/**`, `tools/vm-qa`, `tools/vm-walks/**`, `tools/cloud-test-backend`, `tools/time-to-play`), `handheld-evidence` (`projects/ROCKNIX/packages/sysutils/**`, `projects/ROCKNIX/packages/rocknix/**`), `packaging-and-patches` (`packages/**`, `projects/**`), `rclone-cloud-sync` (`projects/ROCKNIX/packages/network/rclone/**`, `projects/ROCKNIX/packages/rocknix/sources/scripts/**`, `tools/cloud-round-trip`, `tools/last-good-scripts-test`). The four no-front-matter/no-glob rules (`ceremonies`, `least-surprise`, `player-language`, `time-to-play`, `vm-first`) load every session and are in scope. `council-substrate-integrity` matches nothing changed (out of scope).

### 1.3 Git history — distribution repo

- `git log --author='Max Engel' 95e4961fb3..443028ff7a -- projects packages tools .githooks .github/workflows`: **134 commits**, **96 unique files** (list in the running log's tool output; grouped below).
- Groups of changed files:
  - **Package recipe bumps (generic `packages/`)**: brotli, zip, ruby, openjpeg, glib-networking, libtasn1, dmidecode, ryzenadj, libpsl, libsoup, webkitgtk (11 `package.mk`); plus `projects/ROCKNIX/packages/graphics/cairo/package.mk`, `projects/ROCKNIX/packages/network/rclone/package.mk`.
  - **Emulator packages**: fbalpha2012-lr, fbalpha2019-lr, fbneo-lr (+ `scripts/rotation-table.py`), mame2003-plus-lr, mame2010-lr, `rotation-table-fba.py`, `rotation-table-mame.py`; retroarch patches 0013–0016; standalone aethersx2-sa, amiberry, armsx2-sa (`cheevos_armsx2.sh`), bigpemu-sa, drastic-sa, ppsspp-sa (+ `cheevos_ppsspp.sh`), yabasanshiro-sa.
  - **raofflineproxy**: `package.mk` (+ `-libchdr`, `-rcheevos`), patches 004–015, sources `raofflineproxy-cache-images`, `-cache-indexed`, `-ctl`, `-refresh`.
  - **rclone sources**: `cloud_capture`, `cloud_net_ready`.
  - **rocknix**: `profile.d/001-functions`, `sources/post-update`, scripts `rocknix-corekeep`, `runemu.sh`, `setrootpass`, `setsettings.sh`, `wifictl`; `sysutils/busybox/sysctl.d/99-coredump.conf`; `sysutils/systemd/scripts/userconfig-setup`.
  - **emulationstation package**: `package.mk` (pin bumps), `config/common/es_savestates.cfg`.
  - **Guards/CI**: `.githooks/pre-push`, `.github/workflows/fork-checks.yml`.
  - **Tools (fork-only)**: archaeology, build-preflight, ceremony-check, cloud-round-trip, device-act, es-menu-map-check, es-syntax-check, es-untranslated, fork-package-freshness, frame-diff, last-good-scripts-test, png-blackout, qa-accounts, ra-candidate-games, ra-offline-test, retroarch-wrapper-test, time-to-play, vm-qa, vm-upgrade-rehearsal, vm-walks/{claims.txt, masks.txt, manager.steps, suite.txt, README.md, fixtures/*.png}, watch-job, work-log-index.
- Two upstream merges inside the range (`7bd7f31312` 2026-09-19, and the feature merges `0923b9ee9f`/`48384bc70a`/`e18727c7fa`/`71a79a5499`); the conflict-resolution surface to inspect is the 2026-09-19 upstream merge (`1ffec5fd3e`, `2869da3147`, `e424886341` re-add guards "the upstream cleanup dropped").

### 1.3 Git history — EmulationStation repo

- `/home/max/Development/emulationstation-next.worktrees/qa-integration` on `test/qa-integration` at `75ca1dac2` (clean). `ae9c7d56b..75ca1dac2`: 108 commits including merges (non-merge count recorded below); diffstat 108 files, +8571/−1463, the French `.po` +1705 alone.
- Areas: RetroAchievements pages (GuiRetroAchievements*, OfflineAchievements*, OfflineProxyUrl, WebImageComponent), save state manager (GuiSaveState, SaveStateRepository, SaveStateBookkeeper, SaveStateJobQueue tests), cloud transfer/sync (GuiCloudTransfer, CloudTransferJob, ThreadedCloudSync, CloudText), Wi-Fi (GuiWifi, WifiText, wifictl), logging/secrets (Log.cpp, LogPolicy.h, StringUtil maskSecrets), display aspect/rotation (ImageComponent, DisplayAspect*, CaptureRotationText, GridTileComponent, ViewController), foreground pages with CANCEL (GuiScraperRun, ThreadedScraper), help bar (HelpComponent), MultiLineMenuEntry, SwitchComponent dimming, TimeText (12-hour), main.cpp (startup index, crash handler), unit tests (10 new test files).

### 1.2 Issue inventory (bounded `gh issue list --repo maxengel/rocknix --limit 100 --json ...`)

- **Closed since 2026-09-14 (30, matches the brief):** #45, #47, #102, #115, #121, #131, #151, #152, #153, #162, #164, #175, #176, #181, #183, #184, #186, #187, #197, #204, #205, #206, #207, #220, #225, #226, #227, #241, #242, #244.
- **Created since 2026-09-14 (83):** #174–#256. Open ones with code in the range (the RC cuts' targets): #177, #178, #182, #188, #189, #190, #191, #192, #193, #194, #195, #196, #198, #199, #201, #202, #203, #208, #209, #210, #211, #212, #213, #224, #228, #243, #245, #246, #247, #248, #249, #250, #251, #252, #253, #254; and #236 (the RC round page = the spec's checklist), #237, #255, #256 (no code yet or out of range).
- Note: many issues whose fixes are in the range are still OPEN (e.g. #177, #178, #182, #188–#196, #198–#203, #208–#210, #243, #245, #248–#254) — closing discipline (issue-tracking.md) says deliver -> close with the commit; an open issue with shipped code is either awaiting device confirmation under D-QA-032 or a closing-discipline gap. To be checked per issue in Phase 2 (the criterion is the issue's own AC list; state is corroboration only).
- ES non-merge commits in range: **57** (`git log --no-merges`); 108 with merges.

### 1.4 Doctrine read (from the primary, never the worktree)

Read in full: `CLAUDE.md`; rules `es-code-traps`, `es-player-text`, `es-ui-style-guide`, `least-surprise`, `player-language`, `time-to-play`, `vm-first`, `ceremonies`, `change-log`, `working-principles`, `handheld-evidence`, `packaging-and-patches`; the primary-vs-worktree deltas of `engineering-practices` (lanes rule D-UI-079; "a name is not a behaviour"; "a promise is not a mechanism"; ask before the transfer D-QA-011; nothing runs on a device without a yes D-QA-015; fail gracefully D-CLOUD-077/078; device state read from the device; never edit a shell tool mid-run), `upgrade-and-install` (vm-upgrade-rehearsal; busybox shims), `fork-workflow` (fork-only tool list; freshness D-WORKFLOW-024), `issue-tracking` (D-QA-012; VM-first line in issues; putting a box on a checklist), `learning-capture` (work-log index; procedures become tools), `decision-register` (archaeology before "pending"; same-day rows), `instruction-files` (front-matter standard; the index of rules and of the fork's tools — "a new one is added to both lists and to this table, or it is invisible"), `worktrees`, `device-builds` (build-fetched deps `--wrap-mode=nodownload`; reading a crash; SM8550). `docs/blindspot-register.md` 1–52 read in full; the decision rows dated 2026-09-14..24 read (see 1.2).

Constraints that bind the forward audit most directly:
- **Comment quality face (maintainer's words):** true beside the code; says the why / issue / decision ID; ASCII in any comment xgettext can extract (a comment right before `_( )`, `es-code-traps` § "Comments near a translatable string must be ASCII"); a constraint lives beside the option it constrains with date and commit (blindspot 48, engineering-practices).
- **Fork-only tools must be registered in three places**: `.githooks/pre-push` `PERSONAL_PATTERNS`, `fork-workflow.md`'s list, and `instruction-files.md`'s table (instruction-files § "The fork's own tools"). New in range: archaeology, build-preflight, ceremony-check, es-menu-map-check, es-syntax-check, es-untranslated, fork-package-freshness, frame-diff, png-blackout, ra-candidate-games, retroarch-wrapper-test, vm-upgrade-rehearsal, watch-job, work-log-index (14 candidates to check against all three lists).
- **A suite is not wired in until it has been seen to FAIL once** (D-QA-022, blindspot 39/40); a guard with no observed positive is no evidence (blindspot 14).
- **D-UI-078/079**: long work = foreground page with CANCEL, inside the fork's lanes only.
- **D-UI-083**: incremental-save semantics; **D-UI-084**: RetroArch message-queue floor 14 px for that font alone.
- **D-WORKFLOW-024**: fork-introduced packages at latest release or a `# freshness: pinned -- ...` line.
- **Change log** (`docs/cloud-sync-changelog.md`) written the day a player-visible change lands (#244).
- **Menu map** (`docs/es-menu-map.md`) checked by `tools/es-menu-map-check` (D-UI-076).
- **es-native-ui**: a `.cpp` edit passes `tools/es-syntax-check` before the pin moves (blindspot 49).

### 1.5 Prior-audit provenance map (verdicts sequestered — no per-AC PASS/FAIL transcribed)

| Audit | Tier / scope | Who / model | Trust signal | Carry-forward |
| --- | --- | --- | --- | --- |
| #186 `2026_09_14-epic-offline-retroachievements/` | Epic #163, sub-issues #164–#184, at the RC-5 feature branches | Fable 5.1 xhigh + GPT-6 Astra second opinion | thorough: 64 criteria, 33 punch items (0C/2H/19M/12L), refutation pass | **Red flag:** the punch list's YAML index still reads `outcome: open` for 32 of 33 items (only PL-20 `resolved`), while #186 was closed 2026-09-21 as delivered and the 2026-09-14 commits name PL-01..PL-33 in their subjects. The Phase 7 outcomes were never written back to the index. Phase 2 re-derives each PL item from code (`git log --grep '#186 PL'`). |
| #151 `2026_09_13-milestone-stable-before-upstream/` | Milestone, `53f390b1e9..59103cd9cb`, ES `52012829cf..3466b36af7` | Fable 5.1 xhigh + two seats | thorough: 65 criteria, 18 items; 16 resolved, 2 deferred to #156 | #156 remains the home of two deferrals (the 14 status frames, the probe-timeout frame). Out of this audit's range except where #236's B-section ticks PL-07/PL-17 "in this build" — those two ticks are re-derived in Phase 2. |
| #129 `2026_09_12-milestone-cloud-saves-since-60/` | Milestone, cloud saves | one orchestrator | 12 items, all deferred to #144/#145/#146/#129/#139/#42/#127 | Out of range; the deferrals live in open issues (pre-existing tracked scope). |

Tier B (design review / visual QA): the repo has one in the shape of `tools/vm-visual-qa` walks with frames under `docs/qa-frames/<date>/` and the `frame-diff` suite (#252). #236's B-section cites frames per box; Phase 2 verifies the cited frame paths exist and that a UI-touching AC has one.

### 1.7 Mechanical checks run (Phase 1, 03:20–03:40 UTC; cited by the criteria in 02)

All run from the primary `/workspace/repos/rocknix` (tree = `443028ff7a` on every code path, §1.0) unless stated.

| Check | Command | Exit | Counts / result |
| --- | --- | --- | --- |
| pkgcheck | `./tools/pkgcheck <p>` for the 28 packages whose `package.mk` the fork changed in range (brotli zip ruby openjpeg glib-networking libtasn1 dmidecode ryzenadj libpsl libsoup webkitgtk fbalpha2012-lr fbalpha2019-lr fbneo-lr mame2003-plus-lr mame2010-lr aethersx2-sa amiberry bigpemu-sa drastic-sa ppsspp-sa yabasanshiro-sa cairo raofflineproxy raofflineproxy-libchdr raofflineproxy-rcheevos rclone emulationstation) | 0 ×28 | **28 OK, 0 FAIL** |
| last-good-scripts-test | `./tools/last-good-scripts-test` | 0 | **PASSED**; sections a..x, the tail shows x (#209 D-UI-083 modes, #198 password whole to cryptpw/smbpasswd, #247 cut/whole), w (#170), v (#196/#249 state-file contract), u (wifictl join/forget), the 012/#199 image-miss cases |
| register-check | `./tools/register-check` | 0 | 344 IDs, each once; every citation names a row |
| vocabulary-check | `./tools/vocabulary-check` | 0 | 143 strings judged, 0 wrong (1 allowlisted upstream string) |
| work-log-index | `./tools/work-log-index --check` | 0 | index current |
| ceremony-check | `./tools/ceremony-check --no-gh` | 0 | nothing overdue (retro last 2026-09-04, 3 of 5 active days since the 09-21 clock; friction, weekly, monthly, index, register, guards ok; audit/futro not checked without gh) |
| fork-package-freshness | `./tools/fork-package-freshness` | **1** | 22 rows: 13 CURRENT, 3 PINNED (zip, webkitgtk, raofflineproxy-rcheevos), 4 INHERITED, 1 LOCAL, **1 BEHIND: raofflineproxy `4e9bab484e` is 16 commits behind upstream `0711f0b9b6` (2026-09-23)** and carries no `# freshness: pinned` line -> lead for Phase 2 (D-WORKFLOW-024 is a pre-`pr/` gate; the recipe says nothing about the pin) |
| es-menu-map-check | `ES_SRC=<qa-integration> ./tools/es-menu-map-check` | 0 | 51 screens at `75ca1dac2`, 27 mapped, 24 declared unmapped, 0 missing |
| es-untranslated | `ES_SRC=<qa-integration> ./tools/es-untranslated` | 0 | 674 fork strings, 674 with French, 0 without |
| msgfmt | `msgfmt --check -o /dev/null locale/lang/fr/.../emulationstation2.po` | 127 | **host has no `msgfmt`** -> retried with the build root's gettext (below) |
| non-ASCII comments | `grep -nP '^\s*//.*[^\x00-\x7F]|/\*.*[^\x00-\x7F]'` over the 108 changed ES `.cpp/.h` | — | **6 hits**: GuiScraperRun.cpp:177 (`·`), GuiScraperRun.h:38 (`·`), GuiScraperStart.cpp:89 and :164 (em dash), InputManager.cpp:361 (em dash), InputManager.cpp:1268 (French, upstream-looking) -> triage: fork-added in range? directly before a `_( )`? what the build's xgettext invocation tolerates |
| es-syntax-check | `./tools/es-syntax-check --tree <qa-integration> <55 changed non-test .cpp>` | **1** | 52 OK, **3 FAIL** (GuiGameAchievements.cpp, GuiRetroAchievements.cpp: `redefinition of 'class GuiRetroAchievements'` between the worktree header and the farm's copy; InputManager.cpp: `Delegate.h` redefined between the worktree and the build tree) — the shape is the farm's `#pragma once` seeing one header under two paths, a tool artifact to isolate by re-running those three without `--tree` |
| ES unit tests | cmake+ninja into `/workspace/tmp/rocknix-session/es-unit-build-audit`, then `es-unit-tests` | 0 / 0 / 0 | **119 test cases, 1302 assertions, 0 failed** (the 2026-09-23 count was 118 / 1302) |
| guest d identity | `ssh ... 'cat /etc/os-release; uptime'` (read-only) | 0 | `BUILD_ID=443028ff7a396f…`, `BUILD_BRANCH=build/generic-x64`, `BUILD_DATE=Thu Sep 24 01:08:45 UTC 2026`, up 1:06, boot id `f44a0e53…` |

### 1.7b Isolations of the three mechanical leads (03:55 UTC)

- **msgfmt**: the host has none; the build root's `toolchain/bin/msgfmt` exists. `msgfmt --check --statistics -o /dev/null locale/lang/fr/LC_MESSAGES/emulationstation2.po` -> exit 0, **1944 translated messages**, no error.
- **es-syntax-check's three FAILs are the tool's include resolution, not the source.** Re-run without `--tree` -> the same three FAIL with the same `redefinition` errors, now between the worktree header and the build tree's copy. `cmp` says the three headers (`GuiRetroAchievements.h`, `GuiGameAchievements.h`, `Delegate.h`) are **byte-identical** in the worktree and in `build/emulationstation-75ca1dac2…/`; their mtimes differ (worktree 2026-09-09/14/15, build tree 2026-09-23 23:35). GCC honours `#pragma once` across two paths only when mtime and size match before it compares contents, so a header reached under two paths is a redefinition. The build tree holds `GuiGameAchievements.cpp.o`, `GuiRetroAchievements.cpp.o`, `InputManager.cpp.o` all dated 2026-09-23 23:35 under `.x86_64-rocknix-linux-gnu/`, and the image built from that root is the one on guest d -> the three sources compile. Lead for Phase 3/5: the tool yields false FAILs (exit 1, fails closed) on TUs whose header is reachable under two spellings; the 2026-09-22 work log (line 195) records a chain that committed past a FAIL, and the 2026-09-21 log the tool's birth — the rule "not ready to merge until this has passed" is unsatisfiable as-is for these three files.
- **Non-ASCII comments**: of six hits, two are fork-added in range (`GuiScraperRun.cpp:177`, `GuiScraperRun.h:38`, commit `acd7a6fee`, a U+00B7 inside a quoted example); four pre-date the range (`GuiScraperStart.cpp:89/:164` `3e7195eb9`, `InputManager.cpp:361` `2add5f70f`, `:1268` `a763017d2`, upstream French). The two in-range comments precede a function signature / a declaration, not a `_( )` line, and the image built (x64 run 32, H700 run 21), so xgettext did not stop; the rule's pre-bump grep would still have flagged them. Lead for the comment-quality face (Low).
- **fork-package-freshness exit 1**: `raofflineproxy/package.mk` at `443028ff7a` pins `4e9bab484e` with a header comment "main at 2026-09-20 … (#164, #227)" and no `# freshness: pinned -- …` line; upstream `main` is 16 commits ahead as of 2026-09-23. D-WORKFLOW-024's gate is pre-`pr/`, but the rule in `fork-workflow.md` states the tool exits 0; at the frozen tip it does not.
- **Tool registration (instruction-files.md § "The fork's own tools")**: `archaeology`, `ceremony-check`, `frame-diff`, `work-log-index` are in `.githooks/pre-push` and in `fork-workflow.md`'s list but **absent from the rule's table** ("a new one is added to both lists and to this table, or it is invisible"). Lead for Phase 3 (documentation gap, Low).
- **Runtime evidence at the frozen tip** (`/workspace/artifacts/rocknix-images/qa-443028ff7a-webdav-a-20260924-0116/report.md`): twelve suites PASS (scripts 88 s, lifetime, vocabulary, french, menumap, register, pair-identity, round-trip 186 s, exit 26 s, time-to-play 60 s, walks 1326 s over 16 walks / 78 frames, frame-diff 1 s: 0 boxes against baseline `aa8d525a8a`, 4 masks, 0 claims); time-to-play: first frame 0.89 s, exit-sync stamp 1.43 s, game-to-game 2.04 s with the STOP IT AND PLAY question answered at 1.03 s (D-CLOUD-130/131). Upgrade rehearsal `qa-443028ff7a-upgrade-from-b245fd12ac-20260924-0145`: rc 0, **19/19 PASS** (saves byte-identical, marker, setting, remote, conf defaults merged, archive, queue empty, one libcairo, rclone v1.75.1).
- **es-syntax-check mechanism, confirmed**: `GuiGameAchievements.cpp:14` and `GuiRetroAchievements.cpp:11` include `"GuiGameAchievements.h"` by its own-directory spelling (resolves to the worktree copy) while `GuiGameAchievements.h:8` includes `"GuiRetroAchievements.h"` from the build tree's copy reached through `-I`; two paths, unequal mtimes, `#pragma once` does not dedupe. `--with <worktree>/es-app/src` makes both PASS (exit 0). `InputManager.cpp` still FAILs with `--with` on both es-core roots (`Delegate.h` reached under a second spelling via `Settings.h`/`TextListComponent.h`/`ImageGridComponent.h`); the tool needs a content-based or single-root resolution, not a flag per case.

### 1.8 Further leads from the primary reads (04:15 UTC)

- **Change log (`docs/cloud-sync-changelog.md`)**: the last section is headed `(2026-09-22)` and carries, as later bullets, #245/#248 (09-22/23), #249 (09-23), #250 (09-23), #252 (09-23), #209/#198/#247 (09-23), #251 (09-24). Every player-visible change in the window has a claim; the heading's date describes only its first four bullets. Lead: `change-log.md` asks for a `## Title (date)` section per landing day.
- **Menu map**: `REFRESH ACHIEVEMENT STATUS` (D-UI-077, #224) is not in the map because it is not in the ES source at `75ca1dac2` (0 hits) — the distribution ships `raofflineproxy-refresh` (`87d9969bbb`) and the ctl verb without the row; #224 stays open (pre-existing tracked scope, unless its body claims the row). `WI-FI SSID` in the map (line 224) is a historical note only.
- **Stale comments (comment-quality face)**: ES `WifiText.h:48`, `GuiWifi.h:11`, `GuiMenu.cpp:8887` still call the row "the WI-FI SSID row" as if current; D-UI-071 renamed it WI-FI NETWORK (ES `8d7213712`, 2026-09-16). `GuiMenu.cpp:9040` is correctly historical. To verify in context in Phase 3.
- **`post-update`**: the range diff ends with `rm -rf /storage/.config/melonDS; cp -r /usr/config/melondDS /storage/.config/` — `melondDS` (sic). Authorship to check (upstream commit or a fork conflict resolution).
- **`setrootpass`**: the only change is `printf '%s\n%s\n'` to smbpasswd with a why-comment naming #198 (was `echo -ne`, which ate backslashes). `cheevos_ppsspp.sh`: `[ "${enabled}" != "1" ]` quoted (#186 PL-31) and the proxy host routed only when hardcore READ as `0` (`hardcore_raw`), with a why-comment. `cheevos_armsx2.sh`: tests `secrets.ini` (`ARMSX2_TOKEN`) not `PCSX2.ini`, comment names #170. `001-functions`: `redact_credentials` gains `retroachievements\.key`, single-quoted values, bare `t=` after `?`/`&`/space (#186 PL-29); `battery_percent` reads `/tmp/battery.percent` first (no comment says why or which issue -- lead). `99-coredump.conf`: core_pattern to `rocknix-corekeep` with a why (#79, #211). `es_savestates.cfg`: new, Batocera's libretro entry under `retroarch`, header explains #196/D-UI-057 and the two link sites; `post-update` and `userconfig-setup` link it with comments naming #196.

### 1.9 Primary reads of the shipped scripts (04:35 UTC) — leads

- **`post-update` melonDS block** (`rm -rf /storage/.config/melonDS; cp -r /usr/config/melondDS …`, `melondDS` sic) is upstream's `62c343921d` (rocknix, 2026-09-06); `battery_percent`'s `/tmp/battery.percent` is upstream's `d5592dbbe0` (batteryplus). Both out of scope; the typo is an observation to hand upstream (the package installs `/usr/config/melonDS`).
- **`cloud_capture`** (+152): `trap '' TERM` with a why (essway's control-group SIGTERM, D-CLOUD-133, #205); `--retire --unlink` unlinks in `finish()` for mode `retire` when `rc != 2` — the deletion proceeds even when the record failed (rc 1), stated in the comment as the behaviour ES had; `--adopt` (#206, D-CLOUD-134) with `rel_of` (a relative path falls through as already root-relative — the manager passes absolute paths), membership by the layout row, `op_copy` and the jq merge's `copy` op that nulls `replaces`/`pub`/`producer`. Comments cite CAP13, D-CLOUD-133/134, #205/#206.
- **`runemu.sh`** (+84): `parse_savestate_arguments "$@"` reads `-state_file` whole from argv; `--state_file=` handed to setsettings ahead of `--controllers=` with a why (#196). **`setsettings.sh`**: `set_savestates` maps `0|2|false|none` -> `savestate_auto_index false` (#209, D-UI-083) and writes `savestate_max_keep` only when set; `set_autosave` sets `state_slot -1` for a `.auto` file (#249) and prints ` -e N` for a numbered slot (#196); the hardcore test is `= "0"` with a why (#186 PL-31). The melonDS DS fit block and `set_melondsdsopts` moved after `wait` — authorship to confirm (upstream vs a fork conflict resolution).
- **`wifictl`** (+213): `current`/`saved`/`forget`/`join` read NetworkManager (`nmcli -t`, unescaped once the name stands alone; `forget`/`join` match the name via `ENVIRON` not `awk -v`); `join` brings the profile up with `-w 90`, then moves `wifi.ssid`/`wifi.key` onto it (the key read from NM with `-s -g`, never printed) and `pin_wifi`. The ad-hoc/netplay rewrite (`create_adhoc` via NM, `connect_adhoc_client`) is not in the fork's commit subjects — authorship to confirm.
- **`rocknix-corekeep`** (new, 191 lines): armed by a marker on `/storage` (survives an update, D-QA-029; comment says so), bounded by `head -c` on the raw stream per executable (1 GiB for `emulationstation`, 256 MiB else, #247), refuses under 512 MB free, 0700/0600, never collected. Three device-tool dependencies the busybox harness does not shim: `gzip -l` (decides CUT vs whole from the raw count), `head -n -3` (prunes the ring), `date -d @epoch` (the note). Probed on guest d (below). Comment nit: "Turn it on for a device with:" is followed by a paragraph before the command list.

### 1.10 Leads verified from the artifacts (03:05 UTC)

Each read directly (file:line at `443028ff7a` / ES `75ca1dac2`), so it may bear a verdict:
- `raofflineproxy-ctl:1297,1300` `exit ${EX_USAGE}` with no `EX_USAGE=` anywhere in the script (also 0 definitions in `/usr/bin/raofflineproxy-ctl` on guest d): a bad `refresh` argument prints usage and exits 0.
- `raofflineproxy-ctl:685-688` `mark_running` prints `route=%s at=%s%s` with `$1` glued to the epoch; `:1279` `mark_running images`, `:1309` `mark_running refresh` -> `at=<epoch>images`. `do_images`/`do_refresh` (1269-1315) set `trap unmark_running EXIT` only; `trap 'stopped …' TERM` exists at 1318 (scan) and 1413 (topup) and `CLIENT=` at 1137/1513 only.
- `raofflineproxy-cache-images:6` cites `D-RA-0xx`; `tools/register-check:48-58` scans rclone/sources, rocknix/sources/scripts, systemd/scripts and tools — not `network/raofflineproxy/sources`.
- Stale pin comments: `raofflineproxy-ctl:193` and `raofflineproxy-rcheevos/package.mk:6` name `64d03d30`; `package.mk` pins `4e9bab484e` (`41f86ec9ba`) and `raofflineproxy-libchdr/package.mk:6` was updated.
- `aethersx2-sa:12-15` (and bigpemu-sa, drastic-sa) comment "PKG_EMUS lists this package for every device": `virtual/emulators/package.mk:11-12` base list holds only amiberry and yabasanshiro-sa of the five; the other three are added in device arms (43-87). yabasanshiro-sa's and amiberry's comments are true.
- `ppsspp-sa/package.mk:67-71` `post_unpack` seds `flags.make`/`build.ninja` — CMake outputs absent at unpack; upstream's move (`28e750db32`), merged whole, touched again by the fork's `e424886341` for the directory guard.
- `cloud_capture:1308-1316` a retire path outside/not plain -> `RC=1; continue`; `finish():237-244` then `rm -f` every `RETIRE_PATHS` when `rc != 2`, so `--retire --unlink` deletes a file it refused to record. `--adopt` refuses non-members (549-556).
- ES `main.cpp:355-392`: handler LOGs (`Log::mMutex` is `std::mutex`, Log.cpp:70/104/122/187) then `backtrace_symbols_fd`, `signal(SIG_DFL)`, `raise`; installed with `signal()` for SIGFPE/SIGILL/SIGINT/SIGSEGV (642-646). A fault while `mMutex` is held deadlocks the handler.
- ES `DisplayAspect.cpp:44-50` static `known` map never invalidated; `CaptureRotation.cpp:19,27` `sKnown`/`sTables` (tables read once per core). `ImageGridComponent.h:413-417` scroll-loop tiles get `loadTile` and no `mTileDecorator` (applied at 305-309 and 377-381 only).
- ES `SaveStateBookkeeper.cpp:81-85` logs `save state copy recorded` on rc 0; `cloud_capture:549-556` exits 0 with a WARN for a non-member path.
- ES `WifiText.h:47-48`, `GuiWifi.h:11`, `GuiMenu.cpp:8887` say "the WI-FI SSID row" as current; the label is WI-FI NETWORK (`GuiMenu.cpp:9041`, D-UI-071).
- `tools/es-menu-map-check:153` `if title.lower() in text.lower(): continue` — a substring test against the whole map. `tools/ceremony-check:96-108,121` retro marker = any work-log heading matching `\bretro\b|retrospective`; `:147` only `issue: none` counts as open. `.githooks/pre-push:55,229` the `next` gate runs `$hooks_root/tools/ceremony-check` only if it exists in the pushing worktree.
- `tools/es-untranslated:89` default `--upstream 2e1564402` hard-coded. `tools/vm-upgrade-rehearsal:73-92` hard-coded key path/port, `sleep 90`, a `libcairo` check. `tools/fork-package-freshness:73-75` ruby held to its minor series in the tool. `tools/retroarch-wrapper-test:37-43` default source under `/workspace/repos/rocknix.worktrees/devices`; 0 references in `tools/vm-qa`.
- `docs/ra-offline/rocknix-org-offline-achievements.md:25` "You can press **B** to keep it scanning in the background" — reversed by D-UI-078; `GuiOfflineScan.cpp:111-112` B -> `askCancel()`.
- `raofflineproxy-ctl:254` `PROBE_TRIES=9`; #186 PL-04's acceptance (05-punch-list.md:71) says "three probes".
- `tools/lint-audit-artifacts docs/audits/2026_09_14-… --issue 186` -> PASS, exit 0, while that folder's YAML index reads `outcome: open` for 32 of 33 items and #186 is closed: the lint reads the folder's Phase 7 outcomes table (complete, with commits) and never the YAML index (the source the begin-delivery gate parses), so the two can disagree unnoticed.
- `tools/vm-walks/claims.txt` holds the header only; `docs/qa-frames/2026-09-23/` has `252-walk-manager-{fbn,nes}` and no `gb` frame though #252 box 3 cites three.
- Guest d: `es_systems.cfg` names `fbalpha2019` as a core (4 entries) so `fbalpha2019.txt` is read; `/storage/.config/raofflineproxy/online_state.json` absent (the toggle is off on guest d); no `keep-core-dumps` marker (D-QA-029 honoured).
- Reports: `qa-d55169e59e-ra-offline-d-20260921-{1739,1745}` exist under `/workspace/artifacts/rocknix-images/` (the A lead "not found" was wrong).
- Issue hygiene at the tip: #225 closed with five unticked boxes; #247's three boxes unticked though comment 1 claims delivery; #193's three unticked though `e2023b2f3` shipped and frames exist; #194 box 2 unticked though D-RA-022 decided and patches 0013/011 shipped; #203 box 4 unticked though D-CLOUD-129 is a register row; #195 box 4 unticked though `GuiRetroAchievementsSettings.cpp` reads the switch; #211 boxes 1 and 5 unticked though answered in comments.

## Research Summary (Phase 1.6, 03:05 UTC)

**What was planned.** The release-candidate round (#236) for the RG35XX SP: every RC-cut fix the maintainer's device round and the VM found between the RC-6 pin (2026-09-14) and the seventeenth cut `443028ff7a` (2026-09-24) -- offline RetroAchievements hardening (#175..#199, #211..#213, #242), the save state manager and captures (#195..#209, #243..#250), the cloud transfer and sync surfaces, Wi-Fi, logging and French (#152..#210, #241), the package freshness and build guards (#226..#228), the QA tooling and the ceremonies (#252..#254), and the RetroArch toast floor (#251); the #186 audit's 33 punch items (#186 closed 2026-09-21).

**What issues exist.** 30 closed since 2026-09-14 and 83 opened; ~36 open issues carry code in the range. The spec is their `- [ ]` lists. The closing discipline is uneven: several delivered items keep unticked boxes and unamended bodies (1.10).

**What code was changed.** Distribution: 134 fork commits / 96 files (package bumps, the proxy package and 12 patches, the ctl and three helpers, cloud_capture, six rocknix scripts, es_savestates.cfg, four RetroArch patches, five arcade-core recipes with rotation tables, the push guard, the fork CI, 20 tools). ES: 57 non-merge commits / 108 files (+8571/-1463), ten new unit-test files, the French catalogue +1705.

**What constraints apply.** All 27 rules (§1.0), with the comment-quality face the maintainer named; blindspots 1-52 (13, 14, 22, 34, 39, 40, 44, 45, 46, 48, 49, 50, 51, 52 the most relevant); decision rows D-UI-057..084, D-QA-030..040, D-WORKFLOW-024..028, D-RA-005..027, D-CLOUD-129..134.

**Red flags going into Phase 2.** (1) Two rules with a mechanical check that does not pass at the tip: `fork-package-freshness` exit 1 (raofflineproxy 16 behind, unpinned) and `es-syntax-check` false FAILs (a tool defect). (2) #186's punch index never received outcomes and the lint passes it anyway. (3) Tick discipline: ticks from artifacts (script hash, register row) and deliveries with unticked boxes. (4) Device-word boxes cannot be met by this audit (no handheld) and the RG SP -- named in a dozen boxes -- is no longer the RC device (D-QA-031). (5) Three stale pin comments and one `D-RA-0xx` placeholder in shipped scripts; a doc draft reversed by D-UI-078; three "WI-FI SSID" comments after D-UI-071. (6) Guards with no observed positive in range: `--wrap-mode=nodownload` (never seen to refuse), `fork-checks.yml` (never red), the pre-push INDEX warning, `archaeology`'s constructed case (#253 box 1). (7) The unlink-after-refusal path in `cloud_capture`; the crash handler's deadlock mode; caches never invalidated.
