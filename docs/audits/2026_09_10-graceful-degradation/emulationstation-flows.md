# Audit: EmulationStation cloud flows against D-CLOUD-077 / D-CLOUD-078

Read-only; nothing changed. ES read at `/home/max/Development/emulationstation-next.worktrees/qa-integration` (e46093354). Scripts read from `/workspace/repos/rocknix` on `next`. Register rows, `es-native-ui.md`, `engineering-practices.md` (last two sections) and #105 with its D-CLOUD-078 comment read first.

## 1. The table

Legend: "Did not happen" = says what was not done; "In place" = says what state the player now has; "Recover" = says how; "Retry" = a control on the surface that reported it; "Last-good" = the writer keeps a verified previous state (D-CLOUD-078).

| # | Flow | Trigger | Outcome texts today (verbatim) | Did not happen? | In place? | Recover? | Retry in reach? | Last-good kept? | Verdict | Smallest fix |
|---|---|---|---|---|---|---|---|---|---|---|
| 1a | Sync card, running | startup (`main.cpp` `startStartupSavesSync`, `cloudsaves.startup=1`), game exit (`FileData::launchGame`, `cloud_backup --yes --saves-only --recent`), three GAME SETTINGS rows, wizard's BACK UP SETTINGS AND SAVES NOW, TIDY apply | `Working...` (only lowercase string in the flow); `WAITING FOR THE NETWORK...` (the one `>>> doing` the card shows; every other keyword is dropped); `COMPARING SAVE FILES WITH THE CLOUD 12 / 70`; rclone fragments; any script line whose upper-case contains `ERROR`/`FAILED`/`WARN` (`Game saves backup failed: Directory not found or permission denied`, `Game saves backup: COMPLETED WITH ERRORS`) | n/a | n/a | n/a | n/a | n/a | The token filter (`ThreadedCloudSync.cpp:122-126`) passes rclone-code prose and drops the two best diagnoses the scripts own: `check_internet`'s "Your cloud storage did not respond. Check that it is still set up and that its sign-in has not expired." and `cloud_saves_root`'s "…is on a different card than the last time saves were synced…" (neither carries a token). | Replace the token filter with a `>>> why <sentence>` protocol line the scripts emit; show that, nothing else. |
| 1b | Sync card, rc 0 | as above | `COMPLETED SUCCESSFULLY`, bar 100 %, 1.5 s, fades | n/a | implicit | n/a | n/a | rclone rename-on-complete | OK. Wording: `COMPLETED` per #105. | string |
| 1c | Sync card, rc 69 / 75 | as above | `SKIPPED - NO NETWORK CONNECTION` / `SKIPPED - ANOTHER CLOUD SYNC IS RUNNING`, bar removed, 5 s, fades | yes | no ("nothing changed" is true and unsaid) | no | no (card takes no input; `AsyncNotificationComponent` has no `input()`); the row that runs it is not named | scripts write no stamp for a pre-run 69/75; ES stamps `last-sync-<origin>` (tmp+rename, good). A 69 raised *mid-run* is stamped by `clean_exit`/`stop_no_network` — correct | Acceptable words; missing the in-place and next-step clause. | Use the card's action line (`updateText(text, action)` exists, `actionLine=true` by default, currently blank): `NOTHING WAS CHANGED. TRY AGAIN WHEN YOU'RE ONLINE.` / `NOTHING WAS CHANGED. WAIT FOR IT TO FINISH, THEN TRY AGAIN.` |
| 1d | Sync card, cancel | a game launched during an automatic sync (`cancelForLaunch`) | `SKIPPED - A GAME WAS STARTED`; stamp rc 130 | yes | no | no | n/a (exit sync will run) | rclone rename semantics; kill waited for | Good; add the in-place/next clause on the action line: `YOUR SAVES ARE AS THEY WERE. THEY ARE SENT WHEN YOU EXIT THE GAME.` | action line |
| 1e | Sync card, any other rc | as above (incl. `-1` popen/signal, a 130 ES did not cause, rclone 1-8, `cloud_saves_root` refusal, `check_internet` diagnosis) | `FAILED - SEE /var/log/cloud_sync.log`, 5 s, fades | no | no | log path | no | as 1c | **P2**: names a log on a handheld; the diagnosis was printed and filtered out; partial runs (startup chain runs *both* halves; restore OK + backup failed) read as FAILED. A 130 not caused by ES reads FAILED here and `STOPPED` on the row. | `ThreadedCloudSync.cpp:218-223`: `COULDN'T FINISH - <why>` from `>>> why` or rc table; action line = in-place + `TRY AGAIN: GAME SETTINGS > <row>` chosen by `mOrigin`. |
| 1f | CLOUD / GAME SETTINGS rows afterwards | `cloudLastRunDetail(name)` reads `/storage/.cache/cloud_sync/last-<name>` | `LAST <date> <time>  -  SUCCEEDED` / `STOPPED` / `SKIPPED, ANOTHER SYNC WAS RUNNING` / `SKIPPED, NO NETWORK` / `FAILED`; `NOT DONE ON THIS DEVICE YET`. Read for `sync-startup`, `sync-exit`, `backup`, `restore`. | partly | no | no | the row itself | stamps tmp+rename (ES) / direct `>` (scripts) | `FAILED` says nothing; `last-sync-manual` is written (`recordOutcome`) and **never read** — SYNC SAVES WITH THE CLOUD shows a static `BOTH WAYS. NOTHING IS DELETED.`; `last-content-*` stamps have no reader either. | Add a third stamp field (`<epoch> <rc> <why-token>`, additive; readers split on space) and map to the vocabulary; read `sync-manual` on its row. |
| 2a | Transfer page, done | BACK UP TO THE CLOUD / RESTORE FROM THE CLOUD (`cloudOpenTransfer` → `GuiCloudTransfer`) | line 1: `COMPLETED SUCCESSFULLY` (0) / `STOPPED` (130) / `SKIPPED - ANOTHER CLOUD SYNC IS RUNNING` (75) / `SKIPPED - NO NETWORK CONNECTION` (69) / `FAILED` (else, incl. -1). line 3: `N FILE(S)[ · X MB] BACKED UP\|RESTORED` (size only when rc 0) or `NOTHING NEW TO SEND - EVERYTHING WAS ALREADY IN YOUR CLOUD` / `NOTHING NEW TO RECEIVE - EVERYTHING WAS ALREADY ON THIS DEVICE` (shortened to `NOTHING NEW TO SEND`/`RECEIVE` when too wide) — rc 0 only; **blank on failure with nothing moved**. line 5: `UPDATE GAMELISTS UNDER GAME SETTINGS TO SEE THE CHANGE.` (rc 0 content run only). line 6 `ELAPSED m:ss`. line 7 `PRESS ANY BUTTON TO CLOSE`; help bar `b CLOSE`. | `FAILED` says nothing | count of files with no size half-says it; no "the rest is as it was" | no | **none** — `input()` closes on any button | rclone rename-on-complete OK; saves restore is plain `copy` with no `--backup-dir`, so a newer local save overwritten by an older cloud one keeps no previous (D-CLOUD-078 names this) | **P2** no retry, no in-place sentence, `FAILED` bare. | Design in §3 (TRY AGAIN on line 7, in-place on line 5). |
| 2b | Transfer page, partial | any multi-tier run: `rc=0 ; { A ; } \|\| rc=$? ; { B ; } \|\| rc=$? ; exit $rc` (`cloudOpenTransfer` `add` lambda) | rc is the **last failing tier's**: saves 69 + ROMs OK → `SKIPPED - NO NETWORK CONNECTION` over `12 FILES BACKED UP`; saves OK + `nes` rclone 3 → `FAILED` over `N FILES BACKED UP`. The scripts' per-unit lines (`Backup of nes failed (rclone exit 3).`, `Restored gb.`) are **never shown**: `handleLine` acts only on `>>>`, `Transferred:`, `Checks:`, `*` lines. `rescanChangedFolders` and the gamelists note are gated on rc 0, so a partial restore leaves stale lists. | no (which tier?) | partly (count) | no | no | as 2a | **P1** — a partial success reported as SKIPPED or FAILED (D-CLOUD-077: "never as FAILED and never as COMPLETED"). | Echo `>>> tier <label>\|<rc>` after each `{ part ; }`; page: any 0 and any non-0 → `COMPLETED WITH GAPS`; line 4 names the tiers that did not finish; rescan when any tier succeeded. |
| 2c | Transfer page, MATCH apply | MATCH THIS DEVICE TO THE CLOUD → YES → `cloud_content_restore --match --apply` | done: `REMOVED N FILE(S) FROM THIS DEVICE[ · size]` + per-system detail + `FILES YOUR CLOUD HAD AND THIS DEVICE DID NOT WERE DOWNLOADED TOO.` **Mid-run link loss**: `stop_no_network` exits 69 *inside the loop*, before `>>> removed` is printed → `SKIPPED - NO NETWORK CONNECTION` over a **blank** summary, though completed systems' deletions already happened. | no | **no** — the one destructive flow says nothing about what it deleted | no | no | deletions are `sync` — "the cloud still holds what they removed" (true, unsaid) | **P1** | `stop_no_network` (match path) emits `>>> removed <running totals>` before exiting; page: `mRemovedFiles>0 && mExit!=0` → `COMPLETED WITH GAPS` + `N FILES WERE REMOVED FROM THIS DEVICE SO FAR. YOUR CLOUD STILL HAS THEM.` |
| 3a | Content picker, `--scan` fails | CONTINUE with ROMS AND BIOS / GAME CONTENT ticked (`cloudContentSystemPicker`, `GuiLoading` `COMPARING THIS DEVICE'S CONTENT WITH YOUR CLOUD` / `COMPARING YOUR CLOUD'S CONTENT WITH THIS DEVICE`) | script exits 69 with **no stdout** (its `No network connection. Skipped.` goes to stderr, which popen does not capture); ES uses the single-arg `executeScriptLegacy` → exit code discarded → `found.empty()` → restore: `NO SYSTEM IN YOUR CLOUD HOLDS WHAT YOU TICKED.\n\nPUT FILES INTO THE ROMS FOLDER FROM A COMPUTER, THEN SCAN AGAIN.`; backup: `NO SYSTEM ON THIS DEVICE HOLDS WHAT YOU TICKED.` | **false statement** | no | wrong advice | BACK → CONTINUE works but is not said | n/a | **P1** — an empty cloud presented as the cloud's contents (#105 names it). | Use the pair form; rc 69 → `COULDN'T REACH YOUR CLOUD.\n\nTRY AGAIN WHEN YOU'RE ONLINE.`; other non-0 → `COULDN'T READ YOUR CLOUD'S CONTENT.\n\nTRY AGAIN.`; only rc 0 + empty → today's text. |
| 3b | Picker selection file | page close → `cloud_content_restore --set-systems "<list>"` | `OK N selected` (unused) | n/a | n/a | n/a | n/a | **no**: `printf … > ${SELECTION}` — truncate+write, no tmp, no fsync, no previous. Empty file = "none" → `--selected` exits 1 `No systems are selected for this device.` → page `FAILED`, blank. ES also reads the file directly for ITEM i OF n. | **P2** writer | `> "${SELECTION}.tmp" && mv -f` (as `cloud_capture` does). |
| 4a | Connect provider (form) | CREATE REMOTE → `CONNECT TO THIS PROVIDER NOW?\n\nTHE REMOTE IS ONLY SAVED IF IT ANSWERS.` → `CONNECTING...` (`timeout 60 cloud_remote create`) | `THE REMOTE IS CONFIGURED AND WORKING.\n\nGAME SETTINGS > CLOUD SETTINGS IS NOW AVAILABLE.` / `THE REMOTE COULD NOT BE SAVED.\n\n<raw cloud_remote output>` | yes | yes (not saved) | the raw output, sometimes | yes (form stays open) | `cloud_remote` removes an unreachable remote — nothing half | OK; raw tool output is developer-facing on some errors. | none required; P3 wording of the raw tail. |
| 4b | OAuth sign-in | provider row → `cloud_oauth serve` → `STARTING SIGN-IN` (poll ≤30 s) → keyboard/phone pages → CONTINUE | not started: page `SIGN-IN DID NOT START` / `THE DEVICE COULD NOT REACH THE PROVIDER. CHECK THE NETWORK AND TRY AGAIN.` + GO BACK. CONTINUE: `signed-in` → CONNECTED page; `failed` → `THE SIGN-IN DID NOT COMPLETE.\n\nCODES ARE SINGLE-USE AND EXPIRE QUICKLY - START AGAIN TO GET A NEW ONE.` OK/EXIT; else `STILL WAITING FOR THE SIGN-IN.\n\nFINISH IT, THEN CHOOSE 'CONTINUE' AGAIN.` OK/CANCEL SIGN-IN | yes | yes | yes | one level back (provider list) | remote only written on success | Good — the model for the others. | none |
| 4c | Seed folders (done page) | after `--check` passes → `SETTING UP YOUR CLOUD FOLDERS` (`timeout 90 cloud_setup --seed-folders`) → `cloudSetupBuildDoneStep` | always titled `CLOUD SETUP COMPLETE` / `YOUR CLOUD REMOTE IS READY`; lists only `OK <dir>` lines; **`MISSING <dir>` lines are dropped**; on `ERROR no remote` (exit 2) or a 90 s timeout the group reads `SET UP FOR YOU IN YOUR CLOUD ACCOUNT, IF NOT ALREADY THERE:` over nothing, then `PUT ROMS AND BIOS FILES IN THESE FROM A COMPUTER, THEN RESTORE THEM HERE.` | **no** | misleading (an empty list) | no | FINISH only | n/a | **P2** — "THESE" pointing at nothing; a failed seed reported as complete. | Show `MISSING` rows with the warning glyph; no `OK` at all → replace the group with `YOUR CLOUD FOLDERS COULDN'T BE SET UP YET. THEY ARE CREATED AT YOUR FIRST BACKUP.` |
| 4d | CHANGE CLOUD FOLDER | row / wizard fact → keyboard → `CHECKING...` (`timeout 30 cloud_setup --set-syncpath`) | refused: `THE CLOUD FOLDER WAS NOT CHANGED\n\n<script's why lines>` (e.g. `Your provider would not accept "x".` … `Try something like /rocknix-saves-yourname/Saves instead.`); timeout 124: `THE CLOUD FOLDER WAS NOT CHANGED` with no why | yes | yes | yes when the script spoke; nothing on timeout | yes (reopen row) | writer: `sed -i` / `>>` on `cloud_sync.conf` in place; `cloud_sync_helper` keeps `.bak` but overwrites it with whatever is there on every run (same pattern as `chksysconfig`) | Good text; P3 add the timeout sentence; P3 writer. | timeout → `…\n\nYOUR CLOUD DID NOT ANSWER. TRY AGAIN WHEN YOU'RE ONLINE.` |
| 4e | CHECK IT WORKS / wizard gate | `openCloudSetup` row → `cloud_setup --check` (rclone bounded 10 s/30 s; ES has no `timeout` wrapper) | 0: `CLOUD REMOTE CONFIGURED AND WORKING: <name>`; 2: `THE REMOTE EXISTS BUT IS NOT RESPONDING: <name>\n\nUSE 'REPAIR OR MODIFY A REMOTE' TO RENEW ITS SIGN-IN.`; 1: `NO CLOUD REMOTE IS CONFIGURED YET.` Gate variant: `…ITS SIGN-IN MAY BE INCOMPLETE OR EXPIRED. IN THE TERMINAL, RUN:\n'rclone config reconnect <name>'` OK/EXIT | yes | yes | yes | yes (the row) | n/a | OK (computer-side words are correct on the SSH wizard). | none |
| 4f | MATCH preview / confirm | row → `COMPARING YOUR ROMS AND BIOS FILES WITH THE CLOUD` (`cloud_content_restore --match`, exit code discarded) | line starting `Refusing` → upper-cased + `\n\nNOTHING WAS CHANGED.` (its two explanatory lines dropped; a 69 exit's cause dropped). `No systems are selected for this device yet.` is not a "Refusing" line → `changes.empty()` → **`THIS DEVICE ALREADY MATCHES YOUR CLOUD.\n\nTHERE IS NOTHING TO REMOVE.`** — false. Confirmation: `REMOVE N FILE(S) FROM THIS DEVICE?  (size)\n\n<per-system>\n\nANYTHING YOUR CLOUD HAS THAT THIS DEVICE DOES NOT IS DOWNLOADED AT THE SAME TIME.\nGAME SAVES, SAVE STATES, AND SCREENSHOTS ARE NEVER TOUCHED.` | refusal yes; no-selection **no** | yes | partly | yes (row) | preview is read-only | **P2** (false "already matches") | pair form; rc≠0 with no `\|` lines → show all script lines; 69 → the no-network sentence. |
| 4g | TIDY UP YOUR CLOUD FOLDERS | row (only when `--check` rc 0 off-thread) → `CHECKING...` (`timeout 60 --check`) → dialog `<all lines>\n\nMOVE THEM?` MOVE / LEAVE THEM → card (`Origin::None`) | dialog offers MOVE even over `REFUSING: <path> already exists.` (rc 4) or a timed-out partial listing. Card: `say` lines carry no token except `FAILED copying saves. Both folders are intact; nothing was removed.` / `VERIFICATION FAILED for saves. Both folders are intact; the source was not removed.` (good sentences, shown ~1 s) then `FAILED - SEE /var/log/cloud_sync.log`. No stamp (`Origin::None`). | card: momentarily | yes (script's words, then lost) | no | row reappears if `--check` still says so | copy → `rclone check` → purge; refuses merges; resumable — **exemplary** | **P2** dialog; P3 no durable outcome. | pair form on the preview (rc≠0 → OK-only box); carry the script's sentence via `>>> why`. |
| 4h | FINISH RESTORE SETUP / FINALIZE RESTORE (relink) | `.restore-finish-pending` → boot (`openRestoreRelink(true)`), CLOUD page row, NETWORK SETTINGS row | state-glyph rows; `LATER KEEPS THIS LIST` / `IT COMES BACK NEXT TIME YOU START UP, OR FIND IT IN NETWORK SETTINGS > FINISH RESTORE SETUP.`; CHECK CLOUD REMOTE: `YOUR CLOUD REMOTE IS WORKING.` / `YOUR CLOUD REMOTE NEEDS ATTENTION.\n\nUSE CONNECT OR REPAIR CLOUD STORAGE IN GAME SETTINGS > CLOUD SETTINGS > MANAGE CLOUD STORAGE.` FINISH consumes the marker, LATER keeps it | yes | yes | yes | yes | marker consumed on completion — **the model** | Good. P3: the CLOUD page calls it `FINALIZE RESTORE`, the page and NETWORK SETTINGS call it `FINISH RESTORE SETUP`. | one name |
| 4i | Settings restore + `.cloud-journey-pending` | RESTORE FROM THE CLOUD with SETTINGS ticked → `RESTORE SYSTEM SETTINGS FIRST, THEN REBOOT?\n\nYOUR EXISTING CONFIGURATION IS REPLACED. ANYTHING ELSE YOU TICKED IS RESTORED AFTER THE RESTART. WI-FI AND ACCOUNT PASSWORDS MUST BE RE-ENTERED.` YES → `touch .cloud-journey-pending` **then** `/usr/bin/run "cloud_restore --yes --system-only && backuptool restore"` (console) | console text only: `No network connection. Skipped.` / `Backup archive is damaged (<path>). Restore aborted; nothing was changed.` / `Restore reported errors. Check 'logger' output; the device will NOT reboot.` (5 s). On failure no reboot, ES resumes, **marker stays**; next boot `main.cpp:799-808`: `YOUR SETTINGS WERE RESTORED.\n\nDOWNLOAD YOUR GAMES, BIOS FILES, AND SAVES FROM THE CLOUD NOW?` YES/LATER — false — and the marker is **removed on display** (LATER or a crash loses the continuation; the relink marker deliberately does the opposite). YES → `run "cloud_content_restore --all && cloud_restore --yes"` console, nothing returns to ES. | console only | no | developer-facing (`logger`) | no | `backuptool restore` is `tar -xzf … -C /` over the live tree: no staging, no copy of the replaced config, half-restored on a kill; marker set before success | **P1** (marker written before the thing it asserts; lie at next boot; writer with no last-good) + P2 (console hop, developer text) | backuptool touches the journey marker after its own verified extract (beside `.restore-finish-pending`, e.g. `backuptool restore --then-cloud`); `main.cpp` consumes it on YES/LATER; extract to staging then move; run `backuptool backup` first as the last-good; reword `fail` lines. |
| 5a | `es_settings.cfg` writer | `Settings::saveFile()` (`es-core/src/Settings.cpp:458`) — called from `GuiSettings::save()` on every settings page close and ~20 other sites (scraper, collections, UI mode, controllers, audio, `ViewController:77/889`, `SystemView`), i.e. on nearly every interaction, not at exit | `pugi::xml_document::save_file(path)` → `fopen("wb")` on the live path: truncate + write, no tmp, no rename, no fsync; `mWasChanged=false` **before** the write, so a failed write is never retried. Start: missing → defaults silently; empty/unparsable → `LOG(LogError) "Could not parse Settings file!"` → defaults silently, **no message**; the next `saveFile` rewrites the file from defaults+changes, making the loss permanent. No `.backup` anywhere for this file; only a settings archive if the player ever ran a backup. | n/a | n/a | n/a | n/a | **no** | **P1** (D-CLOUD-078's named gap) | tmp + `fflush`/`fsync` + `rename`; after a successful parse at start, copy to `.last-good` once; at start, if parse fails and `.last-good` parses, load it and show once: `YOUR SETTINGS FILE WAS DAMAGED. THE LAST GOOD COPY WAS RESTORED.` |
| 5b | `system.cfg` writer | `SystemConf::saveSystemConf()` (`es-core/src/SystemConf.cpp:103`) — every cloud toggle flip and every settings page close | reads the file (cannot open → `LOG(LogError)`, **change silently dropped**), edits lines, writes `system.cfg.tmp`, then **copies** it over the live file with `ofstream dst(path, binary); dst << src.rdbuf();` (truncate + write in place), then removes `.tmp`. No fsync, no rename. Kill between `dst` open and completion → truncated `system.cfg` with an intact `.tmp` beside it that nothing reads. Start: empty → `confMap` empty → every `get()` is `""` → defaults, no message. Boot `001-setup` → `chksysconfig verify`: `grep a` says binary → `cp .backup system.cfg`; no `system.hostname` → **`rsync -a /usr/config/ /storage/.config`** — reseeds every shipped default (`system.cfg`, `emulationstation/es_settings.cfg`, `retroarch/…`, ~40 dirs) over the player's copies where size/mtime differ. Shutdown `save-sysconfig.service` → `chksysconfig backup` → `cp system.cfg system.cfg.backup` unless `grep a` says binary — an empty or truncated-text file passes and **overwrites the good copy** (#102). | n/a | n/a | n/a | n/a | **no** — `.backup` is "last shutdown's copy", not last-known-good | **P1** | ES: `rename` the `.tmp` instead of copying (one line); OS (#104): validate (`system.hostname` present, parses) before `backup`; `verify` tries `.last-good` before reseeding; scope the rsync to `system.cfg`. |
| 5c | `content-systems` | see 3b | — | — | — | — | — | **no** | P2 | tmp + mv |
| 6 | Capture hook | `FileData::launchGame` exit (`cloud_capture --system … --exit`), `main.cpp:462-474` boot game, `GuiSaveState` DELETE (`--retire` before unlink, `--rescan` after renumber) | non-zero → `LOG(LogWarning) "cloud_capture exited N -- see /var/log/cloud_sync.log and …/capture-failures"` only. Player sees **nothing**; the deletion proceeds regardless. Durable: `last-capture` (one line per mode) and `capture-failures` (last 20) — both tmp+`mv -f`; manifest tmp+`mv -f` with "the previous manifest is intact" on rename failure; content-addressed stage. **No reader in ES** (grep: only a comment in `FileData.cpp:805`). | no | no | no | no | **yes, exemplary** (the manifest is the one writer here that meets D-CLOUD-078 fully) | Silent-at-the-moment is defensible (no network, nothing to choose, the boot `--full` pass re-hashes claimed paths); silent-with-no-durable-surface is the "operation whose only report is transient" anti-pattern with the report removed. **P2** | Read `last-capture` on the CLOUD page (SAVE MANAGEMENT line or a `cloudLastRunDetail`-style fact): `THE LAST GAME'S SAVES WERE RECORDED` / `…COULD NOT BE RECORDED - THEY ARE PICKED UP AT THE NEXT STARTUP.` |
| 7a | Launch gate refusal | `FileData::launchGame:732-736` — a Manual sync running, or `cancelForLaunch` overran its 2 s budget | `YOUR SAVES ARE SYNCING WITH THE CLOUD.\n\nWAIT FOR IT TO FINISH BEFORE STARTING A GAME - THE NOTIFICATION AT THE TOP SAYS WHEN IT IS DONE.` | yes | implicit | yes (wait; where to look) | n/a | n/a | Good. Edge: after an overrun cancel the group has been SIGKILLed, so "WAIT FOR IT TO FINISH" is shown while the card is about to read `SKIPPED - A GAME WAS STARTED`. There is no stop control for a Manual sync, so "wait" is the only honest verb. | P3: `…THE CARD AT THE TOP SAYS WHEN THEY ARE DONE - THEN START THE GAME.` |
| 7b | Cancel card | see 1d | `SKIPPED - A GAME WAS STARTED`; row reads `STOPPED` | yes | no | no | n/a | yes | P3: two words for one event; the stamp cannot distinguish a cancel from an external 130. | third stamp field `cancelled`; row `SKIPPED, A GAME WAS STARTED`. |

Outside #105 but seen while reading: `GuiSaveState` DELETE has **no** `ThreadedCloudSync::isRunning()` check — D-CLOUD-053 says every saves-tree writer refuses while a pass holds `L_T`; it records the deletion (`--retire`) but does not gate it.

## 2. Outcome vocabulary (proposal for `es-native-ui.md`)

Four outcome words, then a why, then what is in place, then how to recover. Every surface uses the same four words; the card and the row differ only in punctuation (the row uses commas because its dash separates the date).

**Outcome words**

| Word | When | Card text (line 2) | Page line 1 | Row token |
|---|---|---|---|---|
| `COMPLETED` | every tier rc 0 (rclone 9 counts as 0) | `COMPLETED` | `COMPLETED` | `COMPLETED` |
| `COMPLETED WITH GAPS - <what>` | some tier/system rc 0 and some not; a match cut after deletions | `COMPLETED WITH GAPS - NES DID NOT FINISH` | `COMPLETED WITH GAPS` | `COMPLETED WITH GAPS` |
| `COULDN'T FINISH - <why>` | nothing succeeded and it is not a sentinel | `COULDN'T FINISH - YOUR CLOUD STOPPED ANSWERING` | `COULDN'T FINISH` | `COULDN'T FINISH, YOUR CLOUD STOPPED ANSWERING` |
| `SKIPPED - <reason>` | **only** 69, 75, and the launch cancel | `SKIPPED - NO NETWORK CONNECTION` / `SKIPPED - ANOTHER CLOUD SYNC IS RUNNING` / `SKIPPED - A GAME WAS STARTED` | same | `SKIPPED, NO NETWORK` / `SKIPPED, ANOTHER SYNC WAS RUNNING` / `SKIPPED, A GAME WAS STARTED` |

`COMPLETED SUCCESSFULLY` and `SUCCEEDED` retire; `FAILED`, `STOPPED` (except as the why `IT WAS STOPPED`) and every log path or exit code retire.

**Why phrases** — from a `>>> why <sentence>` line the scripts print (preferred; replaces the ERROR/FAILED/WARN token filter), else from rc:

| Source | Phrase |
|---|---|
| rclone 3, 4 | `YOUR CLOUD FOLDER WASN'T FOUND` |
| rclone 5 | `YOUR CLOUD STOPPED ANSWERING` |
| rclone 7, 8 | `YOUR CLOUD REFUSED THE TRANSFER` |
| `check_internet` | `YOUR CLOUD DIDN'T ANSWER. ITS SIGN-IN MAY HAVE EXPIRED` |
| `cloud_saves_root` | `THE SAVES FOLDER IS ON A DIFFERENT CARD` |
| 130 not a launch cancel | `IT WAS STOPPED` |
| 1, 2, -1, other | `SOMETHING WENT WRONG` |

**In-place phrases** (one per verb; true because rclone renames on completion and the content scripts never delete outside a match):

| Verb | Partial | Nothing moved |
|---|---|---|
| back up | `WHAT WAS SENT IS IN YOUR CLOUD. THE REST IS STILL ON THIS DEVICE.` | `NOTHING WAS SENT. YOUR CLOUD IS AS IT WAS.` |
| restore | `WHAT ARRIVED IS ON THIS DEVICE. THE REST IS AS IT WAS.` | `NOTHING ARRIVED. THIS DEVICE IS AS IT WAS.` |
| sync (saves) | `THE SAVES THAT MOVED ARE ON BOTH SIDES. THE REST ARE AS THEY WERE.` | `YOUR SAVES ARE AS THEY WERE.` |
| match | `N FILES WERE REMOVED FROM THIS DEVICE. YOUR CLOUD STILL HAS THEM.` | `NOTHING WAS REMOVED.` |
| settings restore | (only after the backuptool staging fix) `YOUR SETTINGS ARE AS THEY WERE.` | same |

**Recovery phrases**

| Surface | Phrase |
|---|---|
| Page | the button: `TRY AGAIN` |
| Card, Manual origin | `TRY AGAIN: GAME SETTINGS > BACK UP SAVES TO THE CLOUD` (or `RESTORE SAVES FROM THE CLOUD` / `SYNC SAVES WITH THE CLOUD`, by `mCommand`) |
| Card, Exit origin | `IT RUNS AGAIN WHEN YOU EXIT A GAME` |
| Card, Startup origin | `IT RUNS AGAIN AT THE NEXT STARTUP, OR UNDER GAME SETTINGS > SYNC SAVES WITH THE CLOUD` |
| no network | `TRY AGAIN WHEN YOU'RE ONLINE.` |
| lock held | `WAIT FOR IT TO FINISH, THEN TRY AGAIN.` |
| game started | `YOUR SAVES ARE SENT WHEN YOU EXIT THE GAME.` |

**Card layout** (title / text / action — the action row already exists and is blank): title `BACK UP SAVES`; text `COULDN'T FINISH - YOUR CLOUD STOPPED ANSWERING`; action `NOTHING WAS SENT · TRY AGAIN: GAME SETTINGS > BACK UP SAVES TO THE CLOUD`. Widths at 640 px must be measured in frames (the action row is `TextSmall`); if the action line clips, drop the in-place clause first — the outcome word already implies it for `COULDN'T FINISH`.

**Page done state**, seven lines unchanged (D-UI-024/026):
1. outcome word
2. `2 OF 4 ITEMS DID NOT FINISH` on gaps; blank otherwise
3. summary as today (`12 FILES · 3.4 MB BACKED UP` / `NOTHING NEW TO SEND …`)
4. the items that did not finish and why: `NES, SETTINGS - YOUR CLOUD STOPPED ANSWERING`
5. in-place phrase (this row is free on any non-zero exit: the gamelists note is rc-0-only)
6. `ELAPSED m:ss`
7. `A  TRY AGAIN     B  CLOSE` when not `COMPLETED`; `PRESS ANY BUTTON TO CLOSE` when it is

**Where TRY AGAIN fits without changing geometry.** Not a button bar — the page has none and D-UI-024 fixes the seven rows. Line 7 (`mFooter`) is already the prompt row and becomes the two-key legend; the window's help bar (`getHelpPrompts`, drawn outside the panel by `Window`) gains `("a", TRY AGAIN)` beside the existing `("b", CLOSE)`. `GuiCloudTransfer::input()` routes `a` → join the finished thread, reset the counters (extract the constructor's initialiser list into a `reset()`), `mFinished=false`, start `threadRun()` again with the same `mCommand`; any other button → close as today. `mCommand` is a plain `std::string` member, so re-running needs nothing new. Caveat: a backup command containing `backuptool backup` re-archives on retry (rotation keeps 3) — acceptable; stripping completed tiers is not worth the complexity.

**Row lines** (D-UI-023 two lines): `LAST 09/10/2026 07:41  -  COULDN'T FINISH, YOUR CLOUD STOPPED ANSWERING`. Needs the stamp to carry a third field: `<epoch> <rc> <why-token>` — additive, both readers split on space and read `[0]`,`[1]`.

## 3. Prioritised fix list

**P1 — leaves an empty/half state or shows nothing (or a falsehood) on failure**

1. **Picker ignores `--scan`'s exit code** — `GuiMenu.cpp` `cloudContentSystemPicker` (single-arg `executeScriptLegacy`). Use the pair form; branch on 69 / other non-0 as in the table. *VM frame:* backend down (or link cut) then CONTINUE with ROMS AND BIOS ticked — before: the `NO SYSTEM IN YOUR CLOUD…` box; after: `COULDN'T REACH YOUR CLOUD`. 1280×800 and 640×480.
2. **`.cloud-journey-pending` written before success, consumed on display** — `GuiMenu.cpp:4258` (`touch` before `run`), `main.cpp:799-808`; `backuptool` restore branch (~250-262). backuptool sets the marker after its verified extract (a flag from ES); `main.cpp` removes it on YES/LATER. *VM:* settings restore with the backend down → reboot → no `YOUR SETTINGS WERE RESTORED` prompt (before: the false prompt).
3. **Settings files rewritten in place, no last-good** — `es-core/src/Settings.cpp:458` (`doc.save_file`), `es-core/src/SystemConf.cpp:186-206` (tmp then copy-over), `chksysconfig` backup/verify, `save-sysconfig.service`, `001-setup:27`. ES half: tmp+fsync+rename, `.last-good` after a good parse, restore-from-last-good at start with a one-time message. OS half → #104. *VM:* `truncate -s 0 …/system.cfg` and `head -c 200` on `es_settings.cfg`, reboot, settings survive; scripted switch flip + `kill -9` of ES in a loop, file always parses. Also diff `/storage/.config` before/after a `verify` reseed to size the rsync blast radius.
4. **Match cut by link loss reports a blank SKIPPED over real deletions** — `cloud_content_restore` `stop_no_network` (exits before `>>> removed`, :633), `GuiCloudTransfer::update` done branch. *VM:* link-loss fixture mid `--match --apply` with two systems.
5. **Partial transfer run reported as the last tier's code** — `cloudOpenTransfer` `add` lambda, `GuiCloudTransfer::update:454-458`, `input():177` (rescan gated on rc 0). `>>> tier <label>|<rc>` + `COMPLETED WITH GAPS`. *VM:* `tools/cloud-test-backend` serving content but refusing the saves path.
6. **`backuptool restore` extracts over the live tree with no staging and no copy of what it replaces** — `backuptool` ~215-262. Stage, move dirs into place, take a `backuptool backup` first as the last-good, reword `fail` texts (`Check 'logger' output` → `YOUR SETTINGS ARE AS THEY WERE. TRY AGAIN…`). *VM:* kill during extract; config intact.

**P2 — no recovery path, or developer-facing**

7. Card outcome `FAILED - SEE /var/log/cloud_sync.log` and the token filter — `ThreadedCloudSync.cpp:119-126, 218-223`; use the blank action line; `>>> why` from the scripts (`check_internet`, `network_lost_during_run`, `cloud_saves_root`, `report_rclone_error`). *VM:* expired-token fixture → card shows the sign-in sentence.
8. Transfer page: no TRY AGAIN, blank summary on failure — `GuiCloudTransfer.cpp` `update/input/getHelpPrompts` (§2 design). *VM:* backend down at start → `COULDN'T FINISH` + `A TRY AGAIN`; press A, re-runs.
9. `content-systems` written non-atomically — `cloud_content_restore:850`. *Check:* `--set-systems` under `strace -e trace=rename` shows the rename.
10. Capture failures have no reader — `FileData.cpp:806-818`, `GuiSaveState.cpp:238-275`, `main.cpp:462-474`; add a `last-capture` reader on the CLOUD page. *VM:* make the stage dir read-only, exit a game, open CLOUD.
11. Seed-folders done page drops `MISSING`, says COMPLETE on error/timeout — `cloudSetupBuildDoneStep`. *VM:* backend down after `--check` passes (or `timeout 1`).
12. Match preview: `No systems are selected…` shown as `THIS DEVICE ALREADY MATCHES YOUR CLOUD`; refusal explanations and the 69 cause dropped — `cloudOpenMatch`. *VM:* empty selection file, press MATCH.
13. TIDY preview offers MOVE over `REFUSING:` or a timed-out partial list; apply has no stamp — `cloudPreviewTidyFolders`, `Origin::None`. *VM:* pre-create the destination folder, press TIDY.
14. Journey continuation and settings restore run in a `/usr/bin/run` console with raw text and exit codes; nothing returns to ES — `main.cpp:806`, `GuiMenu.cpp:4259`. Route the content+saves half through `GuiCloudTransfer` (same command shape); reword backuptool. *VM:* journey YES with backend down.
15. Scripts' stdout carries exit codes: `Backup of ${DIR} failed (rclone exit ${RC}).` (`cloud_content_backup:496`, `cloud_content_restore:947`), `The network went away while … (rclone exit N). Stopped.` (:134/:141), `report_rclone_error` family (`cloud_backup:637-647`, `cloud_restore:656-666`). Code stays in the log line only. *Check:* grep the page/card protocol output in the link-loss harness for `exit [0-9]`.

**P3 — wording consistency**

16. `COMPLETED SUCCESSFULLY` → `COMPLETED` (card, page); row `SUCCEEDED` → `COMPLETED`.
17. rc 130: row `STOPPED` vs card `SKIPPED - A GAME WAS STARTED`; a 130 ES did not cause reads `FAILED - SEE…` on the card and `STOPPED` on the page — third stamp field.
18. `last-sync-manual` written, never read; SYNC SAVES WITH THE CLOUD carries a static line where D-UI-023 wants how it last went.
19. D-UI-022 drift: `RESTORE SYSTEM SETTINGS FIRST, THEN REBOOT? … YOUR EXISTING CONFIGURATION IS REPLACED` (system settings, configuration); `FINALIZE RESTORE` vs `FINISH RESTORE SETUP`; `Working...` lowercase; `SET UP A PROVIDER WITH RCLONE…` names the tool.
20. Launch-gate text after an overrun cancel (`WAIT FOR IT TO FINISH` while the card is about to say SKIPPED).

## 4. Not determinable from reading

- Whether `SystemConf`'s `.tmp` is on disk long enough to matter, and what a `kill -9` mid-`saveFile` leaves in practice (pugixml writes in one `fwrite` burst; the window is small but real) — needs the VM kill loop in fix 3.
- The real blast radius of `chksysconfig verify`'s `rsync -a /usr/config/ /storage/.config` on a populated device (which of the ~40 shipped trees differ by size/mtime and get overwritten) — needs the before/after diff.
- Whether `/usr/bin/run`'s odd failure path (it re-executes `"$*"` as a single word on non-zero exit, which fails with "not found") flashes anything readable before the UI returns.
- `GuiLoading` timeout behaviour for the picker: rclone is bounded by the scripts' net opts, but `--scan` also runs `find`/`stat` over the whole ROM tree; duration on a full card unknown.
- Rendered widths of every proposed string at 640×480 in the shipped theme (card text and action rows, page lines 4/5/7) — frames only.
- `cloud_sync_helper`'s `cloud_sync.conf` rewrite and `cloud_setup --set-syncpath`'s `sed -i` are in place; whether a torn `cloud_sync.conf` is ever produced in practice was not tested.