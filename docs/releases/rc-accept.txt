# Findings tools/rc-preflight may pass over for the current release candidate, one per
# line: <kind> <item> <decided register ID> -- <why>. A line whose ID is not a decided
# row accepts nothing. Kinds: package, base, bug. Rewritten for every candidate
# (release-candidates.md step 0, #271).
package zip D-WORKFLOW-024 -- Info-ZIP 3.0 (2008) is the final release; the recipe states the pin
package raofflineproxy-rcheevos D-RA-029 -- follows the third_party/rcheevos submodule commit RAOfflineProxy 0711f0b names (1433173)
bug #50 D-QA-050 -- fixed in code; two LAN-only facts owed (code trace 2026-09-25)
bug #79 D-QA-050 -- every entry with a found cause fixed; entry 1 has no cause on record
bug #113 D-QA-050 -- the automatic sync bounded; a Dropbox read and a backend mode owed
bug #169 D-QA-050 -- eight scripts and gopher64 held back or blanked; a PSP title owed
bug #170 D-QA-050 -- one Token line on the seed and on an upgrade; a PS2 device owed
bug #177 D-QA-050 -- every logged command line masked; a keyboard-driven observation owed
bug #178 D-QA-050 -- the flush and the WARNING mirror in the pin; a reboot-after-WARNING run owed
bug #239 D-QA-050 -- the VM's own (D-QA-046); the HideWindow lead
bug #249 D-QA-050 -- every manager launch path sets the slot; a pad press owed
bug #275 D-QA-050 -- upstream's netplay path, outside the lanes (D-UI-079); the maintainer's call
bug #198 D-QA-050 -- fixed in the candidate (ES 698f3956b0, 459fc168f); closes on vm-qa's quoting suite over the rebuild
bug #221 D-QA-050 -- fixed in the candidate (de0be5fecd); closes on vm-qa's scripts suite over the rebuild
bug #274 D-QA-050 -- fixed in the candidate (e07a5db069); closes on vm-qa's scripts suite over the rebuild
