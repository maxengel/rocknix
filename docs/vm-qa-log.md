# VM QA log

One row per VM test cycle, so the next cycle starts from the last one rather
than from zero. The ritual that fills it is `generic-x64-vm-testing.md`
§ "After every VM cycle"; the narrative stays in the work logs. A cycle is a
built image driven in the GENERIC_X64 VM against a fixture, with frames read
and the endpoint inspected.

| Date | Scope | Image(s) | What the VM found | What it could not prove | Left behind (tool / walk / rule) |
| --- | --- | --- | --- | --- | --- |
| 2026-09-06 | #76 scraped content switch, systems pages per direction (D-CLOUD-048) | GENERIC_X64 ×4, `next` `44737b76ba`→`2b2a8d385f` | Busybox has no `comm` (counts read 0); `MEDIA_EXCLUDES` passed to neither copy (media uploaded with the switch off; blindspot 30); the switch row wrapped to three lines (D-UI-023) | A restored-then-scraped real library (343 MB SNES media on the RG35XX SP); a real provider | `generic-x64-vm --headless/--daemonize`, `tools/vm-serial`, `vm-visual-qa` stdlib PNG, `cloud-test-backend seed-content`/`seed-device`, `tools/vm-walks/` (wake, to-manage-cloud-storage, systems-to-back-up, systems-to-restore, toggle-scraped-content); rule §§ Headless / Driving ES / busybox / Fixtures / After every cycle |
| 2026-09-06 | #77 game content as a class; CONTINUE opens the systems page; the game list moves with game content (D-CLOUD-049/050) | GENERIC_X64 ×2, `next` `91b8c62ca4`→`0803aa195c` | `BACKING UP SAVES AND ROMS AND BIOS` (a class name containing AND cannot be joined by AND); a backup never carried BIOS once the picker stopped listing it (restore had the union, backup did not) | The RG35XX SP's real library under ROMS AND BIOS alone; a real provider | First cycle run entirely on the previous cycle's tools: `run --headless --daemonize`, `vm-serial wait/script`, `seed-content`/`seed-device`, composed walks — nothing rediscovered. New walks for the CONTINUE flow (`back-up-page`, `restore-page`, `tick-roms`, `tick-game-content`, `continue-to-systems`, `run-transfer`) |
| 2026-09-06 | The transfer flow after the maintainer's first real backup (content page, transfer page, saves-only) | GENERIC_X64 ×3, `next` `58be925a5d`→`088c2bd22e` | rclone's `name:100%` (no space) put the whole line on the name row and `S` as the file; its 80-column cut tore the last field; U+2026 was stripped from names; `BusyComponent::setText("")` is a no-op so WORKING… showed twice; `cloud_content_backup` announced no units so SETTINGS BACKUP showed through the ROMs; every saves label ran the settings phase; **and my own SELECT ALL button rebuilt the bar from inside its callback and killed ES** (black frame) — then came back unfocused | The maintainer's own long ROM names on Dropbox at real speeds | `CLOUD_QA_BWLIMIT` on the backend (a slow-transfer fixture); `MenuComponent::setCursorToButton`; rules: ES § "Never rebuild a button bar from inside one of its buttons", § "What rclone's piped progress actually looks like"; rclone § "A saves label runs `--saves-only`"; a filmed walk (`w-review`: every 2 s through a transfer) |
| 2026-09-06 | Gate 0 (#35): `tools/cloud-round-trip` run for the first time, repaired, passing on WebDAV and MinIO | GENERIC_X64 pair (`tools/vm-pair`) on `088c2bd22e`, no rebuild — `--path-prefix` carried the fixed script | Seven harness checks behind decided behaviour (an undated archive name the uploader dates; a fixture system ES does not declare; a ROMs-only game-list step; `put` with one argument, twice; a `--selected` restore with no selection; a six-field scan read by its tail) and one script defect: `backuptool` never read `SETTINGS_BACKUPS`. Also: `rclone serve`'s one-second listing cache; busybox `touch -d` takes no `@epoch` | The old-image branch of the key handling (no image without the rename on hand); a real provider | `tools/vm-pair`; `cloud-test-backend cat`, and `put`/`reset` waiting out the cache; `cloud-round-trip --path-prefix`; the migration step; rule § "Two guests, one cloud" |

| 2026-09-07 | #35 task 6: the two-device fixtures (A1–A14, the discarded-saves reader, the retention kill) and #9's contract runner under `copy --files-from` and `bisync` | GENERIC_X64 pair on `088c2bd22e`, no rebuild — `--path-prefix` carried the fixed `backuptool` and `cloud_setup` | Every writer of the saves tree resolves a both-changed pair by recency and reports nothing (A1: fourteen checks, blindspot 28 in full); a renumbered file keeps its mtime so `--update` skips it, the delete is undone, and one version ends up nowhere (A9); `cloud_setup`'s folder probe refused every existing folder with subfolders and ES discarded the refusal (A3's setup); the QA WebDAV's modtime is the upload time and it offers no hashes, MinIO keeps both; `rclone serve` keeps a killed PUT's partial file, MinIO does not; `rclone cat` of a missing S3 key exits 0 with nothing (a false PASS on A14, blindspot 31); bisync aborts a one-file tree's change as "all files changed", refuses `--files-from` beside `--filters-file`, leaves a lock after a kill that blocks every later run, and demands `--resync` without its listings | The H700's budget (item 9 is VM seconds); a real provider; every fixture's green side, which waits for #21/#22 | `cloud-round-trip --second-port`/`--only`/`--transport`/`--bisync-flags`, `Pair`, `WRITERS`, `APPLY`, `epath()`, ControlMaster; `cloud-test-backend cat` fails on a missing key; rules: VM § the pair through the harness, rclone § what the QA backends compare by; blindspot 31; D-CLOUD-051 (open) |
| 2026-09-07 | Gate 11 (#9): the ten contract items under `copy --files-from` and `bisync`, plain and with `--compare`/`--download-hash` variants, WebDAV and MinIO | GENERIC_X64 pair on `088c2bd22e`, no rebuild | bisync download-compares a both-changed pair on a hashless backend ("check --download for safety") and skips it when equal, so an outside resolution needs no `--resync`; `--files-from` cannot sit beside `--filters-file`, so a decided run drops the allowlist; a `kill -9` leaves `*.lck` and every later run refuses (`--max-lock`, minimum 2 m, is the shipped remedy); recovery on a torn head renames `.conflict1`/`.conflict2` on both sides; `--resync` is `--resync-mode path1`; without its listings bisync demands `--resync` on both backends; a same-size, same-mtime change is invisible to bisync on WebDAV without `--download-hash`, which downloads the whole tree every pass; `rclone serve` keeps a killed PUT as a file, MinIO keeps nothing; the copy stand-in fetches every remote file to hash it (its budget, not the design's) | The H700's seconds (item 9 is VM time); a real provider's hash type and whether a real WebDAV server keeps a partial PUT; the reconciler's own budget (Gates 4 and 12) | `cloud-round-trip --dump`, `moved:` names, the raw conflict shape, every-spawn budget; rclone rule § bisync facts extended; D-CLOUD-052, D-CLOUD-044 settled |
| 2026-09-07 | The match flow's four notes (dialog width, the done page's removal summary, the game-list note, the exit-sync hold) and #78's folder-refusal dialog | GENERIC_X64 `6adfb524ea` (ES `328593849d`), one image | The confirmation at 0.8 width reads as lines; the done page reads REMOVED 2 FILES FROM THIS DEVICE · 224.00 KB with the per-system line and UPDATE GAMELISTS UNDER GAME SETTINGS TO SEE THE CHANGE; the refusal dialog appears and the setting stays. Found: "1 FILES"; the preview's sync line hardcoded 0 bytes so the two screens disagreed (78 KB vs 224 KB); `GetShOutput` glued the script's lines and carried rclone's raw error; a walk replayed after another starts one screen out of phase (a PICO-8 list); the CLOUD FOLDER editor's field showed no text where the cursor said there was some | The 1.5 s hold (a timing, not a frame); a real Dropbox refusal (path-based providers do not refuse); the editor's text on a real panel | `vm-walks/reset.steps`, `match-dialog.steps`, `to-change-cloud-folder.steps`, `confirm-cloud-folder.steps`; plural fix; preview bytes; the line-by-line refusal (next build) |
| 2026-09-09 | QoL tranche: #85 second pass (delta-only picker rows), #95 (item-first transfer page, one shape per phase), #94 (visible startup sync, per-cause stamps, per-mode `last-capture`), #97 (`--res 640x480`) | GENERIC_X64 `d8bc358248` (ES `e521985f2`), `287f4f7d5f` (ES `41b7b8f10`), `ef43f2ce4b` (scripts exit fix); one guest, WebDAV, throttled to 400/200 KB/s for the frames | Picker: `2.9 MB NOT YET IN YOUR CLOUD · 1 FILE` / `NOTHING NEW TO BACK UP`; restore `98 KB NOT YET ON THIS DEVICE · 1 FILE` / `NOTHING NEW TO RESTORE`. Transfer page: `SNES / ITEM 4 OF 5 / TRANSFERRING big.sfc …` then `SETTINGS / ITEM 5 OF 5 / WRITING THE SETTINGS ARCHIVE...`, n refined 3→4 when BIOS came along on restore, done `6 FILES · 24.7 MB BACKED UP`. Boot card `SYNCING SAVES AT STARTUP` at +25 s, `COMPLETED SUCCESSFULLY` at +27 s; CLOUD page `LAST 09/09/2026 15:54 - SUCCEEDED`. 640×480: everything fits after rounding. Found: row 4 clipped on three-decimal fragments (fixed `e7e672524`); `LEFT` never appended, four-byte separator (fixed `94924c835`); rclone's exit 3 read as the lock sentinel (#99, fixed `28cc392b41`); `wait_lock` spun 4m43s on a stale lock (#98). CAP1–12 and the single-device suite PASSED with the new protocol assertions | The launch gate during a live sync (a launch mid-sync was not driven); "no network" at boot (the backend is on the guest's subnet and the guest keeps an IPv6 default route); the real handheld panel; a real provider's post-upload commit delay | Flag `--res`; walks `tick-settings.steps`, `run-transfer-frames.steps`; rule: restart restores the last game list, `screendump` stale after an ES restart, `vm-serial wait` false-ready, stale settings lock, guest clock lag; README: reboot is the deterministic start |
| 2026-09-09 | #98 (`wait_lock` stale-lock recovery) and #99 (sentinels 75/69 in scripts and EmulationStation) | GENERIC_X64 `d574edf975` (ES `54d4573f7`), one guest, WebDAV | With the flock held over SSH, BACK UP from the hub ended `SKIPPED - ANOTHER CLOUD SYNC IS RUNNING` at once; `tools/wait-lock-test` 21/21 on the guest (stale pid cleared in 6 ms, live holder waited on, killed holder cleared, empty and garbage files stale); harness: `the second sync exited 75`, `exit 69 with no default route`, `a restore with no Saves folder fails with its own code`, protocol assertions, CAP10/CAP12 PASSED | "No network" only via the harness's `ip` shim, not a real link loss; the page's new `SKIPPED - NO NETWORK CONNECTION` branch not seen on screen; the handheld | A test that holds the cloud lock must confirm it free before the next step (23 false failures from an overlapping 90 s holder; busybox `pkill -f` missed the `flock` process) |
| 2026-09-10 | #103 link loss: LINK1-7 fault injection; bounded rclone runs; `cloud_net_ready`; interface off the network; a launch cancels an automatic sync (#101, D-CLOUD-076) | GENERIC_X64 `d574edf975` (guest a) vs `12fd47e341` (guest c, port 10024); WebDAV at 200k | LINK1-7 FAIL on the old image (exit 0 after 46-81 s, stamp 0; 130-156 s with a 120 s outage) and PASS on the new (exit 69 ~30 s after the cut, stamps 69, no partial files, markers untouched, re-runs complete); NETWORK SETTINGS on the new image already filled at 0.3 s; the boot card reads `SYNCING SAVES AT STARTUP`, and two A presses mid-run gave `SYNC SAVES / SKIPPED - A GAME WAS STARTED` over the PICO-8 list, the launch went through (`runemu.sh` ran), stamp `<epoch> 130`, the 8 MB cloud save that had already landed is whole | Same-name re-upload idempotency for the settings archive on WebDAV (`423 Locked`, slirp artifact) -- MinIO or a device; a wedged Wi-Fi driver (the VM has no radio); the handheld | Two-channel LINK cells and `Serial` helper; rule paragraph on cutting the guest's link; walk `launch-during-startup-sync` (kept in the job dir, worth adding to vm-walks with a note that its timing depends on the throttle) |
| 2026-09-10 | #105 tranche A on the VM: the outcome vocabulary on card, page, rows and picker; the settings files' last-good record | GENERIC_X64 `c15050c897` (ES `3aa939eaf`, guest d, port 10025), WebDAV at 200k; remote pointed at a dead port (`10.0.2.2:9011`) for the refused-cloud cases | Page: `COULDN'T FINISH` / `SAVES - YOUR CLOUD DIDN'T ANSWER. ITS SIGN-IN MAY HAVE EXPIRED` / `NOTHING WAS SENT. YOUR CLOUD IS AS IT WAS.` / `A TRY AGAIN B CLOSE`; TRY AGAIN re-ran (two `cloud_backup` runs in the log, stamp rewritten). Card: `BACK UP SAVES` / `COULDN'T FINISH - ...`, 5 s, fades -- and **no action row** (created with the default two-row card; ES fix `eb4148ebc`). Rows: **three lines** at 1280 px with the why appended (D-UI-023; fix D-UI-029). Picker: `cloud_content_restore --scan` **exited 0 against the refused remote** and every system read `NOT YET IN YOUR CLOUD` (fix `2266c73245`; with the fixed script bind-mounted the page shows `COULDN'T READ YOUR CLOUD'S CONTENT. TRY AGAIN.`). `es_settings.cfg` planted torn + reboot: restored from `.backup`, `YOUR SETTINGS FILE WAS DAMAGED. THE LAST GOOD COPY WAS RESTORED.` shown. `system.cfg` truncated + reboot: **not restored** -- `valid()` false on every real file under busybox tr (`[:print:]` read as eight characters), record "damaged too", image defaults reseeded, record then overwritten by ES's save; hostname `localhost.localdomain` because `network-base.service` reads it at 1.7 s, before the repair (fixes `f907e7f526`: byte ranges; `rocknix-sysconfig.service`). Also: NetworkManager/hostnamed rewrote the transient hostname `GENERIC_X64` -> `GENERICX64` while idle (upstream, filed). | The card's action row, the one-line rows, the dialog's why, the sysinit verify and the hostname after a damaged boot -- all need the `854989a639` build; 640x480 not yet framed | `tools/cloud-round-trip` step "a scan the cloud refused..."; `tools/last-good-scripts-test` busybox shims for tr/head/wc/cut/awk + real-file fixtures, `BASE_REF` overridable; walk `card-1s-frames.steps` (job tmp); rules: upgrade-and-install § Verify on a device (tr), engineering-practices § Guards must fail closed (the scan), es-native-ui (card action row), generic-x64-vm-testing (wake first, always) |
| 2026-09-10 | #105 tranche A, second round: the six fixes on `next` | GENERIC_X64 `854989a639` then `70c2ca1af1` (ES `28631cf77`), guest d (10025) at 1280x800 and again at `--res 640x480`; remote at a dead port | On `854989a639`: card with its action row; picker `COULDN'T READ YOUR CLOUD'S CONTENT. TRY AGAIN.` with no bind mount; damaged `system.cfg` + torn `es_settings.cfg` at boot → `rocknix-sysconfig.service` restored the record at 1.75 s before `network-base` read the hostname at 1.85 s, ES showed the DAMAGED dialog — but rows still `NOT DONE ON THIS DEVICE YET` after a run, stamps planted or not (`Utils::FileSystem::exists` caches a miss; ES `28631cf77`). On `70c2ca1af1`, both resolutions: rows `LAST 09/10/2026 00:48 - COULDN'T FINISH` one line; dialog `LAST TIME IT COULDN'T FINISH: YOUR CLOUD DIDN'T ANSWER. ITS SIGN-IN MAY HAVE EXPIRED.`; card three rows (640x480 drops the in-place clause, as designed); transfer page `COULDN'T FINISH` / line 4 / `NOTHING WAS SENT. YOUR CLOUD IS AS IT WAS.` / `A TRY AGAIN B CLOSE`; picker dialog. One more: the SYNC row read the manual backup's outcome (ES stamps `last-sync-manual` for every manual origin) → ES `ad861363d`, pin `ea65ac9bf5`, not yet built. | `COMPLETED WITH GAPS` on screen (no fixture yet); the picker's 69 dialog text (LINK7 proves the exit; the dialog is a sibling branch of the observed one); the KILL cells against these scripts (running) | frames under `/workspace/artifacts/rocknix-images/x64-all-20260910-{c15050c897,854989a639,70c2ca1af1}/shots/`; walks `e-all.steps`, `f-small.steps` (job tmp) |
| 2026-09-10 | #105 harness against tranche A: KILL cells (local), suite, LINK1-7 on the new scripts | GENERIC_X64 `70c2ca1af1`: guest d (KILL3/10/11/12/13/15/16/18, then KILL3/10/11/18 again with the fixed scripts bind-mounted), guest c (suite + LINK1-7); WebDAV at 200k | KILL13 (power cut 0/50/200 ms through ES's writers) PASS with `AtomicFileUtil`; KILL12 PASS (the boot repairs); KILL15/16 PASS. **KILL10/11 FAIL**: `set_setting` lost a last-line key (`sed` `d` before `$a`). **KILL18 FAIL**: tree half restored for good. **KILL3 FAIL**: the cell assumed the old rotate-then-write flow, and the trim went by mtime (A trimmed instead of the oldest). Fixed (`90e18fdb0d`, harness `18eb6ecdd5`): KILL3/10/11/18 PASS, no litter. Suite: the settings-archive step FAILED because its fixture was bytes, not an archive, which the new `cloud_backup` refuses (fixture now a real tar.gz pair of equal size); the scan step "a scan the cloud refused..." PASS; LINK1-7 PASS. Litter scan flagged `cloud_sync.conf.bak`/`-rules.txt.bak` wherever present; the helper takes them every run and the run removes them on completion, so they are the fallback record until then -- the scan now judges against the newest completed run. | KILL1/2/4/5 (endpoint cells) not re-run on the new scripts; KILL6-9/14/17 not implemented; a full-suite PASSED on the final build | `kill-d-local.log`, `kill-d-fixed.log`, `kill-d-fixed2.log`, `suite-c.log` (job tmp); harness edits on next `ffa3b6ecfd` |
| 2026-09-10 | #105 tranche A, final builds: `9847876563` then `7eb713bbd9` (ES `5a3759cde`) on the VM; H700 `7eb713bbd9` built, not staged | GENERIC_X64 `9847876563` (guests c, d), `7eb713bbd9` (guest d); H700 `7eb713bbd9` (`h700-all-20260910-7eb713bbd9`, tar sha `2ead84bc1e6c…`) | A cut-off restore staged by hand (marker naming a real archive, a setting flipped after it): on `9847876563` the sysinit verify ran before `/storage/roms` was bound and said `COULDN'T BE UNDONE` (`shots/interrupted-restore-couldnt-be-undone-before-defer.png`); on `7eb713bbd9` the sysinit run deferred at 1.79 s, the autostart run put the copy back at 3.3 s, the setting read its pre-restore value, and the screen said `YOUR SETTINGS RESTORE WAS INTERRUPTED. YOUR PREVIOUS SETTINGS WERE PUT BACK. TRY THE RESTORE AGAIN.` (`x64-all-20260910-7eb713bbd9/shots/interrupted-restore-put-back-dialog.png`). Suite + LINK1-7 on `9847876563`: one FAIL, the retention step's planted bytes refused as damaged (fixture fixed, `253ce6d8aa`; re-run pending). KILL3/10/11/12/15/16/18 PASS on `7eb713bbd9`, no litter; KILL13's sync control missed because the PUT BACK dialog from the hand-staged test was still up when its walk began; re-run alone: every trial PASS (0/50/200 ms cuts, no-change control, both planted torn shapes). Suite re-run with the real-archive fixture on guest c: PASSED. | KILL1/2/4/5 endpoint cells on the new scripts; the H700 on a handheld (needs the maintainer's yes per device) | artifacts `x64-all-20260910-{9847876563,7eb713bbd9}`, `h700-all-20260910-7eb713bbd9`; logs `suite-c2.log`, `kill-d-final.log`, `kill13-final.log` (job tmp) |
| 2026-09-10 | #107 (Dropbox retries), #108 plain language, #110 provider + check, #111 pass or fail, #112 rows, #106 device name | GENERIC_X64 `ae3d602cf2`, guest d; **and the maintainer's RG SP on Dropbox**, which is where the retry bug was found and measured | The device found what the VM could not: with `--low-level-retries 2`, replacing 12 files on Dropbox failed 6 server-side moves; at 10, none. The QA WebDAV backend has no per-folder write lock, so the harness cannot reproduce it at all -- a gap to note, not to close by pretending. On the VM: `CONNECTED TO WEBDAV` and `CHECK CONNECTION` answer both ways (`WEBDAV ANSWERED. YOU'RE CONNECTED.` / `COULDN'T REACH WEBDAV.` with what to try); `NETWORK NAME GENERIC-X64` sits under `IP ADDRESS` with `HOSTNAME GENERIC_X64` unchanged below; the card reads `COULDN'T FINISH - <why>` over `DON'T WORRY, NOTHING CHANGED. TRY AGAIN FROM GAME SETTINGS > BACK UP SAVES TO THE CLOUD`; no surface can produce `COMPLETED WITH GAPS`. | Dropbox's contention on the VM (no such backend); the scripts' own plain-language pass (in flight); a partial-run fixture for the new pass-or-fail wording | frames `x64-all-20260910-ae3d602cf2/shots/`; the Dropbox measurement is in #107 |
| 2026-09-10 | The three settled decisions: #89 N64 saves, #100 the empty cloud, #92/#117 the exit hotkey | GENERIC_X64 `e5ed60f3df` (guest d) and `20260910` (guest d, bind-mounted scripts) | #89: the guard fires -- shipped rules sent only `n64/save/Libretro Probe 64.eep`, widened rules sent all four saves beside the ROM and left the `.z64`; restore brought back the two deleted. #100: root present with `Saves` gone exits 0 (`There's nothing in your cloud to restore yet.` + `>>> offer create-saves-folder`), whole root gone exits 3; the card then shows `YOUR CLOUD HAS NO SAVES FOLDER YET... CREATE IT NOW, READY FOR YOUR FIRST BACKUP?` and CREATE IT made `Saves/` and `Backups/` in the cloud. #92: double SIGTERM through the shipped `execute_kill` returns 0 with the marker consumed, the same signals without the marker return 1, a zero-byte ROM returns 1, a normal quit returns 0, `killall -9` keeps its 137 branch, and a planted stale marker never survives a launch. **New**: RetroArch's first SIGTERM ends it cleanly in 0.51 s, and its `exit(1)` path does not flush SRAM -- a battery-backed cartridge kept its save through one press and lost it through two (#117), fixed by the debounce (D-LAUNCH-003), proven by driving the shipped function. Full suite PASSED on `e5ed60f3df` including the new N64 fixtures. | The first-SIGTERM behaviour on an H700; EmulationStation's play-count recording through the UI (the launcher was driven directly); the transfer page's own path for the empty-cloud offer (the card carries it) | frames `x64-all-20260910-e5ed60f3df/shots/`; `suite-e5.log` (job tmp) |
| 2026-09-10 | #104: persistent logs, watchdog, crash store, evidence | GENERIC_X64 `aa3df8d178` (vm-pair guest a; kernel 7.1.2 with i6300esb + efi-pstore) | P0: `/var/log` is `/dev/vda2` (a bind of `/storage/.cache/log`), journal 8.0M, `kernel.panic=10 softlockup_panic=1 hung_task_panic=1`, watchdog0 `i6300ESB timer` timeout 15 active, `/var/lib/systemd/pstore -> /storage/.cache/log/pstore`. P1: marker logged before `systemctl reboot` found in `journalctl -b -1`; `es_log.0.txt` present. P2: an err and an info marker logged 75 s before `system_reset` both in `-b -1`. P3: `echo c > /proc/sysrq-trigger`, guest back in 22 s; dump archived to `/storage/.cache/log/pstore/1789073135/001/dmesg.txt` (`Kernel panic - not syncing: sysrq triggered crash`) by the 60 s fallback after the boot-time service was skipped (unmet condition on UEFI). P4: PID 1 frozen via the v1 freezer at 16:52:00, guest gone at +17 s, back at +23 s, previous boot ends on an sshd line with zero shutdown lines, watchdog re-armed. P5: timer pages at boot+60 s and +5 min, `collect` archive 88K, no credential-shaped line in it. P6: `cloud_sync.log` 1,848,894 → 525,146 bytes. Suite: 27 steps, **PASSED**. | H700 DRAM retention of ramoops across the SoC reset (device, needs a yes); softlockup/hung-task detectors firing (no userspace way to provoke); RT-loop starvation does NOT work on this kernel (fair server) -- use the freezer | `x64-all-20260910-aa3df8d178/evidence-proofs.log`, `suite.log`; H700 `h700-all-20260910-aa3df8d178/` tar sha `d02358219cd5...` |
| 2026-09-10 | #114 console hops → transfer page; #115 small-panel cleanups; #109 RetroAchievements offer; #112/#111 frames | GENERIC_X64 `073929659d` (ES `5443c8f95`; vm-pair guest a at 1280x800, guest b relaunched with `--res 640x480`) | Journey YES opens `RESTORING FROM THE CLOUD`; against an empty cloud: `COULDN'T FINISH / 1 OF 3 ITEMS DID NOT FINISH / EVERYTHING - COULDN'T FIND YOUR CLOUD FOLDER / DON'T WORRY, NOTHING CHANGED. / A TRY AGAIN B CLOSE`. Settings restore: RESTORE FROM THE CLOUD with SETTINGS ticked → `RESTORE SYSTEM SETTINGS FIRST, THEN RESTART?` → page runs `cloud_restore --system-only` then `backuptool restore --then-cloud --no-restart` → `COMPLETED / 1 FILE · 16.5 MB RESTORED / THIS DEVICE RESTARTS SO YOUR RESTORED SETTINGS TAKE EFFECT. / PRESS ANY BUTTON TO RESTART`; device stayed up (158-200 s) until a button, then restarted into the journey dialog (both guests). With no archive: `SETTINGS - THERE'S NO SETTINGS BACKUP ON THIS DEVICE YET`, nothing restarted. Every dialog reads `YES  CHOOSE` once; 640x480 dialogs have buttons clear of the text (#48). RA offer on a username with no password at both sizes; YES opens FINISH RESTORE SETUP. Hub: `THE FOLDER IN YOUR CLOUD THAT HOLDS YOUR SAVES. CURRENT: /ROCKNIX/Saves`. Failed exit backup card at 640x480: `COULDN'T FINISH - YOUR CLOUD STOPPED ANSWERING` whole. Rows after a game: `AFTER YOUR LAST GAME - COMPLETED` / `- COULDN'T FINISH`. Suite: 27 steps, **PASSED**. | Launch gate's *stopping* wording (needs a cancel that overruns its budget); a why long enough to need the card's shorter forms; the H700 device proofs (ramoops retention, first-SIGTERM behaviour) | `x64-all-20260910-073929659d/shots/{1280x800,640x480}/`, `suite.log`; H700 `h700-all-20260910-073929659d/` tar sha `712761b58d4b...` |
| 2026-09-10 | #118 wizard wording, #119 maintenance rows, #117 exit cell, #120 runner | GENERIC_X64 `7a90be59fb` (vm-pair, guest b relaunched at 640x480) | `tools/vm-qa` from a fresh pair: scripts PASS 3 s, round-trip PASS 107 s, exit PASS 23 s, walks 588 s (13 walks), one report. #118 at 640x480: provider list titled CONNECT CLOUD STORAGE, WebDAV form with `CONNECTION NAME` and `CONNECT`, FINISH RESTORE SETUP's `CHECK CONNECTION - CONFIRMS YOUR CLOUD STORAGE STILL SIGNS IN AFTER THE RESTORE.` and its result `YOUR CLOUD IS ANSWERING.`. #119 at 640x480: RESET MEDNAFEN → spinner → `MEDNAFEN CONFIG RESET TO DEFAULT.`; RESET RETROARCH with the defaults hidden → `COULDN'T FINISH - THIS BUILD DOESN'T HAVE THE DEFAULT SETTINGS TO PUT BACK, SO NOTHING WAS CHANGED.`; AUDIO RESET under a live ES: pipewire 2→0, ES up, `AUDIO SETTINGS RESET. THIS DEVICE RESTARTS SO THEY TAKE EFFECT.`, OK restarted, back in 8 s with pipewire 2; FACTORY RESET under a live ES: `THIS DEVICE HAS BEEN RESET. IT RESTARTS NOW.` drawn over the emptied tree, OK restarted, guest back fresh (config re-seeded, rclone.conf and SSH key gone). No console anywhere. | the SSH-path pages of the wizard (the glossed step 2, which the agent measured wraps at 480 px like its neighbours) were not reached -- key timing under a host build derailed three walks | `x64-all-20260910-7a90be59fb/shots/640x480/` (11 frames), `vm-qa-report.md`; H700 `7a90be59fb` superseded by `d2eabe879c` |
| 2026-09-11 | `d2eabe879c`: the CloudText extraction and the unit-test binary (#120), on top of #117/#118/#119 | GENERIC_X64 `d2eabe879c` (vm-pair from the image, unattended) | `tools/vm-qa`: scripts PASS 3 s, round-trip PASS 101 s (27 steps, gate on), exit PASS 23 s, walks PASS 583 s (13 walks). Behaviour of the extracted code unchanged on the surfaces the suite and the exit cell touch. | the walks' frames are kept, not compared (#120 box 3); LINK cells not run under this image (run under `7a90be59fb`'s scripts, which are the same) | `x64-all-20260910-d2eabe879c/vm-qa-report.md`; H700 `h700-all-20260910-d2eabe879c/` tar sha `cc5bf7c1604a...` |
| 2026-09-11 | `88b82d94a7`: the credentials-and-rules pass (#116 #52 #71 #39 #74 #100) | GENERIC_X64 `88b82d94a7` (vm-pair from the image, unattended; guest b relaunched at 640x480 for frames) | `tools/vm-qa`: scripts PASS 4 s (incl. bwrap case f: rclone.conf held back, scanner reports `pass =`), round-trip PASS 129 s (31 steps: the four new ones -- user rule above the catch-all keeps its file off the cloud; folder change writes siblings, no nesting warning, archive under `/QA-Custom/Backups/GENERIC-X64/`, migration check calls the layout current; bare RCLONEOPTS keeps the allowlist for backup and restore, a user's own filter is honoured; empty cloud offers, wrong root fails), exit PASS 24 s, walks PASS. Frames at 640x480: FINISH RESTORE SETUP's CHECK CONNECTION with no `rclone.conf` → `NO CLOUD STORAGE IS SET UP ON THIS DEVICE YET. SET IT UP NOW?`. Before the fix, the shipped `cloud_backup` with a bare RCLONEOPTS sent a planted non-save to the cloud (1 copy). | — (the SSH setup page's checkmark and its password page were framed after this row was written: `ssh-setup-step-1-checkmark-from-password-set.png`, `ssh-password-page-read-in-process.png`; the wizard's last row reads `USE A COMPUTER INSTEAD - RUN RCLONE'S SETUP OVER SSH. FOR CONNECTIONS THIS PAGE CAN'T MAKE.`) | `x64-all-20260911-88b82d94a7/vm-qa-report.md`, `shots/640x480/`; H700 `h700-all-20260911-88b82d94a7/` tar sha `aeaa94be2834...` |
| 2026-09-11 | `9d035dfd57`: #121 powerstate, #122 laptop_mode, #126 the skipped-phase words | GENERIC_X64 `9d035dfd57` (guest c, :10025, by hand from the image) | Second boot: one `powerstate` line (`Started`), 0 `laptop_mode`, 0 sysctl errors (the previous image had 4731 such lines in one boot). Upgrade half: the line appended by hand, `/usr/share/post-update` run, gone, `boot.log` says so. `cloud_backup --system-only` with nothing local: `Skipped settings this time ...` / `Game saves: SKIPPED - SETTINGS ONLY` / `Settings backup: SKIPPED - NOTHING TO SEND YET`, exit 0, stamp untouched; with a real archive COMPLETED and stamp `<epoch> 0`; `--saves-only`: `Settings backup: SKIPPED - SAVES ONLY`. First boot still had two `awk: system.cfg` lines from powerstate racing userconfig-setup -> `79729ba204`. Evidence `x64-all-20260911-9d035dfd57/guest-c-evidence.md`. |
| 2026-09-11 | `757ca87084`: #123 field labels, #127 near-name offer, #121 unit ordering | GENERIC_X64 `757ca87084` (guest c recreated at 640x480) | First boot: one `powerstate` line. Startup sync against `/QA-Near/Savez` beside `/QA-Near/Saves`: `last-restore <epoch> 0` and the three-button dialog (`shots/640x480/offer-near-name-dialog.png`); CHANGE FOLDER opens the CLOUD FOLDER editor pre-filled (`offer-change-folder-opens-editor.png`). Forms: `wizard-webdav-form-field-labels.png` (SERVER ADDRESS, SERVER TYPE, USERNAME, PASSWORD, ACCESS TOKEN), `wizard-sftp-form-field-labels.png`, `wizard-s3-form-field-labels{,-scrolled}.png`. Found: the S3 subtitle paragraph (#128, fixed as ES `229f50ad4`, pinned `60d784c850`). Lesson: in the on-screen keyboard `z` types a letter; `esc` cancels. `tools/vm-qa` not yet run on this image -- the pair is held by the #124/#125 agents. |
