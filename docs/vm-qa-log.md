# VM QA log

One row per VM test cycle, so the next cycle starts from the last one rather
than from zero. The ritual that fills it is `generic-x64-vm-testing.md`
§ "After every VM cycle"; the narrative stays in the work logs. A cycle is a
built image driven in the GENERIC_X64 VM against a fixture, with frames read
and the endpoint inspected.

The last three columns are `tools/time-to-play`'s headline numbers for that
image, in seconds, as the median of the run's repeats (#135, D-CLOUD-098):
the press on a game to the game's first frame; `execute_kill` to the exit
sync's `last-sync-exit` stamp; and `execute_kill` to the NEXT game's first
frame with the exit sync in flight. `tools/vm-qa` runs the cell's bounded
form on every image and writes its own table at the foot of the run's
report, where the startup sync, the offline benchmark (D-CLOUD-112) and the
bounds (D-CLOUD-111) are too. **No budget row exists yet**, so a number here
is a record, not a gate; the rows are proposed from the measurement and the
maintainer sets them (D-CLOUD-116).

| Date | Scope | Image(s) | What the VM found | What it could not prove | Left behind (tool / walk / rule) | UI to game (s) | Exit sync (s) | Game to game (s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-06 | #76 scraped content switch, systems pages per direction (D-CLOUD-048) | GENERIC_X64 ×4, `next` `44737b76ba`→`2b2a8d385f` | Busybox has no `comm` (counts read 0); `MEDIA_EXCLUDES` passed to neither copy (media uploaded with the switch off; blindspot 30); the switch row wrapped to three lines (D-UI-023) | A restored-then-scraped real library (343 MB SNES media on the RG35XX SP); a real provider | `generic-x64-vm --headless/--daemonize`, `tools/vm-serial`, `vm-visual-qa` stdlib PNG, `cloud-test-backend seed-content`/`seed-device`, `tools/vm-walks/` (wake, to-manage-cloud-storage, systems-to-back-up, systems-to-restore, toggle-scraped-content); rule §§ Headless / Driving ES / busybox / Fixtures / After every cycle | | | |
| 2026-09-06 | #77 game content as a class; CONTINUE opens the systems page; the game list moves with game content (D-CLOUD-049/050) | GENERIC_X64 ×2, `next` `91b8c62ca4`→`0803aa195c` | `BACKING UP SAVES AND ROMS AND BIOS` (a class name containing AND cannot be joined by AND); a backup never carried BIOS once the picker stopped listing it (restore had the union, backup did not) | The RG35XX SP's real library under ROMS AND BIOS alone; a real provider | First cycle run entirely on the previous cycle's tools: `run --headless --daemonize`, `vm-serial wait/script`, `seed-content`/`seed-device`, composed walks — nothing rediscovered. New walks for the CONTINUE flow (`back-up-page`, `restore-page`, `tick-roms`, `tick-game-content`, `continue-to-systems`, `run-transfer`) | | | |
| 2026-09-06 | The transfer flow after the maintainer's first real backup (content page, transfer page, saves-only) | GENERIC_X64 ×3, `next` `58be925a5d`→`088c2bd22e` | rclone's `name:100%` (no space) put the whole line on the name row and `S` as the file; its 80-column cut tore the last field; U+2026 was stripped from names; `BusyComponent::setText("")` is a no-op so WORKING… showed twice; `cloud_content_backup` announced no units so SETTINGS BACKUP showed through the ROMs; every saves label ran the settings phase; **and my own SELECT ALL button rebuilt the bar from inside its callback and killed ES** (black frame) — then came back unfocused | The maintainer's own long ROM names on Dropbox at real speeds | `CLOUD_QA_BWLIMIT` on the backend (a slow-transfer fixture); `MenuComponent::setCursorToButton`; rules: ES § "Never rebuild a button bar from inside one of its buttons", § "What rclone's piped progress actually looks like"; rclone § "A saves label runs `--saves-only`"; a filmed walk (`w-review`: every 2 s through a transfer) | | | |
| 2026-09-06 | Gate 0 (#35): `tools/cloud-round-trip` run for the first time, repaired, passing on WebDAV and MinIO | GENERIC_X64 pair (`tools/vm-pair`) on `088c2bd22e`, no rebuild — `--path-prefix` carried the fixed script | Seven harness checks behind decided behaviour (an undated archive name the uploader dates; a fixture system ES does not declare; a ROMs-only game-list step; `put` with one argument, twice; a `--selected` restore with no selection; a six-field scan read by its tail) and one script defect: `backuptool` never read `SETTINGS_BACKUPS`. Also: `rclone serve`'s one-second listing cache; busybox `touch -d` takes no `@epoch` | The old-image branch of the key handling (no image without the rename on hand); a real provider | `tools/vm-pair`; `cloud-test-backend cat`, and `put`/`reset` waiting out the cache; `cloud-round-trip --path-prefix`; the migration step; rule § "Two guests, one cloud" | | | |

| 2026-09-07 | #35 task 6: the two-device fixtures (A1–A14, the discarded-saves reader, the retention kill) and #9's contract runner under `copy --files-from` and `bisync` | GENERIC_X64 pair on `088c2bd22e`, no rebuild — `--path-prefix` carried the fixed `backuptool` and `cloud_setup` | Every writer of the saves tree resolves a both-changed pair by recency and reports nothing (A1: fourteen checks, blindspot 28 in full); a renumbered file keeps its mtime so `--update` skips it, the delete is undone, and one version ends up nowhere (A9); `cloud_setup`'s folder probe refused every existing folder with subfolders and ES discarded the refusal (A3's setup); the QA WebDAV's modtime is the upload time and it offers no hashes, MinIO keeps both; `rclone serve` keeps a killed PUT's partial file, MinIO does not; `rclone cat` of a missing S3 key exits 0 with nothing (a false PASS on A14, blindspot 31); bisync aborts a one-file tree's change as "all files changed", refuses `--files-from` beside `--filters-file`, leaves a lock after a kill that blocks every later run, and demands `--resync` without its listings | The H700's budget (item 9 is VM seconds); a real provider; every fixture's green side, which waits for #21/#22 | `cloud-round-trip --second-port`/`--only`/`--transport`/`--bisync-flags`, `Pair`, `WRITERS`, `APPLY`, `epath()`, ControlMaster; `cloud-test-backend cat` fails on a missing key; rules: VM § the pair through the harness, rclone § what the QA backends compare by; blindspot 31; D-CLOUD-051 (open) | | | |
| 2026-09-07 | Gate 11 (#9): the ten contract items under `copy --files-from` and `bisync`, plain and with `--compare`/`--download-hash` variants, WebDAV and MinIO | GENERIC_X64 pair on `088c2bd22e`, no rebuild | bisync download-compares a both-changed pair on a hashless backend ("check --download for safety") and skips it when equal, so an outside resolution needs no `--resync`; `--files-from` cannot sit beside `--filters-file`, so a decided run drops the allowlist; a `kill -9` leaves `*.lck` and every later run refuses (`--max-lock`, minimum 2 m, is the shipped remedy); recovery on a torn head renames `.conflict1`/`.conflict2` on both sides; `--resync` is `--resync-mode path1`; without its listings bisync demands `--resync` on both backends; a same-size, same-mtime change is invisible to bisync on WebDAV without `--download-hash`, which downloads the whole tree every pass; `rclone serve` keeps a killed PUT as a file, MinIO keeps nothing; the copy stand-in fetches every remote file to hash it (its budget, not the design's) | The H700's seconds (item 9 is VM time); a real provider's hash type and whether a real WebDAV server keeps a partial PUT; the reconciler's own budget (Gates 4 and 12) | `cloud-round-trip --dump`, `moved:` names, the raw conflict shape, every-spawn budget; rclone rule § bisync facts extended; D-CLOUD-052, D-CLOUD-044 settled | | | |
| 2026-09-07 | The match flow's four notes (dialog width, the done page's removal summary, the game-list note, the exit-sync hold) and #78's folder-refusal dialog | GENERIC_X64 `6adfb524ea` (ES `328593849d`), one image | The confirmation at 0.8 width reads as lines; the done page reads REMOVED 2 FILES FROM THIS DEVICE · 224.00 KB with the per-system line and UPDATE GAMELISTS UNDER GAME SETTINGS TO SEE THE CHANGE; the refusal dialog appears and the setting stays. Found: "1 FILES"; the preview's sync line hardcoded 0 bytes so the two screens disagreed (78 KB vs 224 KB); `GetShOutput` glued the script's lines and carried rclone's raw error; a walk replayed after another starts one screen out of phase (a PICO-8 list); the CLOUD FOLDER editor's field showed no text where the cursor said there was some | The 1.5 s hold (a timing, not a frame); a real Dropbox refusal (path-based providers do not refuse); the editor's text on a real panel | `vm-walks/reset.steps`, `match-dialog.steps`, `to-change-cloud-folder.steps`, `confirm-cloud-folder.steps`; plural fix; preview bytes; the line-by-line refusal (next build) | | | |
| 2026-09-09 | QoL tranche: #85 second pass (delta-only picker rows), #95 (item-first transfer page, one shape per phase), #94 (visible startup sync, per-cause stamps, per-mode `last-capture`), #97 (`--res 640x480`) | GENERIC_X64 `d8bc358248` (ES `e521985f2`), `287f4f7d5f` (ES `41b7b8f10`), `ef43f2ce4b` (scripts exit fix); one guest, WebDAV, throttled to 400/200 KB/s for the frames | Picker: `2.9 MB NOT YET IN YOUR CLOUD · 1 FILE` / `NOTHING NEW TO BACK UP`; restore `98 KB NOT YET ON THIS DEVICE · 1 FILE` / `NOTHING NEW TO RESTORE`. Transfer page: `SNES / ITEM 4 OF 5 / TRANSFERRING big.sfc …` then `SETTINGS / ITEM 5 OF 5 / WRITING THE SETTINGS ARCHIVE...`, n refined 3→4 when BIOS came along on restore, done `6 FILES · 24.7 MB BACKED UP`. Boot card `SYNCING SAVES AT STARTUP` at +25 s, `COMPLETED SUCCESSFULLY` at +27 s; CLOUD page `LAST 09/09/2026 15:54 - SUCCEEDED`. 640×480: everything fits after rounding. Found: row 4 clipped on three-decimal fragments (fixed `e7e672524`); `LEFT` never appended, four-byte separator (fixed `94924c835`); rclone's exit 3 read as the lock sentinel (#99, fixed `28cc392b41`); `wait_lock` spun 4m43s on a stale lock (#98). CAP1–12 and the single-device suite PASSED with the new protocol assertions | The launch gate during a live sync (a launch mid-sync was not driven); "no network" at boot (the backend is on the guest's subnet and the guest keeps an IPv6 default route); the real handheld panel; a real provider's post-upload commit delay | Flag `--res`; walks `tick-settings.steps`, `run-transfer-frames.steps`; rule: restart restores the last game list, `screendump` stale after an ES restart, `vm-serial wait` false-ready, stale settings lock, guest clock lag; README: reboot is the deterministic start | | | |
| 2026-09-09 | #98 (`wait_lock` stale-lock recovery) and #99 (sentinels 75/69 in scripts and EmulationStation) | GENERIC_X64 `d574edf975` (ES `54d4573f7`), one guest, WebDAV | With the flock held over SSH, BACK UP from the hub ended `SKIPPED - ANOTHER CLOUD SYNC IS RUNNING` at once; `tools/wait-lock-test` 21/21 on the guest (stale pid cleared in 6 ms, live holder waited on, killed holder cleared, empty and garbage files stale); harness: `the second sync exited 75`, `exit 69 with no default route`, `a restore with no Saves folder fails with its own code`, protocol assertions, CAP10/CAP12 PASSED | "No network" only via the harness's `ip` shim, not a real link loss; the page's new `SKIPPED - NO NETWORK CONNECTION` branch not seen on screen; the handheld | A test that holds the cloud lock must confirm it free before the next step (23 false failures from an overlapping 90 s holder; busybox `pkill -f` missed the `flock` process) | | | |
| 2026-09-10 | #103 link loss: LINK1-7 fault injection; bounded rclone runs; `cloud_net_ready`; interface off the network; a launch cancels an automatic sync (#101, D-CLOUD-076) | GENERIC_X64 `d574edf975` (guest a) vs `12fd47e341` (guest c, port 10024); WebDAV at 200k | LINK1-7 FAIL on the old image (exit 0 after 46-81 s, stamp 0; 130-156 s with a 120 s outage) and PASS on the new (exit 69 ~30 s after the cut, stamps 69, no partial files, markers untouched, re-runs complete); NETWORK SETTINGS on the new image already filled at 0.3 s; the boot card reads `SYNCING SAVES AT STARTUP`, and two A presses mid-run gave `SYNC SAVES / SKIPPED - A GAME WAS STARTED` over the PICO-8 list, the launch went through (`runemu.sh` ran), stamp `<epoch> 130`, the 8 MB cloud save that had already landed is whole | Same-name re-upload idempotency for the settings archive on WebDAV (`423 Locked`, slirp artifact) -- MinIO or a device; a wedged Wi-Fi driver (the VM has no radio); the handheld | Two-channel LINK cells and `Serial` helper; rule paragraph on cutting the guest's link; walk `launch-during-startup-sync` (kept in the job dir, worth adding to vm-walks with a note that its timing depends on the throttle) | | | |
| 2026-09-10 | #105 tranche A on the VM: the outcome vocabulary on card, page, rows and picker; the settings files' last-good record | GENERIC_X64 `c15050c897` (ES `3aa939eaf`, guest d, port 10025), WebDAV at 200k; remote pointed at a dead port (`10.0.2.2:9011`) for the refused-cloud cases | Page: `COULDN'T FINISH` / `SAVES - YOUR CLOUD DIDN'T ANSWER. ITS SIGN-IN MAY HAVE EXPIRED` / `NOTHING WAS SENT. YOUR CLOUD IS AS IT WAS.` / `A TRY AGAIN B CLOSE`; TRY AGAIN re-ran (two `cloud_backup` runs in the log, stamp rewritten). Card: `BACK UP SAVES` / `COULDN'T FINISH - ...`, 5 s, fades -- and **no action row** (created with the default two-row card; ES fix `eb4148ebc`). Rows: **three lines** at 1280 px with the why appended (D-UI-023; fix D-UI-029). Picker: `cloud_content_restore --scan` **exited 0 against the refused remote** and every system read `NOT YET IN YOUR CLOUD` (fix `2266c73245`; with the fixed script bind-mounted the page shows `COULDN'T READ YOUR CLOUD'S CONTENT. TRY AGAIN.`). `es_settings.cfg` planted torn + reboot: restored from `.backup`, `YOUR SETTINGS FILE WAS DAMAGED. THE LAST GOOD COPY WAS RESTORED.` shown. `system.cfg` truncated + reboot: **not restored** -- `valid()` false on every real file under busybox tr (`[:print:]` read as eight characters), record "damaged too", image defaults reseeded, record then overwritten by ES's save; hostname `localhost.localdomain` because `network-base.service` reads it at 1.7 s, before the repair (fixes `f907e7f526`: byte ranges; `rocknix-sysconfig.service`). Also: NetworkManager/hostnamed rewrote the transient hostname `GENERIC_X64` -> `GENERICX64` while idle (upstream, filed). | The card's action row, the one-line rows, the dialog's why, the sysinit verify and the hostname after a damaged boot -- all need the `854989a639` build; 640x480 not yet framed | `tools/cloud-round-trip` step "a scan the cloud refused..."; `tools/last-good-scripts-test` busybox shims for tr/head/wc/cut/awk + real-file fixtures, `BASE_REF` overridable; walk `card-1s-frames.steps` (job tmp); rules: upgrade-and-install § Verify on a device (tr), engineering-practices § Guards must fail closed (the scan), es-native-ui (card action row), generic-x64-vm-testing (wake first, always) | | | |
| 2026-09-10 | #105 tranche A, second round: the six fixes on `next` | GENERIC_X64 `854989a639` then `70c2ca1af1` (ES `28631cf77`), guest d (10025) at 1280x800 and again at `--res 640x480`; remote at a dead port | On `854989a639`: card with its action row; picker `COULDN'T READ YOUR CLOUD'S CONTENT. TRY AGAIN.` with no bind mount; damaged `system.cfg` + torn `es_settings.cfg` at boot → `rocknix-sysconfig.service` restored the record at 1.75 s before `network-base` read the hostname at 1.85 s, ES showed the DAMAGED dialog — but rows still `NOT DONE ON THIS DEVICE YET` after a run, stamps planted or not (`Utils::FileSystem::exists` caches a miss; ES `28631cf77`). On `70c2ca1af1`, both resolutions: rows `LAST 09/10/2026 00:48 - COULDN'T FINISH` one line; dialog `LAST TIME IT COULDN'T FINISH: YOUR CLOUD DIDN'T ANSWER. ITS SIGN-IN MAY HAVE EXPIRED.`; card three rows (640x480 drops the in-place clause, as designed); transfer page `COULDN'T FINISH` / line 4 / `NOTHING WAS SENT. YOUR CLOUD IS AS IT WAS.` / `A TRY AGAIN B CLOSE`; picker dialog. One more: the SYNC row read the manual backup's outcome (ES stamps `last-sync-manual` for every manual origin) → ES `ad861363d`, pin `ea65ac9bf5`, not yet built. | `COMPLETED WITH GAPS` on screen (no fixture yet); the picker's 69 dialog text (LINK7 proves the exit; the dialog is a sibling branch of the observed one); the KILL cells against these scripts (running) | frames under `/workspace/artifacts/rocknix-images/x64-all-20260910-{c15050c897,854989a639,70c2ca1af1}/shots/`; walks `e-all.steps`, `f-small.steps` (job tmp) | | | |
| 2026-09-10 | #105 harness against tranche A: KILL cells (local), suite, LINK1-7 on the new scripts | GENERIC_X64 `70c2ca1af1`: guest d (KILL3/10/11/12/13/15/16/18, then KILL3/10/11/18 again with the fixed scripts bind-mounted), guest c (suite + LINK1-7); WebDAV at 200k | KILL13 (power cut 0/50/200 ms through ES's writers) PASS with `AtomicFileUtil`; KILL12 PASS (the boot repairs); KILL15/16 PASS. **KILL10/11 FAIL**: `set_setting` lost a last-line key (`sed` `d` before `$a`). **KILL18 FAIL**: tree half restored for good. **KILL3 FAIL**: the cell assumed the old rotate-then-write flow, and the trim went by mtime (A trimmed instead of the oldest). Fixed (`90e18fdb0d`, harness `18eb6ecdd5`): KILL3/10/11/18 PASS, no litter. Suite: the settings-archive step FAILED because its fixture was bytes, not an archive, which the new `cloud_backup` refuses (fixture now a real tar.gz pair of equal size); the scan step "a scan the cloud refused..." PASS; LINK1-7 PASS. Litter scan flagged `cloud_sync.conf.bak`/`-rules.txt.bak` wherever present; the helper takes them every run and the run removes them on completion, so they are the fallback record until then -- the scan now judges against the newest completed run. | KILL1/2/4/5 (endpoint cells) not re-run on the new scripts; KILL6-9/14/17 not implemented; a full-suite PASSED on the final build | `kill-d-local.log`, `kill-d-fixed.log`, `kill-d-fixed2.log`, `suite-c.log` (job tmp); harness edits on next `ffa3b6ecfd` | | | |
| 2026-09-10 | #105 tranche A, final builds: `9847876563` then `7eb713bbd9` (ES `5a3759cde`) on the VM; H700 `7eb713bbd9` built, not staged | GENERIC_X64 `9847876563` (guests c, d), `7eb713bbd9` (guest d); H700 `7eb713bbd9` (`h700-all-20260910-7eb713bbd9`, tar sha `2ead84bc1e6c…`) | A cut-off restore staged by hand (marker naming a real archive, a setting flipped after it): on `9847876563` the sysinit verify ran before `/storage/roms` was bound and said `COULDN'T BE UNDONE` (`shots/interrupted-restore-couldnt-be-undone-before-defer.png`); on `7eb713bbd9` the sysinit run deferred at 1.79 s, the autostart run put the copy back at 3.3 s, the setting read its pre-restore value, and the screen said `YOUR SETTINGS RESTORE WAS INTERRUPTED. YOUR PREVIOUS SETTINGS WERE PUT BACK. TRY THE RESTORE AGAIN.` (`x64-all-20260910-7eb713bbd9/shots/interrupted-restore-put-back-dialog.png`). Suite + LINK1-7 on `9847876563`: one FAIL, the retention step's planted bytes refused as damaged (fixture fixed, `253ce6d8aa`; re-run pending). KILL3/10/11/12/15/16/18 PASS on `7eb713bbd9`, no litter; KILL13's sync control missed because the PUT BACK dialog from the hand-staged test was still up when its walk began; re-run alone: every trial PASS (0/50/200 ms cuts, no-change control, both planted torn shapes). Suite re-run with the real-archive fixture on guest c: PASSED. | KILL1/2/4/5 endpoint cells on the new scripts; the H700 on a handheld (needs the maintainer's yes per device) | artifacts `x64-all-20260910-{9847876563,7eb713bbd9}`, `h700-all-20260910-7eb713bbd9`; logs `suite-c2.log`, `kill-d-final.log`, `kill13-final.log` (job tmp) | | | |
| 2026-09-10 | #107 (Dropbox retries), #108 plain language, #110 provider + check, #111 pass or fail, #112 rows, #106 device name | GENERIC_X64 `ae3d602cf2`, guest d; **and the maintainer's RG SP on Dropbox**, which is where the retry bug was found and measured | The device found what the VM could not: with `--low-level-retries 2`, replacing 12 files on Dropbox failed 6 server-side moves; at 10, none. The QA WebDAV backend has no per-folder write lock, so the harness cannot reproduce it at all -- a gap to note, not to close by pretending. On the VM: `CONNECTED TO WEBDAV` and `CHECK CONNECTION` answer both ways (`WEBDAV ANSWERED. YOU'RE CONNECTED.` / `COULDN'T REACH WEBDAV.` with what to try); `NETWORK NAME GENERIC-X64` sits under `IP ADDRESS` with `HOSTNAME GENERIC_X64` unchanged below; the card reads `COULDN'T FINISH - <why>` over `DON'T WORRY, NOTHING CHANGED. TRY AGAIN FROM GAME SETTINGS > BACK UP SAVES TO THE CLOUD`; no surface can produce `COMPLETED WITH GAPS`. | Dropbox's contention on the VM (no such backend); the scripts' own plain-language pass (in flight); a partial-run fixture for the new pass-or-fail wording | frames `x64-all-20260910-ae3d602cf2/shots/`; the Dropbox measurement is in #107 | | | |
| 2026-09-10 | The three settled decisions: #89 N64 saves, #100 the empty cloud, #92/#117 the exit hotkey | GENERIC_X64 `e5ed60f3df` (guest d) and `20260910` (guest d, bind-mounted scripts) | #89: the guard fires -- shipped rules sent only `n64/save/Libretro Probe 64.eep`, widened rules sent all four saves beside the ROM and left the `.z64`; restore brought back the two deleted. #100: root present with `Saves` gone exits 0 (`There's nothing in your cloud to restore yet.` + `>>> offer create-saves-folder`), whole root gone exits 3; the card then shows `YOUR CLOUD HAS NO SAVES FOLDER YET... CREATE IT NOW, READY FOR YOUR FIRST BACKUP?` and CREATE IT made `Saves/` and `Backups/` in the cloud. #92: double SIGTERM through the shipped `execute_kill` returns 0 with the marker consumed, the same signals without the marker return 1, a zero-byte ROM returns 1, a normal quit returns 0, `killall -9` keeps its 137 branch, and a planted stale marker never survives a launch. **New**: RetroArch's first SIGTERM ends it cleanly in 0.51 s, and its `exit(1)` path does not flush SRAM -- a battery-backed cartridge kept its save through one press and lost it through two (#117), fixed by the debounce (D-LAUNCH-003), proven by driving the shipped function. Full suite PASSED on `e5ed60f3df` including the new N64 fixtures. | The first-SIGTERM behaviour on an H700; EmulationStation's play-count recording through the UI (the launcher was driven directly); the transfer page's own path for the empty-cloud offer (the card carries it) | frames `x64-all-20260910-e5ed60f3df/shots/`; `suite-e5.log` (job tmp) | | | |
| 2026-09-10 | #104: persistent logs, watchdog, crash store, evidence | GENERIC_X64 `aa3df8d178` (vm-pair guest a; kernel 7.1.2 with i6300esb + efi-pstore) | P0: `/var/log` is `/dev/vda2` (a bind of `/storage/.cache/log`), journal 8.0M, `kernel.panic=10 softlockup_panic=1 hung_task_panic=1`, watchdog0 `i6300ESB timer` timeout 15 active, `/var/lib/systemd/pstore -> /storage/.cache/log/pstore`. P1: marker logged before `systemctl reboot` found in `journalctl -b -1`; `es_log.0.txt` present. P2: an err and an info marker logged 75 s before `system_reset` both in `-b -1`. P3: `echo c > /proc/sysrq-trigger`, guest back in 22 s; dump archived to `/storage/.cache/log/pstore/1789073135/001/dmesg.txt` (`Kernel panic - not syncing: sysrq triggered crash`) by the 60 s fallback after the boot-time service was skipped (unmet condition on UEFI). P4: PID 1 frozen via the v1 freezer at 16:52:00, guest gone at +17 s, back at +23 s, previous boot ends on an sshd line with zero shutdown lines, watchdog re-armed. P5: timer pages at boot+60 s and +5 min, `collect` archive 88K, no credential-shaped line in it. P6: `cloud_sync.log` 1,848,894 → 525,146 bytes. Suite: 27 steps, **PASSED**. | H700 DRAM retention of ramoops across the SoC reset (device, needs a yes); softlockup/hung-task detectors firing (no userspace way to provoke); RT-loop starvation does NOT work on this kernel (fair server) -- use the freezer | `x64-all-20260910-aa3df8d178/evidence-proofs.log`, `suite.log`; H700 `h700-all-20260910-aa3df8d178/` tar sha `d02358219cd5...` | | | |
| 2026-09-10 | #114 console hops → transfer page; #115 small-panel cleanups; #109 RetroAchievements offer; #112/#111 frames | GENERIC_X64 `073929659d` (ES `5443c8f95`; vm-pair guest a at 1280x800, guest b relaunched with `--res 640x480`) | Journey YES opens `RESTORING FROM THE CLOUD`; against an empty cloud: `COULDN'T FINISH / 1 OF 3 ITEMS DID NOT FINISH / EVERYTHING - COULDN'T FIND YOUR CLOUD FOLDER / DON'T WORRY, NOTHING CHANGED. / A TRY AGAIN B CLOSE`. Settings restore: RESTORE FROM THE CLOUD with SETTINGS ticked → `RESTORE SYSTEM SETTINGS FIRST, THEN RESTART?` → page runs `cloud_restore --system-only` then `backuptool restore --then-cloud --no-restart` → `COMPLETED / 1 FILE · 16.5 MB RESTORED / THIS DEVICE RESTARTS SO YOUR RESTORED SETTINGS TAKE EFFECT. / PRESS ANY BUTTON TO RESTART`; device stayed up (158-200 s) until a button, then restarted into the journey dialog (both guests). With no archive: `SETTINGS - THERE'S NO SETTINGS BACKUP ON THIS DEVICE YET`, nothing restarted. Every dialog reads `YES  CHOOSE` once; 640x480 dialogs have buttons clear of the text (#48). RA offer on a username with no password at both sizes; YES opens FINISH RESTORE SETUP. Hub: `THE FOLDER IN YOUR CLOUD THAT HOLDS YOUR SAVES. CURRENT: /ROCKNIX/Saves`. Failed exit backup card at 640x480: `COULDN'T FINISH - YOUR CLOUD STOPPED ANSWERING` whole. Rows after a game: `AFTER YOUR LAST GAME - COMPLETED` / `- COULDN'T FINISH`. Suite: 27 steps, **PASSED**. | Launch gate's *stopping* wording (needs a cancel that overruns its budget); a why long enough to need the card's shorter forms; the H700 device proofs (ramoops retention, first-SIGTERM behaviour) | `x64-all-20260910-073929659d/shots/{1280x800,640x480}/`, `suite.log`; H700 `h700-all-20260910-073929659d/` tar sha `712761b58d4b...` | | | |
| 2026-09-10 | #118 wizard wording, #119 maintenance rows, #117 exit cell, #120 runner | GENERIC_X64 `7a90be59fb` (vm-pair, guest b relaunched at 640x480) | `tools/vm-qa` from a fresh pair: scripts PASS 3 s, round-trip PASS 107 s, exit PASS 23 s, walks 588 s (13 walks), one report. #118 at 640x480: provider list titled CONNECT CLOUD STORAGE, WebDAV form with `CONNECTION NAME` and `CONNECT`, FINISH RESTORE SETUP's `CHECK CONNECTION - CONFIRMS YOUR CLOUD STORAGE STILL SIGNS IN AFTER THE RESTORE.` and its result `YOUR CLOUD IS ANSWERING.`. #119 at 640x480: RESET MEDNAFEN → spinner → `MEDNAFEN CONFIG RESET TO DEFAULT.`; RESET RETROARCH with the defaults hidden → `COULDN'T FINISH - THIS BUILD DOESN'T HAVE THE DEFAULT SETTINGS TO PUT BACK, SO NOTHING WAS CHANGED.`; AUDIO RESET under a live ES: pipewire 2→0, ES up, `AUDIO SETTINGS RESET. THIS DEVICE RESTARTS SO THEY TAKE EFFECT.`, OK restarted, back in 8 s with pipewire 2; FACTORY RESET under a live ES: `THIS DEVICE HAS BEEN RESET. IT RESTARTS NOW.` drawn over the emptied tree, OK restarted, guest back fresh (config re-seeded, rclone.conf and SSH key gone). No console anywhere. | the SSH-path pages of the wizard (the glossed step 2, which the agent measured wraps at 480 px like its neighbours) were not reached -- key timing under a host build derailed three walks | `x64-all-20260910-7a90be59fb/shots/640x480/` (11 frames), `vm-qa-report.md`; H700 `7a90be59fb` superseded by `d2eabe879c` | | | |
| 2026-09-11 | `d2eabe879c`: the CloudText extraction and the unit-test binary (#120), on top of #117/#118/#119 | GENERIC_X64 `d2eabe879c` (vm-pair from the image, unattended) | `tools/vm-qa`: scripts PASS 3 s, round-trip PASS 101 s (27 steps, gate on), exit PASS 23 s, walks PASS 583 s (13 walks). Behaviour of the extracted code unchanged on the surfaces the suite and the exit cell touch. | the walks' frames are kept, not compared (#120 box 3); LINK cells not run under this image (run under `7a90be59fb`'s scripts, which are the same) | `x64-all-20260910-d2eabe879c/vm-qa-report.md`; H700 `h700-all-20260910-d2eabe879c/` tar sha `cc5bf7c1604a...` | | | |
| 2026-09-11 | `88b82d94a7`: the credentials-and-rules pass (#116 #52 #71 #39 #74 #100) | GENERIC_X64 `88b82d94a7` (vm-pair from the image, unattended; guest b relaunched at 640x480 for frames) | `tools/vm-qa`: scripts PASS 4 s (incl. bwrap case f: rclone.conf held back, scanner reports `pass =`), round-trip PASS 129 s (31 steps: the four new ones -- user rule above the catch-all keeps its file off the cloud; folder change writes siblings, no nesting warning, archive under `/QA-Custom/Backups/GENERIC-X64/`, migration check calls the layout current; bare RCLONEOPTS keeps the allowlist for backup and restore, a user's own filter is honoured; empty cloud offers, wrong root fails), exit PASS 24 s, walks PASS. Frames at 640x480: FINISH RESTORE SETUP's CHECK CONNECTION with no `rclone.conf` → `NO CLOUD STORAGE IS SET UP ON THIS DEVICE YET. SET IT UP NOW?`. Before the fix, the shipped `cloud_backup` with a bare RCLONEOPTS sent a planted non-save to the cloud (1 copy). | — (the SSH setup page's checkmark and its password page were framed after this row was written: `ssh-setup-step-1-checkmark-from-password-set.png`, `ssh-password-page-read-in-process.png`; the wizard's last row reads `USE A COMPUTER INSTEAD - RUN RCLONE'S SETUP OVER SSH. FOR CONNECTIONS THIS PAGE CAN'T MAKE.`) | `x64-all-20260911-88b82d94a7/vm-qa-report.md`, `shots/640x480/`; H700 `h700-all-20260911-88b82d94a7/` tar sha `aeaa94be2834...` | | | |
| 2026-09-11 | `9d035dfd57`: #121 powerstate, #122 laptop_mode, #126 the skipped-phase words | GENERIC_X64 `9d035dfd57` (guest c, :10025, by hand from the image) | Second boot: one `powerstate` line (`Started`), 0 `laptop_mode`, 0 sysctl errors (the previous image had 4731 such lines in one boot). Upgrade half: the line appended by hand, `/usr/share/post-update` run, gone, `boot.log` says so. `cloud_backup --system-only` with nothing local: `Skipped settings this time ...` / `Game saves: SKIPPED - SETTINGS ONLY` / `Settings backup: SKIPPED - NOTHING TO SEND YET`, exit 0, stamp untouched; with a real archive COMPLETED and stamp `<epoch> 0`; `--saves-only`: `Settings backup: SKIPPED - SAVES ONLY`. First boot still had two `awk: system.cfg` lines from powerstate racing userconfig-setup -> `79729ba204`. Evidence `x64-all-20260911-9d035dfd57/guest-c-evidence.md`. | | | |
| 2026-09-11 | `757ca87084`: #123 field labels, #127 near-name offer, #121 unit ordering | GENERIC_X64 `757ca87084` (guest c recreated at 640x480) | First boot: one `powerstate` line. Startup sync against `/QA-Near/Savez` beside `/QA-Near/Saves`: `last-restore <epoch> 0` and the three-button dialog (`shots/640x480/offer-near-name-dialog.png`); CHANGE FOLDER opens the CLOUD FOLDER editor pre-filled (`offer-change-folder-opens-editor.png`). Forms: `wizard-webdav-form-field-labels.png` (SERVER ADDRESS, SERVER TYPE, USERNAME, PASSWORD, ACCESS TOKEN), `wizard-sftp-form-field-labels.png`, `wizard-s3-form-field-labels{,-scrolled}.png`. Found: the S3 subtitle paragraph (#128, fixed as ES `229f50ad4`, pinned `60d784c850`). Lesson: in the on-screen keyboard `z` types a letter; `esc` cancels. `tools/vm-qa` not yet run on this image -- the pair is held by the #124/#125 agents. | | | |
| 2026-09-11 | `af2db4ab09`: the seven-issue pass complete (#121-#127) plus #128 -- x64 and H700 | GENERIC_X64 `af2db4ab09` (vm-pair from the image, guest a; `qa-af2db4ab09-20260911-0605`, `-0627`, `-0648`); guest c upgraded in place `757ca87084` -> `af2db4ab09` through `.update`; H700 SYSTEM opened with unsquashfs | `tools/vm-qa`: scripts PASS (4 s), exit PASS (24 s); round-trip FAIL then **PASS 100/100** (157 s) after its empty-endpoint check learnt D-CLOUD-092's truthful empty (`45a0d7cb6a`) -- the #124 lock assertions (0 of 4, 0 of 6, FREE), #126's three words, #127's three offer shapes all PASS; walks FAIL (7) then FAIL (2) then **PASS 13/13** (1109 s) after the runner learnt to seed the QA remote and the content fixture on a fresh pair (`fcb632d403`, `07977a7910`) -- the failures were the runner's fixtures, not the image, and the pre-#125 walks had read PASS on the same screens. Upgrade: a planted `vm.laptop_mode` removed by post-update on the upgrade boot, the boot after with 1 `powerstate` line and 0 `laptop_mode`. Frames: `x64-all-20260911-af2db4ab09/shots/640x480/wizard-s3-form-subtitle.png` (#128) and the runner's 45 walk frames. H700 `h700-all-20260911-af2db4ab09/` (tar sha `29b86dd598389f2c...`) carries the new scripts, the powerstate ordering, no sysctl line, the post-update block and the three new ES strings; **not staged** -- both handhelds were offline on `7eb713bbd9`, per-device yes (D-QA-008/011). | | | |
| 2026-09-12 | `d94ca7b159`: the exit card says SYNCING SAVES TO THE CLOUD (#138, D-UI-040) | GENERIC_X64 `d94ca7b159` (ES `6b49a663aa`; vm-pair from the image, guest a; `qa-d94ca7b159-20260912-0445`); guest c upgraded in place `af2db4ab09` -> `d94ca7b159` for the frames | `tools/vm-qa`: scripts PASS, round-trip PASS 158 s (100/100), exit PASS, walks PASS 13/13 (1120 s) -- first pass, with the runner seeding the remote and the content fixture itself. Frames on guest c: `exit-card-syncing-saves-to-the-cloud.png` (against the dead port; the live line shows rclone's two stats run together -> #140), `exit-card-sync-saves-completed.png` (live remote, one second). The transfer page keeps BACKING UP TO THE CLOUD (`walks/run-transfer/08-transfer-done.png`). The SYSTEM squashfs carries the two new strings and none of the old. H700 building. | | | |
| 2026-09-12 | `a2ee7b9bb2`: #140 the exit card's live line in the player's words; #133's five-backend matrix merged (`ac2db6c113`) | GENERIC_X64 `a2ee7b9bb2` (ES `f93acc2a6`; vm-pair from the image, guest a; `qa-a2ee7b9bb2-webdav-a-20260912-0646`); guest c upgraded in place `d94ca7b159` -> `a2ee7b9bb2` through `.update` for the frames; H700 `a2ee7b9bb2` SYSTEM opened with unsquashfs (tar `cbddc951af027b14...`, not staged) | `tools/vm-qa`: scripts PASS 3 s, round-trip PASS 159 s (100 PASS lines), exit PASS 25 s, walks PASS 1112 s -- first pass. The card framed on guest c through `POST /launch` + `execute_kill`: NOTHING SENT YET against the dead port for two seconds, then the unchanged COULDN'T FINISH outcome; COMPLETED against the live endpoint (`docs/qa-frames/2026-09-12/`). The image's ES binary carries the six new strings and none of the old check-counter line (x64 and H700). The matrix's own run was on `d94ca7b159`: webdav, sftp, smb 99/99; s3 93/99 (#141, #143); ftp 90/99 (#142) | A live-line frame with bytes in flight: the exit sync of one probe save completes in under a second against a host endpoint, so no frame catches `12 KB OF 40 KB`; the unit tests carry that shape. The card under a real handheld panel. | `frame-exit-card.sh` (session scratchpad; a launch through EmulationStation's HTTP API is the same `ViewController::launch` as A on the game); `generic-x64-vm-testing.md` build recipe gains the sources mount; `device-testing-policy.md` gains the read filter. | 0.63 s median (n=5; identical on sftp, webdav, s3 -- nothing cloud is on the launch path) | 1.30 s webdav / 1.43 s sftp / 0.79 s s3 (n=5, `completed` 15/15); the card gone at 3.5 s | 1.01 s (n=5) -- today a launch cancels the exit sync (`cancelled` 10/10); under D-CLOUD-109 the two halves add to ~2.1 s. Runner's own quick cell on guest a (n=1, `--vm`): 0.89 / 1.05 / 1.51 s |
| 2026-09-12 | `0f89c8f1d4`: the audit's three high findings -- #145 the empty-cloud offer on the transfer page (one `>>> ` parser, `CloudOffer::present`), #144 the lifetime harness compiling and run by the runner, #146 the fallback retries -- plus PL-04/PL-12 strings | GENERIC_X64 `0f89c8f1d4` (ES `51639dd09`; vm-pair from the image: guest a `qa-0f89c8f1d4-webdav-a-20260912-0953`, guest b `qa-0f89c8f1d4-webdav-b-20260912-1029`); guest c upgraded in place `a2ee7b9bb2` -> `0f89c8f1d4` for the frames | Six suites now: scripts PASS 3 s (case g proven to fire), **lifetime PASS 1 s** (new), round-trip PASS 159 s, exit PASS 25 s, walks PASS 1106 s; **time-to-play FAIL on guest a** -- the exit stamp never appeared because the cell set `cloudsaves.gameexit` after the boot EmulationStation reads it at (proven by hand: file 1 / interface booted on 0 -> no stamp; rebooted -> `completed`), fixed `a8f8ef487d` and **PASS on guest b** (rebooted with the settings in place). The offer framed on the transfer page against SFTP (`offer-on-transfer-page.png`); the fixed fallback bound logged on guest c with the conf line removed. The image carries the short-form dialogs, WI-FI GPIO, UPDATE GAMELISTS, the two-level menu path | The offer's plain (no near name) form on the page -- the SFTP fixture had `Savez`; a Dropbox-shaped root; the `BRING DATA BACK ONTO THIS DEVICE.` row line under D-UI-045 (the maintainer's call) | `headline_missing` fired for real on the runner (the guard blindspot 39 asked for); `reboot_guest()` in `time-to-play`; `offer-on-page.steps` and `prep-guest-c-offer.sh` in the session scratchpad; the killed-run lesson: a background runner can be stopped by the harness's memory watchdog with 29 GB free -- run the suite in the foreground when it must finish | 0.93 s (guest a, n=1); 0.66 s (guest b) | -- (a); 0.54 s (b, `completed`) | 1.51 s (a and b; launch cancels the sync) |
| 2026-09-12 | `205b80c8cf`: the automatic sync's bound (D-CLOUD-118, `--automatic`), listings on three retries (#143), buckets read as absent when empty (#141 first cut), `cloud_net_ready` settling on any connected state (the RG SP's defect), FINISH RESTORE PROCESS (D-UI-046) | GENERIC_X64 `205b80c8cf` (ES `2aa10246b`; vm-pair from the image, guest a `qa-205b80c8cf-webdav-a-20260912-1427`); guest c upgraded in place for the bounds cells against SFTP and the S3 round trip | Five suites PASS (scripts 14 s, lifetime, exit, time-to-play 59 s, walks); **round-trip FAIL 2/99**: the rclone() wrapper, defined for every run, shadowed the bash function the harness exports as its lock-descriptor probe -- "0 of 0 rclone invocations" -- fixed in `34606305a1` (defined only under `--automatic`). Bounds on guest c against SFTP: online exit sync 1.70 s `completed`; refused port 12.0 s; **stalled endpoint 20.8 s (was 320.9)**, YOUR CLOUD STOPPED ANSWERING on the card; portal 0.93 s. With five low-level retries and `--transfers 1` the refused port is 3.9 s (three: 1.6 s) -- proposed, Dropbox-gated. S3 round trip 93->95/99 in 563 s: three fixture faults on the harness's side (mkdir makes nothing on a bucket; MinIO's directory markers survive `purge`) and the wrapper | Five retries on a Dropbox device (#107's write lock); the slow-but-moving arm (WebDAV-only throttle) | `rclone()` wrapper conditional on `--automatic`; `RCLONE_LIST_OPTS`; `bucket_dir_listed`; `caps` bucket=; S3 reset via the AWS CLI; file fixtures in the empty-cloud step | 0.88 s (a, n=1) | 1.17 s (a, `completed`); 1.70 s guest c/SFTP | 1.51 s (a) |
| 2026-09-12 | `34606305a1`: the wrapper only under `--automatic`; the content restore's listings bounded; the harness's empty-cloud step on buckets | GENERIC_X64 `34606305a1` (vm-pair from the image, guest a `qa-34606305a1-webdav-a-20260912-1456`); guest c with the same scripts staged (`--path-prefix`) for the S3 round trip | **All six suites PASS**: scripts 14 s, lifetime 1 s, round-trip 149 s (the lock probe sees every rclone again), exit 27 s, time-to-play 61 s, walks 1111 s. S3 round trip against guest c: **99/99 in 146 s** (was 93/99 in 578 s) once the S3 reset cleared MinIO's directory markers (`26affac4b0`); then 99/99 in 142 s with the parent-listing rule (`50b53b016f`: root absent fails on a bucket as on Dropbox) and 140 s with `directory_markers = true` in the stanza (`0a72907787`) -- `rclone mkdir` then the parent lists `Saves/` | The runner's own S3 backend pass (`vm-qa --backend s3` on the pair); a Dropbox device for the retry count | `tools/time-to-play over_budget` (the exit sync over 3 s fails, D-CLOUD-119); D-CLOUD-120 | 0.88 s (a) | 1.17 s (a, `completed`) | 1.51 s (a) |
| 2026-09-12 | `c7fba3b8e6`: the night's final tree -- a bucket folder exists when its parent lists it, `directory_markers` in the wizard's S3 stanza (D-CLOUD-120), the S3 QA reset, the exit-sync budget check -- x64 and H700 | GENERIC_X64 `c7fba3b8e6` (vm-pair from the image, guest a; `qa-c7fba3b8e6-webdav-a-20260912-1523` for five suites, `-1546` for the walks -- the harness's memory watchdog stopped the first run after the fifth suite with 30 GB free, so the walks ran detached with `setsid nohup`); H700 `c7fba3b8e6` SYSTEM opened with unsquashfs (tar `ae0c0ddb56583ccc...`), BUILD_ID checked, not staged | **All six suites PASS**: scripts 15 s, lifetime 1 s, round-trip 158 s, exit 27 s, time-to-play 60 s, walks 1111 s. Image scripts identical to the tree (`cloud_backup` diffed); `bucket_dir_listed`, `directory_markers=true`, the readiness `connected*` rule and `RCLONE_SYNC_NET_OPTS_FALLBACK` all present in both images; ES carries `--automatic` on the three automatic commands and FINISH RESTORE PROCESS | The S3 backend under the runner itself (`vm-qa --backend s3`), a Dropbox device for the five-retry tweak, the RG SP's readiness fix on the RG SP (needs staging, per-device yes) | A run that must finish is started with `setsid nohup` and polled from the foreground; the harness stops its own background tasks on a memory heuristic the host does not share | -- | -- | -- |
| 2026-09-12 | `918b3c784b`: the automatic sync's baseline of five low-level retries and one transfer at a time (D-CLOUD-121, the maintainer's call) -- x64 and H700, **the staging candidate for both handhelds** | GENERIC_X64 `918b3c784b` (vm-pair from the image, guest a; `qa-918b3c784b-webdav-a-20260912-1715` five suites, `-1719` the walks, detached); guest c upgraded in place `c7fba3b8e6` -> `918b3c784b`; H700 `918b3c784b` SYSTEM opened (tar `533916164c66e721...`), BUILD_ID and the new default checked, not staged | **All six suites PASS**: scripts 14 s, lifetime 1 s, round-trip 153 s, exit 25 s, time-to-play 58 s, walks 1108 s. Bounds on guest c against SFTP: online exit sync 1.56 s; portal 1.05 s; stall 21.8 s (the ceiling); **refused port 12.1 s -- the upgraded guest kept the first-cut conf line**, a fresh guest carries the new default. `cloud_sync_helper` gains the once-only migration of the first-cut value (`6539f8ccdd`, on `next`, not in this image): proven on guest c -- line rewritten, marker set, a second run changed nothing, refused port **3.98 s**, online 1.42 s | The five-retry baseline on a Dropbox device (#107's write lock; a handheld, with the maintainer's yes); the S3 backend under the runner itself | Upgrade lesson re-learned: a new conf key's first value is what an upgraded device keeps; the helper's exact-value migration is the pattern (`upgrade-and-install.md`) | 0.83 s (c) | 1.17 s (a); 1.56 s (c, SFTP) | 1.51 s (a) |
| 2026-09-12 | `c90022dc57`: the #127 texts in the maintainer's words (D-UI-047), one description for the FINISH RESTORE PROCESS rows, `tools/vocabulary-check` as the runner's `vocabulary` suite -- x64 and H700, **the staging candidate for both handhelds** | GENERIC_X64 `c90022dc57` (ES `5770a5b13`; vm-pair from the image, guest a; `qa-c90022dc57-webdav-a-20260912-1843` six suites, `-1848` the walks, detached); guest c upgraded in place for the frames; H700 `c90022dc57` SYSTEM opened (tar `3ec7a29239718e34...`), strings and the five-retry default checked, not staged | **All seven suites PASS**: scripts 14 s, lifetime 0 s, **vocabulary 0 s (new: 85 back up / backup strings judged, 0 wrong; the guard named 3 of 3 constructed violations)**, round-trip 150 s, exit 27 s, time-to-play 60 s, walks 1118 s. Both offers framed on the transfer page in the final words (`docs/qa-frames/2026-09-12/offer-*-final-words.png`); the image's ES binary carries the five new strings and none of the old | The five-retry baseline on a Dropbox device (a handheld, with the maintainer's yes); the S3 backend under the runner itself | The instruction-file sweep (#147, `docs/rules-sweep-2026-09-12.md`) merged the same night: four homeless rules housed, two dead `paths:` globs widened, nine contradictions resolved, seven register rows, CLAUDE.md/AGENTS.md pointing at all 21 rule files | 0.88 s (a) | 1.2 s (a); 1.4-1.6 s (c, SFTP) | 1.51 s (a) |
| 2026-09-12 | `ec12767b26`: the words aligned (#148, D-UI-049: fourteen SAVE STATE labels, RESTORE SETTINGS FIRST, RESTORE SETTINGS FROM THIS DEVICE); the checker's save-state and Wi-Fi rules -- x64 and H700, **the staging candidate for both handhelds** | GENERIC_X64 `ec12767b26` (ES `52012829c`; vm-pair from the image, guest a; `qa-ec12767b26-webdav-a-20260912-2132` six suites, `-2136` the walks, detached); H700 `ec12767b26` SYSTEM opened (tar `701f725cd10467f8...`), labels and the five-retry default checked, not staged | **All seven suites PASS**: scripts 14 s, lifetime 1 s, vocabulary 0 s (132 strings judged, 0 wrong; 14 named on the previous head), round-trip 149 s, exit 28 s, time-to-play 61 s, walks 1113 s. The image's binary carries the new labels and none of the old | The five-retry baseline on a Dropbox device; the S3 backend under the runner itself | The rules restructure (#147) merged the same night: `es-ui-style-guide.md` is a rule file, `es-native-ui.md` split into mechanics / `es-player-text.md` / `es-code-traps.md`, a front-matter standard and an index in `instruction-files.md` (24 rule files) | 0.9 s (a) | 1.2 s (a) | 1.5 s (a) |
| 2026-09-13 | `878ec8863b`: the settings archive stops carrying the image (#45, D-CLOUD-008), a missing cloud folder judged by its parent (#142, D-CLOUD-123), the manager's help bar follows a rebuilt grid (#93), #27's first cut | GENERIC_X64 `878ec8863b` (ES `bcc82f113`; vm-pair from the image, guest a, `qa-878ec8863b-webdav-a-20260913-0054`, detached) | Seven suites PASS: scripts 14 s, lifetime 1 s, vocabulary 0 s, round-trip 149 s, exit 27 s, time-to-play 59 s, walks 1110 s. **pair-identity FAIL** -- a tool defect, not the image's: `vm-qa` used `$SSHO` without defining it and had passed the night before on the caller's shell (blindspot 40). Frames on guest b closed #93 and #47 and ticked #82's VM half; a fourth guest at 640x480 showed #27's first cut still truncating | Two names on one network (the pair on this image came up GENERIC-X64 and ROCKNIX: the hostnamed race, found here) | `vm-qa` declares SSHO; the pair-identity suite requires two hostnames each equal to its setting; case k and l in `last-good-scripts-test`; guest d (640x480) as a fixture | 0.89 s (a) | 1.57 s (a) | 1.51 s (a) |
| 2026-09-13 | `02f368914e`: the shipped device name becomes the unit's own and survives hostnamed (#50, D-NET-002), #27's second cut, the restore marker read uncached (#47) | GENERIC_X64 `02f368914e` (ES `557a27d20`; vm-pair from the image, guest a, `qa-02f368914e-webdav-a-20260913-0147`, detached) | **All eight suites PASS**: pair-identity now with two names (GENERIC-X64-15ca / -0964, each equal to its setting); round-trip 148 s, exit 27 s, time-to-play 59 s, walks 1131 s. On guest d (640x480) every manager label wrapped whole but neighbouring dates ran together -- the tile font was scaled twice on a small panel | The name at boot on a handheld; the pre-#50 upgrade path on a device the player renamed | `es-code-traps.md` § Three things a font is not; #149 filed (no help bar under the manager on small panels) | 0.88 s (a) | 1.43 s (a) | 1.51 s (a) |
| 2026-09-13 | `db6b42c180`: #27's fourth cut -- the manager's tile font asked for once (not scaled twice on a small panel), labels two lines, dates fitted, the help bar drawn on full-screen-menu panels (#149, D-UI-050) | GENERIC_X64 `db6b42c180` (ES `79fe10878`; vm-pair from the image, guest a, `qa-db6b42c180-webdav-a-20260913-0210`, detached); guest d at 640x480 and guest b at 1280x800 for the frames | **All eight suites PASS**: scripts 15 s, lifetime 1 s, vocabulary 0 s, pair-identity 0 s (two names), round-trip 149 s, exit 27 s, time-to-play 60 s, walks 1121 s. Frames at both sizes closed #27 and #149. The link suite afterwards: 65 PASS and LINK5 failing because #45 had shrunk the archive under the cut; with the fixture padded to 12 MiB, LINK5 alone PASS (29.5 s after the cut, exit 69, marker unchanged; the WebDAV lock's two faces SKIPped) -- #113's VM half. The before-figure, recorded here because #113 boxes 1 and 4 cite this log for it (audit #151 PL-12): the same cell under ten unbounded low-level retries ran **303 s** and exited 5 -- the settings archive re-uploaded after the link came back, against the QA WebDAV stale-PUT lock -- measured 2026-09-10 on the image carrying #107's ten retries (`e73efbc8e0`, the build the tick names), and again on `ec12767b26` (2026-09-12, the deliberate bound's `--timeout 30s` idle limit against `--bwlimit 200k` on the 16.5 MiB archive #45 had not yet pruned); a dead link was ~30 s throughout (the 2026-09-10 `d574edf975` row) | The manager on a real 640x480 panel (the VM's is the same size, not the same glass) | H700 `db6b42c180` built the same hour as the staging candidate replacing `ec12767b26` | 0.88 s (a) | 1.44 s (a) | 1.51 s (a) |
| 2026-09-13 | `1de2e1179d`: the mDNS responder on, after the device has its name (D-NET-003) | GENERIC_X64 `1de2e1179d` (ES `79fe10878`; vm-pair from the image, guest a, `qa-1de2e1179d-webdav-a-20260913-0330`, detached; a first attempt collided with the 640x480 guest's rebuild in vm-pair's directory and was discarded); guest d at 640x480 | **All eight suites PASS**; `avahi-daemon` active at 1.93 s, 190 ms after network-base, `Host name is GENERIC-X64-474c.local`, bound on 5353 over IPv4 and IPv6, no `Daemon not running` line; #67's tab-switch and reopen boxes and #69's three artwork modes framed on guests b and d | A laptop resolving `<name>.local` (no multicast through QEMU's user-mode network); #68's key-empty message (the interface on guest b was not restarted by `systemctl stop emustation`; a reboot is the way) | H700 `1de2e1179d` built (`h700-all-20260913-1de2e1179d/`, tar `6a59e64ec0850a70...`), the staging candidate | 0.88 s (a) | 1.56 s (a) | 1.51 s (a) |
| 2026-09-13 | `697aa68e29`: a refused ScreenScraper login named in English, the right credential blamed (#66) | GENERIC_X64 `697aa68e29` (ES `ae257f778`; vm-pair from the image, guest a, `qa-697aa68e29-webdav-a-20260913-0416`, detached -- a first attempt lost to a full tmpfs); guest d at 640x480 with a bogus developer pair | **All eight suites PASS**; SCRAPE NOW with the bogus pair: SCREENSCRAPER REJECTED THE DEVELOPER ID OR PASSWORD. CHECK THEM UNDER SCRAPER > OPTIONS; the API's body in the journal only. #67 four boxes, #68 three, #69 three ticked from guests b and d | The wrong-account case (a valid pair on the VM); the with-key RetroAchievements pages (the test account arrived at the end of this run) | H700 `a8b2feafcc` (the same package tree) built, the staging candidate; `tools/qa-accounts`; the small guest's disk on `/workspace/tmp` | 0.88 s (a) | 1.43 s (a) | 1.51 s (a) |
| 2026-09-13 | `947072a988`: every ScreenScraper status named in English; the thirteen credential messages in French (D-UI-051) | GENERIC_X64 `947072a988` (ES `db8ae662a`; vm-pair from the image, guest a, `qa-947072a988-webdav-a-20260913-0445`, detached); guest d at 640x480 in `fr_FR` then `en_US`; guest b with the maintainer's RetroAchievements QA account | **All eight suites PASS**; the wrong-pair dialog in French; the RetroAchievements summary and a game's progress page with the QA account and key; the real ScreenScraper account beside a bogus pair still blamed on the pair | The wrong-password case and a real scrape (a developer pair on the VM); every H700 box | H700 `3ecef54574` (the same package tree), the staging candidate; `tools/qa-accounts` rewritten after it reported a write that left es_settings.cfg unparseable | 1.08 s (a) | 1.30 s (a) | 1.51 s (a) |
| 2026-09-13 | `519f40aa0c`: the no-account ScreenScraper case named in English and French (#66, the fourteenth string; D-UI-051) | GENERIC_X64 `519f40aa0c` (ES `3466b36af`; vm-pair from the image, guest a, `qa-519f40aa0c-webdav-a-20260913-0535`, detached) | **All eight suites PASS** (walks 1113 s); nothing new pressed by hand -- the image differs from `947072a988` only by the fourteenth string and its French, and by `tools/qa-accounts` | The no-account dialog on screen (guest d in `fr_FR` and `en_US`, a pair with no account); every H700 box | H700 `59103cd9cb` (the same package tree, BUILD_ID the docs commit the devices worktree synced to), the staging candidate; the milestone code audit running on Fable 5.1 with two Facilitator seats over this scope | 0.88 s (a) | 1.44 s (a) | 1.51 s (a) |
| 2026-09-13 | `d32f47a947`: the milestone audit's punch list (#151) in an image -- the ordered avahi unit shipped, the hostname shape rule, `cloud_device_id --if-present`, the zip restore skip, four fail-closed branches; ES `51edcf728c` (restore-relink rows, pair probe first, offline/unknown sentences, helpRowPerc) | GENERIC_X64 `d32f47a947` (ES `51edcf728c`; vm-pair from the image, guest a, `qa-d32f47a947-webdav-a-20260913-0743`, detached; pair-identity re-run x4 at 0814/0818/0820/0823); guest d rebuilt at 640x480 for the frames | **All nine suites PASS** (`register` new; walks 1109 s); PL-01: 20 of 20 guest-boots publish the kernel hostname over mDNS, `After=` lists `network-base.service`, the "responder not up yet" line every boot; PL-02: a's archive restored on b -> b reboots as `GENERIC-X64-0964` with "another unit's shipped default ... carried here by a settings restore", `Max Deck` kept as `Max-Deck`; PL-03: a legacy `ROCKNIX_BACKUP.zip` restore left `assets/lang/en_US.ini` and CACHE untouched (md5 equal) and brought `ppsspp.ini` in; `last-good-scripts-test` 130 PASS from the primary | The seven ScreenScraper status frames (HTTPS API, no TLS shim); PL-04's two fresh H700 flashes; the 1280x800 helpSize log | `tools/last-good-scripts-test` looked for build roots beside the repo, never under `rocknix.worktrees/` (`401a3a047c`); `set_setting system.hostname default` makes `chksysconfig` call `system.cfg` damaged and restore the old name (filed); guest b now carries a's other settings | 0.88 s (a) | 1.43 s (a) | 1.51 s (a) |
| 2026-09-13 | `f0ab059596`: #155 -- the NETWORK SETTINGS restore row at one line, the FINISH RESTORE PROCESS page in French (ES `8c8be81ba3`) | GENERIC_X64 `f0ab059596` (vm-pair from the image, guest a, `qa-f0ab059596-webdav-a-20260913-0911`, detached); guest d rebuilt at 640x480 | **All nine suites PASS** (walks 1115 s); the row is a label and one line in English and French; the page in French with the link cut shows no English on its own rows (frames `network-settings-finish-restore-row-640x480-f0ab059596[-fr].png`, `finish-restore-process-no-wifi-{top,bottom}-640x480-f0ab059596-fr.png`) | Nothing new for a handheld | The RESTORE group heading above the row and CHECK CONNECTION's CHECKING... were still English -- French added in ES `3d9394660c`, pinned `0f4e4829a8`, the next image | 0.86 s (a) | 1.43 s (a) | 1.51 s (a) |
| 2026-09-13 | `0f4e4829a8`: the punch-list image -- #151's eighteen items and #155, with the RESTORE heading and CHECKING... in French (ES `3d9394660c`) | GENERIC_X64 `0f4e4829a8` (vm-pair from the image, guest a, `qa-0f4e4829a8-webdav-a-20260913-0935`, detached); guest d rebuilt at 640x480 | **All nine suites PASS** (walks 1108 s); PARAMÈTRES RÉSEAU shows RESTAURER over FINIR LA RESTAURATION (`network-settings-finish-restore-row-640x480-0f4e4829a8-fr.png`); VÉRIFIER LA CONNEXION on a guest with no remote opens the set-up offer, so the transient VÉRIFICATION… cannot appear there (catalogue-verified, msgfmt 1302) | The two #156 frame halves (HTTPS shim); PL-04's two fresh H700 flashes; #50's LAN boxes | The NO CLOUD STORAGE offer has no French (noted on #152); the first French-catalogue commit was pushed with its msgfmt check masked by a pipeline (host has no msgfmt) and re-validated with the toolchain's in the next commit | 0.87 s (a) | 1.36 s (a) | 1.51 s (a) |
| 2026-09-13 | `1e5a2818e8`: #157's phase-aware startup sync card (ES `3a121f0152`; D-UI-052 option 1) | GENERIC_X64 `1e5a2818e8` (vm-pair from the image, guest a, `qa-1e5a2818e8-webdav-a-20260913-1356`, detached; built beside the SM8550 cold build); guest d rebuilt at 640x480 and pointed at the QA WebDAV with its own folder | **All nine suites PASS** (walks 1233 s); 21 frames of the card (`startup-sync-*-640x480-1e5a2818e8[-fr].png`, three at 1280x800 on guest a): RECEIVING · STARTING... at 0, RECEIVING · COMPARING SAVES · 566 OF 566 at 0, receiving bytes inside 0-50 ending exactly at the midpoint, SENDING · STARTING... parked at 50, SENDING · COMPARING SAVES at 50, sending bytes inside 50-100 to full, COMPLETED; French on the middle line (RÉCEPTION · COMPARAISON DES SAUVEGARDES · 828 SUR 828 fits the card) | The card's title and outcome (SYNCING SAVES AT STARTUP, SYNC SAVES / COMPLETED) had no French -- added next; the H700 boot (#157 box 5) | rclone lists, compares and transfers concurrently, so on a boot that moves bytes the compare line stands alone only until the first stats block; `cloud_net_ready` exits 69 at once with no default route and the startup sync is skipped silently; WebDAV `vendor = other` does not keep mtimes on upload, so a restore after a backup re-downloads once; *the three timings were taken while the SM8550 cold build ran on the host (fex-emu at CONCURRENCY_MAKE_LEVEL=10) -- twice the previous rows, not the image: re-measure on an idle host | 1.88 s (a)* | 2.66 s (a)* | 3.02 s (a)* |
| 2026-09-13 | `4d7eb1f303`: everything since the handhelds' build in one image -- #157 card, #160 measured header, the card's French (ES `44bcc4d51d`); #153 stall ceiling (D-CLOUD-127), #154 empty hostname line (D-NET-009), #169 no RetroAchievements token in a backup | GENERIC_X64 `4d7eb1f303` (vm-pair from the image, guest a, `qa-4d7eb1f303-webdav-a-20260913-1518`, detached, no device build on the host); guest d rebuilt at 640x480 for the four proofs | **All nine suites PASS** (walks 1109 s; scripts 60 s with 157 checks) | The four guest proofs in flight on d (#154 delete-and-reboot, #153 LINK5 on throttled S3 x2 + one WebDAV cell, #169 archive without the planted token, #160 at Extra Large); the restore-onto-a-fresh-guest half of #169; the H700 boot of the card (#157 box 5) | The previous row's doubled timings are explained: this run on an idle host is back at the 0.9 / 1.4 / 1.5 s shape | 0.87 s (a) | 1.42 s (a) | 1.51 s (a) |
| 2026-09-13 | `9684d8665d`: the second-round fixes -- #160 header aligned as drawn on small panels (ES `b6eb26980a`), #153 newline after a stall kill and the WebDAV drain tolerance (D-CLOUD-128) | GENERIC_X64 `9684d8665d` (vm-pair from the image, guest a, `qa-9684d8665d-webdav-a-20260913-1630`, detached, no device build on the host); guest d rebuilt at 640x480 for the second proofs round | **All nine suites PASS** (walks 1105 s; scripts 61 s, 158 checks) | The second proofs round in flight on d/b/a (LINK5 S3 x2 + WebDAV; #160 frames DEFAULT/Extra Large/summary; #169 restore half onto b; #157 box 3 French at 1280x800); the H700 boot of the card | Nothing new; the timings match the idle-host rows | 0.91 s (a) | 1.43 s (a) | 1.51 s (a) |
| 2026-09-13 | TEST `e1edaaa33b` (feature branches `feature/ra-offline`; ES `23dc0336ff`): the offline RetroAchievements proxy, #166's unlock proof | GENERIC_X64 TEST image on guest d only (640x480); QA RetroAchievements account; Tobu Tobu Girl Deluxe (GBC) from `/workspace/artifacts/rocknix-qa-roms/` | **The proxy half of the milestone's test holds**: online launch cached the set; link cut; `Awarding achievement 100359: Potato-tan Secret` -> `queued_offline`; exit with the link down; link back -> `Connectivity restored; attempting flush` -> `Flush succeeded` 11 s later; the game page shows it unlocked at the unlock's own moment (RA honoured the 190 s offset); relaunch online lists 1 of 29 unlocked. Eight frames | The interface says nothing at exit or at reconnect (phase 2b); the control with the toggle off (Böbl and Niñoid reached no unlock in twelve minutes of key play; Tobu's cheap achievement is spent on the QA account); the `vm-qa` fixture | The proxy injects a synthetic "Warning: Casual Only" achievement awarded every launch (D-RA-003 open); an unscraped game has no VIEW ACHIEVEMENTS in GAME OPTIONS -- the page is reached from MAIN MENU > RETROACHIEVEMENTS; `scp` double-escapes a filename with spaces | n/a | n/a | n/a |
| 2026-09-14 | `31253072d6`: the first release candidate with the offline RetroAchievements proxy merged (ES `f7430b49d0`; package + patches 001-003, ctl, unit, 099 fix, host lines, hold-back) beside the cloud-sync work | GENERIC_X64 `31253072d6` (vm-pair from the image, guest a, `qa-31253072d6-webdav-a-20260914-0308`, detached, idle host); guest d rebuilt at 640x480 (watchdog device off) for the `ra-offline` suite (`qa-31253072d6-webdav-d-20260914-0317`) | **All nine suites PASS** (walks 1112 s) **and `ra-offline` PASSED (32)**: unlock offline, `queued_offline`, `pending = 1`, `Flush complete … flushed=1` 10 s after the link, stamp, the API recording the unlock at 03:19:25, relaunch 27/28; a first run failed only on a port probe that ran before the socket existed (fixed `0ddfbddf0e`) | #175 and #176 are fixed before this becomes the H700 candidate (D-WORKFLOW-021); the page at 1280x800 (#165 box 4); the RG SP round | The `ra-offline` suite spends the QA account's one reachable achievement per run (D-RA-006); `raofflineproxy.service` is Type=simple and listens ~1 s after start | 0.87 s (a) | 1.30 s (a) | 1.51 s (a) |
| 2026-09-14 | `5801ceb5fc` (RC-3): RC-1 plus #175 (a failed sign-in never writes the RetroAchievements switch; refused vs unreachable dialogs; token re-check on network up), #176 (`redact_credentials` in the launcher logs and the support bundle), the hardcore row's French on one line (ES `b6b6f3cea4`) | GENERIC_X64 `5801ceb5fc` (vm-pair from the image, guest a, `qa-5801ceb5fc-webdav-a-20260914-0353`, detached, idle host); guest d rebuilt at 640x480 for the #175/#176 proofs and the `ra-offline` suite (`qa-5801ceb5fc-webdav-d-20260914-0424`) | **All nine suites PASS** (walks 1111 s) **and `ra-offline` PASSED (33)**; #175: an offline boot keeps `=1` and the menu entry, closing the settings page offline shows no dialog and keeps `=1`, the token arrives seconds after the link (2 of 3 runs), a wrong password shows DIDN'T ACCEPT YOUR SIGN-IN with the switch left on (EN/FR frames); #176: a verbose launch logs `cheevos_password = "<redacted>"`, the planted value appears nowhere in `exec.log` or an extracted `rocknix-evidence` bundle (21 files rewritten, 0 left out) | #175's network-up retry is one-shot and can lose to DNS lag (1 of 3 runs; a second link cycle rescued it) -- fixed before the H700 candidate (D-WORKFLOW-021); a standalone emulator's `--ra-password` redaction (no ROM) proven by the suite only | ES INFO lines never reach the file at the shipped LogLevel (read `es_log.0.txt` after the next boot); `system.loglevel` is `verbose` by default on the image, so #176 mattered on every device | 0.88 s (a) | 1.43 s (a) | 1.51 s (a) |
| 2026-09-14 | `c5c50a2d5f` (RC-4): RC-3 plus #175's bounded network-up retry (ES `d9d6083de`: after a token check that could not reach the server while the link is up, 10 s apart, nine attempts; success or a refusal resets to the two-hour schedule; offline stays one failure) | GENERIC_X64 `c5c50a2d5f` (vm-pair from the image, guest a, `qa-c5c50a2d5f-webdav-a-20260914-0442`, detached, idle host); guest d rebuilt at 640x480 for the #175 retry proof (`/workspace/tmp/rocknix-session/175-rc4-evidence/`, quoted on #175) | **All nine suites PASS** (walks 1111 s); #175 box 2: an offline boot logs one failure and no retry line; the link on with the resolver lagging (the image's `/storage/.config/resolv.conf` override at `127.0.0.1`) -- the re-check fails 3 s after activation, `trying again in 10 s` at 1-4 of 9, `Generated a new cheevos token` 44 s after the link, token length 16, exactly one `eth0 -> activated` in the boot; a wrong password logs one ERROR and no retry over a 46 s boot; **upgrade path** (`upgrade-and-install.md`): guest d booted from the kept `ec12767b26` (2026-09-12) image, given a hostname, hardcore on, a RetroArch driver, a ROM and six save states, then updated in place through `/storage/.update` with the RC-4 tar -- on `c5c50a2d5f` 64 s later with the queue emptied, every value and checksum unchanged, the kernel hostname kept, the interface up with no settings parse error and no crash, the offline proxy off (`raofflineproxy-ctl status` -> `enabled=0`) | the `ra-offline` suite was not re-run on RC-4 (the proxy path is unchanged since RC-3, and a run spends a reset of the QA account, D-RA-006); the retry against a real hotspot's DNS is the RG SP's to confirm | the ES log file lags its lines by up to 8 logger batches and WARNING lines have no second channel (#178) -- a live `grep -c` read 0 for lines logged 22 s and 100 s earlier, so the first proof run was read as three FAILs; the credential filter drops the prose `Invalid user/password combination` (mask values, do not drop lines); a backgrounded `reboot` over the serial console never fires (plain `reboot` with `--timeout 8`); `/run/rocknix/resolv.conf` is a symlink into resolved's stub, regenerated at every link change -- the override is `/storage/.config/resolv.conf` | 0.87 s (a) | 1.30 s (a) | 1.51 s (a) |
