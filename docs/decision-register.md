# Decision register

The lookup table for decisions that shape the work but live nowhere findable —
maintainer calls, resolved forks, invalidated assumptions. Without this they sit
in issue comments and chat, and the next session re-derives, contradicts, or
quietly re-decides them.

**Append-only.** A reversal is a new row citing the old ID; never edit or delete
a decided row. Cite rows by ID (`D-…`) rather than re-arguing the choice.

See `.claude/rules/decision-register.md` for when to write and read rows.
This is the index of decisions; `docs/work-logs/` is the narrative,
`docs/blindspot-register.md` the catalogue of weaknesses.

## Decided

| ID | Date | Decision | Refs |
| --- | --- | --- | --- |
| D-CLOUD-001 | 2026-08-28 | **ROCKNIX registers no OAuth client credentials of any kind.** Players supply their own; we make the flow around them painless. An unverified Google app caps at 100 users and expires refresh tokens after 7 days, and rclone is retiring its shared Drive client during 2026 — so a shipped client is a support burden that fails at 100 users and re-breaks annually. | [#51 comment](https://github.com/maxengel/rocknix/issues/51), [#29 comment](https://github.com/maxengel/rocknix/issues/29) |
| D-CLOUD-002 | 2026-08-28 | **Native provider setup is the front door; the SSH wizard is the fallback.** 53 of rclone's 69 backends need no browser, so a form covers them; the 16 that do are listed separately and clearly labelled rather than discovered three screens in. | #51 |
| D-CLOUD-003 | 2026-08-28 | **`rclone.conf` stays out of backups.** A restored device re-runs setup rather than carrying live OAuth tokens through someone's cloud storage. Cheap now that setup is a native page; it was only ever a punishment because setup was hard. | #52 |
| D-CLOUD-004 | 2026-08-28 | **A device with an existing remote is never silently migrated.** Google's device flow is restricted to `drive.file`, so moving an existing user to a ROCKNIX client id would make their cloud files invisible and re-upload duplicates. New setups only. | #51, rclone/rclone#6871 |
| D-CLOUD-005 | 2026-08-29 | **The system-backup upload decides on content, not on the remote's metadata.** A marker records what was last actually uploaded; a differing archive is forced past rclone's comparison. Uploading unconditionally is not available — the game-exit hook runs a full backup on every quit. | #53, `3cbe680764` |
| D-UI-001 | 2026-08-28 | **Mask any field rclone marks `IsPassword` *or* `Sensitive`, with the value visible on opening the row.** Neither flag alone is a correct rule: `sftp.host` is Sensitive, and `s3.secret_access_key` is Sensitive but not IsPassword — so masking either one alone hides a hostname or exposes a secret key. | `675790e8c` |
| D-UI-002 | 2026-08-28 | **Providers are named by us, not by rclone.** rclone's descriptions are written for its documentation and truncate mid-word in a handheld menu. | `675790e8c` |
| D-QA-001 | 2026-08-28 | **Nothing goes upstream until an end-to-end run passes on hardware.** VM verification gates the device build; the device build gates P2. | maintainer, 2026-08-28 |
| D-QA-002 | 2026-08-29 | **The QA WebDAV backend stays bound to loopback.** It holds a test credential; exposing it on the LAN to reach a physical device is not worth it — use a VM, which reaches the host at `10.0.2.2`. | `tools/cloud-test-backend` |
| D-CLOUD-009 | 2026-08-29 | **The per-device cloud identity is seeded from the permanent hardware address, hashed, and stored in `/storage/.config`.** Supersedes the machine-id-only derivation in the first cut of #49. `ethtool -P` reports the address burned into the adapter, so MAC randomisation is irrelevant; hashing keeps a network identifier out of the cloud path; storing it means a wifi-module swap does not strand the backups and the file can be edited to adopt another device's folder. Chosen because a player's "my device" is the handheld, not the installation — so a reflash must find its own backups. | `cloud_device_id`, supersedes part of `b132442778` |
| D-WORKFLOW-001 | 2026-08-29 | **Local build logs are never committed.** A `git add -A` in a build worktree put a 206 MB log into history and GitHub rejected the push; `/build-*.log` and `/publish.log` are now ignored. | `746b6dc3f6`, `4e46985442` |
| D-CLOUD-010 | 2026-08-29 | **OAuth goes through rclone's own `authorize` flow, wrapped by a LAN page — one path for every backend, no provider-specific code.** Settles D-CLOUD-007: no device flow, no hosted callback, no registered client. rclone's `authorize` already runs the exchange; what it lacked was somewhere for a browser to be. The listener binds the LAN only while a sign-in is running, times out on its own, and is gated by a PIN shown on the handheld. Covers 19 backends rather than the 3 a per-provider design would have. | `f63c07c21c`, `cloud_oauth` |
| D-CLOUD-011 | 2026-08-29 | **The typed fallback asks for the PIN rather than carrying it in the address.** A bare address used to answer 403, so typing it in was a dead end unless you typed `?pin=7677` too. The prompt is what lets the handheld show a short address and the code apart — which is what fits beside the QR. Ten wrong answers end the attempt; the QR still carries the PIN so scanning skips the prompt. | `deee4bbed5` |
| D-UI-003 | 2026-08-29 | **Steps that are carried out on the phone live on the phone page, not on the handheld.** The sign-in page cannot scroll, so its space is finite; the phone page has room and is where the redirect-that-fails actually happens. The handheld says only where to go. | `5d626a13e` |
| D-CLOUD-012 | 2026-08-29 | **There is no success page at the OAuth redirect, and there cannot be one.** The provider redirects the *phone* to `http://localhost:53682/`, which is the phone's own loopback; rclone's listener is on the handheld. The redirect URI is registered against rclone's provider app and cannot be pointed at the handheld's LAN address. Serving that page would mean registering our own OAuth client with a hosted callback, which D-CLOUD-001 rules out. Mitigated instead: the failure is described before the click, the paste box is the first thing on the page, and there is a real success page after the paste. | #51, `9612b34d24` |
| D-CLOUD-013 | 2026-08-29 | **The sign-in server outlives any one attempt, and the PIN belongs to the server.** rclone's authorize server answers exactly one redirect and then exits, and provider codes expire in about two minutes — so the ordinary failure ended the attempt and sent the player back to the handheld. A dead attempt is now replaced in place from the phone page. The PIN cannot be re-issued (the handheld is showing it, and a QR built from it), so it moved to the server. | `9612b34d24` |
| D-UI-004 | 2026-08-29 | **The paste box sits above the sign-in button, and the four-digit number is a PIN, never a "code".** Both from a real Dropbox test: the tester approved access, could not see where the address went, and by the time they found it the code had expired. Two things called "code" on one flow is what made it ambiguous. | maintainer, 2026-08-29; `f0af991f9` |
| D-UI-005 | 2026-08-29 | **One cloud entry, not two.** `SET UP CLOUD REMOTE` (the SSH wizard) and `CONNECT CLOUD STORAGE` (native) named the same job; the wizard is now `USE A COMPUTER INSTEAD` one level down, kept because rclone's own config is still the only way to build a crypt/union remote or reconnect a lapsed sign-in. The post-restore advice that named the old entry was updated in the same change. | maintainer, 2026-08-29; `Cloud: one way in` |
| D-UI-006 | 2026-08-29 | **The PIN lives in the address (`http://device:8080/7677`), not on a line of its own.** Only somebody typing the address rather than scanning the QR ever needs it, so displaying it separately was an extra number to read on a page that also asks for something it calls a code. `?pin=` is still accepted, for the prompt's own form and for an older QR. Supersedes the two-line layout in D-UI-004. | maintainer, 2026-08-29; `36b0d51861` |
| D-UI-007 | 2026-08-29 | **A placeholder must never look like a value.** `placeholder="http://localhost:53682/?code=..."` read as a filled-in form; the tester pressed the button on it and the sign-in stopped there. Placeholders on this flow say what to do ("Paste here"), never what the value looks like. | `36b0d51861` |
| D-WORKFLOW-002 | 2026-08-29 | **Adopt from the scaffold estate only where a local failure earned it.** Two of its 41 instruction cards were taken; `git-safety` was rejected because its headline advice (stash before destructive ops) is the one thing this environment forbids, the stash stack being shared across worktrees. | `a371bb77f8` |
| D-UI-008 | 2026-08-31 | **The on-screen keyboard raises itself when the caret lands in a text field, once per field, and the WebView keeps GTK focus throughout.** Auto-raise because a touchscreen handheld focuses a field by being tapped and one without does it by pressing A — neither is a moment to have to know that X exists. Once per field because re-raising a keyboard the player just dismissed, every half second, is worse than never raising it. Focus stays on the page and the d-pad is intercepted before the page sees it: keys that take focus stop the d-pad walking them after the first press, and a WebView that loses focus blurs the field, so the character goes nowhere. | maintainer, 2026-08-31; `cloud-signin-window.c` |
| D-UI-009 | 2026-08-31 | **L1/R1 scroll by running `window.scrollBy` in the page, not by sending Page_Up/Page_Down.** Those keys do nothing while a text field has the caret — which is most of a sign-in form, and exactly the moment the button below the fold is what cannot be reached. | maintainer, 2026-08-31; `cloud_oauth`, `cloud-signin-window.c` |
| D-UI-010 | 2026-08-31 | **One `RECOMMENDED` provider list and one `COMPLETE LIST`, not a split by sign-in type.** Splitting the shortlist into "sign in with your phone" and "sign in with an address and key" grouped providers by our mechanism rather than by anything the player is choosing between, and read as a hierarchy it was not — the second group was a handful of picks, not the rest of the catalogue. The tier now decides what happens *after* a provider is chosen. Restores the pre-`f0af991f9` wording. | maintainer, 2026-08-31; `082edc223` |
| D-UI-011 | 2026-08-31 | **The route is chosen up front — `ON THIS DEVICE` or `WITH MY PHONE` — and the phone is described as a keyboard, never as where the sign-in happens.** The sign-in opened onto the QR screen whichever way the player meant to work, so signing in on the handheld met a page about a phone and stopped there. The provider's page is on the handheld in both routes; the phone is one of two keyboards. Supersedes D-UI-003, whose premise was that the phone carried the sign-in. | maintainer, 2026-08-31; `082edc223`, `ea750c82d` |
| D-UI-012 | 2026-08-31 | **Button-bar spacing is `BUTTON_GRID_HORIZ_PADDING` = 0.022 of screen width, defined once, and button labels are one word.** These are separate fixes: `makeButtonGrid` centres each button in a column of `buttonWidth + PADDING`, so the gap equals the padding regardless of label length — terser labels shrink the buttons but never separate them. The old 0.0052 was 3px on a 640px panel, putting a destructive choice a thumb's width from a safe one. | maintainer, 2026-08-31; `es-native-ui.md` |
| D-UI-013 | 2026-08-31 | **The exit combination is Mode+Start where the pad has a Mode button, Select+Start where it does not, and the on-screen label is read from the hardware rather than written in the source.** Maintainer confirmed Mode+Start is what their fingers reach for on the RG35XX SP, which is also what ES maps its own hotkey to there. Hard-coding it would instruct RG351M and RG353M players to press a button they do not have. `find_gamepad()` already enumerated the pad's key capabilities; `exit_hint()` reports from them, reaching the window via `--exit-hint` and the ES page via `EXIT_HINT=` from `info`. Both combinations remain accepted everywhere. | maintainer, 2026-08-31; `c5f97fa31` |
| D-UI-014 | 2026-08-31 | **Both exit combinations are named on screen, the device's preferred one first** — `MODE OR SELECT + START` where a Mode button exists, `SELECT + START` where it does not. Refines D-UI-013, which named only the detected one: Select+Start already worked and is the convention players arrive with from other handhelds, so naming only Mode+Start told somebody pressing a working combination that it was not the way out. Mode is also not always labelled "Mode" on the shell, where Select and Start almost always are. | maintainer, 2026-08-31 |
| D-CLOUD-014 | 2026-09-01 | **A backup never deletes by default: `BACKUPMETHOD` is `copy`, and a chosen `sync` archives into a dated sibling instead of destroying.** One cloud folder is shared by every handheld, so a mirror makes it match whichever device ran last — it deleted 70 files, another handheld's whole save library, from the shipped upstream default (`9f1fab30f6`). `--backup-dir` must be a sibling of the destination; rclone rejects an overlapping one, verified against the real remote. | `cloud_backup`, this session |
| D-CLOUD-015 | 2026-09-01 | **A changed default is rewritten into existing configs when the old value is destructive.** `cloud_sync_helper` only appends keys it cannot find, so changing a default never reaches a configured device. `sync` → `copy` is rewritten on update with the previous file kept as `cloud_sync.conf.pre-copy-default`, following the `--verbose` strip precedent. Not offered, because somebody who does not want a mirror cannot learn they have one until their saves are gone. | `cloud_sync_helper` |
| D-CLOUD-016 | 2026-09-01 | **The cloud layout migration is offered, never automatic, and moves only the two folders we wrote.** `/GAMES` → `/ROCKNIX/Saves` + `/ROCKNIX/Backups`, server-side, refusing when the destination exists (a move into an existing directory goes file by file and could merge two devices' libraries). Config updated last. Where a player's saves live is theirs to decide — the carve-out in `upgrade-and-install.md` for a choice that is really the owner's. | `cloud_migrate_layout`, maintainer 2026-09-01 |
| D-CLOUD-017 | 2026-09-01 | **Savestates are namespaced by core, with the core build recorded in a per-device manifest rather than in the directory name.** Resolves #10's open question ("arch, chipset, or device?") — none of them. Structure churns permanently; a version in a manifest is data that can be compared and explained at reconciliation. Save files stay one shared pool. Pre-existing states are `unknown`, and consumers must handle that. | maintainer, [#10 comment](https://github.com/maxengel/rocknix/issues/10) |
| D-CLOUD-018 | 2026-09-02 | **The cloud content layout is `ROMs/` and `BIOS/`, not a mirror of `/storage/roms`.** The person filling it in is looking at a file manager on a computer, where `bios` sitting beside `snes` is an accident of how a handheld stores things. Seeding the 126 system folders was tried and rejected in testing — the maintainer looked at the result and still could not tell where ROMs went. Restore reads three shapes (ROMs/BIOS, the flat layout, the pre-`CONTENTPATH` root); backup writes only the first, so libraries migrate themselves. | maintainer, 2026-09-02; `d77d4ea44d` |
| D-CLOUD-019 | 2026-09-02 | **Content sync membership is an allowlist: a system EmulationStation declares, and not something another tier already owns.** It was a denylist — everything under `/storage/roms` bar four names — so `bezels`, `themes`, `music` and a stray `scummvm` folder were uploaded as game systems. Both halves of the rule are load-bearing: ES declares `screenshots` and `savestates` as systems, so the allowlist alone had content sync duplicating what the saves tier already keeps. Matched on `<path>`, not `<name>` — they differ for a dozen systems. | maintainer, 2026-09-02; `d77d4ea44d` |
| D-CLOUD-020 | 2026-09-02 | **Three tiers, by size and cadence: saves (frequent, small, automatic), content (large, static, explicit), system backup (occasional, zipped).** Anything in no tier is a gap to name, not an accident to leave — `/storage/roms/bezels`, `music` and `themes` are currently in none. They do not belong in the zip: it is rewritten and re-uploaded whole each time, which is why it suits small config and not large static assets. | maintainer, 2026-09-02 |
| D-CLOUD-021 | 2026-09-02 | **A backup-format change need not preserve compatibility with `.zip` archives.** Maintainer's call: cloud backup and restore were broken until this work fixed them, so there is no population of working archives to protect. Unblocks #58 (tar over zip, for symlink and permission fidelity — busybox `unzip` aborts a whole restore on a symlink, which has cost a restore here already). | maintainer, 2026-09-02; #58 |
| D-UI-015 | 2026-09-04 | **One cloud entry: `GAME SETTINGS > CLOUD`.** The `CLOUD SERVICES` group in Network Settings is gone, and so are the three save shortcuts that sat in Game Settings beside the hub. Cloud storage is a network service in its plumbing and a game-data service in its job; two doors onto one room also meant one operation could be started two ways, remember different ticks, and stamp two different "last run" answers for the same work. Extends D-UI-005 from the setup wizard to the whole subsystem. | maintainer, 2026-09-04; `a7cee22efb` |
| D-UI-016 | 2026-09-04 | **Floating panels get no drawn border.** Separation comes from opacity (`NOTIFICATION_OPACITY` = 255) and, for a takeover, from dimming the screen behind it — never from a stroke. A themed rim was added to `AsyncNotificationComponent` earlier the same day to make a transfer card readable over box art; it made a RetroAchievements sync look like a different application's widget, because no menu or dialog in this app has an outline. The readability problem was translucency, and the transfer that needed to dominate the screen now has a page of its own. Reverses that change. | maintainer, 2026-09-04; `0c06ad548` |
| D-CLOUD-022 | 2026-09-04 | **A Dropbox conflicted copy is merged with `--ignore-existing`, verified by hash, and only then purged.** 271 directories and 2,788 files appeared under `Content/BIOS` when the desktop client and a device wrote the same paths on the same day; 6 of 21 held the only copy of their data, so blanket deletion would have lost it. The conflicted side is the loser of a sync race, not a newer version, so it fills gaps and never overwrites what Dropbox kept at the real name. Copy, verify, delete — `rclone move` would leave the library split with no record of which files went where. | maintainer, 2026-09-04 |
| D-CLOUD-023 | 2026-09-04 | **A third content action makes the device match the cloud, and its deletions are not recoverable locally.** Backup and restore are both copy-only, so a system removed from the cloud stayed on the device forever — found when `genesis` and `megadrive` turned out to hold byte-identical libraries. Preview, confirm, delete outright: no local `--backup-dir`, because the space reclaimed is the entire point of syncing ROMs selectively and the cloud is the backup by definition, this operation only ever removing what the cloud does not have. Contrast D-CLOUD-014, where a `--backup-dir` is kept precisely because the deleted thing exists nowhere else. Cloud → device only. | maintainer, 2026-09-04; #61 |
| D-UI-017 | 2026-09-04 | **The save actions stay in `GAME SETTINGS > CLOUD SETTINGS`; only NETWORK SETTINGS' cloud group is removed.** Reverses the half of D-UI-015 that deleted `UPLOAD` / `DOWNLOAD` / `SYNC SAVE DATA`. They are not equivalent to the transfer flow that replaced them: they carry their own last-run stamps, which that flow does not show for saves alone, and saves move constantly where ROMs and system settings are occasional — cadence decides placement, not capability. The consolidation also lost `CHANGE CLOUD FOLDER` outright. The single-entry result read as a `CLOUD` group containing one `CLOUD` row. | maintainer, 2026-09-04; `2d63bf317` |
| D-INFRA-001 | 2026-09-04 | **serval's build tree lives on a dedicated volume at `/workspace`, following the fleet convention rather than staying under `~/Development`.** Four device build roots plus a 38 GB source cache had left the 1 TB OS disk with 36 GB free — not enough to rebuild one device, let alone the four an upstream merge had just invalidated. The layout mirrors `tursi-build-box`: `repos/`, `cache/`, `artifacts/`, `builds/`, `containers/`. Chosen over mounting the new disk at the old path because consistency across build boxes is worth a mechanical doc sweep, and over a symlink because the ROCKNIX docker build bind-mounts `$(pwd)`. | maintainer, 2026-09-04; `4a1c6d898b`, lorry `705cf94` |
| D-INFRA-002 | 2026-09-04 | **Destructive disk work stays out of Ansible and lives in a runbook plus a guarded script, run on the target machine.** Inherited from `tursi-build-box`'s runbook 00 (*"Automating these risks data loss on a misconfigured target"*) and confirmed the hard way the same day: hand-written instructions for this migration named `/dev/nvme1n1`, and the newly fitted drive enumerated first, making `nvme1n1` the boot disk. A playbook would have accepted it just as readily. `fleet/scripts/workspace-disk-setup.sh` is the runbook made executable — it identifies the device itself, refuses system disks and non-empty disks, refuses to guess between candidates, and is idempotent. Ansible verifies the result and owns the tree on top. | maintainer, 2026-09-04; lorry `9af4354`, `be718ff` |
| D-CLOUD-024 | 2026-09-04 | **The save-conflict wizard ships in the same feature drop as the cloud-sync revamp, not after it.** Encouraging players onto a cloud backend and then telling them conflicts are avoided rather than resolved puts the burden on exactly the users most likely to hit one — including the maintainer, testing across five devices. There is no time deadline, and no elegant way to exercise conflict handling without the wizard. Milestone "Cloud Saves: Visual Conflict Resolution" (#11, #19–#25) therefore blocks the drop. Design is implementation-ready at `docs/conflict-wizard-ia.md` rev 4. | maintainer, 2026-09-04 |
| D-CLOUD-025 | 2026-09-04 | **#19's savestate-compatibility protocol runs on real hardware before the wizard's compatibility badge is designed as a safeguard or a convenience.** The bench now holds every family the protocol names — H700 ×2 (RG35XX SP, RG SP), RK3326 (RG351M), RK3566 (RG353M) — plus SM8550 (Retroid Pocket Nova). Building conservative and relaxing later was the fallback if hardware were scarce; it is not. | maintainer, 2026-09-04; `docs/savestate-compat-test.md` |
| D-CLOUD-026 | 2026-09-04 | **The cloud layout migration copies, verifies by content, then deletes — it no longer moves.** Supersedes the server-side `rclone move` in D-CLOUD-016. `move` deletes as it goes, so a wifi drop mid-migration split a library across two paths with no record of which files went where (#57). `relocate()` copies, runs `rclone check --one-way` reading the whole output, and purges only on a verified pass; `resumable()` distinguishes an interrupted partial copy (everything in the destination came from the source) from a foreign destination, so the merge-refusal of D-CLOUD-016 stands while a re-run can finish. Found by the phase retro: the changelog had claimed this behaviour while the code still moved. | `b9ea9f3fe8`; retro on #26 |
| D-INFRA-003 | 2026-09-04 | **serval is reached at 192.168.1.246, its wireless address; the router reserves both of its addresses, so the hub's bootstrap address is settled.** Wired `enp128s31f6` holds the reservation at .53 and has no cable; wireless `wlp129s0f0` holds .246 and is the interface that is up. The "lease change" recorded on 2026-09-04 was the inventory naming the wired address of a machine on Wi-Fi. It moves back to .53 only if serval is cabled, which a build box moving 38 GB caches would rather be. The wrong cause in the inventory comment was corrected rather than left, since a false explanation of an outage is the kind that gets acted on next time. | maintainer, 2026-09-04; lorry `2199a91` |
| D-QA-003 | 2026-09-04 | **Fork builds stay without ScreenScraper and TheGamesDB; scraping waits.** ES compiles a scraper only when its credential is in the build environment (`SCREENSCRAPER_DEV_LOGIN`, `GAMESDB_APIKEY`, `HFS_DEV_LOGIN` — `CMakeLists.txt`, `Scraper.cpp`); IGDB and ArcadeDB remain, and IGDB wants the player's own Twitch client ID and secret. The build cost is one line in `~/.ROCKNIX/options`; the cost that matters is that a developer login is ScreenScraper's to grant, and upstream's belongs to upstream's CI. The journey's scrape step is exercised on an official image if and when it is needed. | maintainer, 2026-09-04 |
| D-CLOUD-027 | 2026-09-04 | **The conflict wizard's audit log lives with the other logs, in the OS's persistent log directory: `/storage/.cache/log/cloud_audit.log`, append-only text.** The maintainer's requirement is that it is centralised with the rest of the logs. On a shipped device `/var` is tmpfs (`var.mount`) and `/var/log` becomes a bind of `/storage/.cache/log` only under `debugging` or `/storage/.cache/debug.rocknix` (`var-log.mount`), so every cloud log written to `/var/log` today is gone at reboot. An audit log exists to be read after the reboot that follows a wrong choice, so it goes where the OS itself puts logs when they persist: beside `cloud_sync.log` whenever persistent logging is on, and still there when it is not. Retention: rotate at 1 MiB keeping one predecessor, in-script, no external rotator (#14). Visibility: support-only for now; the wizard's summary page is what the player sees, and a history view reading the file is a later addition, not a blocker. Whether the SQLite index in `docs/conflict-wizard-ia.md` rev 4 is still needed beside a text log is #20's call. | maintainer, 2026-09-04 |
| D-INFRA-004 | 2026-09-04 | **Refines D-INFRA-003: serval's wired port is cabled and down, not uncabled; the hub address stays .246 until the wired link is back.** D-INFRA-003 explained the outage as a missing cable. The maintainer says the cable is in, and the kernel log agrees it was fine: `enp128s31f6` linked at 1000 Mb/s full duplex four seconds after probe in the 15:40 UTC boot, and has had no carrier at all since the 16:01 boot — carrier-up count 0, every counter zero, in six and a half hours. Same kernel (7.0.0-31), same BIOS (M5PKT36A), same command line; the PHY answers (address 2, 10/100/1000 supported and advertised, autoneg on, no driver errors). The one thing that changed between those boots is that the case was opened and the 4 TB NVMe fitted — it enumerates for the first time in the 16:01 boot, and the NIC's BAR and IOMMU group shifted with it. So the fault is between the PHY and the far end: cable seating after the case work, the cable, or the switch port; a PHY stuck low-power across the reboot is second. Driver-side resets need root, which this session lacks. The lesson D-INFRA-003 already drew — do not record an unverified cause as fact — was broken by the row that drew it. | maintainer, 2026-09-04; lorry `3a55949` |
| D-INFRA-005 | 2026-09-04 | **Closes D-INFRA-003/004: the wired outage was a failed ethernet cable; the hub manages serval at 192.168.1.53 (wired) again, with .246 the address to try before declaring it dead.** The maintainer swapped the cable at 23:25 UTC; the link negotiated 10 Mb/s half-duplex for three seconds, then 1000 Mb/s full, and has not flapped since. DHCP handed out the reserved .53, the default route moved to the wire (metric 100 over Wi-Fi's 600), the hub pings .53 at 4 ms and `ansible -m ping` answers pong. .53's host keys were pinned on the hub only after byte-comparing the scan against serval's own `/etc/ssh/*.pub` for all three key types — no trust-on-first-use. The first two attempts at that comparison failed closed (an empty operand, then ssh-keyscan's banner lines in the diff); both were the guard working, and both were resolved by looking at the data rather than loosening the check. | maintainer, 2026-09-04; lorry `2a7ee94` |
| D-QA-004 | 2026-09-05 | **Refines D-QA-003: Skraper on a PC is the interim scraping route for fork builds.** Skraper (skraper.net; free, closed source; Windows/.NET 4.8.1, Linux/mono, macOS via WINE with community support; v1.4.1, development recently resumed) carries its own ScreenScraper developer id and signs in with the player's own ScreenScraper account, so it needs no credential of ours. It scrapes `/storage/roms` in place over the SMB share ROCKNIX already exposes, writing gamelist.xml and media in the RecalBox/EmulationStation layout our batocera-derived ES reads; scrape with ES idle, then reload gamelists, because ES rewrites entries it holds in memory. One scrape serves every device: `cloud_content_backup` carries gamelist.xml on its own rules and excludes only saves, so media travels with the ROMs through the content tier. Limits: no headless or command-line mode, so it runs on the maintainer's PC or the mac mini, not serval; the ScreenScraper account tier sets the thread count; the on-device scraper stays absent, so a newly added game is re-scraped from the PC. Interim, not permanent — a developer id of our own (D-QA-003) remains the answer if on-device scraping is ever wanted. | maintainer, 2026-09-05 |
| D-CLOUD-028 | 2026-09-05 | **The game-exit sync pushes only saves changed since the last successful backup, keyed on time rather than the game's name; it never probes or tidies the remote, skips the system-settings archive, and answers "no network" locally and at once.** The maintainer exited a game and watched an 18-second sync that moved nothing, reading rclone's check counter as every game being uploaded. Measured on the H700: rclone itself took 0.9 s; the rest was three remote round trips, a second rclone start for `listremotes`, the system-archive phase, and seven seconds of console pauses. The maintainer asked for a per-game filter; **time was chosen over the ROM name** because standalone emulators (PPSSPP by game ID, Dreamcast's shared VMU) do not name saves after the ROM, and a name filter would miss them silently. `cloud_backup --saves-only --recent` uses `--max-age` from the last-backup stamp plus ten minutes and `--no-traverse`, forces `copy`, and skips `mkdir` and `rmdirs`. Exit 4 = no default route, shown as SKIPPED like exit 3, no stamp. Nothing changed: 18 s → 5 s. The maintainer's requirement that nobody waits without a network is met by `ip route`, not a probe. Full passes (boot, menu rows) are unchanged and cover files with wrong mtimes. | maintainer, 2026-09-05; ES `6b0d34c0f`; round-trip steps added |
| D-UI-018 | 2026-09-05 | **The hub's SAVE DATA tick and the three save rows in GAME SETTINGS > CLOUD SETTINGS both stay.** They are the same script, flags and stamp file, so neither can drift from or misreport the other; what differs is the workflow each serves. The rows are the after-every-game job — two presses, two-way SYNC, the quick card. The tick is the occasional bundle — "back up everything" or "restore everything" in one action, the full-screen page that outlives it. Maintainer: *"contextual to the two workflows."* Closes the question opened 2026-09-04. Follow-up left with the maintainer, not decided here: the tick is remembered per direction, so unticking SAVE DATA once to push only ROMs makes every later BACK UP TO THE CLOUD skip saves silently — defaulting it on every visit would remove that trap. **Settled by D-UI-020: it stays remembered.** | maintainer, 2026-09-05 |
| D-UI-019 | 2026-09-05 | **The BIOS check is one page listing every system, ordered by what needs doing, with a drill-down per system — no tabs.** The two tabs, "Installed systems" and "All", filtered by whether ES had loaded a system (which it does only when it has games); the maintainer could not tell that from the label, and the strip's 1.6 px underline made the tabs read as stray words. Four shapes were weighed: All/Installed/Missing tabs, Installed/Missing, All/Missing, and **one view with icons — chosen**, refined so that with 3,603 files the missing ones are not buried: one row per system (worst-status icon, name, "2 OF 7 MISSING"), problem systems first and those with games first among them, complete systems collapsed, a press opens the file list. Needs `rocknix-systems --all`, which adds PRESENT lines; the default output and the launch-time warning are unchanged. UNTESTED (present, hash unknown) gets the warning triangle it never had. The screen leaves the tab component, which #63 still owes to five other screens. | maintainer, 2026-09-05; ES `feature/bios-tabs`, pinned by the bump commit |
| D-UI-020 | 2026-09-05 | **Refines D-UI-018: every tick on the hub's BACK UP / RESTORE pages is remembered per direction, and each has a first-visit default — SAVE DATA on, ROMS AND BIOS off, SYSTEM SETTINGS off.** This is how the pages are built (`cloudsync.pick.backup.*` / `cloudsync.pick.restore.*`, `GuiMenu.cpp`), so no code changes. The note on D-UI-018 — that unticking SAVE DATA once makes later bundle backups skip saves until noticed — is accepted as the cost of the page remembering what you chose; the maintainer's rule is "everything should remember, and we also should have defaults." The GAME SETTINGS save rows carry no ticks and always move saves, so the everyday path is unaffected. | maintainer, 2026-09-05 |
| D-INFRA-006 | 2026-09-05 | **Build credentials live only in `~/.ROCKNIX/options` (0600) on the build box, never in the repo and never in a published image.** The maintainer's rule: builds are local, so a value in a build log is tolerable; a secret leaving through a build or through git is not. Guards, each proven against a constructed violation: `scripts/get_env` forwards no secret-shaped variable into the container except the four scraper/achievement keys by name, and `.env` is 0600 and removed after the container exits; the ES recipe logs key names, not values; `tools/fork-publish-release` refuses an EmulationStation binary embedding a developer password or an RA key unless told the accounts were created for the fork; `.githooks/pre-push` scans every pushed branch for credential-shaped values (placeholders in angle brackets pass); GitHub secret scanning and push protection were already on for both public repos. Found on the way: the pre-push guard had not run since the 2026-09-04 move (blindspot 26). Personal ScreenScraper (developer pair, once issued) and RetroAchievements credentials may go in the options file for images that stay on the maintainer's devices. | maintainer, 2026-09-05 |
| D-INFRA-007 | 2026-09-05 | **Refines D-INFRA-006: the line is who can download it, not whether it is called a release.** Maintainer: *"if the build is only being generated on my build server and only being played by me, the key can be in my build, but beyond that, it needs to be stripped out."* A leaked key in a personal, private pre-release is acceptable; in a public pre-release it is not. `tools/fork-publish-release` therefore keys on the target repo's visibility: a binary with embedded credentials publishes to a **private** repo with a note, and is refused for a public or unknown one (unknown counts as public). Drafts are not a private channel — the tool un-drafts on purpose, because interrupted drafts once masqueraded as releases. "Stripped out" means rebuilt without the keys; nothing edits a built binary. | maintainer, 2026-09-05 |
| D-QA-005 | 2026-09-05 | **Fork builds take the ScreenScraper developer pair at runtime, on the device, not compiled in (#64).** The maintainer's idea: rows for developer id and password beside the account rows, and a flag so upstream builds omit them. Implemented as `SCREENSCRAPER_RUNTIME_DEV_LOGIN`, set only by the fork's package recipe: the scraper is always built, `screenScraperDevLogin()` returns the compiled string when `SCREENSCRAPER_DEV_LOGIN` exists and otherwise `devid`/`devpassword` from settings, the OPTIONS page grows DEVELOPER ID / DEVELOPER PASSWORD, starting a scrape with them empty says so, and `backuptool` strips `ScreenScraperDevPass` alongside `ScreenScraperPass`. Upstream builds are unchanged byte for byte. Consequence for D-QA-003/004: fork images no longer lack ScreenScraper; anyone with their own developer access enters it, and Skraper remains the route for those without. D-INFRA-006/007's publish guard now requires a value after `devpassword=`, since the bare fragment is in every fork binary. | maintainer, 2026-09-05; ES `feature/ss-runtime-dev-login` |
| D-UI-021 | 2026-09-05 | **The tab strip is a focus stop: left/right switch tabs only while the highlight is on the strip; rows keep left/right and cycle in place.** Chosen over flattening the scraper page to one grouped list. `ComponentTab` already rendered a focused state that `MenuComponent` never let it reach — the strip sat in the grid as non-focusable and took every left/right on the page. Fixing the component means all five tab screens inherit it and the page keeps upstream's shape. Maintainer's call. | #65, #63 |
| D-QA-006 | 2026-09-05 | **Refines D-QA-003: fork devices use the shared developer account ScreenScraper created for the JELOS distribution (forum thread 7455), entered on the device; the runtime-entry setup of D-QA-005 stays as it is.** ScreenScraper's terms, quoted by the maintainer: one account common to all JELOS developers, "at the slightest problem with this account it will be closed" and no other opened for the distribution; software name at our convenience (short, no special characters, plus a version). The pair is public but never enters the build or the repo — a compiled-in copy would broadcast a credential whose loss is everybody's, and the on-device entry keeps each device accountable for its own use. Maintainer: "we can keep the setup as-is"; working on the H700 the same night. | #64, #66 |
| D-CLOUD-029 | 2026-09-05 | **The shipped write paths stay as they are — no `--update` stopgap on the game-exit upload — until conflict resolution is built.** Maintainer: they are the only user until then, and with or without `--update` the risk is the same in kind: a clobber, or a sync that does not land the intended state; the flag only changes which copy loses. Blindspot 28 stands as the description of today's behaviour; #22 replaces it wholesale rather than patching it. | #22, futro on #11 |
| D-CLOUD-030 | 2026-09-05 | **A save version is identified by the sha256 of its stored bytes; slot number and file name are attributes. After a sync, two slots of one game holding the same hash are compacted: the higher slot is removed, only after both files are re-read and the hashes found equal, and the removal is written to the audit log.** ES renumbers by moving files, so the hash survives a rename and the slot does not; the same hash in two places is one version, not a conflict. Maintainer's reasoning: the player is already asked to resolve real conflicts, so clutter that piles up on its own must not be added to that — a duplicate is nothing to decide. Distinct from *keep discarded saves*, which retains deliberately chosen losers up to a count. `docs/save-manifest-schema.md` §1. | #24, #20 |
| D-CLOUD-031 | 2026-09-05 | **The save manifest is one JSON file per device at `savestates/.rocknix/manifest-<device-id>.json`, describing in-game saves as well as states by path relative to the sync root; the local agreement record is kept unsynced under `/storage/.cache/cloud_sync/`.** Refines D-CLOUD-017 (`states-<id>.json`). Forced by the allowlist: nothing beside an in-game save syncs, and XML is excluded outside `savestates/`. Maintainer signed off on `docs/save-manifest-schema.md` rev 1 (#20's acceptance gate), asking that the approach be checked for alignment against all prior and planned cloud-sync work before capture is built. | #20 |
| D-WORKFLOW-003 | 2026-09-05 | **The council skill is adopted into this repo and runs on serval, with its artifacts stored here; nothing is farmed out to marvin.** Maintainer: council runs for the projects worked on here belong on the same machine so one agent holds all the context directly, and marvin has no good home for this project. Source is the `pfi/pfi-collaboration` lineage (marvin's clone, council at `d51c09c`, 2026-09-03), not the scaffold, whose copy is stale and lacks the lint script. Roster through OpenRouter on a single key the maintainer supplies over a channel that never passes through chat; the key lives in a 0600 file outside the repo (D-INFRA-006 applies). Earning case under D-WORKFLOW-002: the manifest alignment review was one model checking its own design. | #70 |
| D-CLOUD-032 | 2026-09-05 | **Undoing a conflict decision is a first-class requirement of the wizard, and *keep discarded saves* is the mechanism rather than a nicety — an option the player controls, on by default, shallow.** Maintainer, restating the milestone's purpose while the council deliberated: the goal is *"to preserve the sanctity of the user's saves and to empower them with the choice to make a decision about a conflict,"* and undo of that choice *"is also something a user should have the option to do."* On depth: *"they likely aren't going to be going 8 steps back with the save, but may accidentally make the wrong choice with the conflict and want to undo that choice. That's going to be the primary use case."* So the retained loser of a resolution is not clutter and not history — it is the answer to a mis-press, and one step back is the case to design for. Settles the open question `gpt-revised_plan.md` raised: `docs/conflict-wizard-ia.md` rev 3 had the setting OFF and rev 4 did not revisit it, so the council's converged default-ON needed a decision rather than a comment. It has one. Retention is bounded by the IA's count selector; no default count is named anywhere in the corpus, so #22 picks one and states it. Second half of the call, on how much the edge cases may shape the design: *"some of the edge cases around card disconnection, etc., are good to think about, but again, are edge cases that shouldn't constrain our approach unnecessarily."* Consequence for the council's live disagreement: the argument that deletions must never propagate *because* an unmounted card is indistinguishable from a delete is an edge case driving the architecture, and cannot decide the design on its own. The deciding question is whether a wrong outcome is reversible. An unexplained absence must still fail closed — failing closed is cheap and loses nothing — but a design is not obliged to give up deletion propagation to buy it. | maintainer, 2026-09-05; #11, #22, #25 |
| D-CLOUD-033 | 2026-09-05 | **Refines D-CLOUD-032: version one retains the discarded copy and ships no undo control. Restoring one is a separate tool, and #25 is its home.** The wizard's done page says what was discarded and that the copies are kept; nothing in the resolution flow offers to put one back. Maintainer: *"I don't want to add more complexity to the user during conflict resolution. The goal there is to get the user going as quickly as possible."* Settles the council's second tie, whose two vote pairs were two readings of D-CLOUD-032 — retain the bytes and build less, against make undo a control the player can press in version one. The first reading is correct. The maintainer's shape for the tool that does the restoring: *"an option that allows you to essentially go back through conflict resolution flow and use it as a history restore flow, almost like a time machine, to overwrite the existing save with something from the past"* — the same compare-and-choose surface, pointed at a game's retained past versions instead of at a live conflict, and reachable outside the moment of resolution. **The engineering consequence lands in version one even though the tool does not.** Maintainer: it *"might be something we need to think about how we build if we're saving it."* Nothing reads the discard store in V1, so nothing would catch a shape that a restore tool cannot use — a store keyed only by timestamp, or one that drops which game, which slot, which device and which side won. #22 therefore designs the store as a record a later reader can drive a picker from, and #25 owns the reader. | maintainer, 2026-09-05; #22, #23, #25 |
| D-CLOUD-034 | 2026-09-05 | **A correctness fix that costs complexity, CPU or memory has to earn it; the cheapest sufficient form wins.** Maintainer, on the four blockers the winning council plan carries: *"those concerns seem valid. we need to not overcomplicate our solution in a way that adds undue complexity, computational, and memory overhead to our processes."* Binding on the consensus integration and on #21-#24. This is a handheld: a busybox userland, a five-second budget on the path the player watches after a game, and an SD card doing the writing. Applied to the four blockers, three are free or negative: holding a torn multi-file save instead of promoting it after two unchanged passes **removes** a promotion rule; failing closed on split local roots is one check; turning a lock availability test into a real acquisition is `flock` semantics, not machinery. Only the first has a real price — capture may not hard-link a save into staging, because a hard link shares the inode and an emulator writing a battery save in place then changes bytes already treated as sealed, so it must be an independent copy of a few megabytes per changed save at exit. That one is paid because the alternative is uploading bytes that are not the bytes that were hashed. The general rule the row states: name the cost of a safeguard before adopting it, prefer the form that removes a rule over the form that adds one, and do not buy a guarantee the one-player model does not need. | maintainer, 2026-09-05; #22, council r4 |
| D-CLOUD-035 | 2026-09-06 | **One menu entry owns both the conflict queue and the restores: MANAGE GAME SAVE RESTORES AND CONFLICTS, reached from a badge that counts what is waiting.** Maintainer: *"Makes sense to show a badge and then add a menu item to 'Manage game save restores and conflicts'."* Settles the council's queue-and-badge question (P8) and gives #25's restore tool its home in the same row rather than a second one. An unattended pass therefore never opens the wizard over a player; it counts and badges. The entry does the queue in V1 and grows the history view when #25 lands, so the player learns one place. | maintainer, 2026-09-06; #23, #25 |
| D-CLOUD-036 | 2026-09-06 | **The discarded copy of a resolution lives in the cloud, not on the device. Retention defaults to 3 per save, selectable 1 to 9.** Reverses the council consensus plan's local store and dissolves its `.cache`-versus-`.local` question (C4, P2). Maintainer's argument, which is better than the one it replaces: *"If a conflict is only found when cloud sync is happening, why not just use the cloud as the source of truth? ... If sometimes you can restore from the device and sometimes you can[not], what solution provides the most consistency to the user?"* A conflict cannot exist without a sync, so requiring the network to undo one is consistent rather than a regression, and a local store would let a player resolve on one device and find the undo missing on another. **Ordering is load-bearing and this row is not safe without it: the loser must be verified in its cloud location before the winner replaces it anywhere.** KEEP LEFT is a server-side move of a file already in the cloud and costs no upload; KEEP RIGHT uploads the local loser first and the resolution does not apply if that upload cannot be verified. Retention covers save states as well as battery saves, so thumbnails travel too — a numbered state is 28-51 KB and its PNG about 48 KB, a battery save 8-128 KB, so nine kept resolutions of one game stay under a megabyte. The PPSSPP directory case is measured by the census gate before the ceiling is fixed. | maintainer, 2026-09-06; #22, #23, #25 |
| D-CLOUD-037 | 2026-09-06 | **An unexplained absence is shown to the player as a question, not held as a state they have to learn about.** Maintainer: *"be very transparent about an unexplained absence and just let the user decide what they want to do ... In all likelihood, an unexplained absence would be caused by user behavior and user error. I don't want to overcorrect in a way that would add confusion."* So the wizard already being built asks it, in its own words: this save is gone from this device and still in the cloud, bring it back or remove it everywhere. Refines the consensus plan's hold-and-report, which was correct about never inferring deletion from absence and wrong to make the player discover a new state. Causes it covers: a save deleted over the network or with a file manager instead of through the interface, a swapped card, an interrupted layout migration, a desktop cloud client moving something, a delete made on a device still on the old firmware. The mass case stays separate and stays automatic — an unmounted or empty saves root with agreement on record refuses the whole pass rather than producing a hundred questions. | maintainer, 2026-09-06; #22, #23 |
| D-CLOUD-038 | 2026-09-06 | **A sync that can touch saves gates game launch, extending the guard that already ships rather than adding a new mechanism.** Maintainer: *"We should design the sync moments that involve conflict resolution to gate game launch, similar to how when the scraper is running you can't update gamelists and access other menus."* `FileData::launchGame` already refuses to start a game while `ThreadedCloudSync::isRunning()` and says so on screen. The gap is that it only knows about syncs EmulationStation started, so the boot sync — which runs outside it and downloads while an emulator may be flushing SRAM — is unguarded today. The reconciler's session lock becomes the thing that makes the shipped message true for every sync, whoever started it. Replaces the consensus plan's bounded-wait-then-refuse design (B2's ES half) with a pattern players already recognise from the scraper. | maintainer, 2026-09-06; #22, ES `FileData.cpp` |
| D-CLOUD-039 | 2026-09-06 | **The bisync spike is decisive, not informational: it is scored against a written contract, and a narrow gap is filed upstream rather than worked around.** Reverses the council's framing (C6 made the spike informational once bisync was demoted). Maintainer: *"My general concern is that we'd wind up rebuilding a lot of what bisync should be offering. It's a question of whether we're better off figuring out how to work with bisync and then filing requests upstream than building a sync service on top of a sync service."* The distinction the spike must test: the metadata layer is not a re-implementation of bisync, because which device wrote a version, which core, when, and its thumbnail are not on disk and no sync tool can produce them — they are recorded at exit by the only thing that knows. The overlap is the comparison alone. So the spike asks whether bisync can be the transport under that layer without becoming a second authority, and its fixtures include the ones the council believed would fail it: a multi-file save resolved as a unit, a hashless backend, an external resolution with no `--resync`, and an interrupted run. A failure that is narrow and upstream-shaped becomes a request to rclone, not a fork. | maintainer, 2026-09-06; #9, #22 |
| D-UI-022 | 2026-09-06 | **Cloud vocabulary: two verbs, the noun says what, the destination says where. *Back up* and *restore* are the only verbs; the tiers are *settings*, *saves* (game saves, save states, and screenshots), and *ROMs and BIOS*; *system backup* is retired because the archive holds settings and nothing else; *sync* is reserved for the automatic two-way behaviour saves get after #22; the wizard's kept losers are *discarded saves*.** Maintainer, reviewing `docs/cloud-vocabulary-audit.md`: the upload/archive split was rejected — *"A user won't understand the difference between an upload and an archive. Isn't the archive uploaded too?"* — and destination adopted as the axis: *"Isn't this distinct to say 'back up to device' or 'back up to cloud'?"* All names change **now**, config keys and the archive filename included, read-old-if-new-absent on every reader so an upgraded device keeps its values: *"We should change all of the names now and then make sure it's all uniformly implemented … I'm the only one using this, and I haven't even set up cloud saves on my second device."* The one residual on *discarded saves*: a copy a sync replaced without anyone deciding is not discarded by anybody, so #25's restore tool labels those separately. Extends the rules in `es-native-ui.md` (*back up* / *backup*; *game save* / *save state*; serial comma). | maintainer, 2026-09-06; `docs/cloud-vocabulary-audit.md` §4 |
| D-CLOUD-040 | 2026-09-06 | **Refines D-CLOUD-029 lineage: the restore-root setting is removed; saves have one local folder.** Design intent verified before removal, per `engineering-practices.md`: the split arrived with the original ROCKNIX import (`9f1fab30f6`, 2025-07-13, upstream's code, still carried upstream), and in that script the restore root had exactly one consumer — the folder rclone downloaded into. Its comment is its whole intent: *"Changing the below to be different from BACKUPPATH will prevent data from being replaced on restore."* A safety valve from when restore was a mirror with no conflict handling. Once every writer is the reconciler and nothing overwrites without a decision, the danger it guards against no longer exists, and a split would leave the classifier without one local tree — the same conflict re-offered on every pass forever. Maintainer: *"Why not force these to be the same?"* Removing the key is how. The sentence above is what the upstream PR carries. | maintainer, 2026-09-06; #22 |
| D-INFRA-008 | 2026-09-05 | **The device-flashing documentation targets the maintainer's own bench — the private owned-device list in `device-builds.md` and `docs/device-flashing-runbook.md` — not a public installation guide.** Maintainer's clarification when the fresh-card procedure was first written down. The audience is whoever flashes cards for these units, so the runbook records unit-specific evidence ("maintainer's unit is LPDDR3", with the regulator or boot0 reading and the date) rather than model-wide claims a downloader could act on. The public-side answer for H700 downloaders stays with #44. | maintainer, 2026-09-05 19:16 UTC (work log); audit #72 PL-003 |
| D-INFRA-009 | 2026-09-05 | **On serval, raw-device access for flashing goes through a container that receives only the pinned block device and the read-only image — never `--privileged`, never an all-`/dev` bind, and never an administrator password relayed through chat.** Refines D-INFRA-002 for the flashing case. serval's maintainer account has Docker-group access and no passwordless `sudo`; docker-group access is root-equivalent, so the container is the narrower channel only while it is confined to the one device the runbook's §3 checks pinned. A step that needs the kernel to re-read a partition table is done by reinserting the card, or by granting `--cap-add SYS_ADMIN` to that single command with the same one device. | fork resolved during execution, 2026-09-05 (work log 19:29); runbook §4; audit #72 PL-003/PL-004 |
| D-UI-023 | 2026-09-06 | **A menu row is a label and at most one line under it; a description that would make a third line moves into the row's confirmation dialog where it is additive.** Maintainer, on 3.5- and 4-inch panels: *"adding a fuller description isn't necessarily always better ... we don't want to have lots of tiny text"*, and on the cloud settings rows that stacked what a row moves on top of how it last went: *"if the description can be moved into the confirmation dialog and it serves an additive function, that's the best-case scenario in principle (because it allows us to keep it to two lines max)."* Corollaries: a row that opens a page with more than one action is a submenu whose label carries the verb and whose page carries the choices; a row that genuinely needs explaining wants a page, not a longer line. Applied the same hour: the cloud hub row became MANAGE CLOUD STORAGE over its submenu's three section names; the sync row lost the two-way sentence that was already its dialog; the two saves rows keep only how they last went; the transfer page's tier rows keep only what they carry, because that page launches with no confirmation. Refines D-UI-017/018's three-line class rows, which had fixed a run-on sentence by making a stack. | maintainer, 2026-09-06; ES `257116b3bb`, `es-native-ui.md` |
| D-QA-007 | 2026-09-06 | **If the VM can test it, the VM tests it first; a handheld confirms, it does not discover.** Maintainer: *"if we can test something on the VM, we should test on the VM and certainly test it first there."* Made after the vocabulary sweep (#73) was verified on two handhelds — the config migration, the split-root refusal, the archive rename — every one of which the GENERIC_X64 image could have run first with the same busybox, scripts and binary, against `tools/cloud-test-backend`, at no risk to anyone's evening. What the VM cannot prove is enumerated in `engineering-practices.md` (boot-loader selection per board, a real panel, a real provider, and history the VM lacks — which its upgrade recipe reproduces). A device finding the VM missed is a fixture gap to close. Refines D-QA-001/002's VM-as-QA-target posture into an ordering. | maintainer, 2026-09-06; `engineering-practices.md`, `generic-x64-vm-testing.md`, `upgrade-and-install.md` |
| D-CLOUD-041 | 2026-09-06 | **Refines D-CLOUD-036, correcting its labels: the cloud is the left column, so KEEP LEFT keeps the cloud copy and the device copy is the loser that must be uploaded and verified first; KEEP RIGHT keeps the device copy and the cloud loser is a server-side move.** D-CLOUD-036 had the two labels swapped; its mechanics — the loser is verified in the cloud before the winner replaces it anywhere, and only the device-side loser costs an upload — are unchanged. Caught by the council's Step 5 draft against `docs/conflict-wizard-ia.md` ("Cloud is always the left column", settled at rev 3). An orchestrator transcription error, recorded rather than edited because the register is append-only. | council step 5; `docs/conflict-wizard-ia.md` |
| D-CLOUD-042 | 2026-09-06 | **The cloud store of discarded saves is the folder `Saves-discarded` beside the saves folder (`<SAVES_REMOTE>-discarded/`, so `/ROCKNIX/Saves-discarded/` by default), and a legacy config whose restore root differs from its saves folder is refused, not migrated.** Closes two of the four items the council's Step 5 draft left for the maintainer. The name follows the vocabulary (D-UI-022: *discarded saves*) and the `-replaced/` sibling precedent (D-CLOUD-014), so it needs no new config key. Refusal is what D-CLOUD-040 already says and what shipped and was exercised on the RG35XX SP; the draft had proposed migrating and warning once, and the maintainer chose refusal: *"I think 'refuse' is the cleanest."* | maintainer, 2026-09-06; #22, #25 |
| D-QA-008 | 2026-09-06 | **No reboot, update, or power-cycle of a device without asking, at the moment it would happen, naming the device; no standing authorisation.** Maintainer, after a session rebooted the RG SP to apply an image while a restore was running on it: *"you just rebooted my device without asking ... it definitely was surprising to have my device reboot while I was in the middle of doing a system restore."* The session had checked for a running emulator only, and had treated the evening's permission to push builds as permission for every reboot after it. Staging the tarball is inert and allowed; the reboot is the question. The idle check must cover cloud transfers, and is a prerequisite to asking, not a substitute. `engineering-practices.md`, `device-builds.md`, blindspot 29. | maintainer, 2026-09-06 |
| D-CLOUD-043 | 2026-09-06 | **BIOS is not a system: the systems picker no longer offers it, and the ROMs and BIOS tier brings the BIOS folder whenever the cloud has it.** Maintainer: *"BIOS files aren't a system ... The other option would be to remove BIOS and firmware from the ROMs page."* Chosen over a combined page with three sections because the picker is a network round trip and the tick page opens instantly; merging them would put a listing delay before every backup and restore to gain sections of one switch each. The row existed because bios was once treated like any other folder, and the code comment defending it recorded that nobody could say why it was offered. A device that had BIOS un-ticked receives BIOS files on its next content restore, by design. | maintainer, 2026-09-06; #73, ES `970ffead9` |
| D-UI-024 | 2026-09-06 | **The cloud transfer page is seven fitted lines under its title: the ROM, its transfer, the system being copied, that system's files and bytes, the bar, elapsed, and the notice — each clipped to the width, never wrapped.** Maintainer's layout, after a long ROM name wrapped into the totals beneath it and a second line was unexplained: *"1. ROM being copied 2. the transfer info 3. system being copied 4. the system info 5. the progress bar 6. elapsed 7. the notice that this can take a while."* The scripts announce each system to the page (`>>> unit <system>|<i>|<n>`), so rclone's per-run totals become the system's line; the plus-number becomes AND N MORE; the opening status is PREPARING. Refines the four-tier table in `es-native-ui.md` (the long-job page) with its content. | maintainer, 2026-09-06; ES `970ffead9` |
| D-CLOUD-045 | 2026-09-06 | **Save manifest rev 2 (refines D-CLOUD-031, D-CLOUD-017).** The manifest gains, additively: per entry `unit`, `producer` (when imported), `published_at`, `pub`; top level `units` and a bounded `retired` list. The `schema` integer stays `1` because no cloud has received rev 1 and every field is optional to a rev 1 reader. Agreement is recorded only on verified equality and names how it was verified. Manifests are read as a claim set — a device's claim about a path, never the head; the listing is the head. The own manifest's working copy and every foreign manifest live under `/storage/.cache/cloud_sync/`, outside the saves tree, and reach the cloud only as decided transfers. An unchanged manifest is never rewritten. No SQLite index is built; the audit log is the lineage beyond `replaces` (D-CLOUD-027). Drafted by the council's Step 5 seat; adopted with the handoff as it stands, maintainer: *"let's apply the draft as it stands."* | council step 5; #21, #22 |
| D-CLOUD-046 | 2026-09-06 | **The exit path after the cutover (refines D-CLOUD-028).** Capture runs on every exit and spawns nothing. An idle exit spawns no rclone. A changed exit pushes to the cloud only: fresh head evidence per unit before any write, correspondence proven before any agreement advances, three to four spawns on backends with hashes and three plus a capped re-fetch on backends without, and an admission ceiling — measured by Gate 4 on the H700 against Dropbox and the QA backend — above which the unit waits for the next full pass. The "nothing changed ≈ 5 s" contract is preserved. Numbers are filled in from Gate 4, not chosen. Drafted by the council's Step 5 seat; adopted with the handoff as it stands, maintainer: *"let's apply the draft as it stands."* | council step 5; #22 |
| D-CLOUD-047 | 2026-09-06 | **Deletion and absence (refines D-CLOUD-030; applies D-CLOUD-032 and D-CLOUD-037).** A deletion made through the interface is published as a `retired` record naming the `pub` it retires, and applies only to that publication; a later restore is a republication with a new `pub`. A save gone from one side with agreement on record and no applicable retirement is a question for the player, in both directions; an unmounted or empty saves root, or an empty or failing cloud listing, with agreement on record refuses the whole pass. For moves the cloud's layout wins and compaction follows in the same plan. Propagated deletions and compactions are not retained as discarded saves; where `copy --backup-dir` proves reliable (Gate 7), the cloud's `-replaced/` sibling is their record and #25 labels them apart. Residual, stated and accepted: a device holding a deleted version with no agreement for its path re-publishes it. Drafted by the council's Step 5 seat; adopted with the handoff as it stands, maintainer: *"let's apply the draft as it stands."* | council step 5; #22, #23, #25 |
| D-CLOUD-048 | 2026-09-06 | **Scraped game content is a switch on the systems pages, SCRAPED GAME CONTENT, off by default; when off, neither the transfer nor the counts include it, and the counts on both sides use exactly the transfer's rule.** Maintainer, after a restored-then-scraped device read "different size" on every system: *"We could always add a toggle for scraped game content to the list and let the user decide, and then calculate based on this setting."* Label: *"scraped game content ... feels the most literal and explanatory."* Default off *"like on other systems"*. Why: the cloud side counted every file under a system and the device side every file except saves, and neither excluded the game list or the scraper's folders — on the RG35XX SP the SNES folder alone held 343 MB of images, videos and manuals the cloud never had, so totals differed while every ROM matched. Totals cannot answer "do I have what the cloud has" in either direction; a per-file comparison of names under the same rule can, and that is what the pages show now (ON THIS DEVICE / N FILES NOT ON THIS DEVICE / IN YOUR CLOUD ONLY, and the mirror for backup). The scraper's folders under a system: images, videos, manuals, screenshots, fanart, boxart, wheel, mix, maps, media. Each direction gets its own page — SYSTEMS TO BACK UP lists what this device holds, SYSTEMS TO RESTORE what the cloud holds — reached from the ROMS AND BIOS row on its transfer page; the hub's direction-neutral row goes. The systems selection stays shared. | maintainer, 2026-09-06; #76 |
| D-CLOUD-049 | 2026-09-06 | **The game list belongs to game content.** `gamelist.xml` is what the scraper writes beside the artwork, so it moves and counts with GAME CONTENT, not with ROMS AND BIOS: a ROMs-only backup neither uploads nor counts it, and a restored-then-scraped device reads IN YOUR CLOUD under ROMS AND BIOS alone. Favourites and play counts live in that file too; the class's description says GAME LISTS so that is not a surprise. Settles the question parked the same day; #61's "should backup upload a gamelist" is answered by the tick. Maintainer, 2026-09-06: *"I agree with you."* | #76, #77 |
| D-CLOUD-050 | 2026-09-06 | **Game content is a class on the transfer page, and the systems page follows the ticks.** Refines D-CLOUD-048: WHAT WOULD YOU LIKE TO BACK UP? offers SAVES, ROMS AND BIOS, GAME CONTENT, SETTINGS; CONTINUE (shown when either per-system class is ticked) opens SYSTEMS TO BACK UP / RESTORE, whose first line says what moves and whose group is SYSTEMS ON THIS DEVICE / SYSTEMS IN YOUR CLOUD, listing systems that hold anything of the ticked classes, counted by file under those classes. No switch on the systems page, no entry row on the transfer page. **Game content moves on its own** (`--media-only`) as well as with ROMs (`--with-media`). Maintainer, 2026-09-06: *"Continue would take you to the selection of what systems you're backing up the content from, and then you would move on … it would just say 'systems on this device' and tell you what you are backing up."* | #77 |
| D-QA-009 | 2026-09-06 | **Conflict and bisync testing runs on VMs only — a pair of GENERIC_X64 guests against one QA remote, once on WebDAV and once on MinIO — never on a handheld.** Maintainer: *"our comprehensive bisync testing for conflict management … on devices for random testing, it's too high-stakes to risk something going wrong there. If we need another VM to test against so we can create conflicts, we should do that and have two VMs for that."* Refines D-QA-007 (VM first) into VM-only for anything that manufactures a conflict or interrupts a transfer; a handheld sees the reconciler only after it ships, through its own ordinary use. Provider differences (blindspot 8) are not checked from a device; if they are checked at all it is from a VM against a scratch folder, on a decision made then. Other devices (RG353M, …) join once the VM pair passes. | #9, #11, #35; futro 2026-09-06 |
| D-CLOUD-052 | 2026-09-07 | **Gate 11's verdict: the reconciler's own transport for every pass — `rclone copy --files-from --ignore-times` per direction (the handoff's I22); bisync is not used.** Scored on the VM pair against #9's contract, both backends, rclone v1.75.0 (D-CLOUD-039's rule: any architectural FAIL decides). Architectural: **item 10** on both backends — bisync cannot run without its own listings and demands `--resync`, whose default `--resync-mode path1` is a silent resolution in the device's favour (the milestone's cardinal rule), and which every device's *first* link and every loss of `/storage/.cache/rclone/bisync` would need unattended; **item 4** on the hashless backend — no agreement hash, so a same-size, same-mtime change is invisible unless `--download-hash` fetches the whole tree every pass; **item 6** on the hashless backend — recovery after `kill -9` renames a torn head `.conflict1`/`.conflict2` on both sides inside a wet run no dry-run guard reaches. Items 1, 2, 3, 5, 7, 8, 9 PASS. The maintainer's concern (D-CLOUD-039) answered by the runs: what the copy transport needs that bisync gave — retired records for a renumber (item 7), a pending-publish record for an interrupted push (item 6) — #21/#22 already specify for the wizard; bisync beside them would be a second state machine, not a saving. | #9 (the scoring table and the raw runs), #22; `tools/cloud-round-trip` 747a53b578 |
| D-CLOUD-044 | 2026-09-07 | **Moot.** The spike did not fail narrowly: item 10 is architectural on both backends (D-CLOUD-052), so there is no upstream-filed gap for the reconciler to hold around. Cites the parked question of 2026-09-06. | #9 |
| D-QA-010 | 2026-09-07 | **Gate 12 (the census) runs in two rounds, by hardware.** Round one on the RG35XX SP covers what it holds: libretro cores — in-game saves beside the ROM and save states with their thumbnails, including a save-state deletion so ES's renumber is seen. The standalone layouts (N64 `.eep`/`.mpk`, PPSSPP savedata, DuckStation memory cards, Flycast VMU) wait for a device that runs them well; maintainer: *"These are all systems that are a bit too powerful and not well optimized for the control systems … we might want to wait and do a second round of testing once I have the Retroid Pocket Nova going."* #21's unit table therefore lands in two parts, and its N64 acceptance box waits for round two. | #11 (Gate 12), #21, #22; `tools/cloud-census` |
| D-CLOUD-053 | 2026-09-07 | **Every writer of the saves tree inside EmulationStation is gated by the transfer lock, and a deletion made from a menu is recorded.** Refines D-CLOUD-038 (the launch gate) and #22 R6. Maintainer: *"we might not want to let someone modify a game save or start the same game they're exiting, or any game for that matter, until cloud saves are complete. It could create a weird race condition if someone exits a game, it's backing up to the cloud, and then they try to delete a save or select a new one."* So: a game launch (shipped today for a sync ES started, `FileData::launchGame`; extended to every sync by D-CLOUD-038), the save-state manager's DELETE and the renumber it triggers (`GuiSaveState` → `SaveState::remove` → `SaveStateRepository::renumberSlots`, which knows nothing of the cloud today), and any later editor of saves check `L_T` first and refuse with the shipped running-sync message while a pass holds it — non-blocking, nobody waits (R6). And a deletion the player makes from the manager writes a `retired` entry (#21 R3), so the next pass propagates a decided deletion instead of asking an unexplained-absence question (D-CLOUD-037). The bytes already in flight were never at risk — capture seals an independent copy (D-CLOUD-034) — the race is over the tree the next pass reads. | #22 (R6), #21 (R3), #23; the maintainer's census session, 2026-09-07 |
| D-QA-011 | 2026-09-07 | **Ask before staging a build on a device, not only before the reboot.** Refines D-QA-008, which had allowed the copy into `~/.update` without a question because it is inert until the next boot. Maintainer: *"Why wouldn't you just ask me if it's okay to transfer? I thought that was our policy. It's fine if you're waiting for it to be idle, but you might as well just ask."* The copy is a gigabyte over the device's Wi-Fi and a write to the card; it is asked for, once per batch if the maintainer says so; idle-waiting is a courtesy after the answer, never instead of it; no unattended waiter stages a device. The reboot stays a per-device question every time. | this session; `engineering-practices.md`, `device-builds.md` |
| D-CLOUD-054 | 2026-09-08 | **No. A saves root that changed cards never reaches the reconciler; the transfer refuses first.** On a two-card device `/storage/roms` is the second card when it is mounted and the internal card when it is not (RG SP, 2026-09-08: 101 stale files on the internal card, written by a boot restore at a boot without the card — #83). `cloud_saves_root` remembers the filesystem UUID under `SAVESPATH` in `/storage/.cache/cloud_sync/saves-root` (the internal card, whichever card the saves are on) after every successful saves backup or restore; when it differs at the next transfer the saves phase refuses with the reason and exit 1, until the owner runs `cloud_setup --accept-saves-root`. No record means nothing to compare, so an upgraded device records on its first transfer and is never asked. So the sync context (I1) carries no identity field. When #22's reconciler lands, `accept` is where its per-device agreement is cleared, so the first pass on the new root re-links — identical pairs seeded, differing ones queued, nothing replaced — on the owner's word rather than on a UUID compared in every context. Proven on GENERIC_X64 guest a: refusal on backup and on restore, accept, re-record, and a run with no record. | #83, #22 (R7), `89f1cb15b7` |
| D-CLOUD-055 | 2026-09-08 | **The saves-root guard waits before it refuses, and checks the card again after the transfer.** Refines D-CLOUD-054. The RG SP's second tree came from a boot race, not a missing card: udev starts the automount when the card is detected, the automount unmounts `/storage/roms` and binds the second card back a second later, and the interface and its boot restore start in that same second (journal 2026-09-08 23:48:57–58). So `cloud_saves_root check --wait 15` re-reads a mismatch once a second before refusing — the race becomes a delay — and `record IDENTITY` refuses to record, and the phase fails, when the folder is on a different card at the end than at the start. Proven on GENERIC_X64 guest a (wait passes; wait times out; flip under a throttled copy; unflipped regression). The root cause is a unit-ordering defect in ROCKNIX (the automount is not in the boot transaction) and gets its own issue and its own review. | #83, `9618b63d7c` |
| D-CLOUD-056 | 2026-09-08 | **The wrong-card writes are fixed at the source: `automount` waits for an expected external card before binding internal.** Root cause of D-CLOUD-054/055. `rocknix-automount` scans for the external card once and, if it has not enumerated yet, binds the *internal* card over `/storage/roms`; the boot restore then writes to the wrong card (#83's second tree, both cards in throughout). Unit ordering was a red herring — in monotonic time the automount finished ~10 s before the interface on both handhelds; the wall-clock journal only looked otherwise because NTP corrected the clock mid-boot. `find_games` now retries for up to 15 s when the device expects an external card (`system.merged.device=external` or a pinned `system.gamesdevice`); a one-card device does not wait. This protects every tier that writes into `/storage/roms`, above the per-tier `cloud_saves_root` guard. Verified on GENERIC_X64 against the real function body. | #84, `accaadc3c0` |
| D-CLOUD-057 | 2026-09-08 | **The automount card-wait costs a normal boot nothing.** Refines D-CLOUD-056 after the maintainer asked whether it adds 15 s to boot. It does not: the retry runs only when a candidate external block device is present but not probed yet (`external_node_present()` over `/sys/block`, non-internal disk > ~8 GB). A removed card has no node and boot falls to internal after a 2 s grace; the ceiling is 10 s, paid only by a card present-but-unreadable; a card ready on the first pass waits nothing. Also fixed a `$#`-expansion bug the test surfaced (the sed was double-quoted). Verified on GENERIC_X64. | #84, `b82d19ab94` |
| D-QA-012 | 2026-09-08 | **Every out-of-band maintainer request is filed as a fork issue in the same session, quoting their words.** Maintainer: *"we should create issues to cover them ... so we have the right paper trail of what we've been doing."* Small items raised together may share one issue with a section each (#85); a concern that becomes a requirement on existing work is recorded where it lands and also made findable as an issue. Rule in `issue-tracking.md`. | #85, #86 |
| D-CLOUD-058 | 2026-09-08 | **The seal stage is content-addressed and pub-keyed, and its price is stated.** Capture copies every recorded version to `/storage/.cache/cloud_sync/stage/<sha256>` (D-CLOUD-034's independent copy: the hash names bytes a later flush cannot change) and #22 pushes that file, not the tree's. GC deletes a stage file referenced by no entry or only by entries whose `pub` is non-null. Until #22 sets `pub` the stage holds one copy of every recorded version — ~5–20 MB on the round-one census device, a few hundred MB once round two adds PPSSPP savedata; above 256 MiB capture WARNs with the bytes and the count and prunes nothing, because pruning a referenced seal would silently weaken acceptance (3). #22's obligation, recorded: push `stage/<entry.sha256>` and set `pub` on success. | #21, #22; #21; `feature/capture` (hash at commit) |
| D-CLOUD-059 | 2026-09-08 | **Thumbnails are declared members without entries.** A state's `.png` gets no `sha256`, `slot` or `pub`: a fourth `kind` or a new per-entry field would reopen §6's frozen shape (D-CLOUD-045). Its membership is carried by `units[].members`, derived from its state's `screenshot`; #22 pushes it by path with its state; its torn test is declared-member-versus-listing, which needs no entry. Residual, accepted: a publication that carried the state and dropped only the thumbnail is indistinguishable from a deleted thumbnail by manifests alone, and acceptance (3) does not extend to it — if #22 needs more, an optional rev 2.1 `sidecar_of` with the maintainer's nod, not capture's to invent. Never a substitute image (#23). | #21, #22, #23; #21; `feature/capture` (hash at commit) |
| D-CLOUD-060 | 2026-09-08 | **A verify pass records unknown provenance and never adopts a path that has no entry.** `cloud_capture --full` and `--rescan` hash what the manifest already claims; a new version at a claimed path sets `emulator`/`core`/`core_build` to `"unknown"` and `core_display_version` to `null` rather than carrying the previous entry's core — inventing authoritative-looking wrong data is worse than leaving it unknown (`upgrade-and-install.md`). Neither pass adopts a stray `.state7`, a foreign `.srm` or another client's file; §4's `unknown` provenance is the answer for those. `--rescan`'s one exception is a MOVE onto a renumbered slot, from a version this device already claimed in the same unit. | #21; `feature/capture` (hash at commit) |
| D-CLOUD-061 | 2026-09-08 | **A member with no entry is recorded only when this session wrote it.** Exit mode is called with `--started <tstart>`; an unclaimed member whose mtime predates it is not recorded (one INFO line), so an upgraded device's pre-existing library is never stamped with today's core — the `core_build` #19 will compare must be a pin somebody observed. `--started 0` is the explicit record-everything form for QA and a manual run. Consequence, accepted: a file this device never recorded stays unrecorded until a transfer touches it (#22 R1) and is conflict-tested by hash alone. | #21, #19; #21; `feature/capture` (hash at commit) |
| D-CLOUD-062 | 2026-09-08 | **The frozen pair is read from the finished command line, and `_changeCommandlineArgument` is fixed for the `--key=` form.** `LaunchGameOptions` gains `launchedEmulator`/`launchedCore`, filled in `getlaunchCommand` after `setupSaveState` from the `--emulator=`/`--core=` tokens the way runemu.sh reads them; `launchGame` passes them to capture, never `getEmulator()`/`getCore()` re-resolved at exit. Schema §9's sentence that pointed #21 at those accessors is corrected in the same build. `SaveState::_changeCommandlineArgument` matched `-core` inside ROCKNIX's `--core=` token and would have produced `--coremgba`, dormant only because no `es_savestates.cfg` ships; fixed (joined form first, token boundary on the legacy path) because a failure you find is yours, and because it is what makes acceptance (2) observable — a QA `es_savestates.cfg` becomes a working divergence instead of a broken launch. Acceptance (2) is reworded to the token rule. | #21; ES `feature/capture-hook` (hash at merge) |
| D-CLOUD-063 | 2026-09-08 | **Capture never refuses on a wrong-but-stable card and always discards a run during which the card changed.** Refines D-CLOUD-054/055 for a non-transfer: R5 says every exit, and the refusal those rows describe is scoped to the transfer. `cloud_saves_root check` failing (exit mode: no wait; `--full`: `--wait 10`, D-CLOUD-057's ceiling) is a WARN, a `!card` marker in the durable stamp, and the write is recorded — the file is real and its claim belongs (I5). An identity that differs between the start and the end of the run discards the run with an ERROR, because that record would carry a `replaces` step across two trees. | #21, #83; #21; `feature/capture` (hash at commit) |
| D-CLOUD-064 | 2026-09-08 | **The boot autostart runs `cloud_capture --full`, gated on a cloud-saves toggle and backgrounded.** Amends the judged design, which ran it above and outside the `cloudsaves.startup` gate: `102-cloud-saves` runs `--full` only when `cloudsaves.startup` or `cloudsaves.gameexit` is `1`, as `setsid nohup … >/dev/null 2>&1 &` under `nice`, so boot is never delayed and a device with cloud saves off never runs it (#84/D-CLOUD-057: boot time is not ours to spend). Consequence, accepted: on such a device an ES abort mid-game, a shell edit or an equal-size equal-mtime rewrite waits for the first boot after a toggle goes on; with cloud saves off nothing publishes, so nothing is lost meanwhile. Two further consequences of backgrounding, both accepted. **The pass can still be finishing when the first game exits**, so the two are concurrent writers of one manifest: capture takes no lock, the loser sees the file's inode or mtime change between its load and its rename, and an exit, `--rescan` or `--retire` that loses **re-runs itself once** against the winner's document (`CLOUD_CAPTURE_RETRY`), because a discarded first version is not self-healing — with no entry and an mtime below the next `--started` nothing would ever record it; `--full` never retries. **On a two-card device that has never synced** there is no saves-root record, `cloud_saves_root check --wait 10` returns at once, and the pass may hash the internal card's copies before the automount binds the second card — the next exit then records the real file with `replaces` pointing at that stale sha, a bogus lineage step; a flip *during* the pass is discarded, the pre-first-sync window is not (a device that has never synced has published nothing yet). #22's reconciler runs `--full` inside its own lock as the first step of every pass. | #21, #84; #21; `feature/capture` (hash at commit) |
| D-CLOUD-065 | 2026-09-08 | **The runemu.sh `cloud.backup` exit hook is removed as the second exit-path spawn.** It came in with the initial ROCKNIX import (`9f1fab30f6`), read SystemConf `cloud.backup` (shipped `0` in `system.cfg`) and ran `/usr/bin/run /usr/bin/cloud_backup` synchronously before `quit` — before ES's exit path resumed, so before any capture — and nothing has written the setting since ES took the sync over (the rclone `package.mk` game-end comment). Deleted with its shipped default: #21's DOES-NOT-BUILD list forbids a second exit-path spawn, and capture is the one process the exit path gains (R5). An upgraded device keeps a dead `cloud.backup` key in its own `system.cfg`; nothing reads it, and nothing removes it. | #21; `runemu.sh`, `system.cfg` on `feature/capture` (hash at commit) |
| D-CLOUD-066 | 2026-09-08 | **The standalone N64 unit row follows the emulator's `SaveSRAMPath`; the sync allowlist is not widened in #21.** Every shipped `mupen64plus.cfg` sets `SaveSRAMPath = "/storage/roms/n64"`, so capture's `mupen64plus` row is `<romdir>/<stem>.{eep,mpk,sra,fla}` with the historical `<romdir>/save/<stem>.*` as a second candidate, and CAP6 proves the pair as one unit locally. The judged design also corrected `cloud_sync-rules.txt` and its `.defaults` (`+ /**/*.{eep,mpk,sra,fla}` beside `+ /**/*.srm`; `- /savestates/**/*.bak` ahead of `+ /savestates/**`), but widening what a backup carries for every device with N64 content is its own decision: #89 owns it, and #21 does not change what syncs. Until it is decided those saves are recorded and not published. | #21, #89, #88; #21; `feature/capture` (hash at commit) |
| D-CLOUD-067 | 2026-09-08 | **The settings archive carries the device's label between the date and ROCKNIX, and cloud retention counts only archives carrying this device's label, inside this device's own folder.** `backuptool` writes `<stamp>-<label>-ROCKNIX_SETTINGS.tar.gz` with `cloud_device_id --label` (the device-tree name, the same source as the cloud folder, D-CLOUD-009). The label sits in the middle, not in ROCKNIX's place: the name still ends in `-ROCKNIX_SETTINGS.tar.gz`, so `newest_backup` — unchanged, in this build and in every image already on a device — matches it; the review found that a first cut which replaced ROCKNIX was invisible to an older image's finder. Maintainer's ask: two handhelds' archives were indistinguishable by name. Fork resolved: `CLOUD_BACKUP_KEEP` counts and removes **only archives carrying this device's label** in `SETTINGS_REMOTE/<device id>/`; another device's own folder is never touched; an archive carrying another label, or none (the pre-label `ROCKNIX_` shapes), is **left alone rather than counted** — it cannot be attributed, deleting what cannot be attributed is the failure being fixed, and the pre-label ones are already bounded by the old rule. A same-label archive from another device can be in this folder only as a re-uploaded copy (the #26 journey) or through a shared id (a cloned id, #86); it counts as this device's own, and the id is deliberately not in the name. `cloud_restore` prefers this device's newest labelled archive and otherwise takes the newest overall, logging whose it was. `cloud_device_id --label` reads `HW_DEVICE` from `/etc/os-release` when the environment lacks it, and the cloud scripts read `OS_NAME` the same way. | `backuptool`, `cloud_backup`, `cloud_restore`, `cloud_device_id`, changelog *The archive says which handheld made it*; review findings 1–2, 2026-09-08 |
| D-UI-025 | 2026-09-08 | **The transfer page's bar shows the lower of two percentages rclone printed — the transfer's (bytes, or files transferred when the bytes have no number) and the checks' — and never a number nobody printed.** Fork resolved during the transfer-page fix pass. The bar read the byte line alone, so a saves backup with everything already in the cloud (`0 B / 0 B, -`, no per-file lines, no count line) showed the spinner from start to COMPLETED — the maintainer's *"we seem to have lost the progress bar."* The brief's bytes-first-then-checks order was implemented and reviewed: it pins the bar at 100% over a live CHECKING 120 OF 400 FILES once one changed save lands. The reviewer's minimum over all three was rejected because the count of files transferred counts *completed* files — the VM capture of two files moving together reads `0 / 2, 0%` while bytes climb 24 → 93%, so that minimum sits at zero for most of a run. Each stats line owns its percentage (`-` parses to -1) and nothing is reset between blocks, because rclone 1.75.0 prints a stats line in every block once it has printed it at all (`stats.go`: the counters only grow; the retry loop resets errors alone); `>>> unit` resets to the spinner. Refines D-UI-024's line 5. | maintainer, 2026-09-08; review finding 1; ES `GuiCloudTransfer.cpp` `refreshPercent()`; changelog *The transfer page shows a bar for a run that only compares* |
| D-CLOUD-068 | 2026-09-08 | **The device id is seeded from a validated hardware address or nothing; a stored id ending in the shared constant `ee5013fc56` is healed once, and the folders written under it are read but never written.** Both handhelds carried `ee5013fc56` because `usable_interfaces` was a name denylist and `permanent_address` a three-value denylist: on every kernel with `CONFIG_IPV6_SIT=y` the sorted list is `lo sit0 wlan0`, `ethtool -P sit0` → `notset` passed, and md5(`notset|unknown`)[0:10] is that constant (`DEVICE` is never set on a device, so the "family" term was always `unknown`). Now an interface counts only with `/sys/class/net/<n>/type` = 1 and a `device` link, and a seed only if it matches `^([0-9a-f]{2}:){5}[0-9a-f]{2}$` case-insensitively and is not all-zero or broadcast — the shape is the guard, not a list of known bad values. **The hash recipe is unchanged** (`md5(seed|${DEVICE:-unknown})`), so every correctly seeded device keeps its id and its `--legacy` name; switching the term to `HW_DEVICE` would be a second identity change and is not made. **Healing is scoped to the poisoned suffix**: when a validated address is present the old id is appended to `cloud_sync-device-id.previous`, `<label>-<hash>` is written and `old -> new` logged (`logger -t cloud_device_id`, `/var/log/cloud_sync.log`); with no address the stored id is returned unchanged; any other stored id — including one unlike this hardware's hash — is left alone (D-CLOUD-009's module-swap and adopt-a-folder cases). `cloud_device_id --previous` lists `.previous`, then `<label>-ee5013fc56` and `<hostname>-ee5013fc56` deduplicated — **the two guessed names only where `/sys/class/net/sit0` exists**, because a device without one could not have hashed `notset` and every image ships the hostname `ROCKNIX`, so an ungated list sent a fresh RK3326 to the RG35XX SP's folder before the root tier (review finding, fix pass; the trade — a healed device reflashed onto a later build with `sit` as a module loses the guessed names — is accepted); `cloud_restore` tries own → `--legacy` → each `--previous` → root and logs which it took (WARN on a `--previous` folder, which this device *may* have written and a same-model or same-hostname device may share); `cloud_backup` writes and trims only the current folder, and **its upload marker is keyed on destination as well as content** (`<hash> <destination>`; a bare-hash marker from an older build forces exactly one upload) so the first backup after healing populates the new folder even when the archive is unchanged — a marker keyed on content alone left the healed folder empty until the next `backuptool backup`. `cloud_capture` **adopts `manifest-<previous id>.json` as `manifest-<new id>.json`** when nothing sits under the new name (read both shapes, write the new one), once, before the pass. Consequences, accepted: the RG SP becomes `Anbernic-RG-SP-f058e3e9e8` and the RG35XX SP `Anbernic-RG35XX-SP-a431b25ede` on the first call of the new build that sees the adapter's address; their old folders are left as they are. Two devices of one model on an older build still share a folder. | #86; `cloud_device_id`, `cloud_restore`, `cloud_backup`, `cloud_capture`, `tools/cloud-round-trip` A15; changelog *The device id is seeded from a hardware address or nothing*; review fix pass 2026-09-08; `feature/device-id-seed` (hash at commit) |
| D-LAUNCH-001 | 2026-09-08 | **`runemu.sh` reports a launch that ended by the exit hotkey's signal as a clean exit (0); every other non-zero status still collapses to 1.** Fork resolved during #90's fix pass. Once `wait_lock`'s trap stopped re-exiting with rm's 0, `runemu.sh`'s `quit 1` for any non-zero `ret_error` reached EmulationStation for the first time — and `ret_error` is not "the launch failed": the global exit hotkey (`input_sense` `execute_kill`) ends a standalone emulator with `killall -9` (35 of the 36 standalone start scripts; Steam on arm64 is the exception) and RetroArch with SIGTERM, so a player leaving mednafen, Dolphin or xemu by the hotkey after an hour returned 137, and ES (`FileData::launchGame`) records PlayCount, GameTime and LastPlayed only on 0 and hands the same code to `cloud_capture --exit`. Two ways out were on the table. Rejected: ES recording play stats whenever elapsed ≥ 10 s regardless of `exitCode` — it would count a launch that failed slowly, and it puts the fix in a second repository for a contract `runemu.sh` owns. Taken: 137 and 143 (128+SIGKILL, 128+SIGTERM) map to `quit 0` with a log line; codes are **not** passed through, because ES reserves 200–300 for messages it can name and an emulator's own codes would land there. Consequences, accepted: an emulator the kernel's OOM killer ends (SIGKILL) is indistinguishable from the hotkey and reads as clean; a standalone `start_*.sh` whose last command is cleanup still reports the cleanup's status, as before; the durable stamp's `emu-exit=` is therefore the launch's exit as ES saw it (0/1), not the emulator's code, and is worded so wherever it is described. Pre-existing and unchanged, named in the upstream body: `wait_lock`'s trap replaces a caller's own EXIT/INT/TERM trap (`rocknix-bluetooth`, the SM8xxx `fancontrol`), and a lock-taking script that systemd stops now exits 143 rather than 0 (`fancontrol.service`, `Restart=on-failure`, logs `Failed with result 'exit-code'` around `runemu.sh`'s `systemctl restart fancontrol` on devices whose fallback branch calls `set_setting`). Limitation found in the same pass and not resolved here: RetroArch's Unix signal handler runs `exit(1)` on the **second** SIGTERM, and on the VM the first did not end it in 150 s, so a forced quit of RetroArch by the global hotkey reports 1 — the failed-load code — and records no play stats; RetroArch's own quit still exits 0. #92 holds it, D-LAUNCH-002 (open) the question. | #90, #92; `runemu.sh`, `001-functions` on `feature/exit-status`; review fix pass 2026-09-08; VM proof in the 2026-09-08 work log |
| D-UI-026 | 2026-09-09 | **The transfer page names the item first and numbers it across the run (refines D-UI-024).** Row 1 is the item (`BIOS`, `NES`, `SAVES`, `SETTINGS`), row 2 `ITEM i OF n` where n is the whole run's count (ES composes the run, so it knows), row 3 the activity line (`CHECKING n OF m FILES`, `TRANSFERRING <file> ...`, and while the settings archive is written a line that says so), row 4 that item's totals; bar, elapsed and notice unchanged. **Every phase shows the same shape**: the settings and saves phases announce their unit with an index before the work starts, and `backuptool`'s output is no longer discarded. Maintainer: *"It should probably show the item first, so it would say it's working on BIOS. Instead of just saying '10 of 10', say 'Item 10 of 10', and then below it, show the file that is being evaluated or how many are being checked ... Also, settings backup appears pretty different than the others ... consistent layouts on the 'Backing up to the cloud' screen."* | #95, #85 |
| D-UI-027 | 2026-09-09 | **A content picker row quantifies only the delta, in the direction of the page: `253.9 MB NOT YET IN YOUR CLOUD · 14 FILES`, or `NOTHING NEW TO BACK UP`; restore reads `NOT YET ON THIS DEVICE` / `NOTHING NEW TO RESTORE`. No totals on the row** (refines #85 item 1, which had `ALREADY IN YOUR CLOUD (<total>)` for the zero case). Maintainer: *"what the user really wants to know is ... does anything need to be backed up or not? ... When I see, for example, NES already in your cloud, 253.9 MB, I don't know what that means ... instead of saying 'already in your cloud,' what we're trying to address is 'not yet in your cloud.'"* | #85 |
| D-CLOUD-069 | 2026-09-09 | **The settings-backup folders left in the cloud by the poisoned device id (`ROCKNIX-ee5013fc56/`, `Anbernic-RG-SP-ee5013fc56/`) are removed; the cloud matches the end-state layout (one labelled folder per device, #86) rather than carrying legacy folders the restore chain tolerates.** Maintainer: *"if we need to clean up leftover or earlier version files, I'm fine. I'd rather have everything match the end state we're working toward when it comes to file backups, etc. It's relatively easy to keep backing up."* Done copy-verify-then-delete: every file first copied to the build host (`/workspace/artifacts/rocknix-images/legacy-cloud-backups-20260909/`) and every archive tested, then `rclone purge` from the RG SP. Accepted consequence: the RG35XX SP, whose only cloud archives sat in `ROCKNIX-ee5013fc56/`, has none until its next backup. | #96, #86, #49 |
| D-CLOUD-070 | 2026-09-09 | **`last-capture` keeps one line per mode (`exit`, `rescan`, `full`, `retire`), each replaced only by a run of the same mode (refines D-CLOUD-065's stamp).** Found on the RG SP: a game exit at 08:19Z wrote the stamp, a power-off and boot ten hours later ran the backgrounded full pass, and its line replaced the exit's, so the only record of how the game exited was gone while the manifest (I8) survived. A stamp with the old single line still parses; readers take the line for the mode they ask about. Maintainer: *"Your proposal sounds good."* Lands with #94's "how it last went" rows. | #94 (criterion added), #21 comment 2026-09-09 |
| D-CLOUD-071 | 2026-09-09 | **Out-of-date settings archives that do not conform to the current policy — unlabelled names, folders under a superseded id — are removed on each device and in the cloud, locally as well as remotely (refines D-CLOUD-069), always after a verified copy exists on the build host.** Maintainer: *"I'm fine creating a new data backup as well, and we can remove any out-of-date backups that don't conform to our newer policies so that we're using clean backups only."* Applied to the RG SP's `archive/` the same day; the RG35XX SP follows when it is next online and has written a labelled archive. | #96 comment 2026-09-09 |
| D-CLOUD-072 | 2026-09-09 | **EmulationStation owns the startup saves sync and shows it (refines D-CLOUD-038, D-CLOUD-064; delivers #94).** The autostart keeps only the `cloud_capture --full` pass; ES runs restore-then-backup (`--method=copy --update --saves-only`) through `ThreadedCloudSync` once the interface is up, with the same card and outcome the exit sync shows. Fork resolved during execution: the first cut held the launch gate for the whole network wait (up to two minutes offline), which fails the #84 standard for boot cost. Taken: **no route means no wait** (exit 4 within a second, `SKIPPED - NO NETWORK CONNECTION`); a link without internet is probed for at most 60 s under `WAITING FOR THE NETWORK...`; **a game launched during that wait cancels the sync** (`setsid` group killed, outcome `SKIPPED - A GAME WAS STARTED`, stamp rc 130) instead of being refused; the refusal (`YOUR SAVES ARE SYNCING...`) applies only once rclone is moving files. Every ES-started sync stamps `last-sync-<startup|exit|manual>` (`<epoch> <rc>`), and the two toggles on the CLOUD page read them as `LAST <date> <time> - SUCCEEDED|STOPPED|FAILED|SKIPPED, ...`. Accepted residuals: the sync re-runs on an ES restart (once per process); a launch in the first probe's four seconds is refused, not cancelled; dates are absolute, matching the neighbouring rows. | #94; ES `296d326f5` on `feature/startup-sync`, merged `e521985f2`; `102-cloud-saves` `234cedd7dc` |

## Open decisions

| ID | Question | Home |
| --- | --- | --- |
| D-CLOUD-051 | **What is the wizard's apply step called from a shell?** The two fixtures that need a decision — A5's reader and the retention-ordering kill — call `cloud_reconcile --apply --unit <unit> --keep left\|right`, `left` keeping the cloud's copy (so the device's loser is pushed to the store first, D-CLOUD-036's ordering). Named once in `tools/cloud-round-trip` (`APPLY`) so the fixtures run before the command exists; #22/#23 settle the interface and change the constant in the same commit. | #22, #23 |
| D-LAUNCH-002 | **Should the exit hotkey mark its press so `runemu.sh` can report any exit it caused as clean?** RetroArch's forced quit ends in RetroArch's own `exit(1)`, which no code-based rule can tell from a failed load (D-LAUNCH-001 maps only 137/143). A marker touched by `input_sense` before `killall` and read by `runemu.sh` would cover every emulator and drop the OOM ambiguity D-LAUNCH-001 accepts, at the cost of a change across two packages; sending RetroArch `-9` instead is rejected on its face (it skips RetroArch's shutdown). Whether a handheld ends RetroArch on the first SIGTERM is unverified. | #92 |
| D-CLOUD-008 | **Does the 17 MB archive get trimmed?** It is almost entirely OS-shipped PPSSPP assets, which is what makes size collisions ordinary rather than rare. Shrinking it narrows the #53 window but does not close it. | #45 |
