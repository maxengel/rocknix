# 2026-08-19 Work Log

## ES menu map, style guide, and an audit against them

- Deductive pass over es-app/src/guis plus a screenshot survey of the
  running build produced docs/es-menu-map.md (IA, Mermaid) and
  docs/es-ui-style-guide.md (conventions, each traced to shipped code).
  Published as an artifact for viewing.
- Finding that reframes things: upstream documents NO menu structure or
  UX. THEMES.md only covers repainting menus. The Batocera wiki menu
  tree is the de-facto placement spec and its overview page has the only
  stated interaction rules. So our style guide is the only prescriptive
  source we have - worth remembering before treating a rule as
  inherited.
- I had told the user descriptions render only on the focused row. That
  was WRONG: descriptions are always visible; multiLine controls wrap vs
  marquee-on-focus. The real bug was the dimmed path dropping the
  description entirely. Corrected in the guide and to the user.

## Audit of our cloud UI against the guide

- Descriptions were not wrapping. All four Cloud Tools descriptions used
  the 4-arg addWithDescription whose multiLine defaults false, so long
  ones were clamped to one line and marqueed only while focused -
  unreadable at rest, defeating the point of adding them (round 6).
  Now passed explicitly. Confirmed on screen afterwards: FINISH RESTORE
  SETUP's description wraps to two lines.
- cloudSetupAddFact hand-rolled a label/value row with its own font and
  padding; the app already expresses that as addWithLabel + a
  TextComponent (NETWORK SETTINGS' IP ADDRESS). Switched to the idiom.
- Prose was hand-split across fixed rows, which breaks on narrower
  screens and in translation. Now one wrapped MultiLineMenuEntry.
- Audited and already correct: confirmations keep the back accelerator
  on OK/NO, strings upper-cased in source with only button help lower,
  and no call site touches the two addEntry params that MenuComponent
  and GuiSettings swap.

## #31 verified end-to-end in the VM

- Marker planted -> reboot -> page auto-opens; marker still PRESENT
  while displayed; FINISH -> GONE. That is the crash-safety fix proven
  (old code removed it before drawing).
- Conditional rows confirmed absent when unconfigured (no ACCOUNTS, no
  NETPLAY groups). Glyphs, button order, and Cloud Tools entry all
  confirmed on screen. Closed #31.
- Outstanding: both-markers ordering needs a live cloud remote (#35).
