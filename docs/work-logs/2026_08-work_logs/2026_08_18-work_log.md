# 2026-08-18 Work Log

## Cloud program reorganized into milestones/epics with acceptance criteria

- New milestone **Cloud Saves: Fresh Handheld Journey** (#2) carrying the
  program's acceptance test (fresh device → link → restore all three data
  classes → re-link credentials → play → back up → repeat on device 2);
  precedes the existing Visual Conflict Resolution milestone (#1).
- Epics (labeled `epic`, children wired as real GitHub sub-issues):
  #26 journey (→ #34 full snapshot, #35 two-VM round-trip QA),
  #18 backuptool (→ #30 land P1 baseline, #31 post-restore re-link
  wizard, #32 credential leak-audit), #15 native ES (→ #33 active-remote
  selection), #11 conflict resolution (→ #19–#25, previously body-only).
- New issues #30–#35 all carry acceptance-criteria checklists. #26 and
  #18 bodies rewritten: progress boxes checked (wizard rounds, content
  upload, one-touch actions, SYNCPATH editor), phases mapped to children,
  #18 gains a definition-of-done tied to #35.
- Closed: #8 (game-exit sync shipped, d3bb9c6406), #13 (consolidated
  into #18 P2), #28 (split: SYNCPATH shipped / remote selection → #33 /
  chooser → #12). #7 annotated: boot half shipped via 102-cloud-saves;
  remaining shutdown push + liveness with suggested acceptance.
- Labels: `epic`, `cloud-saves` created; cloud-saves applied across the
  family (#7–#29). The re-link inventory (device password fan-out via
  setrootpass, wifi, ScreenScraper, RA, cloud remote, controllers,
  VPN/mesh identities) lives in #31 + the #18 comment trail.
- Conventions codified in issue-tracking.instructions.md: milestone
  carries the acceptance test; epics own scope with sub-issue children;
  every actionable issue has observable acceptance criteria; closing
  discipline (completed-with-commits vs not-planned-with-pointer).
