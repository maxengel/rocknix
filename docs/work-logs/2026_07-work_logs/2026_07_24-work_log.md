# Work log — 2026-07-24

## 05:55 UTC — upstream PR CI validates commit message format

- PR #3055's `validate-commit` check failed: upstream's
  `validate-pull-request.yml` requires the commit title to match
  `^[a-zA-Z0-9_*./-]+:[[:space:]].+$` (`package: text`, no spaces before the
  colon), title ≤ 72, blank line before body, body lines ≤ 72, no merges.
- The historical `<pkg> - <subsystem> - text` style fails this check; use
  `package: text` for squashed upstream PR commits. Recorded in
  `fork-workflow.instructions.md`.
- Fixed by amending the squashed commit to `rclone: safer restores,
  RSYNCRMDIR, drop legacy mount code`; check now passes.

## 07:10 UTC — issue #15 L1: native ES cloud-sync entries + rclonectl UI discovery

- **Discovery:** ES (`emulationstation-next` GuiMenu.cpp, Network Settings → CLOUD
  SERVICES) had a "MOUNT CLOUD DRIVE" switch calling `rclonectl` — the issue #6
  removal sweep missed it because it only grepped the distribution repo. Lesson
  captured in `rclone-cloud-sync.instructions.md`: CLI changes need an ES + site sweep.
- **L1 implemented** on `maxengel/emulationstation-next` `feature/cloud-sync-menu`
  (`690df88b2`): replaced the dead mount toggle with CLOUD BACKUP / CLOUD RESTORE
  entries (GuiMsgBox confirm → `/usr/bin/run "/usr/bin/cloud_backup|cloud_restore"`,
  gated on script existence) — same fullscreen TUI as the Tools entries.
- Validation build running: GENERIC_X64 `docker-package emulationstation` with the ES
  package temporarily pinned to the fork commit (uncommitted pin in the generic-x64
  worktree; revert before any PR). ES upstream PR to follow once it builds.

## 09:30 UTC — Cloud Saves: mount capability history + full feature batch

- **rclonectl capability history** recorded on issue #6: FUSE mount of the whole
  remote at /storage/cloud; substrate of the original mount+rsync backup design
  (whence RSYNCRMDIR) and general SSH file access; no other OS consumers.
  Dead `set_setting clouddrive.mounted 0` removed from 001-setup (folded into
  PR #3055's squash).
- **Cloud Saves feature** (maintainer direction; supersedes two parity entries):
  - OS `feature/cloud-saves` (stacked on rclone-cleanup): `--yes`/`--method`/
    `--update` flags in both scripts (#14), autostart `102-cloud-saves`
    (boot-time bidirectional copy --update when `cloudsaves.startup=1`),
    ES game-end hook at /usr/bin/scripts/game-end/ (backs up when
    `cloudsaves.gameexit=1`). Hooks live in the read-only image — zero
    install/upgrade seeding needed (userconfig-setup only seeds first boot).
  - ES `feature/cloud-sync-menu`: CLOUD SAVES group (Sync To Cloud/Device,
    Bidirectional Sync, Change Sync Settings, both toggles). Compile-verified
    for GENERIC_X64 (ES binary built; pin reverted).
  - Site docs staged on `cloud-saves-docs` (stacked on the #176 branch).
- Bidirectional = copy --update both ways (newest wins, no deletes); real
  conflict resolution remains issue #11.
- Gotcha: quoted bash heredocs in the agent tooling strip C-string backslashes —
  build replacement text with chr(92) construction when patching C++ via python.

## 11:20 UTC — cloud_setup: QR-based remote configuration (no SSH)

- Research (rclone.org/remote_setup): rclone's OAuth callback is fixed to
  localhost:53682 with exact redirect-URI matching — a phone browser can never
  complete the OAuth hop for a headless device. Supported paths: authorize
  token paste, config copy, SSH tunnel. QR-only OAuth would need a
  ROCKNIX-registered OAuth app + hosted redirect (future option).
- Shipped: `cloud_setup` serves `rclone rcd --rc-web-gui` with one-time creds,
  QR via new `qrencode` package (cmake needed CMAKE_POLICY_VERSION_MINIMUM=3.5);
  ES "SET UP CLOUD REMOTE" entry gated on the script. Key/token providers work
  fully from the phone; OAuth providers get walked through authorize paste.
- Build-verified qrencode + ES for GENERIC_X64; site docs updated on
  cloud-saves-docs.

## 12:10 UTC — rclone 1.74.4 + auto-login QR

- Bumped rclone 1.71.0 → 1.74.4 (PKG_VERSION-only per package convention);
  package build verified, binary reports v1.74.4.
- cloud_setup QR now embeds the web GUI login_token (harvested from rclone's
  NOTICE log line — it's percent-encoded, regex must include %) so scanning
  signs the browser in without typing credentials; works for desktop browsers
  via the same URL. rclone rcd logs the token URL only when auth is set.

## 13:30 UTC — backuptool P1: baseline fixed (issue #18)

- Verified defects: restore never rebooted (running ES clobbered restored
  es_settings.cfg), bogus dialog claims, zero error handling, OS-managed
  es_systems/es_features symlinks captured-as-content and restored as stale
  files, plaintext wifi.key/root.password/RA+netplay password/token and
  ScreenScraperPass in a cloud-synced zip, unbounded ARCHIVED_*.zip in the
  cloud-matched folder, useless adapter-bound bluetooth keys.
- Rewrote backuptool (feature/backuptool-baseline): unzip -t verify, error
  reporting + exit codes, sync+reboot after restore, real ES paths + symlink
  exclusion, secret-stripped archive copies via mktemp re-zip, archive
  rotation to backup/archive/ (outside allowlist, keep 3), + collections and
  es_input.cfg now included. ES dialogs fixed on feature/backup-restore-dialogs.
- Site docs note staged on backup-restore-docs. Credential stores mapped:
  system.cfg (SystemConf) vs es_settings.cfg (ScreenScraperPass only).

## 14:30 UTC — backuptool P2 + native password re-entry reminder

- Backend: --system-only/--saves-only phase selection in cloud_backup/
  cloud_restore (forces BACKUPFILE_*_OPTION for the system phase);
  backuptool restore drops /storage/.config/.restore-finish-pending.
- ES: BACK UP TO CLOUD / RESTORE FROM CLOUD entries in System Management
  (gated on cloud tools); boot-time native reminder (marker one-shot) telling
  the user where to re-enter wifi/RA/netplay/ScreenScraper passwords —
  usernames stay in the backup, passwords never do.
- Compile-verified for GENERIC_X64; site docs updated. Next: VM QA, P3/P4.

## 15:10 UTC — ES UI/UX sweep → es-native-ui.instructions.md

- Systematic sweep of emulationstation-next GUI stock: primitives (GuiMsgBox,
  GuiTextEditPopupKeyboard, GuiInfoPopup), components (BusyComponent,
  AsyncNotificationComponent, OptionListComponent), patterns (GuiSettings
  addGroup/addEntry/addSaveFunc, addInputTextConfigRow for on-screen-keyboard
  credential entry, GuiLoading<T>, ThreadedBluetooth async-notification model),
  gating (ApiSystem capabilities vs file-existence), Scripting event dirs.
- Key precedents: GuiBackupStart/GuiBackup (batocera native backup, hidden on
  ROCKNIX — model for #18 P2+ native page), GuiFileBrowser (P3 directory
  picker), GuiMoonlight (settings+actions), store/theme-downloader (async
  install). Anti-patterns recorded (dialog drift, CLI hops as end-state).
- Captured in .claude/rules/es-native-ui.md.

## 16:20 UTC — L2: background cloud sync + native credential re-entry

- ThreadedCloudSync (ThreadedBluetooth model): sync actions now run headless
  backends on a worker thread with an async progress card + result toast;
  no more console takeover. Gotchas hit: inner GuiMsgBox lambdas must capture
  window; ApiSystem::enableWifi is 3-arg on !WIN32 (country from SystemConf).
- FINISH RESTORE SETUP page replaces the post-restore msgbox: masked
  addInputTextConfigRow rows bound to SystemConf/Settings, wifi reconnect via
  addSaveFunc. Both ES branches compile for GENERIC_X64.

## 17:40 UTC — feature work complete; QA integration build started

- L3: ThreadedCloudSync streams backend output live into the progress card.
- P3 MVP: cloud_content_restore (--list / --all / <dirs>; copy-only into
  /storage/roms, CONTENTPATH root override) + ES "RESTORE CONTENT FROM CLOUD"
  picker (GuiLoading fetch → GuiSettings dir list → background copy).
- Integration branches for QA (never PR): ES test/qa-integration
  (cloud-sync-menu + backup-restore-dialogs, 03a240e43), OS
  test/qa-integration (cloud-saves + backuptool-baseline + ES pin), and
  test/qa-generic-x64 (that + the GENERIC_X64 device) — full image building
  from the cached generic-x64 worktree tree.
- QA checklist for QEMU: Cloud Saves menu (setup QR/auto-login, sync trio with
  progress card, content restore picker), System Management (backup/restore
  local + cloud, reboot-after-restore, FINISH RESTORE SETUP page, secrets
  absent from zip), hooks (startup sync, game-exit sync), rclone 1.74.4.
