# Work log — 2026-07-25

## QA rev 2: discoverability + unconfigured-state UX + VM networking

- Cloud tools moved from Network Settings to Game Settings → CLOUD TOOLS;
  backup entries renamed to the explicit To/From Device/Cloud names.
- Unconfigured rclone: Cloud Tools actions render dimmed (manual row,
  alpha-dimmed text color + makeAcceptInputHandler) and offer cloud_setup;
  System Management cloud entries guard via requireCloud lambda (must be
  captured in entry callbacks — caught by build).
- VM/QEMU: profile gains extra_forwards (host 15572 → guest 5572) so the
  cloud web GUI works from the host browser; generic-x64-vm gains
  --net bridged (+ --bridge) for LAN-joining QA; cloud_setup prints the
  forward address when it detects 10.0.2.x slirp addressing.
- Image rebuilt + release dev-generic_x64-20260725 updated (rev 2 notes).
