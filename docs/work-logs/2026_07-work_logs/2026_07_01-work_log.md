# Work log — 2026-07-01

## 21:15 UTC — GENERIC_X64 (x86_64 VM) build spike
- Re-derived a bootable **GENERIC_X64** VM/QA target onto `feature/generic-x64` (off `next`),
  from the stale `x64-build-fix` (~2465 commits behind). Foundation + ~18 reactive fixes;
  build reached image assembly (last cores being cleared). Detached-build workflow, logging to
  `~/Development/generic-x64-build.log`. Requires the `.git` mount for docker builds from a
  worktree (`scripts/image` runs `git rev-parse`).
- **Key pivot:** dropped Intel `iris` HW GL for **llvmpipe (software GL)** — iris pulls an
  unresolvable SPIR-V version cascade (glslang-15.1.0 vs SPIRV-LLVM-Translator-20.1.5 need
  incompatible spirv-headers). llvmpipe is the reliable VM path anyway.
- Notable fixes: restore `intel-ucode`; graphene AVX header bug; gcc **libatomic** for x86_64;
  glslang install bundled SPIRV-Tools libs (libplacebo link); libsamplerate `-fPIC`;
  dosbox/a5200/gliden64/ppsspp-lr arch fixes; installer drops grub (syslinux for x86 UEFI).
- **Concessions (deliberate, for a fast QA boot)** tracked in fork issue **#17** (path to a
  full/bare-metal build): `X64_SKIP_CORES` (parallel-n64-lr, flycast2021-lr, duckstation-lr,
  idtech-lr), SM8250-derived emulator configs (placeholders), no HW/Vulkan accel. VM build = #16.
- Next: finish image (`.img.gz`), then add a VM-image output (qcow2/vdi/img) + boot-test in QEMU.
