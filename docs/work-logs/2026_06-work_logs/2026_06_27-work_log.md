# Work log — 2026-06-27

## 19:04 UTC — Cloud-sync deep dive, fork workflow, and backlog logging

### Decisions / outputs
- **Fork dev workflow** established and documented (`fork-workflow.instructions.md`): develop on
  `feature/<n>` off `next`, then build a clean PR branch with
  `git rebase --onto upstream/next next pr/<n>` (no cherry-pick). Added `.githooks/pre-push`
  guard for personal artifacts; rebased `next` onto current `upstream/next`.
- **rclone cloud-sync review** → issue #5 (bug punch list).
- **Legacy `rclonectl`/rsync** → issue #6 (exploring removal; the FUSE-mount approach was set
  aside per maintainer recollection — unverified, to re-validate before removing).
- **Backlog features** → #7–#13; **QOL polish** → #14; **native ES port** → #15.
- **New practice**: this learning-capture loop (memory → instruction abstraction + work log),
  documented in `learning-capture.instructions.md`.

### Learnings (candidate generalized practices)
- **Verify design intent before "fixing" an apparent bug.** Restore-side `--delete-excluded`
  looked like data loss, but is gated by the default `RESTOREMETHOD=copy` (delete is a no-op for
  `copy`) and the rules are an allowlist (blast radius would be the whole non-save library);
  `RESTORE_RCLONEOPTS` is dead code. Checking git history (original ROCKNIX commit) settled
  intent. → candidate for a dedicated practices instruction file.
- **Keep `cloud_backup` and `cloud_restore` structurally in sync** — most fixes belong in both.
- **ES integration is two layers**: CLI tools = the `modules` system; native UI = the separate
  `ROCKNIX/emulationstation-next` C++ repo (`GuiMenu.cpp addEntry`, `Platform::runSystemCommand`).

### Follow-ups / sequencing
- Foundation-first: #14 non-interactive backend unblocks #7/#8/#15; #9 bisync underpins #11.

## 22:45 UTC — Formalize "verify design intent before fixing" practice

Promoted the candidate practice from the 19:04 entry into
`.github/instructions/engineering-practices.instructions.md` (with the rclone
`--delete-excluded` episode as the case study). Applied the learning-capture loop:
instruction-file abstraction + this work-log entry + a repository memory.
