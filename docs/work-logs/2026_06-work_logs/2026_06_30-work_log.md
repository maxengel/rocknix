# Work log — 2026-06-30

## 03:14 UTC — Session recovery + worktree workflow convention
- **Recovered the interrupted session.** Nothing was actually lost: the issue #5 "safe batch"
  rclone fixes are committed on `feature/rclone-cleanup` (`9d2afed1db`) and pushed clean as
  `pr/rclone-cleanup` (`cb7492d253`); the instruction guardrail work is already in `next`
  (`5d0bb2b90b`). A dangling stash (`8f646ece`, "alignment-personal") was identical to `next`.
  Still outstanding: open the upstream PR for the cleanup; #5 leftovers (#3 RSYNCRMDIR, docs).
- **Codified the worktree convention** (mirrors the groundhog pattern): primary checkout
  `~/Development/rocknix` stays a reflection of the canonical branch (`next`; this fork has no
  `main`), and feature worktrees live in sibling `~/Development/rocknix.worktrees/<leaf>`.
  Added `.github/instructions/worktrees.instructions.md`; updated `fork-workflow` worktree path
  from `../rocknix-<name>` → `../rocknix.worktrees/<name>`.
- **Resumed work** in a worktree for the existing `feature/rclone-cleanup` at
  `~/Development/rocknix.worktrees/rclone-cleanup` (rebased onto current `next`).

## 05:18 UTC — Clarify "memory" vs in-repo records; broaden docs/ exclusion
- Resolved a confusion: a **"memory" = GitHub Copilot memory** (`store_memory`), **not** a repo
  file. The in-repo records of learnings are **instruction files + dated work logs**; there is
  **no separate memories file** (kept current, per decision). Codified the distinction in
  `learning-capture.instructions.md`.
- Broadened the personal/upstream-excluded path from `docs/work-logs/` → the **whole `docs/`**
  (it's absent upstream): pre-push guard + fork-workflow personal-paths list.
