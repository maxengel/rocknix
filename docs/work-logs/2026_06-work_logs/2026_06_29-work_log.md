# Work log — 2026-06-29

## 01:37 UTC — Intent-alignment pass + start #5 safe batch
- Applied the "verify intent" lens to the cloud-sync backlog. Added a **scope guardrail** to
  rclone instructions and to #9/#12/#13: changes sync only the allowlist
  (saves/savestates/screenshots + system-backup zip), never risk ROMs/BIOS/art; bisync/sync
  must preserve excludes; dir chooser stays in save dirs; system-backup stays partitioned.
- Started #5 safe batch on `feature/rclone-cleanup`: exit-code 9 relabel, log-level comment,
  drop dead `RESTORE_RCLONEOPTS`, stats on main path, remove never-matching literal regexes.

## 01:45 UTC — Cardinal rule: preserve progress over recency
- Biggest failure = losing player progress. Conflict resolution must not lean on recency
  (newer != more progress). Default non-destructive (keep-both, never auto-delete loser);
  prefer progress heuristics over timestamps. Captured in rclone instructions; reframed #11/#9.
