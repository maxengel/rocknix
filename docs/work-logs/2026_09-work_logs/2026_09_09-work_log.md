# 2026-09-09 work log

## 07:27 UTC — Three maintainer observations from the b6bd7dc3b7 build, filed before the stash

The maintainer used the RG SP on `b6bd7dc3b7` and sent three observations
out of band; each is now an issue quoting their words (D-QA-012):

- **#94** — the startup saves sync shows nothing on screen: *"How do I know if
  saves were synced during startup? It doesn't show any foreground identifier,
  like when you update game lists."*
- **#85, item 1 second pass** — the picker's zero-delta row
  `ALREADY IN YOUR CLOUD (253.9 MB)` reads as an amount about to move. The row
  quantifies only the delta or says `NOTHING NEW TO BACK UP` (D-UI-027).
- **#95** — the transfer page reads bottom-up (file, its transfer, then the
  system with its `10 OF 10`), the counter exists only for content items
  because `cloud_backup` announces `>>> unit SAVES||` with no index, and the
  settings phase begins with `backuptool backup >/dev/null`, so it sits on
  `PREPARING...` over a spinner while the archive is written. Item first,
  `ITEM i OF n` across the run, one shape for every phase (D-UI-026).

None of the three is implemented yet; they are the next UI tranche after the
stash, alongside #22. All three live in `GuiCloudTransfer`/`GuiMenu` and the
`>>> unit` protocol in `cloud_backup`/`cloud_restore`.

**Device confirmations.** The maintainer: *"I can confirm that the backup was
successfully made as `2026_09_09-031728-Anbernic-RG-SP-ROCKNIX_SETTINGS.tar.gz`"*.
Read over SSH: it sits in `dropbox:/ROCKNIX/Backups/Anbernic-RG-SP-f058e3e9e8/`
beside `device.json`, with the pre-#86 folders `Anbernic-RG-SP-ee5013fc56/`
and `ROCKNIX-ee5013fc56/` still present and readable by the restore chain
(#85 item 4, #49 commented). The RG SP's `last-capture` is the boot full
pass (`07:11:44 UTC full rc=0`); no game has been exited on this build yet,
so #21's device half stays open. The RG35XX SP was off the network
(`No route to host`).

**Housekeeping.** This worktree's `docs/` was 128 commits behind `next`
(register ending at D-CLOUD-048, no 09_07/09_08 logs). Rather than append to
stale copies and merge by union later, `next`'s eight files were checked out
into the branch first and the new rows appended on top; they are staged with
the session state, uncommitted, awaiting the maintainer's word on the stash
commit.

## 18:20 UTC — The exit capture on a real handheld, the stamp the boot pass erased, and the legacy folders removed

**#21, device half.** The maintainer played a GBA game on the RG SP
(`b6bd7dc3b7`) and reported a clean exit. Over SSH: the manifest was written at
08:19:33Z with one unit and three entries (`.srm`, `.state`, `.state.auto`),
every `sha256` equal to `sha256sum` of the file on the card, three stage copies
each named by the hash of its own bytes, `core_build` equal to the image's
`core-pins` line and the repo's `PKG_VERSION` for `mgba-lr`. Two boxes ticked
in the body (the exit box; the pin-equality half of the `core_build` box, split
so the `unknown`/wizard half stays open for #22).

**Finding.** `last-capture` read `... 0 full - emu-exit=? -/-`: the device had
been powered off and on, and the boot full pass overwrote the exit stamp ten
hours later. The manifest is the durable record and survived (I8); the stamp
cannot answer "how did the last game exit go?" across a reboot. Proposed on #94
(where a row would read it): one line per mode in the stamp, each replaced only
by its own mode.

**#96, cleanup (D-CLOUD-069).** On the maintainer's word the two pre-#86
folders on the settings remote were removed, after copying all seven files to
`/workspace/artifacts/rocknix-images/legacy-cloud-backups-20260909/` and
testing every archive. `ROCKNIX-ee5013fc56/device.json` named the RG35XX SP as
its last writer (2026-09-06), so those were that device's only cloud archives;
accepted, it backs up again when it is next online (it was unreachable all
session). End state: `Anbernic-RG-SP-f058e3e9e8/` and `README.txt`.

**Shell gotcha, one retry.** `while read size path; do ssh ...; done < listing`
copied one file: `ssh` inherited the loop's stdin and swallowed the rest of the
listing. `ssh -n` (or `< /dev/null`) inside a read loop, always.

## 18:26 UTC — Stash committed; the per-mode stamp accepted; local unlabelled archives removed

Maintainer: *"Your proposal sounds good. I did power off the console before I
went to sleep. I'm fine creating a new data backup as well, and we can remove
any out-of-date backups that don't conform to our newer policies so that we're
using clean backups only. Let's commit the stash."* Register: D-CLOUD-070 (stamp
line per mode, criterion added to #94) and D-CLOUD-071 (non-conforming archives
removed locally too). The RG SP's `archive/` lost its two unlabelled archives
after a size check against the host copy; it holds one labelled archive, the
same one the cloud holds. Build question answered in the state file: no build
carries the 2026-09-09 items yet; `b6bd7dc3b7` carries the 2026-09-08 ones.
