# 2026-09-08 work log

## 04:35 UTC — The RetroArch crash is a wrapper bug; patched here, reported and fixed upstream

Crash 3 on #79 came with a core dump: RetroArch 1.22.2's threaded video
wrapper has a single-slot command mailbox whose reply wait matches on the
command *type*, so the achievement badge upload (main thread) and the
automatic screenshot task (task thread) posting `CMD_CUSTOM_COMMAND` at the
same instant hand the video thread a `gl3_texture_cmd_t` on a returned stack
frame (`video_texture_load_wrap_gl3_mipmap`, `ldr x0,[x0]`).

- ROCKNIX: `0011-video-thread-wrapper-serialise-posters.patch` on
  `next` (`1c4fa3b308`) — a user-side lock held from send to reply. Linux
  has no same-thread re-entry into the wait, so a plain `slock_t` is enough.
- Upstream: libretro/RetroArch#19517 (report) and #19518 (fix on master,
  from `maxengel/RetroArch`). Master's rework still keeps one command
  outstanding and only asserts it in DEBUG. The master fix is a poster slot
  (`cond_user`, owner, depth) that is re-entrant on its owner and pumps
  the cocoa trampoline while waiting, because on Apple the reply wait can
  re-enter the wrapper on the waiting thread. Compile-tested release, DEBUG
  and `-std=c89`; a runtime result waits for the H700 build.
- Lesson: an upstream fix has to survive platforms the fork never builds.
  The first draft was the ROCKNIX lock verbatim; reading the `cond_reply`
  comment on master showed it would deadlock a macOS main thread.

## 04:40 UTC — #83: the RG SP's second saves tree, cleaned and guarded

101 stale files removed from the internal card (audit posted first: 99
identical, 2 older, 0 unique). Prevention: `cloud_saves_root` + the refusal
in both saves phases + `cloud_setup --accept-saves-root` (`89f1cb15b7`,
D-CLOUD-054 decided). Proven on GENERIC_X64 guest a first (D-QA-007).

- Guest a had lost its rclone remote and the backend on :9010 was MinIO,
  not WebDAV; `tools/cloud-test-backend rclone-conf` / `saves-remote` under
  `CLOUD_QA_BACKEND=s3` is the way to re-point a guest, not a hand-written
  WebDAV stanza.
- `cloud_setup` sources `/etc/profile`, which rewrites `PATH`; helpers are
  resolved beside `$0` now (rule updated).

## 05:25 UTC — #83 was a boot race, not a missing card; the guard now waits and re-checks

The maintainer asked how two cards that were never removed produced two
trees. The RG SP journal answered: udev starts `rocknix-automount` when the
card is detected (the unit is disabled, not in the boot transaction, so the
compositor's `After=` on it is inert); the script unmounts `/storage/roms`
at 23:48:57 and binds the card back at 23:48:58; the interface started at
23:48:57 and its boot restore at 23:48:58. A restore that got in first, or
that the bind landed under, wrote to the internal card.

- `cloud_saves_root check --wait 15` / `record IDENTITY` (`9618b63d7c`,
  D-CLOUD-055): the race is a delay, a mid-run flip is a failed run.
- VM lesson: a flip is simulated with a tmpfs *copy* bound over the tree,
  and the bind only lands mid-copy if rclone is throttled
  (`RCLONE_BWLIMIT`); two unthrottled attempts finished before the bind and
  looked like a passing post-check that had never run (blindspot 31's
  shape — the harness agreeing with itself).
- The ordering defect itself is ROCKNIX's; filed separately for its own
  review rather than riding this build.

## 05:45 UTC — `9618b63d7c` on both handhelds; #83 closed

Staged with a device-side checksum and rebooted at 05:32Z on the
maintainer's authorisation for this build (given mid-turn: "you're all
clear to deploy it to the RG SP and RG35XX SP, and then reboot"). Idle check
first on both: no emulator, no transfer, `flock -n` free, update queue empty.

- RG SP: automount finished 01:36:15, interface 01:36:24, boot restore and
  backup SUCCESS into `/dev/mmcblk1p1`, record `uuid:81083868-…`, 0 files
  under `games-internal/roms` with a ctime since boot. #83's last box ticked
  from that; issue closed completed. Follow-ups on #84.
- RG35XX SP: boot pair SUCCESS, record `uuid:bc8d18b8-…` (its one card).
- The RetroArch wrapper patch is on both; the crash test is the maintainer's
  (an unlock with AUTOMATIC SCREENSHOT on).
- Waiting for a reboot: a background `until` loop over ssh with a deadline,
  one notification — not a foreground sleep and not a self-matching pgrep.

## 06:10 UTC — #84: the cause was a card-enumeration race, not unit ordering

Corrected my own #84 diagnosis. Monotonic-time boot logs show
rocknix-automount finished ~10 s before EmulationStation on both
handhelds; the wall-clock journal that suggested the reverse was jumping
because the RTC set failed and NTP corrected the clock mid-boot. Lesson:
read boot ordering in monotonic time, never wall time, on a device whose
clock is set by NTP during boot (blindspot-shaped: a timestamp that jumps
under the events you are ordering).

Real cause: `automount`'s `find_games` scans for the external card once
and binds internal over /storage/roms if it has not enumerated yet. Fix
`accaadc3c0`: retry up to 15 s when the device expects an external card
(`system.merged.device=external` or pinned `system.gamesdevice`);
one-card boot unchanged. D-CLOUD-056. Tested on the VM against the real
function body with mocked blkid/dev-node — six scenarios. The udev re-run
already guards against tearing down a correctly mounted card
(`RESULT==""`), so no change there.

Must verify on the RG SP when it is back: `system.merged.device` is
`external` (or a games device pinned) so the wait engages there.

## 15:40 UTC — #84 boot cost: wait only when a card is actually present

Maintainer asked whether the 15 s retry adds 15 s to boot. It does not —
a present card mounts on the first pass. Refined so the wait engages only
when a candidate external block node exists but its filesystem is not
readable yet (`external_node_present()` over /sys/block); a removed card
falls to internal after a 2 s grace, ceiling 10 s. `b82d19ab94`,
D-CLOUD-057. The scenario harness surfaced a real `$#`-expansion bug in
the detector's sed (double-quoted `"s#p[0-9]*$##"` → bash expands `$#`);
single-quoted now. Lesson reinforced: a test written to check behaviour B
found bug A — run it anyway.

## 18:05 UTC — #85 archive names: a reviewer timed out; its findings were recovered

The transfer-flow workflow's archive-names reviewer died on an API request
timeout after verifying two defects on the VM; `agent()` returned null and
the script's fix stage treated "no review" as "nothing to fix" — a design
flaw in the script (null must read as unknown). The transcript still held
the evidence: the shipped `backuptool` glob `*-ROCKNIX_SETTINGS.tar.gz`
returns EMPTY for a labelled name, and label-keyed retention deleted a
same-label archive of a second same-model device. Decisions D1 (name
`<stamp>-<label>-ROCKNIX_SETTINGS.tar.gz`, old readers keep matching) and
D2 (retention semantics stated exactly; the same-folder case is #86's
cloned id) posted on #85; a focused fix→review workflow is applying them.
Memory: `recover-timed-out-agent-from-transcript`.
## 17:55 UTC — #21 capture at game exit: design judged; hook removal, schema §9/§10, D-CLOUD-058–066

The design for `cloud_capture` — the producer behind the rev 2 manifest — was
judged from three candidates against four "understand" maps and is executing
on `feature/capture` (ROCKNIX) and `feature/capture-hook` (ES). Decisions are
D-CLOUD-058..066. Two override the judged spec: 064 (the boot `--full` is
gated on a cloud-saves toggle and backgrounded — #84, boot time is not ours
to spend) and 066 (the sync allowlist is not widened here; #89 owns that).

This slice:

- `runemu.sh`: the dormant `cloud.backup` exit hook is gone, with the
  shipped `cloud.backup=0` in `system.cfg`. It came in with the initial
  ROCKNIX import (`9f1fab30f6`), read a setting nothing has written since ES
  took the sync over, and ran `/usr/bin/run /usr/bin/cloud_backup` before
  `quit` — before ES's exit path resumed, so before any capture could. A
  two-line comment marks the spot. Grep of the tree (build roots excluded)
  and of the ES `es-app`/`es-core` trees finds no other reader; an upgraded
  device keeps a dead key in its own `system.cfg`. `bash -n` clean.
- `docs/save-manifest-schema.md`: §9 no longer tells #21 to pass
  `getEmulator(true)`/`getCore(true)` at `FileData.cpp:836` — that
  contradicted R5 and pointed the next implementer at exactly the accessors
  that re-resolve. It states the frozen-token rule now. §10 gains an
  addendum: the content-addressed stage, thumbnails as declared members
  without entries, `units` derived from entries, unknown provenance on a
  verify pass, the `--started` rule, `RETIRED_MAX` 200, the per-emulator
  unit table. `schema` stays 1.
- `.claude/rules/rclone-cloud-sync.md`: `cloud_capture` in the inventory
  with its two unique rules (no flock, no `/etc/profile`), the exit ordering,
  and the durable stamps `last-capture` / `capture-failures`.

Two corrections to the issue text, for the record:

- ES holds no flock around a launch. Grep of es-app/es-core finds no
  `cloud_sync.lock`; the launch guard is the in-process
  `ThreadedCloudSync::isRunning()`. So "the lock held" at exit means another
  cloud_* process holds it, and capture's not touching fd 9 is sufficient.
- This busybox HAS `diff` and `cmp` (CONFIG_DIFF=y, CONFIG_CMP=y, verified on
  guest a). What is missing is `comm`, `find -newermt`/`-printf`,
  `ls --time-style`, `stat -f` and a `printf` binary. The I8 comparison uses
  `cmp` on two `jq -S -c` renderings deliberately.

Found on the way:

- Every shipped `mupen64plus.cfg` has `SaveSRAMPath = "/storage/roms/n64"` —
  beside the ROM — while the allowlist admits only
  `+ /n64/save/*.{eep,mpk,sra,srm}`. Standalone N64 saves have never synced,
  and A8 planted the allowlist's path rather than the emulator's, so it
  passed. Filed as #89; #21 records those saves (CAP6) and does not change
  what syncs.
- `SaveState::_changeCommandlineArgument` matches `-core` inside ROCKNIX's
  `--core=` token and would produce `--coremgba`, dormant only because no
  `es_savestates.cfg` ships. Fixed in the ES half; it is what makes
  acceptance (2) observable at all.

What the VM cannot prove, and what the rebuilt GENERIC_X64 image is for: the
harness drives `cloud_capture` directly (CAP1–CAP10, one guest, no MinIO) but
cannot drive EmulationStation, and ES spawns `/usr/bin/cloud_capture` by
absolute path from a read-only `/usr`. So the ES half — toggle off, a crash
exit, the frozen core under a QA `es_savestates.cfg`, the manager's
retire + rescan, the boot `--full`, the upgrade path — needs an image built
from `feature/capture` with the ES bump, run on guest a first (D-QA-007). The
VM still cannot prove a real panel, a real provider, or a two-card handheld's
automount; those are the handheld's confirmation, each reboot asked for by
device (D-QA-011).

## 19:15 UTC — #21 fix pass: the merge, the race, and a gate that could not fail

Review of the capture slice found 22 things, five serious; all applied except
the two optional halves named below. What the serious ones were:

- **A PUT at a MOVE source was deleted with the source.** `cp -p state1
  state2` then new bytes at state1 is a move and a put in one pass; the merge
  computed `$dead = move.from − move.to` and deleted it after the reduce, so
  state1's new version vanished — and stayed unrecorded (no entry, mtime
  below the next `--started`). `$dead` now also subtracts put/meta paths;
  CAP7 carries the case.
- **Backgrounding the boot `--full` (D-CLOUD-064) made it a concurrent writer
  with the first game's exit capture**, and a discarded exit is not
  self-healing for a first version. The loser now re-runs itself once
  (`CLOUD_CAPTURE_RETRY=1 exec "$0" "${ORIG_ARGS[@]}"`; exec runs no EXIT
  trap, so `cleanup` is called by hand) for exit/rescan/retire; `--full`
  never retries. CAP11 stages the race with a `cloud_device_id` shim that
  rewrites the manifest on the `--label` call — the one sibling call between
  load and rename — and proves both the retry and the non-retry.
- **ES spawned capture for systems with no `<emulators>`** (tools, st-v):
  `--emulator ''` was a usage error, a capture-failures row per run. ES skips
  the spawn on an empty `launchedEmulator`; the producer treats it as nothing
  to record (rc 0); CAP9 asserts it.
- **The boot game (`launchStartupGame`) was a second exit path with no
  capture.** It now captures from the stored command's `-P`/`--emulator=`/
  `--core=` tokens; `launchArgument` moved to `es-app/src/LaunchCommand.h`
  (header-only, 9-assertion host test) and is shared with FileData.cpp.
- **CAP10(d) could not fail**: it compared line numbers, and the sync block's
  own toggle test sits above anything appended after its `fi`. It now
  resolves the enclosing `if` (depth-counted over `fi`, `else`/`elif` at
  depth 0 is a fail) and requires both toggles, `setsid|nohup`, `nice`, and a
  single trailing `&`. Proven against five constructed violations on the host
  before trusting the PASS.

Minors, all producer-side unless named: the seal's inode invariant compares
`%d %i` (an inode number means nothing across filesystems); `sort -V` is
asserted at preflight and the sorted count is checked (a sort that returns
nothing was an rc-0 no-op); `--exit` is rejected by `--rescan`/`--full`/
`--retire`; the rescan walk key is `__all__`, refused as an `--emulator`
value; `locate_unit` runs before the device-id and card steps (a no-layout
exit is a stat and a sed); `--rescan`/`--full` hash the live file first and
copy only bytes the stage does not already hold (the manager's thread no
longer pays a copy per slot); a rev-1 entry's unit is derived from
`system:rom` at load and stamped on the first write (`unit` op). Harness: A8
attributes the allowlist gap only when the evidence matches; A9 asserts the
retired row; the cache snapshot is restored only when it exists; `.away`
roots and `/tmp/qa-pins` are cleaned on failure; CAP3(b) states its premise;
CAP9(g)'s static grep covers rclone/curl/wget. GuiSaveState logs a nonzero
from `--retire`/`--rescan`. `hatarisa|scummvmsa` join the pins filter.

Left as documentation, deliberately: the two-card pre-first-sync window (a
`--full` skip when `saves-root` is absent would break every harness `--full`
fixture after `local_reset`, and a single-card device that has never synced
would never get a boot pass) — recorded in D-CLOUD-064 and the autostart
comment. D-CLOUD-064's consequence text was amended in place: the row is this
session's and uncommitted.

Guest a (staged `/tmp/qa-bin`, `--only CAP1..CAP11`): **140 PASS, 0 FAIL, 2
spec-mandated SKIP.** Host sandbox (constants redirected, stub siblings)
reproduced each fixed path first, including that the retried attempt sees
`CLOUD_CAPTURE_RETRY` and stops rather than loops.

Pair run (`--only A8,A9,A14`, guest b :10023, MinIO s3): **12 CHECK(S)
FAILED, all expected** — A8 blocked on #89 (and attributed to it only because
the `.srm` reached the cloud and neither pair member did), A9's reconciler
checks and A14's push half are #22's. New this pass and passing: A9's
"the manager's deletion left a retired row for state2 with S2's sha", A14's
working-copy half. Guest a's `/storage/.cache/cloud_sync` came back complete
through `restore_cache`; no `.away` root, `/tmp/qa-two` or `/tmp/qa-pins`
left behind.

## 20:05 UTC — #86: the shared device id was a constant seed; healed once, old folders kept readable

Both handhelds printed the hash `ee5013fc56` (RG SP `Anbernic-RG-SP-ee5013fc56`,
RG35XX SP `ROCKNIX-ee5013fc56`). The issue's investigation had the cause: on
every kernel with `CONFIG_IPV6_SIT=y` (H700, RK3566, RK3576) the sorted
interface list is `lo sit0 wlan0`, `sit0` was not on `usable_interfaces`'s
denylist, `ethtool -P sit0` → "Permanent address: not set" → the script's own
`sed`/`tr` → `notset`, and `permanent_address`'s three-value denylist accepted
it. `DEVICE` is never exported on a device, so the hash was
md5(`notset|unknown`)[0:10] on every one of them. The VM (sit a module, `eth0`
sorting first) never hit it.

Done on `feature/device-id-seed` (uncommitted, from `next` 208c6f9273):

- **`cloud_device_id`**: an interface counts only with `/sys/class/net/<n>/type`
  = 1 and a `device` link (the names `sit* ip6tnl* gre* wg* dummy* bond* ifb*`
  join the case for the reader, but do not decide); a seed only if it matches
  `^([0-9a-f]{2}:){5}[0-9a-f]{2}$` case-insensitively and is not all-zero or
  broadcast. **Hash recipe unchanged** (`hash_of`), `poisoned_hash` computes
  the constant from `notset`, and the comment that claimed the family joins
  the hash is corrected. A stored id ending in the constant is healed once,
  when a validated address is present: old id appended to
  `cloud_sync-device-id.previous` (no duplicates), `<label>-<hash>` written,
  `old -> new` to `logger -t cloud_device_id` and `/var/log/cloud_sync.log`.
  No address → returned unchanged; any other stored id → untouched. New
  `--previous`: `.previous` lines, `<label>-ee5013fc56`, `<hostname>-ee5013fc56`,
  deduplicated in that order.
- **`cloud_restore`**: own → `--legacy` → each `--previous` → root, logging
  which folder and why (WARN on a `--previous` folder — a same-model or
  same-hostname device may share it); `folder_has_archives` helper; a helper
  from before `--previous` existed answers with the current id, which the
  chain skips.
- **`cloud_backup`**: comments only — it already writes and trims only
  `device_dest`, and now says the old folder is never reached.
- **Harness**: `SOLO_FIXTURES` registry (one device + the QA endpoint,
  `--only A15`, no `--second-port`), `run_solo`, `--list` section. A15 sources
  the staged script minus its dispatch line in a driver of its own and
  overrides `usable_interfaces` there — no hook in the product. A11 untouched.
- **Docs**: changelog *The device id is seeded from a hardware address or
  nothing*, two earlier sentences amended; D-CLOUD-068; schema §7 example
  annotated and the `device.id` row; the `cloud_backup` device.json comment.

Evidence, guest a (`/tmp/qa-bin` staged, `CLOUD_QA_BACKEND=s3
CLOUD_QA_BUCKET=rocknix-qa-a15`): **A15 25 fixture checks PASS, 0 FAIL** (28
PASS lines with the three setup checks; the earlier "29" also counted the
trailing PASSED) — `sit0: type 776,
no-device; eth0: type 1, device`; the old pipeline gives `notset` for sit0;
`permanent_address yields nothing for sit0 (exit 1)` and `52:54:00:52:4e:58`
for eth0; `GENERIC-X64-ee5013fc56 healed to GENERIC-X64-15ca35b6b4`, the hash
this guest generated before the change; `--previous lists
['GENERIC-X64-ee5013fc56', 'GENERICX64-ee5013fc56']` and adds
`A15HOST-ee5013fc56` under that hostname; `cloud_restore found the archive
under GENERIC-X64-ee5013fc56 through the --previous tier`, the own folder wins
when it holds one, the backup lands in the healed folder and the old folder
`still holds its 6 archives` (keep+2 of this device's own label planted there,
retention did not reach them); a second run and a healthy foreign id write
nothing; narrowed to sit0 the poisoned id comes back and nothing is written.
**The single-device suite then ran on the same guest: PASSED**, every step.
Guest put back (id `GENERICX64-15ca35b6b4`, no `.previous`, no sit0, hostname,
conf, remote); the private bucket removed afterwards.

Lessons:

- **A check that names the bad values it knows cannot fail on one it does
  not.** The denylist rejected "", all-zero and broadcast, and accepted
  `notset`. The shape of a valid value is the guard (engineering-practices
  *Guards must fail closed*, applied to a value rather than an exit status).
- **The VM could not hit this by accident**, so the fixture manufactures the
  condition (`modprobe sit`, list narrowed in the harness's shell). The first
  run FAILED one check — mine: it compared the whole healed id with the guest's
  stored one, and the image's `cloud_device_id` labels from the hostname
  (`GENERICX64`) while the tree's labels from `HW_DEVICE` (`GENERIC-X64`). The
  hash was identical. The check now compares the hash and prints the label
  change as information.
- **A harness that shares a MinIO with another session must run under its own
  bucket.** `cloud-test-backend reset` purges `S3_BUCKET`; the default
  `rocknix-qa` held guest b's other session's content fixture (302 objects,
  unchanged before and after). `CLOUD_QA_BUCKET=<own>` isolates it; the guest's
  `SETTINGS_REMOTE`/`SAVES_REMOTE` follow because the harness reads them from
  the backend. Worth a line in `generic-x64-vm-testing.md` (not done here).

Follow-ups: the handhelds heal on the first `cloud_device_id` call of the new
build (a backup, a restore, capture at game exit, or the boot `--full`) — ask
before any reboot (D-QA-011); `cloud_capture`'s local
`manifest-<old id>.json` is not carried to the new name (nothing published yet,
#21); `tools/cloud-census` could assert the suffix is not `ee5013fc56` against
the real handhelds (issue proposal 5(i), not done); #86's remaining acceptance
criteria need the device run.

## 20:40 UTC — #86 fix pass: the marker knew what, not where; every new check shown to fail first

Review of `feature/device-id-seed` returned thirteen findings; all applied,
none contradicted the design. The review also re-ran the single-device suite
on guest a against the final scripts of the previous pass (`cloud_device_id`
md5 `77755d6c85…`, private bucket `rocknix-qa-review`): PASSED, 61 PASS
lines, exit 0 — the run the changelog's "suite passed" sentence rested on.

- **`cloud_backup`**: the #53 upload marker held only the archive's hash, so
  after a heal a game-exit backup said "unchanged; nothing to send" and the
  new folder stayed empty (no archive, no `device.json`) until the next
  `backuptool backup` — the changelog's "the next backup writes to the new
  folder" was false on the common path. The marker now holds
  `<hash> <destination>` and both must match; an old bare-hash marker
  mismatches once (read-both/write-new, `upgrade-and-install.md`).
- **`cloud_device_id`**: the two guessed `--previous` names are listed only
  where `/sys/class/net/sit0` exists (an ungated list sent a fresh RK3326 to
  `ROCKNIX-ee5013fc56` before root; trade recorded in D-CLOUD-068); the two
  `> "${ID_FILE}" 2>/dev/null` writes are braced so an unwritable file is
  reported by the WARN line alone.
- **`cloud_capture`**: when `manifest-<current id>.json` is absent, the
  first `manifest-<p>.json` for `p` in `cloud_device_id --previous` is
  renamed to it before the pass — once, never over an existing file, a
  concurrent run's rename recognised. The provenance recorded since #21
  follows the device instead of sitting unread beside an empty manifest.
- **`cloud_restore`**: the `--previous` WARN says *may have written* — the
  guessed names are listed on devices that were never healed.
- **Harness A15** (34 fixture checks now): plants `<hostname>-ee5013fc56`,
  the RG35XX SP's shape, so the label line of `--previous` has independent
  coverage; a macvlan `qa0mv` on `eth0` (type 1, no `device` link, no name
  pattern) isolates the sysfs tests; the journal is marked by line count
  before the run (six identical lines from earlier runs made the old check
  unfailable); the marker regression (bare hash equal to the local archive,
  then backup → healed folder, marker `<hash> <folder>`, run after sends
  nothing); `cloud_capture --full` adoption, once; (d)/(e) compare `stat -c
  %y` after `touch_offset -5`; new (f) unloads `sit` and, under the A15HOST
  hostname so neither guess dedups into the recorded id, expects only the
  recorded line. The device's manifests are moved aside for the run and the
  marker saved and restored.
- **Docs**: changelog sentences corrected ("on any run that prints the id",
  "first call that can see the adapter's address", the marker, the adoption,
  the sit0 gate); D-CLOUD-068 (uncommitted draft) amended; schema §7 note.

Evidence, guest a, bucket `rocknix-qa-fix86`, staged `cloud_device_id`
`18012c7cc43c`, `cloud_backup` `cc39a635b9c2`, `cloud_restore`
`950c2812ab61`, `cloud_capture` `fc85ae8bac7e`:

- **Violation copy first** (`/tmp/qa-bin-viol`: the previous pass's
  `cloud_backup` and `cloud_capture`, and a `cloud_device_id` with the
  `logger` line, both sysfs tests and the label line deleted and `main`
  rewriting the id file on every call): `--only A15` → **13 CHECK(S)
  FAILED**, one per removed guard — macvlan admitted; journal empty;
  `--previous` short by the label line, twice; manifest not adopted; backup
  not in the healed folder, no `device.json`, bare marker, run-after log
  shape; three mtime rewrites; the guessed names present without `sit0`.
  A first cut of (f) and of the run-after check passed against this copy
  (the hostname guess dedups into the recorded id; the transfer banner also
  says `to <folder>`) and were tightened until they failed.
- **Fixed scripts**: `--only A15` → **PASSED, 34 fixture checks + 3 setup,
  0 FAIL**; `GENERICX64-ee5013fc56 healed to GENERIC-X64-15ca35b6b4`;
  `--previous lists ['GENERICX64-ee5013fc56', 'GENERIC-X64-ee5013fc56']`;
  `adopted manifest-GENERICX64-ee5013fc56.json as
  manifest-GENERIC-X64-15ca35b6b4.json`; `the marker now records where as
  well as what: 768a35c6… qa-cloud:/rocknix-qa-fix86/BACKUPS/GENERIC-X64-15ca35b6b4`;
  old folder `still holds its 6 archives`; put back: id
  `GENERICX64-15ca35b6b4 (as before)`, no `.previous`, no marker, `sit0`
  absent, `qa0mv` gone, hostname `GENERICX64`.
- **Single-device suite** on the same guest with the same staged scripts
  (`cloud_capture` `fc85ae8bac7e` included): **PASSED, 61 PASS, 0 FAIL, 24
  steps, exit 0**; A15 re-run against that final set afterwards, 34/34.
  Guest put back (id `GENERICX64-15ca35b6b4`, no `.previous`, no marker, no
  manifests, no `sit0`, no `qa0mv`, hostname, conf and remote as before);
  `/tmp/qa-bin-viol` removed; the private bucket `rocknix-qa-fix86` purged,
  `rocknix-qa` (guest b's session) untouched.

Follow-up not done here: `generic-x64-vm-testing.md` still lacks the line
about running the harness under its own `CLOUD_QA_BUCKET` when a MinIO is
shared (noted in the 20:05 entry).

