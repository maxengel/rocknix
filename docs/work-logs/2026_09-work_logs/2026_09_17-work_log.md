# Work log -- 2026-09-17

## 00:45 UTC — Build 10: every outstanding defect to date, not only this round's (#205, #206, #203, #170)

- Maintainer: *"it seems like we should get the sig term fixed now as well. Are there any other bugs we've discovered along the way today that we should fix? It'd be good to make sure we're not just in a cycle of fixing bugs found in the current round and the next round, but instead take care of everything to date with the build we're about to do."* Inventory from the tracker, not memory: of the nineteen open `bug` issues, fifteen are fixed and await a device confirmation or a closure (#182's dimmed rows since RC-11 build 2, #183, #181, #178, #177, #175, #174, #169, #160, #121, #113, #102, #50), #79/#161 are lists, #162 belongs to the RA-offline program (#173). Genuinely outstanding and in reach: the SIGTERM window (#205), #206, #203's transfer-page stamps, #170.
- **The SIGTERM window, fixed where it is** (D-CLOUD-133): upstream commented `signal(SIGTERM, …)` out (85ee8f8fc, external terminations save no gamelists), so handling it in EmulationStation would change what every stop does and run the post-game path through a shutdown. Instead the deletion's two halves live in one process: `cloud_capture --retire --unlink` writes the row and unlinks the files, and the script ignores SIGTERM (`trap '' TERM`; children inherit SIG_IGN; systemd's SIGKILL comes only at the stop timeout). The worker removes what the script left, if anything.
- **#206** (D-CLOUD-134): `cloud_capture --adopt <copy> --from <source> --system --rom` -- a `copy` op in the merge (the source's entry at the new path, own slot/size/thumbnail, replaces and pub null) or an unknown-provenance entry when the source had none; the X handler gated like DELETE and recording on the worker (`SaveStateDeleter` -> `SaveStateBookkeeper`, jobs with a kind; a copy pending hides nothing). French for both gate sentences.
- **#203's follow-up**: `CloudTransferJob::restampStoppedParts` -- after a stop for a game, every stamp a script named in the command wrote during this run (`stat` mtime >= the job's start) is rewritten `130 cancelled`; a part that never started keeps its last real stamp.
- **#170**: `cheevos_armsx2.sh` tested PCSX2.ini for the Token line it wrote to secrets.ini; it tests secrets.ini and gives a header-less secrets.ini its section once. Harness section w. (three checks, PASS).
- Tests: ES unit 112 cases / 1224 assertions; `tools/last-good-scripts-test` PASSED with w.; `tools/cloud-round-trip` CAP13 (retire --unlink: row, files, usage, SIGTERM) and CAP14 (adopt) against guest b with the script staged under `/tmp/qa-bin`. **CAP13 caught a parser bug on its first run**: `--retire` collected paths only until the next `--` word, so `--retire --unlink PATH` exited 2 -- the exact form the worker sends. Fixed before anything was built; a guard that had never fired would have shipped it.

## 00:50 UTC — Two slips on the way to build 10, both caught before anything shipped

- **A merge refused, a chain that did not stop.** The merge of `feature/build10-fixes` into `next` was refused (the register and work log were staged, uncommitted, in the same tree), the chain script ran on regardless -- `set -e` did not end it -- and a build started on a `next` that carried the ES changes without the script they call. Caught within a minute from the merge's error line; the chain script was killed and the stray x64 build left to finish (killing a build in flight poisons packages, device-builds.md), never filed. The lesson is old and now bitten: gate every step of a chain on the step before it with `||`, not with `set -e` in a tool shell.
- **CAP9 stale, not regressed.** The full capture set on guest b read one FAIL (`aside holds ''`). Against the build 9 script it failed the same way: the fixture (2026-09-08) expected a corrupt manifest's aside to remain, and `finish()` has removed it after a completed run since D-CLOUD-078 (2026-09-10). Nothing had run CAP1-14 since. The fixture now expects the log to name the aside and the completed run to remove it.
- `pkill -f <pattern>` matched the tool's own shell again (exit 144) -- the self-matching watcher, once more; the bracket trick (`chain-…62[6]`) is the fix and is in the memory file.

## 01:15 UTC — Build 10 `0923b9ee9f` proven: nine of nine, guest d's four cases, CAP1-14

- Nine suites PASSED (`qa-0923b9ee9f-webdav-a-20260917-0046`, 00:46-01:11 UTC; a first launch had named yesterday's image file after midnight and died at `vm-pair up` -- the date rolled over under the script). H700 built 00:41-00:44, filed `h700-all-20260917-0923b9ee9f` (tar sha `3518b9e34bdf6a3c…`); both H700 binaries carry `--adopt`. Not staged; the RG SP off the network all night.
- Guest d (upgraded in place from build 9): COPY TO FREE SLOT 0.346 s and the copy's entry in the manifest within two seconds -- the source's sha, unknown provenance because the fixture slot itself had no entry (CAP14 (a) covers the recorded source); `cloud_capture --full` keeps it. DELETE: YES 0.263 s; `--retire --unlink` in the journal; `unlinked 2 file(s) after the retire (--unlink)` from the script; the copy, its thumbnail and its entry gone. The restart-in-flight case landed before the deletion started -- files and entry present, nothing split -- so CAP13 (d) is the proof of the SIGTERM half. The transfer page: a saves backup at 8 KB/s to `/GAMES-d`, left with B, START NEW GAME over it -> the question -> STOP IT AND PLAY -> `CloudTransferJob: last-backup restamped as stopped for a game`, the stamp `130 cancelled`, the GAME SETTINGS row `LAST 09/16/2026 20:49 - SKIPPED, A GAME WAS STARTED` (build 9: COULDN'T FINISH). Guest d's cloud config put back afterwards (no remote, `/ROCKNIX/Saves`, no bwlimit).
- Left open, said so: #206 box 3 (the copy's gate against a running sync -- built, not exercised); #170 box 1 (a PS2 title twice) and #205 box 1 (the feel) are the device's. Issues #205, #206, #203, #170 carry the build, the proofs and the frames.

## 02:10 UTC — Build 10 staged on the RG SP

- Maintainer: *"The console is online now. You have my permission to copy the new build over."* -- the transfer only (D-QA-011). Idle check clean (no emulator, no cloud process, lock free, queue empty; charging at 95 %). `stage-rgsp-run-0923b9ee9f.sh` over Tailscale, 01:48-02:08 UTC: the tar under a `.part` name in `/storage/.cache`, the device's own sha256 `3518b9e34bdf6a3c…` equal to the artifact's, moved into `/storage/.update` (1,304,268,800 bytes); BEGIN/END in the device-actions log, boot id `da484969` before and after. A first run of the wrapper named yesterday's artifact directory and stopped before the device (logged rc=1, nothing staged). Inert until a reboot, which is asked for by name and not issued (D-QA-015). The RG SP goes from build 6 to build 10: builds 7-10 land together.

## 02:25 UTC — Build 10 applied on the RG SP

- Maintainer: *"You may reboot."* Idle check at 02:16:28 UTC clean (no emulator, no cloud process, lock free, the build 10 tar alone in the queue, charging 97 %, the interface up). `tools/device-act rgsp "reboot-apply-h700-0923b9ee9f (maintainer yes 2026-09-17: 'You may reboot.')" -- 'sync; reboot'` at 02:16:41; back after ~80 s on `0923b9ee9f`, boot `bae3b500`, queue empty, interface / proxy / Tailscale up, offline toggle on, hardcore off, index-at-start on, `ready=247 pending=0`, charging 97 %. The RG SP went from build 6 straight to build 10: D-CLOUD-130 (the automatic syncs ask), #205 (the instant deletion, the deletion as one unit in the script), #206 (the copy gated and recorded), #203's stamps, #170 -- all on the device together. The maintainer's round follows; the device's own `(--retire --unlink, N ms)` line is read after their first deletion.

## 02:58 UTC — #207: the flash after a deletion is the completion rebuild; fixed on ES `fa4ada9d3`, build 11 `ea828b286a` building

- Maintainer, on build 10 on the RG SP: *"while the deletion issue has been fixed and deletions show up quickly within Emulation Station and the GameStateManager, there's a flash that briefly happens after the deletion is made and shown in the UI. So you see the deletion happen, and then moments later just see a brief flash as if it's redrawing that element."* Filed as #207 (D-QA-012), under #11.
- The device first (read-only, `tools/device-act`, boot `bae3b500`): their two deletions ran as `(--retire --unlink, 1160 ms)` and `(970 ms)` -- the whole unit on the worker; build 6's lines in the same log show `--retire` alone at 860-1020 ms, four times, before the unlink and a rescan. Recorded on #205, boxes 1 and 5 ticked with their words. `/var/log/cloud_sync.log` survived the reboot, so it is not tmpfs-only as #205 had assumed.
- The mechanism: `GuiSaveState::update()` refreshed the repository and ran `loadGrid()` when `completed()` moved -- `clear()` + `onSizeChanged()` + one `add()` per tile, the last-cursor reset, the selection animation replayed. Reproduced on guest d (build 10) with `flash-check.py` (83 ms/frame; idle page 0 changes in 3 s): DELETE 0.34 / **0.60** / 0.69 s, COPY 0.26 / **0.52** / 0.61 s; the bold frames are the manager sheet with no tiles.
- The fix (D-UI-074): `loadGrid()` records the files it built from; `update()` compares them with what the repository would show now and rebuilds only on a difference, logging it -- that path is now a deletion that did not take. ES `ee4786940` -> merge `fa4ada9d3` on `test/qa-integration`, pushed; unit tests 112 / 1224; pin `ea828b286a` on `next`; build worktrees synced before the chain started (x64 -> suites + H700, `chain-ea828b286a.sh`).
- Next: the same capture on guest d on build 11 (one change each), the directory fixture for #207 box 3 (a file the unlink cannot remove brings its tile back), #206 box 3, the H700 tar, then the staging question.

## 02:59 UTC — Build 11 `ea828b286a` proven on guest d; a fixture lesson; the worker's line made truthful (build 12 `794bdbb945`)

- x64 built 02:49 (ES `strings` carries the new INFO line, `BUILD_ID=ea828b286a`), H700 02:53 (tar sha `7bc199ca68928cc3…`, 1,304,268,800 bytes, archived under `h700-all-20260917-ea828b286a/` with the DDR3/DDR4 images; the `failed to determine git HEAD` line in its log is in build 10's too). Guest d upgraded in place from build 10 (tar sha equal on both sides), back at 02:52.
- `proof-11.sh`, the same capture as the reproduction: DELETE -> YES **1 change** (0.44 s, the tile going; build 10: 3), COPY **1 change** (0.34 s; build 10: 3); the journal has the worker's deletion and no rebuild line. Frames `docs/qa-frames/2026-09-17/207-*`. #207 boxes 2 and 4 ticked.
- Box 3's first fixture was wrong, not the code: an empty directory in the file's place is removable -- the script's `rm -f` refused it, but the worker's fallback `remove()` rmdirs an empty directory -- so the deletion took and the page rightly held (and the cleanup's `rmdir && mv` left the moved file as `.Bobl.state4.keep` until restored). `chattr +i` (busybox has it) is the fixture: `proof-11c.sh` -- 3 changes (0.26 s gone, 0.44 / 0.53 s back), the script's `unlinked 0 file(s)`, ES `a landed job left the disk differing from the page (4 shown, 5 on disk); rebuilt`, the file still on disk. Box 3 ticked. The lesson is the standing one: construct the violation and watch the guard fire -- a fixture that can be removed proves nothing about a file that cannot.
- The box-3 run found the worker logging `save state deleted` for the refused unlink. ES `058c375ac` stats the file after the fallback and warns `save state deletion did not take: ... is still present` instead; merge `e4b441d9e`, pin `794bdbb945` on `next` (build 12). Its x64 build waits for build 11's suites, which booted from the image file the build would overwrite; then suites, H700, a guest d re-run of both proofs, and the staging question.
- Guest d fixture now: Bobl `state1..4` + auto, `state4` without a thumbnail and with three scratch `retired` rows behind it.

## 03:24 UTC — Build 11's nine suites PASSED; build 12 `6c0c13dc4e` built and proven on guest d

- Build 11 suites: nine of nine PASSED (`qa-ea828b286a-webdav-a-20260917-0250`, 02:50-03:15 UTC; walks 1109 s). QA row written. The CAP cases are not in the suites (the harness stages the tree's `cloud_capture` by hand on guest b); the scripts are build 10's, unchanged.
- Build 12 is `6c0c13dc4e`, not `794bdbb945`: the build worktrees were synced after the docs commits, so `BUILD_ID` is next's head at that moment. The chain scripts carry the pin's id in their names; every artifact and record carries the image's. Started only after build 11's suites finished -- an x64 build rewrites the `…-20260917.img.gz` the suites booted from.
- x64 BUILD OK 03:18 (ES `strings`: the manager's rebuild line and the worker's `deletion did not take` warning both present); H700 BUILD OK 03:22 (same two strings, `BUILD_ID=6c0c13dc4e`; tar sha `9c04e61e723fb78b…`, 1,304,268,800 bytes); both archived under `*-all-20260917-6c0c13dc4e/`.
- Guest d upgraded in place from build 11 (`upgrade-d.sh`: sha equal, back in ~50 s). `proof-12.sh`: DELETE one change (0.46 s), COPY one change (0.45 s), no rebuild logged. `proof-12c.sh` (`chattr +i`): three changes (0.44 gone, 0.80 / 0.99 s back), ES `(4 shown, 5 on disk); rebuilt`, the worker's WARNING `save state deletion did not take: … is still present; the manager shows it again` and no `save state deleted` claim; the script `unlinked 1 file(s)` (the thumbnail), the state still on disk. Posted on #207.
- Build 12's nine suites started 03:19 on guests a/b; then CAP13/14 on guest b with the tree's scripts staged (#207 box 5), the QA row, and the staging question.

## 03:47 UTC — Build 12 `6c0c13dc4e` complete on the VM side: nine suites, CAP13/14, box 5; the staging question is the maintainer's

- Suites nine of nine PASSED (`qa-6c0c13dc4e-webdav-a-20260917-0319`, 03:19-03:44 UTC; 0.88 / 1.31 / 2.01 s). CAP13 (a)-(d) and CAP14 (a)-(d) PASSED on guest b (`cap-b-6c0c13dc4e.sh`: the tree's three scripts under `/tmp/qa-bin`, `--path-prefix`, `--only CAP13,CAP14`). #207 box 5 ticked; boxes 2-5 are the VM's, box 1 the RG SP's. QA row written.
- H700 build 12 archived with `SHA256SUMS.tar` and `BUILD_INFO.txt` (build 11's directory completed the same way and marked superseded). `stage-rgsp-run-6c0c13dc4e.sh` is the build-10 template with one guard added: it refuses to run unless `QUOTE` carries the maintainer's words verbatim (proven: `refusing …`, rc 2). The reboot stays a second question (`rgsp-after-reboot.sh 6c0c13dc4e` reads the device afterwards).
- Memory written: `build-id-is-the-synced-head` (name a build by the image's id; never start an x64 build while `vm-qa` runs).
- Next: #206 box 3 on guest d now that the QA folder is free; then whatever the maintainer answers.

## 04:00 UTC — #206 box 3 proven on guest d (build 12): the copy's gate holds while a sync runs

- Setup: `tools/cloud-test-backend --backend webdav rclone-conf` -> guest d's `rclone.conf` (0600), `cloud_sync_helper`, `SAVES_REMOTE=/GAMES-d`, `--bwlimit 1k`; the config copied aside first and restored after (no rclone.conf, `/ROCKNIX/Saves`, no bwlimit). The suites had finished, so the QA backend had one writer.
- First attempt found no sync to collide with: a 504 KB saves backup at 1 KB/s finished in about four minutes while the walk went wrong twice. `ret` inside the manager is LAUNCH (Bobl started with state4 auto-loaded; ended cleanly with the shipped `execute_kill`, exit sync off on guest d). On the game list here X is GAME OPTIONS and Y is SEARCH (a text popup that swallows typed keys; Escape leaves it and the list moved to PICO-8); LAUNCH from GAME OPTIONS during the sync asked D-CLOUD-130's STOP IT AND PLAY / KEEP WAITING question before any manager -- so the manager is reached by **holding A** (SAVE STATES (HOLD), `hold.py x 1500`).
- Second attempt with a 900 KB scratch state planted: the card BACKING UP SAVES 64 KB OF 1.0 MB; the manager open; X on a tile -> *YOUR SAVES ARE SYNCING WITH THE CLOUD. WAIT FOR IT TO FINISH BEFORE COPYING A SAVE STATE - THE NOTIFICATION AT THE TOP SAYS WHEN IT IS DONE.*; rclone still running; no `Bobl.state5`. Frame `206-manager-copy-refused-during-sync-640x480-6c0c13dc4e.png`. Box ticked; #206 has every box ticked and waits on the maintainer's round.
- Cleanup: rclone stopped (`last-backup` on guest d now `143 SOMETHING_WENT_WRONG`, honest), the scratch state removed; Bobl's exit capture recorded 2 new (the auto save the launch wrote). A frame of the running game carried the RetroAchievements QA account name; it stays in the session directory and is not published.
