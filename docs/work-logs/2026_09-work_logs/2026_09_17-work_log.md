# Work log -- 2026-09-17

## 00:45 UTC — Build 10: every outstanding defect to date, not only this round's (#205, #206, #203, #170)

- Maintainer: *"it seems like we should get the sig term fixed now as well. Are there any other bugs we've discovered along the way today that we should fix? It'd be good to make sure we're not just in a cycle of fixing bugs found in the current round and the next round, but instead take care of everything to date with the build we're about to do."* Inventory from the tracker, not memory: of the nineteen open `bug` issues, fifteen are fixed and await a device confirmation or a closure (#182's dimmed rows since RC-11 build 2, #183, #181, #178, #177, #175, #174, #169, #160, #121, #113, #102, #50), #79/#161 are lists, #162 belongs to the RA-offline program (#173). Genuinely outstanding and in reach: the SIGTERM window (#205), #206, #203's transfer-page stamps, #170.
- **The SIGTERM window, fixed where it is** (D-CLOUD-133): upstream commented `signal(SIGTERM, …)` out (85ee8f8fc, external terminations save no gamelists), so handling it in EmulationStation would change what every stop does and run the post-game path through a shutdown. Instead the deletion's two halves live in one process: `cloud_capture --retire --unlink` writes the row and unlinks the files, and the script ignores SIGTERM (`trap '' TERM`; children inherit SIG_IGN; systemd's SIGKILL comes only at the stop timeout). The worker removes what the script left, if anything.
- **#206** (D-CLOUD-134): `cloud_capture --adopt <copy> --from <source> --system --rom` -- a `copy` op in the merge (the source's entry at the new path, own slot/size/thumbnail, replaces and pub null) or an unknown-provenance entry when the source had none; the X handler gated like DELETE and recording on the worker (`SaveStateDeleter` -> `SaveStateBookkeeper`, jobs with a kind; a copy pending hides nothing). French for both gate sentences.
- **#203's follow-up**: `CloudTransferJob::restampStoppedParts` -- after a stop for a game, every stamp a script named in the command wrote during this run (`stat` mtime >= the job's start) is rewritten `130 cancelled`; a part that never started keeps its last real stamp.
- **#170**: `cheevos_armsx2.sh` tested PCSX2.ini for the Token line it wrote to secrets.ini; it tests secrets.ini and gives a header-less secrets.ini its section once. Harness section w. (three checks, PASS).
- Tests: ES unit 112 cases / 1224 assertions; `tools/last-good-scripts-test` PASSED with w.; `tools/cloud-round-trip` CAP13 (retire --unlink: row, files, usage, SIGTERM) and CAP14 (adopt) against guest b with the script staged under `/tmp/qa-bin`. **CAP13 caught a parser bug on its first run**: `--retire` collected paths only until the next `--` word, so `--retire --unlink PATH` exited 2 -- the exact form the worker sends. Fixed before anything was built; a guard that had never fired would have shipped it.

## 00:50 UTC — Two slips on the way to build 10, both caught before anything shipped

- **A merge refused, a chain that did not stop.** The merge of `feature/build10-fixes` into `next` was refused (the register and work log were staged, uncommitted, in the same tree), the chain script ran on regardless -- `set -e` did not end it -- and a build started on a `next` that carried the ES changes without the script they call. Caught within a minute from the merge's error line; the chain script was killed and the stray x64 build left to finish (killing a build in flight poisons packages, device-builds.md), never filed. The lesson is old and now bitten: gate every step of a chain on the step before it with `||`, not with `set -e` in a tool shell.
- **CAP9 stale, not regressed.** The full capture set on guest b read one FAIL (`aside holds ''`). Against the build 9 script it failed the same way: the fixture (2026-09-08) expected a corrupt manifest's aside to remain, and `finish()` has removed it after a completed run since D-CLOUD-078 (2026-09-10). Nothing had run CAP1-14 since. The fixture now expects the log to name the aside and the completed run to remove it.
- `pkill -f <pattern>` matched the tool's own shell again (exit 144) -- the self-matching watcher, once more; the bracket trick (`chain-…62[6]`) is the fix and is in the memory file.

## 01:15 UTC — Build 10 `0923b9ee9f` proven: nine of nine, guest d's four cases, CAP1-14

- Nine suites PASSED (`qa-0923b9ee9f-webdav-a-20260917-0046`, 00:46-01:11 UTC; a first launch had named yesterday's image file after midnight and died at `vm-pair up` -- the date rolled over under the script). H700 built 00:41-00:44, filed `h700-all-20260917-0923b9ee9f` (tar sha `3518b9e34bdf6a3c…`); both H700 binaries carry `--adopt`. Not staged; the RG SP off the network all night.
- Guest d (upgraded in place from build 9): COPY TO FREE SLOT 0.346 s and the copy's entry in the manifest within two seconds -- the source's sha, unknown provenance because the fixture slot itself had no entry (CAP14 (a) covers the recorded source); `cloud_capture --full` keeps it. DELETE: YES 0.263 s; `--retire --unlink` in the journal; `unlinked 2 file(s) after the retire (--unlink)` from the script; the copy, its thumbnail and its entry gone. The restart-in-flight case landed before the deletion started -- files and entry present, nothing split -- so CAP13 (d) is the proof of the SIGTERM half. The transfer page: a saves backup at 8 KB/s to `/GAMES-d`, left with B, START NEW GAME over it -> the question -> STOP IT AND PLAY -> `CloudTransferJob: last-backup restamped as stopped for a game`, the stamp `130 cancelled`, the GAME SETTINGS row `LAST 09/16/2026 20:49 - SKIPPED, A GAME WAS STARTED` (build 9: COULDN'T FINISH). Guest d's cloud config put back afterwards (no remote, `/ROCKNIX/Saves`, no bwlimit).
- Left open, said so: #206 box 3 (the copy's gate against a running sync -- built, not exercised); #170 box 1 (a PS2 title twice) and #205 box 1 (the feel) are the device's. Issues #205, #206, #203, #170 carry the build, the proofs and the frames.

## 02:10 UTC — Build 10 staged on the RG SP

- Maintainer: *"The console is online now. You have my permission to copy the new build over."* -- the transfer only (D-QA-011). Idle check clean (no emulator, no cloud process, lock free, queue empty; charging at 95 %). `stage-rgsp-run-0923b9ee9f.sh` over Tailscale, 01:48-02:08 UTC: the tar under a `.part` name in `/storage/.cache`, the device's own sha256 `3518b9e34bdf6a3c…` equal to the artifact's, moved into `/storage/.update` (1,304,268,800 bytes); BEGIN/END in the device-actions log, boot id `da484969` before and after. A first run of the wrapper named yesterday's artifact directory and stopped before the device (logged rc=1, nothing staged). Inert until a reboot, which is asked for by name and not issued (D-QA-015). The RG SP goes from build 6 to build 10: builds 7-10 land together.

## 02:25 UTC — Build 10 applied on the RG SP

- Maintainer: *"You may reboot."* Idle check at 02:16:28 UTC clean (no emulator, no cloud process, lock free, the build 10 tar alone in the queue, charging 97 %, the interface up). `tools/device-act rgsp "reboot-apply-h700-0923b9ee9f (maintainer yes 2026-09-17: 'You may reboot.')" -- 'sync; reboot'` at 02:16:41; back after ~80 s on `0923b9ee9f`, boot `bae3b500`, queue empty, interface / proxy / Tailscale up, offline toggle on, hardcore off, index-at-start on, `ready=247 pending=0`, charging 97 %. The RG SP went from build 6 straight to build 10: D-CLOUD-130 (the automatic syncs ask), #205 (the instant deletion, the deletion as one unit in the script), #206 (the copy gated and recorded), #203's stamps, #170 -- all on the device together. The maintainer's round follows; the device's own `(--retire --unlink, N ms)` line is read after their first deletion.
