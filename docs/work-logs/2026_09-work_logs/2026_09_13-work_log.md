# 2026-09-13 work log

## 00:05 UTC -- #45: the settings archive stops carrying the image

`backuptool` (`4fae9fe2e4`, on `next`) now leaves out of the settings archive
anything the image ships: a file under `/storage/.config` byte-identical to
its `/usr/config` counterpart, and PPSSPP's `assets/` and `PSP/SYSTEM/CACHE/`
always. The restore side skips those two prefixes from any archive, so the
archives already on devices and in clouds -- every one of them 16-17 MB of
PPSSPP program data -- stop putting an older emulator's files under a newer
one. D-CLOUD-008 moved from the open list to a decided row; the Cheats
question in #45 is answered by the identity rule (shipped `cheat.db` and
per-title files stay, a player's own or edited file travels).

Measured on guest c (x64, `ec12767b26` scripts replaced under `/tmp/qa-bin`):

| | archive | entries | assets | cheat.db |
| --- | ---: | ---: | ---: | ---: |
| shipped script | 17,317,741 B | 187 asset entries among them | 187 | 1 |
| patched script | 9,218 B | 16 | 0 | 0 |

`ppsspp.ini` came back byte-identical through `restore --no-restart`; the
shipped script's archive, restored by the patched one after an asset was
marked, left the asset untouched and restored the ini. The log line says
`left out 88 file(s) identical to the image's own copies under /usr/config`.

`tools/last-good-scripts-test` gains case k (eight checks, both sides). Run
against the script one commit earlier with `BASE_REF=53f390b1e9 --old` it
fails four: seeds travel, assets travel, no log line, the asset overwritten
on restore. The whole suite passes in 14 s on the current tree.

Two things learned in the fixtures, neither a blindspot:

- **bwrap cannot make a mountpoint under a read-only bind.** `--ro-bind /usr
  /usr` then `--ro-bind <fake> /usr/config` fails with "Can't mkdir
  /usr/config: Read-only file system" because the host has no `/usr/config`.
  Bind `/usr` a subdirectory at a time (`for d in /usr/*/`) so `/usr/config`
  is created on the tmpfs root. Case k does this.
- **Do not mark a binary fixture with `echo >>` and unmark it with `sed
  '$d'`.** The PNG's last bytes carried no newline, so the delete took eight
  bytes of `IEND` with the marker; guest c's `7z.png` was restored from
  `/usr/config`. Mark a text file, or keep a copy.

Follow-ups: the hardware half of #45's criterion 2 when the next build is
staged (per-device yes, D-QA-015); #113's LINK5 re-run once an image carries
this (its 16.5 MiB archive over `--bwlimit 200k` is what hit `--timeout
30s`); the next x64 and H700 images should carry it together with the
`cloud_sync_helper` migration from #107.

## 01:05 UTC -- #142: FTP's "missing" is not rclone's "not found"

The #133 matrix's FTP cells (pyftpdlib) failed nine of the round trip's
checks on `ec12767b26`, reproduced tonight exactly (90/99). One cause: the
server answers a missing folder with `501 "No such directory."` where the
standard reply is 550; rclone maps 550 to "directory not found" (exit 3) and
relays 501 as a plain error (exit 1). Every branch that read exit 3 as "not
there yet" read the 501 as "your cloud is broken" -- the content scan printed
no rows and said the cloud couldn't be read, the wizard's folder setter
refused a folder that did not exist yet (and two later checks failed
downstream of the unchanged conf). The same first copy into a new folder
failed its destination listing, and `--retries 1` made that the whole run.

`6c13475947`: `absent_not_broken()` (in `cloud_content_restore` and
`cloud_setup`, identical) walks up to the nearest listable parent and reads
absence from that parent lacking the name -- D-CLOUD-120's bucket rule made
the general tie-breaker, run only after a code other than 3 (D-CLOUD-123).
`cloud_content_backup` runs `rclone mkdir` on each target before the copy.
Probed from guest c first (rclone 1.75): lsf/lsjson/lsd all exit 1 on the
501; copy into a new folder with `--retries 1` exits 1 (3 of 3 landed this
time, 2 of 3 in #133's run -- the transfers race the failed listing); after a
mkdir the same copy is clean. Patched scripts staged on guest a under
`/tmp/qa-bin` and the harness run with `--path-prefix`: **100/100**.

`last-good-scripts-test` case l: eleven checks over a shim rclone whose cloud
holds `/QA/Saves` and `/QA/Content/ROMs` and answers everything else with a
501. All eleven fail against `next`'s scripts before the change -- one of
them only after a fix to the check itself: "cloud_setup carries the identical
function" compared two `sed -n` extractions, and two missing functions are
identical. `[ -s fn.sh ]` first. A check that passes when the thing under
test is absent is the shape engineering-practices § guards warns about.

Also tonight, again: a wait loop matched its own Bash call. The pattern was
bracketed (`cloud-round-tri[p]`) but the same call carried the literal
`tools/cloud-round-trip --host ...` in its start line, and `bash -c`'s argv
holds the whole script. 270 s waited on a harness that had exited. The memory
already said "no literal anywhere in the command that runs the kill"; it now
says the same for the wait. Anchor on the interpreter (`^python3? `), or
write a pidfile.

Follow-up: the FTP fixture stays up on 9015 for the runner; guest a keeps the
three patched scripts under `/tmp/qa-bin` until its next reboot.

## 01:55 UTC -- PL-08 re-keyed on the maintainer's call; the register gets a guard

The one item still on #129 was mine to put to the maintainer and theirs to
decide: `D-INFRA-008` named two decisions. Maintainer: *"I agree with your
recommendation to clerically rekey it, as long as we have a solution in the
future to handle this."* Done as D-WORKFLOW-013 (`b1fdf555ad`): the
2026-09-11 credential-boundary row is `D-INFRA-010` (ID cell only), the
2026-09-12 row that restated it is gone, the two live citations that meant
it (`docs/cloud-sync-changelog.md`, ES `GuiMenu.cpp`, ES `a29e2ce49`) say
D-INFRA-010, and dated records stay as written. The solution for the
future is `tools/register-check`: a duplicate ID or a citation that names
no row fails it, over the register, the rules, the live docs, the script
trees and (with `ES_SRC`) EmulationStation. Its first run found a second
defect of the same family -- D-CLOUD-010 "settles D-CLOUD-007" with no such
row, because the open question had been removed in August instead of
moved down -- so a row holds that ID now. 228 IDs, each once. #129 closed.

The maintainer's second sentence is a standing instruction for every fix of
this kind: *"if this is something we need to handle during update flows, we
should always be thinking about how we handle that when a system upgrade
runs, to avoid any errors and perform any cleanup that needs to happen."*
For the register that was the citation sweep; for #50 below it is the rule
itself.

## 02:00 UTC -- #50: the shipped device name becomes the unit's own

`network-base-setup` (`d857ba04c9`, D-NET-002): a `system.hostname` equal to
the build family (or empty) is the shipped default, and at boot it becomes
`<FAMILY>-<four hex of cloud_device_id>` once, written back; any other name
is the player's and is left alone (D-NET-001). That rule *is* the upgrade
path -- an upgraded device still called H700 gets its own name at the first
boot of the image, a renamed one keeps its name. Run by hand on the three
guests: `GENERIC_X64` -> `GENERIC-X64-491f`, `-15ca`, `-0964`; a second run
unchanged; "Max Deck" untouched; empty -> suffixed. `last-good-scripts-test`
case m runs the whole script in a sandbox with a fixed id; four of six fail
against the previous script. mDNS stays as it was and is D-NET-003, open:
`avahi-daemon.service` has been commented out since the fork's first
commit with no reason on record, and nothing user-facing promises `.local`.

Two things met on the way:

- **`hostnamectl --transient` is refused on the image** ("static hostname is
  already set") because `/etc/hostname` is a symlink to
  `/proc/sys/kernel/hostname`, so systemd-hostnamed reads the *current*
  kernel name as the static one, caches it, and puts it back over whatever
  the script just wrote. At boot the echo lands first and hostnamed starts
  after it, so the boot path works by construction; a re-run without a
  reboot does not apply a changed name (seen on guest c). Not changed
  tonight; noted for the day it matters.
- **In a sandbox, put the shims on PATH after the profile**: `001-functions`
  prepends `/usr/bin`, so a shim directory exported before it loses to the
  host's real `logger`. Case m's "the log says so" check passed over an
  empty log until the order was fixed.

Not yet in an image: `878ec8863b` (built 00:42, the runner on it now)
predates this commit. The next x64 and H700 builds carry it; `vm-qa`'s
pair-identity suite gains "the two guests' hostnames differ and equal the
cleaned setting" once the runner has finished with the file.

## 02:40 UTC -- the device sometimes boots as ROCKNIX, and why (#50, second defect)

The pair-identity suite's new check (kernel hostname equals the cleaned
setting) failed on guest b of `878ec8863b`: hostname `ROCKNIX`, setting
`GENERIC_X64`. The boot journal in monotonic time:

| t | who | what |
| ---: | --- | --- |
| 1.87 s | systemd-resolved | using system hostname 'ROCKNIX' |
| 2.25 s | NetworkManager, via D-Bus | activates systemd-hostnamed |
| 3.21 s | network-base-setup | writes GENERIC-X64 to the kernel |
| 3.21 s | hostnamectl --transient | "static hostname is already set, so the specified transient hostname will not be used" |
| 3.21 s | systemd-hostnamed | "Hostname set to <ROCKNIX> (static)" |

`/etc/hostname` is a symlink to `/proc/sys/kernel/hostname`, so hostnamed
takes whatever the kernel is called when *it* starts as the static name,
and re-applies that cached name when it answers a hostname1 call. Our own
`hostnamectl --transient` was that call. Whether the device boots with the
right name depends on whether NetworkManager's D-Bus activation of hostnamed
lands before or after network-base.service; guest a won the race, guest b
lost it, and a handheld does the same. Reproduced by hand on guest b: with
hostnamed holding ROCKNIX, echo then `hostnamectl --transient` ends as
ROCKNIX; with hostnamed stopped, the echo sticks and hostnamed reads it as
static on restart.

`af9295025a`: `network-base.service` orders itself
`Before=systemd-hostnamed.service NetworkManager.service`, and the script no
longer calls hostnamectl -- the write is the whole job. `vm-qa` declares
`SSHO` (blindspot 40) and its pair-identity suite requires two names, each
equal to its setting; case m checks the ordering and the absence of the
call. The proof at boot needs an image that carries it; `878ec8863b` does
not, and the suite fails on it as it should. Related, not changed:
`avahi-set-host-name` logs "Daemon not running" on every boot, because the
responder is off (D-NET-003).

## 02:45 UTC -- #93 and #27 framed on guest b (1280x800, `878ec8863b`)

The save state manager on the new build: four tiles (START NEW GAME, AUTO
SAVE, SLOT 1, SLOT 2), every label whole, on a sheet that is half the
screen. Deleting a state -- the AUTO SAVE, then each slot -- lands the cursor
on START NEW GAME with the help bar reading BACK / LAUNCH, where the old
build kept the deleted slot's DELETE / COPY TO FREE SLOT. Frames in
`docs/qa-frames/2026-09-13/`. Seen on the way: deleting the auto save
renumbered SLOT 1 / SLOT 2 to SLOT 0 / SLOT 1 (`renumberSlots`, D-CLOUD-053's
compaction) -- expected behaviour, noted because a reader of the frames will
ask. The 640x480 frame #27 asks for needs a guest at that size on the new
image; guest c is 640x480 but on `c90022dc57`.

## 03:50 UTC -- #47, #82 and #93 framed on guest b; #27's first cut fails at 640x480

Guest b (1280x800) and a fourth guest, d, at 640x480, both on `878ec8863b`.

**#93 closed.** Three deletes in the save state manager; after each the
cursor is on START NEW GAME and the help bar reads BACK / LAUNCH (ES
`a45c4c1b1`). **#47 closed.** The FINISH RESTORE PROCESS page at boot on a
device with no cloud storage: neutral icons, one line under each row, the
cloud row greyed and, pressed, NO CLOUD STORAGE IS SET UP ON THIS DEVICE
YET. SET IT UP NOW?; LATER KEEPS THIS LIST under the buttons. **#82's VM
half.** A PNG planted while ES ran appeared in SCREENSHOTS after a launch
and exit; a third, brought down by RESTORE FROM THE CLOUD (SAVES) from the
transfer page, appeared when the completed page was closed. Frames in
`docs/qa-frames/2026-09-13/`.

Two findings on the way, one fixed, one noted:

- **`Utils::FileSystem::exists()` caches for the session.** The restore
  marker planted after boot did not make the FINISH RESTORE PROCESS row
  appear in NETWORK SETTINGS -- the page's gate read the boot-time
  "absent" from the cache. ES `613f6152b`: both gates read uncached, as the
  `rclone.conf` gate beside them already did. No player met it (a restore
  is followed by a restart in every shipped flow), the walk did.
- **A system with no games at boot is never created**, so a device whose
  screenshots folder is empty at boot has no SCREENSHOTS entry to refresh;
  its first screenshot appears at the next boot. Noted on #82, not filed.

**#27 is not done.** At 640x480 on `878ec8863b` the tiles still read START
NEW G... / AUTO SAVE... / SLOT 1... -- worse than before, since the slot
tiles lost their second line too. The label share had fallen to its 0.30
floor: `Font::getHeight` is the tallest glyph rasterised *so far*, so
measured in the constructor, before any label had drawn, it under-reported;
and TextComponent's AUTO wrap threshold (1.8 lines of that same moving
height) was judged later, with more glyphs loaded, and found the area
short. At 1280x800 the margins covered it. Second cut, ES `2088eadb9`:
rasterise the printable range before reading the height; give the grid
tile's text element the theme's `multiLine` property and set it `true`, so
the wrap no longer hinges on a threshold; the sheet at 0.55 of the screen
with the share capped at 0.50. Image `02f368914e` carries it (with the
uncached marker and the network-base ordering); the runner is on it and
guest d is being rebuilt from it for the 640x480 frame.

Also seen: the API's JSON is pretty-printed (`"name": "nes"`), so a grep
for `"name":"` matches nothing and reports an empty system list; and a
check of the SCREENSHOTS list made while the transfer page is still up
reads the old list, because the rescan is posted when the page is closed.

## 04:30 UTC -- #27's second cut wraps; the dates collide; a third cut and #149

Guest d rebuilt on `02f368914e` at 640x480: every label wraps to two lines
now -- START NEW / GAME, AUTO SAVE / 09/12/2026 21:47, SLOT 1 / date -- and
the date, the widest line a tile shows, is as wide as the tile, so two
neighbours' dates read as one string ("21:4709/12/2026"). Third cut, ES
`d8e95118c`: where the current date in the small font would take more than
0.86 of the tile's width (the grid's own arithmetic), the label font
shrinks to fit with a gap, and the two-line height is measured on that
font. D-UI-050 records the whole shape; `5776ede212` pins it and is
building.

Also on the frames: no help bar under the manager at 640x480, on the old
build too (`c3-mgr.png` from the 12th shows the same). `Window::render`
draws the prompts only when fewer than two pages are open or full-screen
menus are off; on a small panel they are on, the manager is the second
page, and only `ViewController` draws the prompts early. The manager's
layout reserves a bottom row for the bar, which says where they were meant
to be. Filed as #149 and fixed in the same ES commit: `GuiSaveState::render`
draws them early into that row when it is the top page.

The pair on `02f368914e` passed pair-identity with two names, each equal to
its setting (GENERIC-X64-15ca, GENERIC-X64-0964), and guest d came up as
GENERIC-X64-474c: D-NET-002 and the hostnamed ordering at boot, three of
three.

## 05:00 UTC -- #27's third cut: the bar is there, the dates still collide; the fourth finds the cause

`5776ede212` on guest d at 640x480: BACK / LAUNCH under the tiles and BACK /
DELETE / COPY TO FREE SLOT / LAUNCH on a slot -- #149's fix works. The
dates still ran together: the shrink never fired. Cause, and the cause of
the whole issue: `Font::get` scales the requested size by the screen's
`fontScale()` (1.31 under 720 px) and `getSize()` returns the scaled size;
the menu theme's small font is made with the menu scale (also 1.31). The
manager handed `TextSmall.font->getSize()` (20 on a 640x480 panel) into
the tile's `<fontSize>`, which went back through `Font::get` -- 26 px. The
labels were drawn a third larger than the menu's own small text from the
day the page was written, and my date measurement, taken on the theme's
20 px object, read narrower than the tile drew it. Fourth cut, ES
`7c935a39d`: ask for `getSize() / fontScale()` (15 -> 19 px effective, the
theme's own), measure the date on that font, shrink from there only if it
still would not fit. `db6b42c180` pins it. Three font/file traps written
into `es-code-traps.md`.

## 06:40 UTC -- the fourth cut holds at both sizes; #27 and #149 closed; `db6b42c180` built for x64 and H700

`db6b42c180` on guest d (640x480): START NEW GAME on one line at the
theme's own size, AUTO SAVE / date and SLOT n / date each inside its tile,
BACK / LAUNCH under them and BACK / DELETE / COPY TO FREE SLOT / LAUNCH on a
slot. On guest b (1280x800): unchanged labels, the bar drawn once. #27
closed with the frames and the cause; #149 closed. D-UI-050 is the shape.

The runner on `db6b42c180`: **eight suites PASS** (`qa-db6b42c180-webdav-a-
20260913-0210`), as on `02f368914e`; `878ec8863b` had seven and the
pair-identity tool defect. H700 `db6b42c180` built in six minutes on the
warm root (`h700-all-20260913-db6b42c180/`, tar `dad874eef611b53f...`),
the staging candidate that replaces `ec12767b26`: it carries #45, #142,
#50 with the hostnamed ordering, #93, #47 with the uncached marker, #27's
four cuts and #149, and the #107 helper migration. Not staged; each
handheld is a question (D-QA-015). The LINK cells run on the pair now for
#113's last VM box.

Three small things: the runner's report directory is named by the start
time, so `ls -dt | head -1` after a second run on the same image finds the
new, empty one -- the QA-log row was first written with question marks
from the link run's report and then corrected from the full run's; guest
c (`c90022dc57`, WebDAV, the #45 fixtures) is still up on a deleted disk
image and dies at its next restart; and the maintainer's handhelds have
not been touched tonight.

## 07:05 UTC -- LINK5 under the bound: the fixture's premise restored, the lock's second face

The link suite on `db6b42c180`: 65 PASS, LINK5 FAIL -- the other way round
from the 12th. #45 shrank a stock guest's settings archive from 17 MB to a
few KB, and the cell's upload was over before the harness could cut the
link. The pre-#45 archive had supplied the cell's premise (a multi-MiB
upload) by accident. `cc2a9bfd3a`: twelve MiB of random bytes under
`/storage/.config/retroarch` before `backuptool backup`, removed right
after (a default location, no `/usr/config` twin, so the identity prune
keeps it).

With the cut landing again, one check still failed: the receiving side's
copy no longer matched after the re-run. The harness already skips the
re-run's exit under the QA WebDAV server's stale-PUT lock (423 Locked:
slirp holds the cut PUT open), and the same artifact has a second face --
when slirp finally drops the connection the server commits or aborts that
PUT and the stored file changes. Comparing it judges the QA server, not
the script; the check is skipped with the reason, as its sibling is, and
both say to assert idempotency on MinIO/S3 or a real provider. LINK5 then
PASSED: 29.5 s after the cut, exit 69, no partial, marker unchanged. #113's
VM half ticked; the Dropbox half is the maintainer's device.

A fixture that works by accident is a fixture that stops working when the
product improves. The 12 MiB pad is the premise stated.

## 07:50 UTC -- the maintainer's four calls: mDNS on, a pointer in the ES fork, the QA handheld, the docs last

Answered in one message and acted on the same hour:

- **mDNS** (D-NET-003 settled): "We should turn back on the mDNS responder."
  `e76bafe860`: `avahi-daemon.service` enabled, ordered `After=
  network-base.service` by name -- `network.target` does not order after
  the units wanted by it, and the daemon publishes whatever the kernel is
  called when it starts -- and `network-base-setup` no longer calls
  `avahi-set-host-name` (never running that early; failed every boot). On
  the guest nothing had answered on 5353 and resolved runs mDNS off, so
  there is no second responder to collide with. `1de2e1179d` built for x64
  (runner on it) and H700 (building), the staging candidate in place of
  `db6b42c180`. The VM proves the daemon up and publishing; only the LAN
  proves a laptop resolving `RG-SP-xxxx.local` -- slirp carries no multicast.
- **Rules in the ES fork** (D-WORKFLOW-010 settled): "I agree on the pointer
  in the ES fork. We need to make sure that we have that guard in place." A
  `CLAUDE.md` on `test/qa-integration` names the distribution checkout as
  the one home of the rules, the four ES rules and the principles to read
  first, the checks that read the tree, and the 640x480 framing rule; a
  `.githooks/pre-push` is the distribution fork's in miniature, comparing
  `pr/*` against `upstream/master` (the ES upstream's branch) and scanning
  every branch for credential-shaped lines. Proven both ways: a `pr/*` ref
  carrying the pointer is refused naming `CLAUDE.md` and `.githooks/`,
  `upstream/master`'s tip passes. Two mistakes on the way, both blindspot
  26's shape: `core.hooksPath` first named the main checkout's `.githooks`,
  which does not exist there (that checkout sits on a feature branch), so
  the hook would have run nothing and said nothing; and the first draft
  compared against `upstream/next`, which the ES repo does not have. The
  path now names the worktree that always holds `test/qa-integration`, and
  the pointer says to `ls` it.
- **The QA handheld** (D-QA-016 settled): "Once we're done testing the H700
  builds and move on to conflict resolution, we could turn the RG35XX SP
  into our testing-only handheld because I have the RG353, RG351, and the
  Retroid Pocket Nova. If we'd like a more powerful test device, I could get
  a Rockchip-powered one as well." No purchase now; the accounts are #131's
  remaining half.
- **The docs page last** (D-WORKFLOW-014): "With 42, we'll need to tackle
  that before we actually do our PR, but that should be the last thing we
  do, since we'll need to make sure all the updates are in place."

## 09:10 UTC -- the four ES issues: three were built on the 5th and never pressed; the fourth is written

Maintainer: "with the four issues, I think they all make sense to fix now."
Reading first paid: #67 (ES `3e7195eb9`), #68 (ES `2bdb5d0e7`) and #69 (theme
patch `638eedc5b3`) were implemented on 2026-09-05 and have shipped in every
image since; their boxes stayed open because each wanted to be pressed and
reported individually, and nobody had. So the work tonight was pressing.

- **#67** on guest b: ALL survives the OPTIONS-and-back tab switch; ALL and
  NO survive leaving the scraper and reopening it; a fresh guest shows
  today's defaults. Four boxes ticked; left: deselecting two systems (needs
  three platform systems), opening from a game list, SCRAPE NOW using the
  values (needs an account), the H700.
- **#69** on guest d (640x480): Tools icons under Boxart, Logo and Image; a
  NES entry given a thumbnail shows it under Boxart. Three boxes and the VM
  half of the fourth; left: the H700 look, and the patch offered to the
  theme's author -- an outward-facing PR, which waits for the maintainer.
- **#68** on guest b: with a username and no key, RETROACHIEVEMENTS from the
  main menu says RETROACHIEVEMENTS NEEDS YOUR WEB API KEY. ENTER IT UNDER
  RETROACHIEVEMENTS SETTINGS, NEXT TO YOUR PASSWORD; no 401 body. A backup
  taken with a throwaway key and password carries neither. `strings` on the
  built binary finds no `z=...&y=` pair. Left: the two with-key pages (a
  RetroAchievements test account, the maintainer's offer), the H700.
- **#66** split as the maintainer asked, and the cause recorded first: the
  API's own French body is shown verbatim (`getThreadCount` returns
  `getErrorMsg()`), there is no translation layer for API bodies, and the
  wording that blames the account is the API's for `ssuserInfos.php` when
  asked with a bad pair and an account. ES `f33fa23ac`: the body goes to the
  log; a login failure is told apart by one call with the pair alone; the
  screen names the credential and the tab. Pinned by the build running now.

Two things about the VM learned on the way: `systemctl stop emustation`
does not stop the interface on the image (`essway.service` wraps it; the
process stayed up and the settings written under it were read only after a
reboot -- the memory that says "reboot" was right), and the RETROACHIEVEMENTS
main-menu entry is gated on `global.retroachievements`, a username and the
`RetroachievementsMenuitem` setting, all read at start.

## 10:20 UTC -- #66 pressed on `697aa68e29`; two guest starts lost to a full tmpfs

`697aa68e29` (ES `ae257f778`, #66) built for x64 and H700 (`h700-all-
20260913-697aa68e29/`), the staging candidate again. On guest d with a
bogus developer pair and no account, SCRAPE NOW answers SCREENSCRAPER
REJECTED THE DEVELOPER ID OR PASSWORD. CHECK THEM UNDER SCRAPER > OPTIONS --
no French. The wrong-account half wants the real pair on the VM and a test
account; the H700 box stays.

The night's two failed guest starts had one cause, and it was not the one I
first wrote down. `/tmp` is a 31 GB tmpfs. Guest c (the old image, whose
disk `vm-pair up` had deleted under it days ago) still held that disk's
blocks; a raw image is 4.3 GB and each new disk 2 GB; and starting the
runner and the small guest together ran two conversions at once. The tmpfs
filled, `qemu-img convert` wrote what it could -- 0.9 GB, 1.8 GB -- and
both guests booted to the firmware's "select boot device" menu. The vm-*
wipe I blamed at 03:13 was real but not the killer. Freed 15 GB by stopping
guest c, moved the small guest's disk to `/workspace/tmp`, started the two
one after the other: the pair up in 50 s with 2.19 GB disks, guest d the
same. Rule note extended. Also: `systemctl stop emustation` does not stop
the interface on the image (`essway.service` wraps it); a settings change
that the interface reads at start wants a reboot.

## 11:20 UTC -- French on the screen; the QA account arrives; #68's pages pressed

The maintainer asked whether the messages could follow the device's
language, "at least English and French". They can and now do (D-UI-051):
`system.language` keys every `_("")` string to
`locale/lang/<lang>/LC_MESSAGES/emulationstation2.po`, the build merges new
msgids into every language, and until tonight the fork's strings had no
French anywhere. Thirteen entries written for the credential messages of
#66 and #68 (ES `ce107ec99`), by a script that read the msgids from the
sources. On guest d with `system.language=fr_FR` and the bogus pair:
SCREENSCRAPER A REFUSÉ L’IDENTIFIANT OU LE MOT DE PASSE DÉVELOPPEUR.
VÉRIFIEZ-LES DANS SCRAPEUR > OPTIONS. `947072a988` x64, H700 `3ecef54574`
(same tree), the staging candidate.

The maintainer created QA test accounts. The first file was empty -- an
`export` in a terminal reaches no file -- the second carried five names, the
RetroAchievements three with values and the ScreenScraper two without.
`tools/qa-accounts` (fork-only) carried the account into guest b, printing
only the names; the summary page and a game's progress page (the probe ROM
given `<cheevosId>1</cheevosId>`: Sonic the Hedgehog, 0/35) both opened, no
401 in the journal. #68's VM boxes are all ticked. Two things on the way:
`a` in a game list is SEARCH and `s` is GAME OPTIONS on this build's
mapping, the reverse of what the manager's help bar suggested; and OK on an
empty search keyboard applies an empty text filter, which hides every game
until a reboot. A memory notes where the accounts live and that their
values never enter a chat or a log.

## 12:40 UTC -- the public pair, and #66's last three paths on the VM

"We use the jelos/jelos pair that they publicly shared for JELOS." A
developer pair is issued by ScreenScraper, not created, and this one is
public, so it went into `~/.ROCKNIX/qa-accounts` and onto guest d beside
the QA account. Asked with the pair alone, the API lists systems (200,
XML) and refuses the user-info and every game request (403 "identifiants
utilisateurs"): anonymous scraping is not on, at least for that pair. So a
device with a valid pair and no account would have been told a password it
never typed was rejected; ES `5e7245b51` checks the account fields before
the probe and says SCREENSCRAPER NEEDS YOUR ACCOUNT TO SCRAPE. ADD IT UNDER
SCRAPER > ACCOUNTS, English and French; `519f40aa0c` building.

On guest d (`947072a988`): public pair + made-up account -> REJECTED YOUR
USERNAME OR PASSWORD ... ACCOUNTS; bogus pair + real account -> REJECTED THE
DEVELOPER ID OR PASSWORD ... OPTIONS, the API having blamed the user both
times; public pair + real account -> a real scrape of the two unscraped
games ran and finished, no extra request. #66's VM boxes are all ticked;
the H700 remains. `tools/qa-accounts` was rewritten first: its ScreenScraper
write had lost the attribute quotes through two shells, left es_settings.cfg
unparseable, and reported success -- the fail-open shape again; the lines
now travel over ssh's stdin, only the names written are replaced, and the
report reads the file back (proven with a password carrying an ampersand,
after a first proof attempt failed on my own unquoted test file).

## 06:20 UTC — The milestone audit runs on Fable 5.1, with two Facilitator seats beside it

The maintainer asked for a comprehensive code audit with the code-auditor skill and
a second opinion from another model, then made two requirements while it ran: the
audit subagent must itself be Fable 5.1 at xhigh or max effort ("not Opus or a
different model"), and if that cannot be guaranteed the OpenRouter route is the
fallback. The first orchestrator had been launched pinned to Opus; it was stopped in
Phase 2 and its uncommitted folder deleted, and the relaunch is pinned to Fable 5.1
(`model: "fable"`; the session's configured effort is `xhigh`, which the
general-purpose agent inherits). The orchestrator's Phase 0 records the model its own
system prompt names and that the skill and its references are identical to `next`.

Two second opinions ran through the council Facilitator over one self-contained brief
(SKILL.md, the rules, the decisions, the claimed criteria, the complete diffs; checked
to carry no credential values): the Claude seat (`anthropic/claude-fable-5.1`, xhigh,
14.1 min, $4.08) and the GPT seat (`openai/gpt-6-astra`, max, 16.0 min, $2.72 on the
attached OpenAI key). Both passed the identity and effort gates. They agreed, unprompted,
on the zip-era restore being unprotected by the new PPSSPP skip, on the ScreenScraper
no-account early return hiding the pair message, on a timed-out probe blaming the
credentials, on the bucket guard failing open, on the RetroAchievements values passing
through ssh's argv, and on the LINK5 SKIP still counted in "65 PASS". The orchestrator
verifies each seat finding against the code before Phase 4 and folds confirmed ones
into the punch list.

The maintainer's call on the punch list: "We always want to close out all items of
all levels: high, medium, and low, so we'll want to fix all of these" (D-WORKFLOW-015).
Severity orders the work; it never excuses an item.

## 06:35 UTC — The build order after the punch list: H700, then SM8550 for the Pocket Nova

The maintainer: "Once all the punch-list items are complete, we can go ahead and create
the H700 build. If we want to create the build for the Pocket Nova, we can, or we can
wait to test it first on the H700 before proceeding. It looks like the Pocket Nova uses
an SM8550." Checked against upstream release 20260901: the SM8550 image's device list
names the Retroid Pocket Nova. Filed as #150 (D-QA-012), decided as D-QA-023: the punch
list closes at every severity first, H700 builds, SM8550 builds after it (one device
build at a time), and the SM8550 image waits for the H700 round on the RG SP and the
RG35XX SP before it is staged on the Nova. `device-builds.md` gained the Pocket Nova
row; the devices worktree has no SM8550 build root, so that first build is cold.

## 07:27 UTC — #151, the distribution-script items: PL-01, 02, 03, 04, 10, 14 and 15 on feature/sync-bounds

Seven commits, one per punch item, in the order the harness needed them:
PL-10 `cabc506222` (the suite's `--old` reaches cases l and m through one
fail-closed fetch, `src_of`; k's tar and unzip and m's applets are the
image's busybox), PL-01 `f8e476787a` (the avahi override gains the generic
recipe's `rm -rf ${INSTALL}/usr/lib/systemd`, so the fork's unit ships;
`network-base-setup` tells a responder that is already up the name and logs
a not-yet-up one; case m grades the unit in the built image), PL-02
`0f69be38cb` (another unit's generated name, carried by a settings restore,
is re-derived; D-NET-004), PL-04 `56f156ee24` (`cloud_device_id
--if-present` never seeds; the namer keeps the shipped name without an
adapter; D-NET-005), PL-03 `64b27dab23` (the zip restore skips PPSSPP's
assets and cache; D-CLOUD-124), PL-14 `f38b80fbc7` (four fail-open branches
closed: the prune's grep exit 2, `backend features` parsed as yes/no/unknown
with unknown refusing, `grep -qFx --` in both `absent_not_broken`, the
empty-id log), PL-15 `134c9077e1` (D-CLOUD-125 -- the identity prune's limit
for the maintainer to confirm or ask for a manifest; "substring" not
"prefix"; the folder probe bounded). Every new check was seen to FAIL at
`6e09b6fcca` or `53f390b1e9` before it passed.

On the pair, before the fix: a's settings archive restored on b and b
rebooted as `GENERIC-X64-15ca` -- a's name, PL-02 reproduced. The fork's
avahi unit dropped into b's `/storage/.config/system.d` ordered after
`network-base.service`, pulled `avahi-defaults` in through `Requires=`
(it has no `[Install]`, so `enable_service` on it is a no-op and the
Requires= is what runs it) and published the kernel name at 3.10 s. The
finished script run by hand on b renamed it back to `GENERIC-X64-0964` with
"another unit's shipped default (GENERIC-X64-15ca), carried here by a
settings restore", and the running responder republished
`GENERIC-X64-0964.local`. Both guests were then returned to the shipped
state (snapshot back, unit and archive removed, reboot).

Suite: the tree runs 128 PASS and `1 CHECK(S) FAILED` -- case m grading the
avahi unit in the x64 build root, which predates the recipe fix and holds the
stock unit; the header says so, and it passes once avahi and the image are
rebuilt. `BASE_REF=53f390b1e9 tools/last-good-scripts-test --old`:
`36 CHECK(S) FAILED` (k 6, l 13, m 17; the audit's run had 4, all in k).
Default base: `73 CHECK(S) FAILED` across a-f, j, k, l, m. `pkgcheck avahi`
clean; `vocabulary-check` 0 wrong; `register-check` 239 IDs.

Still owed elsewhere: PL-01's acceptance on a rebuilt x64 image (After= on
every pair guest, ten boots publishing the kernel name) and part 3, the
vm-qa mDNS assertion, in the other stream; PL-02's boot-time proof on that
image; PL-04's two fresh H700 flashes. One boundary for the maintainer:
D-NET-004 re-derives only this family's shape, so a cross-family restore
(an RK3566's settings on an H700) carries `RK3566-xxxx` and keeps it, as
the bare `RK3566` is kept -- flagged, not decided.

Lessons: a check nobody has seen fail is a check with no evidence -- l's
eleven and m's nine had never once failed because `--old` never reached
them; `journalctl -u X -t Y` ANDs across fields and prints nothing, so
query one unit at a time; a suite FAIL that is true of the image on this
machine is kept and explained, not softened.

## 07:20 UTC — #151 tools and records: PL-01 part 3, PL-08, PL-09, PL-11, PL-12, PL-16, PL-18 (Fable 5.1)

Five commits on `next`, `4b219ef937..1ee141207f`, one per item, from `feature/audit-151-tools`;
the push was the first under the new hook, which scanned all five commits' added lines and
passed. Learned along the way: (1) `register-check` refused its own header the moment the two
example IDs were spelled out -- a tool that reads `tools/` is its own input, so examples in its
comments are placeholders; (2) a suite that returns at its first failure hides the second --
the PL-02 stream rebooted guest b with guest a's settings mid-session, so the live
pair-identity run reported the shared hostname and never reached the mDNS check; it now prints
every FAIL, and the mDNS assertion was proven on the recorded 06:5x lost-race state through a
fake `ssh`; (3) `avahi-defaults.service` has never run on any guest (no `[Install]`, so
`enable_service` made no symlink; the stock daemon unit does not `Requires=` it) --
`avahi.conf` and `avahi.disabled` exist nowhere, which the research notes had read from the
source, not the guest; (4) the hook's header must record the synthetic secret as
`devpassword=<sixteen letters>`, since the hook reads its own added lines. Filed #152 (French
follow-up); #42 carries five more surfaces; #50 box 6 unticked to the shipped state; #113
boxes 1 and 4 point at the row that now holds 303 s. Owed: the D-NET-006 row (landed after
the scripts stream released the register; PL-18 ticked then); the PL-06 comment in
`qa-accounts` is neutral, not fixed; the live mDNS FAIL on guest b awaits its next lost race.

## 07:55 UTC — The image after the punch list: d32f47a947 built, and the suite that graded the wrong tree

The three fix streams landed (`32efdc093b`, `1ee141207f`, ES `51edcf728c` pinned by
`c694d733c6`), D-NET-006 followed (`d32f47a947`), and GENERIC_X64 rebuilt from that
`next` in three minutes -- fast enough to check the artifact rather than the report:
the built EmulationStation carries the six new strings, the built
`avahi-daemon.service` has `After=network.target network-base.service
avahi-defaults.service`, the built `backuptool` has the zip exclusions. Then
`tools/last-good-scripts-test`, run from the primary checkout, failed four checks in
case k and graded the avahi unit from the source "(no build root on this machine)".
The build root was there; the suite looked for it beside the repo (`${ROOT}/../*/`)
and inside it, never under `rocknix.worktrees/`, so from the primary it fell back to
Ubuntu's busybox, whose `tar` has no `-T`/`-X`, and backuptool failed for reasons that
had nothing to do with backuptool. The scripts stream had run the same suite from its
worktree, where `../*/` does reach the build roots, and seen 128 PASS. Fixed in
`401a3a047c`: the worktrees layout is in the glob; from the primary the run is
`130 PASS` with case m grading "the built image build.ROCKNIX-GENERIC_X64.x86_64".
The lesson is the one blindspot 34 already states, seen from the other side: a check
that cannot find the artifact and says so is honest, but it still printed PASS, and a
reader skimming for FAIL would have taken the source's word for the image's.

On the pair brought up from the new image (runner `qa-d32f47a947-webdav-a`): both
guests name themselves at ~2.84 s, log "mDNS responder not up yet; it publishes
<name>.local when it starts", and avahi comes up at ~3.95 s ordered after
`network-base.service` and `avahi-defaults.service`, publishing the kernel name;
`/storage/.cache/services/avahi.conf` now exists on both. scripts, lifetime,
vocabulary, register (new) and pair-identity (with the mDNS assertion) PASS; the rest
of the runner is in flight. #47 reopened (closed with four boxes open). #151 ticks so
far: PL-08, -09, -10, -11, -12, -14, -15, -16, -18 and the register-rows box.

## 08:15 UTC — PL-13 (#151): LINK5 run on MinIO; a throttled S3 endpoint (Fable 5.1)

`cloud-test-backend` now honours `CLOUD_QA_BWLIMIT` for `--backend s3` by moving MinIO
to loopback 9022 and fronting 9012 with an inline asyncio token-bucket proxy (rclone's
shape: 4 MiB burst, per-direction buckets; the host's rclone bypasses it).
`backend_throttled()` reads `<state>/<backend>.pid` for webdav and s3. LINK5 against
MinIO (guest d, 519f40aa0c) at 40 s and 120 s outages: every receiving-side check
passed -- no partial, whole by content, re-run accepted with no lock, matching after --
and the cut PUT's 8.4 of 12 MiB was discarded by the atomic PUT, so the WebDAV SKIPs
now cite the run. What the run found instead: the deliberate `cloud_backup
--system-only` is not bounded on s3 -- the S3 backend spends `--low-level-retries 10`
re-dialling through the AWS SDK's backoff, which no timeout covers, and completed with
exit 0 at 88.8 s (40 s cut) and 163.6 s (120 s cut), stamping success (D-CLOUD-075 vs
D-CLOUD-083 on s3; the automatic path's busybox `timeout` ceiling is unaffected). Its
third consequence: a successful `--system-only` run ends on `Settings backup:
COMPLETED`, which the LINK cells' outcome-line pattern does not accept -- never seen
before because a cut run had never succeeded. Not fixed in the PL-13 change; filed as
its own issue for the maintainer's call. Commit `96284d5544`; s3 restored unthrottled;
guest d clean; `generic-x64-vm-testing.md`'s "only WebDAV can be throttled" corrected.

## 08:25 UTC — The H700 build that started too early, and the rule that stops the next one

With the x64 runner green on `d32f47a947` and only the VM proofs of PL-01/02/03 and the
640x480 frames outstanding, an H700 build was started from `96284d5544` so the maintainer
could try it sooner. The maintainer, ten minutes later: "no builds should start for any
device until all of the punch-list items have been completed and tested through our VM
process first. I don't want to rush out any builds." D-WORKFLOW-016. The build was
stopped at package 540 of 663 (`docker kill`), the H700 roots swept for packages left
mid-flight per `device-builds.md`, and nothing from it is kept. Code-complete is not
punch-list-complete; the VM proofs are part of the item, not a formality after it.

## 08:40 UTC — PL-01, PL-02, PL-03 proven on the pair (Fable 5.1)

Twenty guest-boots of `d32f47a947`: every one published the kernel hostname over mDNS
with avahi ordered after `network-base.service`, reached through the ordered path each
time ("mDNS responder not up yet; it publishes <name>.local when it starts" at ~1.8 s,
`Host name is` at ~3.0 s), never the fallback re-publish; pair-identity PASS four times.
Guest a's archive restored on b: b rebooted as its own `GENERIC-X64-0964`, logging
"another unit's shipped default (GENERIC-X64-15ca), carried here by a settings restore";
`Max Deck` survives as `Max-Deck` through a reboot and through its own restore. A legacy
`ROCKNIX_BACKUP.zip` restored on a left `assets/lang/en_US.ini` and the CACHE listing at
their md5s and brought `ppsspp.ini` in; the exclusions are silent by construction (the
KEEPING line names only live symlinks), so the hashes are the evidence. #151 PL-01/02/03,
#45 box 4 (both archive kinds) and #50 box 6 ticked. Found on the way and filed as #154:
`set_setting system.hostname default` deletes the line `chksysconfig` treats as the
file's sentinel (deliberate since #102), so the old name comes back as a "damaged"
recovery. Guest b now carries a's other settings; a holds its archive and a PRE_RESTORE
snapshot.

## 08:50 UTC — #151 PL-05/06/07/17 frames on `d32f47a947` at 640x480 (guest d)

- The restore page fits 640x480 whole with every row at two lines; the empty
  circle on WI-FI PASSWORD needs the guest to have **no address**, and
  `ApiSystem::getIpAddress` says so only when no interface has one -- so the
  link is cut with `set_link net0 off` *before* the reboot and the page is
  framed over the serial console (`tools/vm-serial` still works with the link
  down). Twelve frames in `docs/qa-frames/2026-09-13/`.
- EmulationStation on GENERIC_X64 runs under **`essway.service`**
  (`ExecStart=/usr/bin/start_es.sh`), not `emustation.service`; a drop-in on
  the latter changes nothing. `--debug` went in through a drop-in on
  `essway.service` whose `ExecStart` is a copy of `start_es.sh` (sourcing
  `/usr/bin/es_settings`) plus the flag; `Debug` in `es_settings.cfg` is in
  `settings_dont_save` and is not loaded either.
- `es_log.txt` is written through a buffered ofstream flushed every N
  batches; an INFO line can sit unflushed for minutes. ERROR lines (and every
  line under `--debug`) also go to stderr, so `journalctl -b -o cat` is the
  live source.
- The scraper's API host is **`api.screenscraper.fr`** (`API_URL_BASE` in
  `ScreenScraper.h`), not `www.screenscraper.fr`; a black-holed route to the
  wrong host let a 403 through and cost one run. Both scraper requests ride
  one HTTP/2 connection (`curl -v`: "Connection #0 ... left intact",
  re-used), so no route change between them can fail the probe alone:
  COULDN'T REACH SCREENSCRAPER is not producible on the VM without a CONNECT
  proxy that cuts the tunnel after the first exchange.
- One `set_link net0 off` was accepted by the monitor and took effect ~30 s
  later, after the press it was meant to precede; the run read as a 403 with
  "the link down". Verify `cat /sys/class/net/eth0/carrier` over serial
  before every offline press, not the monitor's echo.
- Found on the way: the NETWORK SETTINGS row for FINISH RESTORE PROCESS
  (`GuiMenu.cpp:9041`) is three lines at 640x480, and eight of the page's
  strings have no French msgstr -- filed on the fork.

## 09:10 UTC — #155 fixed in EmulationStation; PL-17 equal at 1280x800; the Phase 7 table re-derived

The frames stream's finding (#155): the NETWORK SETTINGS row that opens FINISH RESTORE
PROCESS ran to three lines at 640x480, and eight strings on the page had no French. ES
`de9d022b9` gives the row the one-line string its sibling already carried (measured
731/548 px against budgets of 780/620 with the stream's glyph-advance measurer, which
reproduces the earlier stream's figures); `8c8be81ba` adds eighteen French entries for
the page (msgfmt 1299 translated), each description measured under both budgets, the
device-password label shortened to keep SERVEUR DE FICHIERS whole under the CET APPAREIL
heading. Pinned by `f0ab059596`; GENERIC_X64 rebuilding from it for the runner and the
two #155 frames. PL-17's other half: on guest a at 1280x800 the manager logs
`help row 0.164167 of a 440 px sheet` for both the constructor and onSizeChanged -- the
same share as the 264 px sheet at 640x480 (`819c33704a`, a two-tile frame because guest
a has no save states and none were seeded onto a cloud-sync fixture). The audit's Phase 7
table now carries re-derived outcomes (`d30f01d7df`): 16 Resolved, PL-07 and PL-17
Resolved in code and Deferred to #156 for the frames the VM cannot produce without an
HTTPS shim; `lint-audit-artifacts --issue 151` PASS. Whether the H700 build waits for
#156 is the maintainer's call; D-WORKFLOW-016 holds it until #155 is framed either way.

## 10:00 UTC — The punch-list image: 0f4e4829a8 through nine suites; the msgfmt check that did not run

`f0ab059596` (ES `8c8be81ba3`, #155) passed all nine suites and its two frames landed
(`953cb7d3b4`): the NETWORK SETTINGS restore row is one line at 640x480 in both
languages and the FINISH RESTORE PROCESS page reads French end to end -- except the
RESTORE group heading above the row and CHECK CONNECTION's CHECKING..., both fork
strings with no msgstr. Added them (RESTAURER, VÉRIFICATION…, and BACK UP → SAUVEGARDER
which was missing too) in ES `a69327c63d` -- and pushed that commit with its `msgfmt -c`
never having run: the host has no `msgfmt`, the command failed, and `| tail -1` handed
the pipeline a zero. The shape `engineering-practices.md` names, committed by the
orchestrator an hour after re-reading the rule. `3d9394660c` re-validated with the
toolchain's msgfmt (`.../toolchain/bin/msgfmt`, 1302 translated) and took the U+2026
ellipsis every other msgstr uses. Pinned `0f4e4829a8`; GENERIC_X64 rebuilt (the
superseded `c81efb572e` build was allowed to finish rather than killed, so nothing was
left mid-flight); nine suites PASS (`qa-0f4e4829a8-webdav-a-20260913-0935`); guest d
frames `1d67ccdfb3`: RESTAURER over FINIR LA RESTAURATION; VÉRIFIER LA CONNEXION on a
guest with no remote opens the set-up offer before any CHECKING... could show, and that
offer has no French (#152). #151: 16 Resolved, PL-07/PL-17 Resolved in code and
Deferred to #156 for the frames the VM cannot produce; lint PASS. D-WORKFLOW-016 holds
the H700 build for the maintainer's word on whether those two halves wait for #156.

## 11:20 UTC — H700 `0b1a1d4db0` built from the punch-list tree; staged on the RG35XX SP

The maintainer: "Let's do the 700 build now I'll have both devices online and you can
transfer the images and reboot" (D-WORKFLOW-017). Devices worktree synced to
`0b1a1d4db0` (the punch-list image `0f4e4829a8`'s tree plus docs), `build-dev.sh H700`
OK in six minutes on the warm root (the `systemd` package had been cleared after the
killed early build). Artifacts under `/workspace/artifacts/rocknix-images/h700-all-
20260913-0b1a1d4db0/` (DDR3 and DDR4 images, the shared update tar, checksums verified;
tar sha256 `21c0f644…`). Runbook step 3 on the tar's SYSTEM (`unsquashfs -cat`):
`BUILD_ID=0b1a1d4db0…`, `HW_DEVICE=H700`, the new EmulationStation strings present,
`avahi-daemon.service` `After=network.target network-base.service avahi-defaults.service`;
both H700 bootloader overlays and the SP device trees listed. RG35XX SP (`rg35xxsp`,
192.168.1.81, on `af2db4ab09`): model Anbernic RG35XX SP, vdd-dram 1.10 V, 176 GB free,
empty update queue, cloud lock free; the tar uploaded to `/storage/.cache/…tar.part`,
its SHA-256 verified on the device, moved into `/storage/.update/`, synced -- **staged**,
not applied. The reboot is asked for by name (D-QA-015). The RG SP (`rgsp`,
192.168.1.175) is not on the network yet. `pgrep -c` does not exist on the device's
busybox; the idle check counts with `pgrep -f … | wc -l`.

## 11:25 UTC — RG35XX SP on 0b1a1d4db0; the RG SP staging

Reboot asked for by name and given; the RG35XX SP came back in under six minutes on
`BUILD_ID=0b1a1d4db0`, model and 1.10 V DRAM unchanged, update queue empty, mounts and
176.7 GB free, the new EmulationStation strings in `/usr/bin/emulationstation`, avahi
ordered after `network-base.service`. Its boot log shows the D-NET-002 path on real
hardware: the typed name `rg35xxsp` is kept, `network-base-setup` logs "mDNS responder
not up yet; it publishes rg35xxsp.local when it starts" at 14.1 s and avahi publishes
exactly that at 15.7 s (the ordering the VM proved twenty times, now once on a handheld;
#50's LAN boxes still want a second name on the router and `.local` from a laptop). The
RG SP appeared at 192.168.1.177 (its ssh-config address is .175 -- DHCP moved it); model
Anbernic RG-SP, 1.20 V DRAM (the DDR3 unit), 27.4 GB free, idle; the shared update tar
is uploading to its cache with the same checksum-then-move staging.

## 11:45 UTC — Both H700 handhelds on 0b1a1d4db0; the SM8550 build begins

RG SP: reboot asked for by name and given; the updater's two boots took under four
minutes; `BUILD_ID=0b1a1d4db0`, model Anbernic RG-SP, 1.20 V DRAM, queue empty, mounts
and 27.4 GB free, the new EmulationStation strings, `essway.service` active after a
first read during startup had it inactive (verify after the second boot settles, not
during it), avahi ordered after `network-base.service` and publishing `RG-SP.local` at
15.5 s after the same "responder not up yet" line at 13.8 s. Two handhelds, two boot
logs, the same ordered path the VM proved twenty times. The RG35XX SP went off the
network at about 11:35 UTC, after its verification was complete. With H700 built,
copied out and on both devices, `build-dev.sh SM8550` started at 11:37 UTC from
`6e1de9e09f` (D-QA-023): a cold root, hours; the Retroid Pocket Nova is staged only after
this round's device halves, and only after the maintainer says how the Nova takes an
image (an in-place update of a ROCKNIX it already runs, or a first install).

## 12:05 UTC — The maintainer's first note from the H700 round: the sync card's second 113

On both handhelds the startup sync showed "113 of 113", a second count that went by
too fast, "113 of 113" again, then Complete -- "it gives no indication that it is
working correctly." Read against `ThreadedCloudSync.cpp`: a sync is two rclone runs
(restore, then backup), each with a compare phase that the card names COMPARING SAVES ·
N OF M and deliberately leaves the bar alone (a comparison is not a transfer), so the
still bar and the repeated count are the two runs' compares back to back, with the
restore's near-empty byte line between them. Filed as #157 with the maintainer's two
options and a third (name the half in the line); parked as D-UI-052 with a
recommendation (phase-aware bar plus the half named; a spinner only if a slow compare
still reads as stalled). Not started: the maintainer chooses the shape first.

## 12:15 UTC — Two notes from the maintainer: captive portals, and how the Nova takes an image

"Take our embedded browser from rclone config and use it when trying to connect to
captive WiFi networks" -- filed as #158 (D-NET-007): the embedded browser is
`cloud-signin-window`, the fullscreen WebKit view `cloud_oauth` launches for on-device
sign-in (not a browser: no address bar, no tabs, navigation refused outside the provider);
a captive mode detects the portal through NetworkManager's connectivity check, offers the
page in the player's words, follows only the portal's own redirects, and closes when the
check turns full. The VM proves it against a simulated portal first. Future scope, no
milestone. And: "The pocket nova is also currently running Rocknix, but it is a fresh
install" -- so the SM8550 update tar applies in place through `/storage/.update` like the
H700s' did, with the runbook's checksum-then-move staging and a per-device yes for the
reboot (#150).

## 12:25 UTC — Future item: remember and forget wireless networks (#159, D-NET-008)

"Another future item is the ability to remember and forget wireless networks." Today the
interface knows one network (`wifi.ssid`/`wifi.key`) and `wifictl connect` deletes the
previous NetworkManager profile before creating the next, so joining is forgetting. The
task keeps profiles, marks remembered networks in the scan list, adds FORGET with a
two-line confirmation, and has FINISH RESTORE PROCESS ask for every remembered network's
key while the archive still carries none (D-INFRA-010); the current pair stays so an
upgraded device is asked nothing. VM-provable but for real radios. Future scope, no
milestone.

## 12:40 UTC — D-UI-052 settled: the phase-aware bar first; the exit sync as the model

The maintainer chose #157's option 1 ("the least intrusive and the right place to
start") and added the observation that the game-exit sync "has more steps between
exiting the game and having the save sync complete" -- more points where the status
updates, not more work -- and should be the model for the startup card. The startup
command (`main.cpp`) runs `cloud_restore` then `cloud_backup` and prints its
`>>> tier` line only after each half, with the exit code, so the card learns which half
it was in once that half is over; the exit sync is a single `cloud_backup --recent`
whose own `>>> doing` lines drive the card as it goes. Implementation handed to a
Fable 5.1 stream: the half announced before it starts, the bar split 0-50 / 50-100 with
compares parked at the half's start, the half named in the line, and the startup path
made to carry the same status points the exit path already shows.

## 13:50 UTC — #157 implemented; two more calls from the maintainer; a guard that never fired

ES `3a121f0152` (pinned `1e5a2818e8`, GENERIC_X64 rebuilt in nine minutes beside the
SM8550 build): the startup command echoes `>>> doing receive` / `>>> doing send` before
each half; `CloudText::phaseBar` maps a run's percentage into its half (0-50 receiving,
50-100 sending), a compare parks at the half's start and the bar never moves backwards;
the half is named in the line; `Checks: 0 / 0` is no longer shown; ten card strings gain
French (the card had none). The stream's investigation corrected two things the brief
assumed: `cloud_backup` and `cloud_restore` print no `>>> doing` lines at all (only
`cloud_net_ready` does), and the exit sync has fewer status points than startup, not
more -- what it has is distinct words at each step, which the half names now give the
startup card. `rclone-cloud-sync.md` gained the stats-block section. The maintainer chose
option 1 for #153 (the deliberate path's ceiling; the summary line may change) and #154
(the empty `system.hostname=` line): D-CLOUD-126, D-NET-009, in a Fable 5.1 stream now.

A guard of mine never fired: the "no build container up" check before `fork-worktree
sync` grepped `docker ps` for `rocknix-build`, but the build containers carry random
names (`jolly_turing`), so it always answered zero. No sync ran during a build today --
each sync preceded its build -- but the check proved nothing. Use
`pgrep -f 'make docker-[A-Z]'` (a pattern that cannot match its own shell) instead; the
x64 build for `1e5a2818e8` fast-forwarded only its own worktree by hand because the
SM8550 build was in flight in the devices worktree.

## 14:15 UTC — The maintainer's RetroAchievements notes from the H700 round

Four observations from play on an H700 clamshell, each now an issue quoting them: an
achievement unlocked during play was lost after a Wi-Fi drop and a game exit (#162);
the '!RA!' badge bottom-right (#162 -- RetroArch's disconnected indicator,
`rc_client_update_disconnect_state`: visible exactly while an award or leaderboard
retry is scheduled; `rc_client_unload_game` keeps the retry but ROCKNIX's one-process-
per-game RetroArch exits with it, so nothing persists the award); the Wi-Fi drop on a
phone hotspot (#161: association lost vs path lost -- to be read from a journal when a
device is up; both handhelds went unreachable minutes after being put down today,
which looks like idle power-down, a separate question); and the game page's completion
bar drawn over the header lines at 640x480 (#160: `GuiGameAchievements::render` places
the bar at 0.55 of the width by fraction, not measurement -- blindspot 41's shape). And
a milestone idea: RAOfflineProxy (GPL-3, approved by RetroAchievements.org, lists
ROCKNIX, casual-only) as the cure for the lost award -- epic #163, D-RA-001 open
(system-wide vs per emulator; hardcore; fork-first vs upstream-first).

## 14:40 UTC — Milestone "Offline RetroAchievements" (#4), D-RA-001 settled, worktree feature/ra-offline

The maintainer: a toggle for the proxy, explained as beta and casual-only, "would still add
tremendous value"; contribute back if we can; "the right long-term solution"; a milestone,
worked in parallel with conflict resolution in its own worktree; the QA RetroAchievements
account and the VMs make it testable. Milestone 4 carries the acceptance test (cached game,
no network, unlock, exit, reconnect, award recorded and said so; hardcore unchanged and
says so; the VM scenario gates the handheld). Epic #163 has five phase sub-issues; #162
rides the milestone too. What the project's Linux side actually is (read from
`linux/README.md`): a stdlib-only Python proxy that patches `retroarch.cfg`
(`cheevos_custom_host`, and forces hardcore off while active), caches game data, queues
casual awards and flushes them on reconnect; `linux/rocknix/` holds a bundle builder and
scripts; RetroArch and PPSSPP on ROCKNIX. So per emulator by construction -- D-RA-001's
"system-wide" means the toggle covers every supported emulator. D-RA-002 open: the
hardcore stance, the token cache vs D-INFRA-010, RetroArch alone or with PPSSPP. Worktree
`/workspace/repos/rocknix.worktrees/ra-offline` on `feature/ra-offline` from `next`.
The maintainer is travelling; the handhelds will come back over Tailscale for #161's
journal and the device rounds.
## 14:45 UTC — #153: the deliberate run's ceiling is a stall ceiling (D-CLOUD-126, D-CLOUD-127)

Option 1 said "the same busybox `timeout` ceiling the automatic path has, sized from
its bound". Two facts made the literal reading wrong before anything was written. The
image's `timeout` is coreutils' (busybox ships no such applet -- checked on the x64
root), and a fixed ceiling from the call's start, however sized, ends the transfers a
deliberate run exists for: the automatic run is small by construction, the deliberate
one is a first save-state backup or a theme pack. The LINK5 capture in the scratchpad
supplied the alternative: rclone's transferred count never falls on the S3 retry
(8.902 MiB flat for 45 s while the total grew 12.0 to 20.9 MiB), so "a count above its
previous high" is an unambiguous progress signal and the ceiling can be measured from
the last progress. Sized as `--timeout` + 6 s = 36 s: a healthy link's retry shows
progress within the grace; a dead link's run ends before a 40 s outage is over, exit 69
like WebDAV. rclone's stdout goes through a fifo and `tee` to a trace so the page sees
the same bytes; `child_running` reads `/proc` because `kill -0` says yes of an unreaped
child. Verified by hand against the real capture under both awks, then
`tools/last-good-scripts-test`: case h moved onto `src_of` (it read the working tree
whatever `--old` said -- PL-10's gap, still open there) and gained the deliberate stall
and the deliberate why; case n runs `cloud_backup --system-only` whole in bwrap against
a shim that hangs, completes and crawls. New suite PASSED; `BASE_REF=next --old` fails
exactly the ten new checks. `-P` alone refreshes every 500 ms, so the wrapper appends
`--progress` where absent and never `--stats 1s` (that would halve the card's signal).
The summary's last line is now a sentence (`Completed.` / `Couldn't finish: <why>. Try
again.` / `Skipped: <reason>.`); ES reads no prose line for the outcome, the
round-trip's `Settings backup:` readers filter by prefix and are unaffected. Noted for
the orchestrator, not fixed: a `>>> why` sentence is shown raw and uppercased by ES, and
the French catalogue carries none of the why sentences, not even the rc-keyed ones.

## 15:05 UTC — #154: system.hostname's default is an empty line, not an absent one (D-NET-009)

Option 1, as the row says. The one-rename awk write that `set_setting` carried since
KILL10/11 moved into `write_setting_line`, so `del_setting` can use it: for
`system.hostname` alone, a delete (or `set_setting ... default`) writes
`system.hostname=` instead of removing the line, and every other key is removed with
the same `sed -i` as before. `chksysconfig`'s sentinel is untouched, and
`network-base-setup` already read an empty name as the shipped default, so the next
boot names the unit. `tools/last-good-scripts-test` case o drives the real functions
from the real files: the empty line left, another key removed, no temporary, then
chksysconfig's `valid` and `backup` accepting the file (the old code refused it with
"not backing up system.cfg ... damaged", the exact line from guest b), then case m's
network-base-setup sandbox renaming `H700-9a3f`. PASSED on the tree; `BASE_REF=next
--old` fails five of its seven checks (the two that hold on the old code are the
no-temporary check and the namer, which re-derives from an empty read as well as from
an absent line). Nothing here runs under busybox sh: 001-functions is bash's, and the
image's /bin/sh is bash.

## 14:46 UTC — RA offline phase 1: RAOfflineProxy's ROCKNIX side read, design note written (#164)

Read `misantronic/RAOfflineProxy` at `64d03d30633ca6e7719c26d732cef3c97dedcc27` (main,
2026-09-12; 19 commits past `v1.13.0-alpha1`, none touching what ROCKNIX runs except a muOS
revert fix) and wrote `docs/ra-offline/2026_09_13-phase-1-design-note.md`. Three things that
change the shape of phase 2. The proxy's config patching is mostly moot on ROCKNIX:
`setsettings.sh` rebuilds RetroArch's cheevos keys from `system.cfg` at every launch through
the appendconfig, which is why upstream patches `system.cfg` (`global.retroachievements.hardcore=0`)
as well as `retroarch.cfg`; and the fork already has the native mechanism -- `runemu.sh:281`
appends `cheevos_custom_host` to that appendconfig for lowerdeck on dual-screen devices -- so
the native design is that line behind a setting, and nothing persistent is written, nothing
travels in a settings backup, nothing needs reverting. The token cache
(`/storage/.config/raofflineproxy/proxy.sqlite3`: the `login2::` row, `auth::invalid_token`,
and `t=` in every queued award body, plus the HMAC key) is outside `backuptool`'s default
allowlist already; propose holding it back outright for a custom `LOCATIONS`, like
`rclone.conf`. And the hardcore switch upstream is silent (the SDL menu has no such string)
and undone by a crash; propose mutual exclusion the player chooses on both rows, the unit
refusing to start in hardcore, `setsettings.sh` honouring the per-game effective value.
Findings for upstream: "re-enforces periodically" has no implementation; `MAX_RETRIES` gates
nothing; a flush runs only on a link transition; the storage-corruption log upload to the
developer's AWS endpoint is automatic and ungated (moot on ROCKNIX, which has SQLite, but
shipped); usernames are not redacted in uploaded logs. Fork finding, unrelated to the proxy:
`ppsspp_retroachievements.dat` holds the raw RA token and travels in every settings backup
(`backuptool` archives `/storage/.config/ppsspp/*` and strips only four named files) -- filed.
Python on the image: 3.14.7, `.pyc`-only stdlib, `_sqlite3`/`_ssl`/`_hmac`/`_ctypes` present,
so the package must `python_compile` too; the service is stdlib-only, the bundle's pygame and
zig-built hasher are for the menu and Add ROM, which v1 does not ship.
