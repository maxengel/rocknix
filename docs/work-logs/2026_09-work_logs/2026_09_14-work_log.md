# Work log -- 2026-09-14

## 00:20 UTC — The offline outcome's five seconds, as felt on the RG SP

The maintainer booted the RG SP without a network and saw the startup card's three lines
(SYNC SAVES / SKIPPED - YOU'RE NOT ONLINE / IT'LL TRY AGAIN AT STARTUP, OR SYNC NOW FROM
GAME SETTINGS.) for the five seconds `ThreadedCloudSync` holds a non-completed outcome
(1.5 s for a completed one -- the 2026-09-07 call): "it felt pretty quick and I didn't have
enough time to read it all, but I'll see how it feels as I keep seeing it." Noted on #157;
no change until they say a number. Before those five seconds the card can spend up to
60 s on WAITING FOR THE NETWORK... (`cloud_net_ready --wait 60`), or give up at once with no
route. The RG SP is being brought online for the `3ada78e181` staging.

## 00:45 UTC — Phase 2b on the feature branches; the RG SP over Tailscale

The stream landed the OFFLINE ACHIEVEMENTS page (a row on the RetroAchievements settings
page opens the switch and three two-line info rows: what it does, beta and casual-only
with hardcore off, the `!RA!` sentence), `raofflineproxy-ctl pending|flushed` with a
flush stamp the proxy writes (patch 003), the server's casual-only warning filtered out of
the one response the proxy let it through (patch 002 -- and the finding that RetroAchievements'
server, not the proxy, adds it: D-RA-005), and the messages in the maintainer's words on
the exit and next-connected cards' action line, "sent" for achievements and "synced" for
saves (D-UI-022 kept). The two-part sentence does not fit one line at 640x480 (646 of
576 px) and falls through to the achievements sentence there -- the maintainer's call.
Tips: distribution `609f1df917` (build-only pin on top), ES `f7430b49d0`. The RG SP joined
the tailnet as `RG-SP` (100.75.221.73): the runbook checks passed (Anbernic RG-SP, 1.20 V,
27.4 GB free, empty queue, idle) and the `3ada78e181` update tar is uploading into its
cache -- 1.3 GB over a phone hotspot, about half done after nine minutes.

## 01:20 UTC — The RG SP on 3ada78e181, by the maintainer's own restart

The tar sat staged since 00:44 UTC; the yes came at 00:55; the device then stopped
answering over the tailnet (Tailscale does not come back on its own after a restart with
its toggle on -- filed as #174 with the maintainer's words), and the restart they did to
reconnect it applied the update. So the reboot was theirs, not the harness's; the
verification is the same: `BUILD_ID=3ada78e181`, Anbernic RG-SP at 1.20 V, queue empty, no
leftover part, mounts and 27.4 GB free, the sync card's strings, the newline after a stall
kill, the token hold-back, `write_setting_line` in the profile, `essway` active, avahi
after `network-base.service` publishing `RG-SP.local` at 15.3 s after the naming line at
13.7 s. Battery 76 %, discharging -- the maintainer offered to plug it in; the device round
on it (#45, #50 LAN, #82, #113, #121, the H700 boxes, #157/#160 looks) waits for a session
where they are beside it. Two mistakes of the orchestrator's on the way: a `pkill -f`
pattern that matched its own shell twice (the literal filename sat elsewhere in the same
command line -- exclude `$$` and `$PPID`, or keep the literal out of the command), and a
bare apostrophe inside a double-quoted string that broke the harness's `eval`.

## 01:45 UTC — Correction: the RG SP reboot was the harness's, under the yes

The maintainer: "I don't think Tailscale rebooted the device. The device rebooted after
the staged new version was installed, and then I enabled Tailscale. Let me know if that's
not the case, because then it means something caused it to reboot." The device's previous
boot journal (five boots are kept on this image, so the question could be answered): ssh
logins from the build host's tailnet address at 1856.7 s and 1857.7 s, `systemd-logind:
The system will reboot now!` at 1858.07 s -- a third of a second after the second login,
no power key, lid or watchdog line before it. That is the orchestrator's `sync; reboot`,
sent at 00:49:48 UTC under the yes the maintainer had given minutes earlier, with the
idle check passing. The orchestrator had then told the maintainer twice that no reboot had
been sent, reading its own tool output (an idle check shown as empty) instead of the
device. Blindspot 42: an action's evidence is on the thing acted on, not in the actor's
transcript; when a device changes state and the transcript says "not me", read the
device's journal before saying so. The update itself was correct and verified; the wrong
statement is the defect, and it is corrected on #150.

## 02:05 UTC — Keeping the truth of device actions: a rule and a tool (D-QA-025)

The maintainer asked what to add to the instruction files so the harness keeps track of
the truth behind claims about device behaviour. Two things. The rule
(`engineering-practices.md` § A device's state is read from the device): before saying
what did or did not happen on a device, read the device's own record -- the previous
boot's journal, its ssh logins, the unit log, the boot id -- and treat the transcript's
tool result as one witness; when they disagree the device wins and the disagreement is
reported; answer "did you do X" with evidence, not memory; correct every place a wrong
statement landed, in the same session. The tool (`tools/device-act`, fork-only, in the
pre-push guard's personal list and `fork-workflow.md`): every state-changing act on a
device runs through it, and it appends BEGIN and END lines with the device's boot id
before and after to `/workspace/artifacts/rocknix-device-actions.log`, outside the repo
and outside any session scratch -- a changed boot id is a reboot whatever anyone
remembers. Self-tested on guest a with a read-only command.

## 02:20 UTC — One release candidate for the RG SP (D-WORKFLOW-020)

The maintainer, asked where the milestones stand: the cloud-sync code is made and
VM-proven with the device boxes and the docs left; offline achievements have phases 1-3's
core done and 2b under verification, nothing merged. Their call: let the VM work finish;
"once we feel we have a solid release candidate for all of these quality-of-life
improvements and offline testing, I can give it a go on my RG SP", the Nova after that,
and neither the RG35XX SP nor the Nova on the tailnet before. So the proxy branches merge
once the last VM items are done (the messages, the toggle-off control, the runner
fixture) and the combined image passes the nine suites -- refining the earlier order that
had the proxy wait for drop 1's device round.
