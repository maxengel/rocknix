# Work log — 2026-09-15

## 00:40 UTC — #190: RETROACHIEVEMENTS hangs on PLEASE WAIT with Wi-Fi off (RG SP, RC-7); RC-9's first half

The maintainer, mid-test on the RG SP with Wi-Fi off: *"trying to launch Retro Achievements from the
main menu just gets stuck on 'Please Wait' while I'm offline."* -- *"It also isn't timing out."* Two
causes fit and the device's log will name it: the interface decided "online" from the proxy's state
file (rewritten only when the proxy's next probe fails, after the link drops) and asked the web with
no total timeout; and the device path over 247 saved games is 494 requests to a proxy that, while it
still believes it is online, tries upstream first with a 15 s timeout each -- hours on a dead link.
PL-09's VM proof had waited for the state file to say offline before opening the page (blindspot 44
again: the fixture's timing, not the person's).

RC-9 `122fdfd828` (ES `fix/offline-summary-hang` `6e0e89f3f`): `proxyOffline()` is true when no wired or
wireless interface has an address (the network watcher's own question; a VPN tunnel does not count),
and the three page requests are bounded at 15 s. On the VM both scenarios answer in 1.7 s -- but the
VM cannot show the hang: name resolution on its dead link fails at once, and QEMU's `set_link off`
leaves eth0's address in place (so the new check was not what answered; the instant DNS failure was).
The robust fix follows for RC-10: the interface asks the proxy for the store only (a header the fork's
proxy honours before it considers upstream), and a one-call `raofflineproxy-ctl summary` over sqlite
replaces the 494 requests. H700 RC-9 built and filed (`h700-all-20260914-122fdfd828`), not staged.
Lesson for the proof scripts: a "second change" timer measures nothing when the answer is in the first
screenshot; measure the frame that then holds still.

## 03:40 UTC — The first real offline achievement, read from the RG SP itself

- **It worked end to end.** The maintainer's fully-offline test on the RG SP (RC-7 `3387cf5da0`): Wi-Fi off, Mario Tennis: Power Tour (2171) from a game save. RetroArch's `exec.log`: `Using host: 127.0.0.1:8080`, login served from the proxy's store (`online=False`, the upstream login failing on name resolution), `Identified game: 2171`, `39/50 achievements active`; 21:16:35 EDT `Awarding achievement 14600: Ready for the Real Stuff?` → `Achievement 14600: queued_offline`; the proxy `Queued offline award: achievementId=14600`. The device was turned off at 21:36 EDT and on at 23:18 with the network back: `Connectivity restored; attempting flush` → `Flush sending: achievementId=14600 offsetSeconds=7288 clamped=False` → **`Flush succeeded`** → `pending_remaining=0`. RetroAchievements' own API (`API_GetUserRecentAchievements`, the device's account) lists the unlock at **2026-09-15 01:16:37** — the offline moment, softcore. The achievement lost while travelling, the one that started #163, is back. #167 box 1 ticked with the lines; the site check on the same comment.
- **The reconnect stamp works, the read of it does not survive a careless reader.** Patch 003 wrote `last-flush` = `1789442290 1` at 23:18:10 EDT and the startup card of that boot had not consumed it by 23:24. Then `raofflineproxy-ctl flushed`, called as a "read", consumed it (mv + rm, by design — the message fires once). The write to put it back unchanged (through `tools/device-act`) was refused by the session's permission gate, so the stamp is gone until the maintainer restores it or allows the write; #173 has the note and the one-line restore. Rule from it: on a person's device, read the stamp file with `cat`, never the ctl; check a subcommand's header comment for side effects before calling it as a read.
- **#195, the save times, from the card:** clock and zone right (`timedatectl`: America/New_York, synchronized, RTC in UTC; the kernel set the clock from the RTC at the offline boot). The AUTO SAVE row's "9:07" is `<tennis>.state.auto`, mtime **2026-09-13 09:07:27 EDT** (Sunday morning), shown `09/13/2026 09:07` in the 24-hour `%x %R` format: the interface restores the pre-launch auto save after a game played from a slot (`SaveState::onGameEnded` deletes RetroArch's exit state and renames the `.bak` back), so Monday's 12-minute session left no save state, only the SRM (21:19:02). The duplicated save the maintainer saw ahead: no copy written on 2026-09-14 exists on the card, so the file is gone or was made elsewhere; a fresh duplicate plus `stat` decides display vs file. Asked on the issue which screen the duplicate was made with.
- **RC-10's guest-d proof run is void from its walk on**: the walk's START did not open the main menu (frame `01-main-menu.png`, 00:58:10 UTC, the carousel on PICO-8), its presses walked the carousel and pressed LAUNCH in Böbl's save state manager (`exec.log` 00:58:49 UTC); every later frame — the walk, `rc9-hang.sh` (a)/(b) at 176 s, `rc6-pl09.sh`'s "list at 2.6 s" — is the game. Named frames prove nothing about their contents; a `pgrep retroarch` before every walk and a frame that proves the menu opened go into `rc6-proofs.sh` before the rerun. Session state stashed on `feature/conflict-resolution` (`c1f6ae93e8`) with the maintainer's six notes from the test quoted verbatim (#190–#195).

## 05:50 UTC — Save states: the loss is distribution-side, and the fix is Batocera's route (D-UI-057 decided)

- **Batocera's players do not lose the quit state; ROCKNIX's do.** Same interface code (ROCKNIX upstream ES `master`, the fork and Batocera `master` are identical in `SaveState.cpp`, `SaveStateConfigFile.cpp` and `GuiSaveState.cpp` apart from fork commits; RetroPie's original ES has no save state manager). The parking of the auto save and the delete-and-restore in `onGameEnded` sit behind `racommands`, which `SaveStateConfigFile.cpp:236` sets **false for every emulator read from `es_savestates.cfg`** and true only on the built-in fallback. Batocera ships that file with a `libretro` entry (`package/batocera/emulationstation/batocera-emulationstation/es_savestates.cfg`), its configgen turns the interface's `-state_file` into RetroArch's `-e <slot>` (`libretroGenerator.py:389-393`) and a `.auto` file into `savestate_auto_load=true` (`libretroConfig.py:614-631`), and its distro patch `001-no-next-slot` makes the launched slot the current one. ROCKNIX ships no `es_savestates.cfg` (checked on guest d) and `runemu.sh` knows only `-state_slot`/`-autosave`. An earlier claim that Batocera players get the same loss was wrong and is marked corrected on #196.
- **Decision (D-UI-057, moved from open to decided):** parity with Batocera -- ship the cfg entry, teach `runemu.sh`/`setsettings.sh` the state-file contract, apply patch 001; interface untouched; the exit-destination option is dropped. Maintainer's words in the row. #196's criteria rewritten to that shape; #197 (slot numbers on the tiles) stays a separate call.
- Lesson for the ledger: when a behaviour "in upstream's code" is being judged, read the distribution's config and launcher too -- the same binary takes a different path under a one-line config, and the wiki described the configured path, not the fallback.

## 05:47 UTC — #194: the login toast says "(offline)" through the proxy, and a toast's backdrop follows its font (`feature/ra-login-toast`)

- **Proxy patch 011** (`011-offline-login-header.patch`): every response the proxy writes carries `X-RA-Proxy: 1`; the login `handle_offline_login` answers from the store carries `X-RA-Offline: 1`. Bodies, statuses and `Content-Length` unchanged. The harness (`tools/last-good-scripts-test`, case t) reads the three shapes back through `http.client` -- 293 PASS, 0 FAIL, the 011 predicate among them. Offered upstream as-is, like 001-010 (D-RA-002, D-RA-016).
- **RetroArch 0013** (`0013-cheevos-offline-proxy-toast.patch`): `rcheevos_client_http_task_callback` records the two headers in `rcheevos_locals` (`last_response_via_proxy`, `last_response_offline`) just before rc_client's callback runs, so `rcheevos_client_login_callback` reads the login's own answer and pushes `MSG_CHEEVOS_LOGGED_IN_AS_USER_OFFLINE` -- `RetroAchievements: Logged in as "%s" (offline).`, French `... (hors ligne).` -- else the stock string. The English wording lives in `intl/msg_hash_us.h` alone, for the maintainer's "Connected as" call after the build. A log line names the proxy's part; no credential in it.
- **RetroArch 0014** (`0014-widgets-backdrop-follows-the-font.patch`) and the finding behind it: the fork's `0001-Increase-ozone-widget_size.patch` multiplies the widget scale by 1.5 in `gfx_widgets_iterate` **only**; `gfx_widgets_context_reset` still sets `last_scale_factor` from the unmultiplied value, so every context reset lays the widgets out small and the next frame lays them out large again -- and `msg->width`, measured once at push, is never re-measured. At 640x480 on KMS (pixel scale 0.363 -> 0.545) the msg font is 9 px in one layout and 10 px in the other; `RetroAchievements: Logged in as "MaxEngel".` in the shipped widget font (`xmb/monochrome/font.ttf`, per `video_font_path`) is 199 px vs 226 px against the 17 px a single line's backdrop extends past its text -- the text runs ~10 px past the backdrop. The numbers come from a pure-Python `hmtx` reader over the font (scratch `ttfwidth.py`; no fontTools on the host). Fix: one `gfx_widgets_get_scale_factor` used by both functions (reading `settings->arrays.menu_driver` when the menu id is still UNKNOWN at a reset), and `gfx_widgets_msg_queue_measure` re-measures a plain notification as it leaves the queue and every one on screen when the layout runs. Which reset stands in front of the toast on the RG SP is the VM frame's to confirm; the fix does not depend on the answer.
- **Lessons.** (1) A fork patch that changes a value the code derives in two places must change both -- grep for the second derivation (`last_scale_factor` was set in two functions; the 1.5 went into one). (2) `gcc -w` silences `-Werror=declaration-after-statement` too: the first "decl-after-stmt: rc=0" checks proved nothing and were redone without `-w` (the one hit is 0012's `cheevos_cmd`, pre-existing). (3) Patch numbering: the brief said 0007 "after the existing ones"; the directory runs to 0012, and 0007 would have sorted before 0008-0012 under `scripts/unpack`'s glob -- so 0013/0014.
- Not done on this branch: no build, no VM frame, nothing merged. The VM proof (640x480, offline launch through the proxy, a frame 2-3 s in; `exec.log` for `Login answered through the offline proxy, from its store (offline)`) is in the hand-over.

## 06:40 UTC — RC-11 under way; the proof scripts guarded

- RC-11 scope settled with the maintainer (everything reported since RC-7 plus the older unbuilt items): six Fable subagents on their own branches -- #196 parity (distribution + ES), #191 network page (ES + wifictl), #193/#195/#182 (delivered: `feature/ui-small-fixes-rc11`, three commits, 74 unit tests green), #194 toast (proxy header + RetroArch patch), #192/#187 cloud surfaces, #177/#178 logging. #185 left for its own round (design-sized); #186's PL-20 is upstream; #176 closed (done since RC-3). Approvals the maintainer gives after the build: the #194 wording (A `Logged in as "Name" (offline)` built, B `Connected as ...`), the literal AM/PM for French and padded `09:07 AM` vs the register's `9:07 AM` (#195), the summary page's offline line (D-RA-021), the MANAGE NETWORKS labels, Batocera's patch 001 applied (#196).
- The proof scripts now refuse a game in the foreground and prove the menu opened (`rc6/guards.sh`: `no_game`, `menu_open`; rule section added to `generic-x64-vm-testing.md`); `rc6-pl09.sh`'s bound allows the frame tool's latency (list within 6 s of A and 3 s of the card). Guest d rebooted clean (RC-10, toggle on, 102 cached); the RC-10 interface proofs rerunning (`rc10/run-2-f55cc952f3.log`).
- Register: D-RA-020 (store-only asks, one summary read -- the RC-10 decision that was owed), D-RA-021 (the offline lines' wording), D-UI-059 (date-only tiles, #197 closed).

## 07:10 UTC — RC-11 build 1 (`66bfd20330`) built; the suites and the guest-d proofs running

- All six subagent branches delivered and merged: ES `test/qa-integration` at `137309b7d` (91 unit tests; the tests' CMakeLists and README merged as unions), distribution `next` with `feature/wifictl-saved`, `feature/savestates-parity` (harness section re-lettered to `v.`), `feature/ra-login-toast`. Full harness on `next`: PASSED. The ES push was refused once by the repo's own secret scan -- the credential mask's unit test hands it credential-shaped strings on purpose -- so the hook now skips exactly those lines (`es-app/tests/unit/*Tests.cpp`, a line calling `maskSecrets(`), commit `3ea7316b2`; the first x64 attempt had failed at the ES fetch (`not our ref`) for that reason, no package poisoned.
- Build 1 `66bfd20330` OK in 2.5 min warm (systemd, rocknix, retroarch, raofflineproxy, emulationstation rebuilt); the binary carries the new strings and the image ships `es_savestates.cfg`. Filed `x64-all-20260915-66bfd20330`. Nine suites running on the pair; guest d rebuilt from it and running rebuild/fully(+#194 toast frames)/check1/walk then `rc9-hang`; H700 building from the devices worktree.
- **#199 filed and in work** (a seventh subagent): PL-09 on RC-10 with 202 games listed in 134 s because 100 of them were cached by id without icons and each icon miss waited ~34 s upstream while offline; RC-11 build 2 will carry proxy patch 012 (an offline or store-only image miss answers at once) and the interface's store-only header on proxy image requests.
- Records: RC-10's QA log row; #190 box 2 ticked with `rc9-hang` (b) 1.7 s, box 1's FR frame and box 3's 250-game run left for RC-11's frames; #176 closed (done since RC-3); #198 filed (setrootpass unquoted, found by the #177 work); the cloud tier table and menu map updated for #187/#192; `cloud_net_ready`'s header names the card's new words.
- Approvals the maintainer gives after the build, as agreed: #194 wording A/B; #195 literal AM/PM for French and padded `09:07 AM` (the row D-UI-058 shows `9:07 AM`); #193's summary line (D-RA-021); #191's labels (IN USE, FORGET <name>?); #196 Batocera's patch 001 applied; #187's hub row showing the outcome only until seen (no whole-run stamp).

## 06:30 UTC -- #199: the "34 s per icon" was the proof's timer, not the proxy; patch 012 and the store-only header for images anyway

- **The 137 s is a measurement artefact, and the same one the 00:40 entry named.** The PL-09 rerun's
  frames (`rc6/frames-pl09-100/`): `wait-card.png`, taken 1.7 s after A, is byte-identical to
  `01-52-summary-offline-100-games.png`, taken 135 s later -- the "wait card" the script timed was
  already the list. Its second loop then waited for a change from a screen that had already changed
  and ran out its 120 shots at 05:45:59; "the list 134.0 s after A" is that timeout, and the proxy's
  01:43:46 -> 01:46:03 gap is the same idle interval, ended by the tool's `key down x6` scrolling new
  rows (new icon requests) into view. The code agrees: `ProxyRuntimeServer` is a
  `socketserver.ThreadingMixIn` TCPServer (a thread per connection), no lock spans an upstream call,
  and with the link cut by `set_link off` eth0's operstate is down, so `has_active_network_interface()`
  short-circuits the probe and an offline image miss was a 204 at once. Blindspot shape: a tool that
  judged its run by whether it *ran*, not by what it *measured* -- `rc6-pl09.sh` needs the fix the
  00:40 entry asked for (measure the frame that then holds still), before it times anything again.
- **What the proxy really charges, and patch 012.** An image miss called `is_online()` twice per
  request (the handler's log line and the static-asset path); while the tracker says unreachable
  `should_probe_retroachievements` returns True on every call, so each is a HEAD to
  retroachievements.org whose DNS lookup the 5 s timeout does not bound, spared only when no
  interface is "up"; a yes was followed by a 10 s-timeout fetch. On a link that is up and leads
  nowhere that is tens of seconds per icon, one per visible row. `012-offline-image-miss.patch`: a
  miss with `X-RA-Store-Only: 1` or with the tracker offline is a 404 at once (no probe, no fetch;
  the log line reads the tracker for image requests), a hit is served, an online miss fetches as
  before. 404 not 204: RetroArch writes a badge only for a 200 (`cheevos_client.c`), and ES's
  `HttpReq` keeps a non-2xx body in a `.tmp` it removes, where a 204 landed an empty file the
  component had to delete (#180). Harness case t: four 012 checks through the real
  `ProxyRequestHandler` over a fake socket (329 PASS on the tree; `BASE_REF=70b6c70ca7 --old`
  fails all four, the old client asking `is_online` twice and fetching).
- **ES `feature/store-only-images`:** `Utils::OfflineProxy` (`es-core/src/utils/OfflineProxyUrl.h`)
  holds the one address and the header line; `OfflineAchievementsText::requestUrl`/`badgeUrl` and
  `askProxy` use it, and `WebImageComponent` sends the header when `isProxyUrl(url)` -- 4 doctest
  cases (28 assertions); the suite 98 cases, 1124 assertions, all green. Nothing else about the
  request changed (no timeout was added).
- **Fixture:** `rc6/cache-100.py --icons` stores each game's icon (the icon alone, synchronously,
  counted from the file) through `rewrite_image_urls` + `download_static_image`; default unchanged.
- **Upstream nit, not fixed here:** `image_cache._rewrite_icon_fields` reads `ImageIconUrl`, but
  RA's `patch` response carries `ImageIconURL` (rcheevos `rc_api_runtime.c`) and a relative
  `ImageIcon`, so the patch-row icon download is a scheme-less URL `urllib` rejects, swallowed by
  `download_static_image`'s broad except. Harmless on ROCKNIX because `cache_game` also fetches
  `achievementsets`, whose absolute `ImageIconUrl` stores the same file -- the scan and a launch do
  cache icons; only the by-id fixture (`cache_images=False`) does not. Worth a line in the #168 list.
- **Open for the integrator:** #199's "rows show placeholders where icons are missing" -- a row's
  `WebImageComponent` shows nothing on a failed fetch (`:/cartridge.svg` only when the URL is empty),
  so the criterion as written needs either a fallback image on a 404 or a reworded criterion.

## 07:45 UTC — Correction: the 134 s was my timer, not the proxy; RC-11 build 1 green; build 2 building

- **Wrong reading, corrected.** The PL-09 "134 s" on RC-10 (06:30 entry, #199's first version, #190's comment, the QA log row) was `rc6-pl09.sh` waiting for a *second* screen change after the wait card when the first change 1.7 s after A already was the list (icon slots blank); the frames at 1.7 s and 135 s are byte-identical; the proxy answered every offline icon miss with a 204 at once. The rule was already in `generic-x64-vm-testing.md` ("a second-change timer measures nothing when the answer is in the first screenshot") and had been applied to `rc9-hang.sh` on 2026-09-14 -- and not to the other timer in the same directory. Both now measure the first frame that then holds still. #199 stays as a hardening (patch 012: a miss while offline or store-only answers 404 with no probe; the interface asks the proxy store-only for images) with its body corrected; #190's comment and the QA log row corrected in place.
- **RC-11 build 1 `66bfd20330`: nine suites PASSED** (`qa-66bfd20330-webdav-a-20260915-0604`); guest d: fully-offline start PASS with the #194 toast on screen at +3 s (`RetroAchievements: Logged in as "<account>" (offline).`, backdrop covering it; box 1 ticked); check1/walk: the switch, the scan and the stamp PASS (two harness artefacts: the PL-27 PATH shim check and a PL-07 poller that looked after a 15-second scan had ended); `rc9-hang` (a) 1.7 s, (b) 1.7 s. #196 on the VM: LAUNCH on a dated tile sent `--state_file=` and `state_slot=1`, runemu added `-e 1`, RetroArch loaded `Bobl.state1`; after a SIGTERM quit `Bobl.state.auto` carries the quit time (02:17:05) and no `.bak` exists; the slot file kept its bytes and time and was renumbered to slot 0 by upstream's `renumberSlots` (Batocera's behaviour too). H700 build 1 filed (`h700-all-20260915-66bfd20330`, not staged).
- **Build 2 `a6d032bf5e`** (RC-11 + #199: ES `8d5ad005d`, proxy patch 012) building for x64 and H700; the interface's unit tests at 98 cases. Frames so far under `/workspace/tmp/rocknix-session/rc11/frames/` (the manager 24-hour and after the quit; the toast, redacted); the 12-hour manager and the offline game page EN/FR being retaken -- the first attempt died when `set_link off` cut ssh (the serial console is the channel while the link is down).

## 08:20 UTC — RC-11 build 2 `a6d032bf5e` proven on the VM; frames filed; what is left for the maintainer

- **Build 2** (build 1 + #199) built for x64 (`x64-all-20260915-a6d032bf5e`) and H700 (`h700-all-20260915-a6d032bf5e`, tar sha256 `8bbe6a3c…`, not staged). Nine suites PASSED on both builds. Guest d on build 2: fully-offline start + toast, check1, #190 (a) 4.6 s / (b) 1.7 s each proven by the interface's own `ctl summary` call, PL-09 over 200 icon-less games 4.6 s with one ctl call; #196, #195, #191, #182 framed (`docs/qa-frames/2026-09-15/`, README lists each). Records: QA log row RC-11; #190/#194/#193/#199 comments with the evidence; #197 closed (dates only, D-UI-059); #198 filed (setrootpass unquoted).
- **Two more harness false positives caught by looking at the frames**, both fixed and in the rule: the interface never saw a toggle flipped from the shell (a NOT CONNECTED dialog graded PASS because it held still) -- `ensure_toggle_on` restarts the interface when `system.cfg` is newer than it, and the proofs now grade the device path by the interface's log line; `set_link off` keeps the address, so scenario (a) now drops it with `nmcli dev disconnect` the way Wi-Fi off does on a device.
- **Left for the maintainer** (approvals as acceptance checks, per their word): #194 wording A on screen (B is one string); #195 padded `02:17 AM` (the register example reads `9:07 AM`) and a literal AM/PM for French; #193's French line wrapping at 640 (shorten, or a second text line) and the summary line (D-RA-021); #191's labels (IN USE, FORGET <name>?); #196's patch 001 applied and the slot renumbering upstream does; #187's hub row showing the outcome only until seen (D-UI-060). Then: stage RC-11 build 2 on the RG SP on their word (D-QA-011), reboot as its own question, and their eyes on the RG SP for every "the maintainer's word" box.

## 14:20 UTC — RC-11 build 2 on the RG SP

- Maintainer, 2026-09-15: *"The device is back online now, and you can make the transfer and perform a reboot."* Idle check clean (no game, no cloud process, lock free, queue empty, 27 GB free). Staged 13:52-14:08 UTC (`stage-rgsp-run-a6d032bf5e.sh`, BEGIN/END in the action log), the device's sha256 `8bbe6a3c…` matched, tar in `/storage/.update`. Reboot 14:10:10 through `tools/device-act` with the maintainer's words on it; back after ~85 s: `BUILD_ID a6d032bf5e`, boot id `7ddd012a`, queue empty, interface and proxy up, Tailscale up, toggle on, hardcore off, index-at-start on, `ready=247 pending=0`, battery 95 %. RETURNED line in the action log.
- Now the maintainer's eyes on the device: the offline summary with Wi-Fi off (#190 box 4), the login toast (#194), a game page's lines (#193), the manager after a slot session (#196), the Wi-Fi SSID row and MANAGE NETWORKS (#191), the startup card's first step (#192), the reconnect sentence at the next sync card (#173), and the approvals listed at 08:20.

## 18:55 UTC — RC-12 build 1 `2514d317f7`: the maintainer's first two calls on the RC-11 round

- Maintainer, on the RG SP's NETWORK SETTINGS on RC-11: *"we don't need to list the Wi-Fi SSID under the Wi-Fi SSID label in network settings, since it shows you what network you're currently connected to. It's redundant to have it in both places."* and *"where we currently say 'manage networks,' we should change it to 'manage saved networks.'"* Then: *"But when would the subliner under Wi-Fi SSID differ from the right-side value?"* -- when autoconnect has joined a saved network other than the configured one (the report that opened #191), when the device is on no network, when NetworkManager gives no answer. Register: D-UI-061, D-UI-062.
- ES `daafb69f1` (`feature/network-words-rc12`, merged as `7dd9de6a1` on `test/qa-integration`, pushed): `WifiText::ssidLine` decides the line (None / ConnectedTo / NotConnected / CouldNotCheck); the WI-FI SSID row is built one line high and grows only for an answer worth a line -- `MultiLineMenuEntry::setDescription` now re-lays single-line entries (`layoutRows`, shared with the constructor) and `GuiSettings::updateSize` re-lays the page, the shape the scraper row's late description already used; `GuiSettings::buildInputTextConfigRow` takes `withDescription` so an entry can be built with an empty line to fill. MANAGE SAVED NETWORKS / SAVED NETWORKS / NO SAVED NETWORKS / COULDN'T READ THE SAVED NETWORKS. TRY AGAIN.; French ENREGISTRÉS. Unit tests 101 cases / 1132 assertions (3 / 8 new); GuiMenu, GuiSettings, MultiLineMenuEntry, WifiText syntax-checked with the GENERIC_X64 toolchain.
- Pin `2514d317f7` on `next`; build worktrees synced before the build; x64 building (`build-x64-rc12.sh`, log `build-x64-rc12.log`). The VM proof to take on guest d with the `wifictl` stand-in: the row one line when the stub's current equals `wifi.ssid`, `CONNECTED TO <other>` when it does not, NOT CONNECTED / COULDN'T CHECK from the stub's other answers; the page's new title and group; EN/FR frames. H700 after.
- The punch list for the maintainer's RG SP round and the decisions still open are #200 (filed 18:35 UTC from their request for *"a punch list of what you want me to review"* and the wording options).

## 19:40 UTC — RC-12 build 1 proven and filed; build 2 = the phone paradigm for Wi-Fi (#201)

- **Build 1 `2514d317f7`**: nine suites PASSED (`qa-2514d317f7-webdav-a-20260915-1857`); guest d upgraded in place from RC-11 build 2 through `.update` (about a minute -- the upgrade path exercised); the row's four shapes and MANAGE SAVED NETWORKS framed EN/FR (7 frames filed); H700 built (`h700-all-20260915-2514d317f7`), not staged. A `pgrep -f` self-match scare on the way: counting `tools/vm-q[a]` and "qemu-img converts" from a command line that contained both words read as another run in flight; `ps -eo lstart,args` showed only mine (the memory `self-matching-process-watchers` again -- the literal was in an echo string this time).
- **The maintainer's third call** on seeing build 1's frames: *"Why won't the right-hand value always be the network you're connected to? This matches the paradigm on other operating systems, phones, etc. ... in the screen where it shows the networks ... show previously connected networks and allow you to select one of those networks to connect to if it was also available ... It should only show the ones that are also available."* Answered: the row was ES's input field bound to `wifi.ssid`, so its value was the setting; boot never reads that setting (NetworkManager autoconnects), so the row's claim and the connection drift. Filed as #201; register D-UI-063 (the value is the connection; supersedes D-UI-061) and D-UI-064 (the picker: in-range only, CONNECTED/SAVED marks, a saved one joins on a press).
- **Build 2 `0242500826`** (ES `e7d5029fb` = `404d3a9a3` the paradigm + `ae521676e` the French WI-FI hyphen; distribution `f0b71adfac` `wifictl join`): `GuiWifi` rewritten -- rows from `WifiText::pickerRows` (pure, 4 cases / 25 assertions new; 102 cases / 1149 in all), a saved row joins through `ApiSystem::joinWifiNetwork` -> `wifictl join` (`nmcli connection up`, the settings moved onto the profile on the OS side with the key read from NetworkManager and never printed, `pin`), any other row asks the key on the on-screen keyboard and connects through `enableWifi`, the page rebuilt on success; the WI-FI SSID row is a hand-built label/value/arrow row filled from `wifictl current`; `addInputTextConfigRowWithDescription` and `openWifiSettings` removed (no caller). Harness section u. grew seven `join` checks (19 PASS; the nmcli shim answers `connection up`, the profile's psk, `device show`, `connection modify`; the stub `set_setting` records its calls -- the first assertion of "settings untouched" was wrong because the script's preamble seeds `wifi.adhoc.channel`, reworded to "no wifi.ssid/wifi.key line"). 337 PASS whole. Found on the way and worth its own note: `wifictl connect` deletes an existing profile and rebuilds it from the key given, so picking a previously joined network from the old picker destroyed its saved key.
- x64 build 2 building; then guest d in place again, the picker framed (marks, a join, a new network's key popup), the suites, H700. The RG SP waits for the maintainer's word on RC-12 as a whole (#200).

## 20:00 UTC — RC-12 build 2 `0242500826` VM-complete; H700 filed, not staged

- Nine suites PASSED on the build 2 image (`qa-0242500826-webdav-a-20260915-1935`). Guest d, upgraded in place from build 1: the WI-FI SSID row's value is the stand-in's network; the picker with CONNECTED / SAVED marks; a join of a saved network (toast, the page rebuilt, `wifi.ssid` and `wifi.key` moved by the stand-in as `wifictl join` does); a new network through the WI-FI KEY keyboard (START with nothing typed = an open network); the two refusal dialogs with nothing changed afterwards; French (`RÉSEAUX WI-FI`, `CONNECTÉ`, `ENREGISTRÉ`, `NOM DU RÉSEAU WI-FI` with one hyphen). 10 frames filed; #201 boxes 1-4 ticked; #191 and #200 brought to build 2 (#200 also carries the row's label as decision 11: WI-FI SSID / WI-FI NETWORK / WI-FI).
- H700 `h700-all-20260915-0242500826` built (tar sha `9350a37a85e9a8f2…`) and filed; not staged -- the RG SP stays on RC-11 build 2 until the maintainer says so (staging asked, the reboot asked, D-QA-011/015). `next` pushed to origin.
