## 00:05 UTC — ES changes before the other devices' RC (D-QA-032)

The maintainer, on the punch-list question: make the EmulationStation
changes now, cut a new build, and work the checklist on the RG35XX SP
meanwhile. So #151's PL-07 (an offline scrape told the server erred; an
inconclusive probe blaming the developer pair) and PL-17 (`helpRowPerc`
measured with an unthemed font) are code now, not deferrals to #156, and
#186's PL-20 is a line on #168's upstream list. The checklist page and
#236 gain a B group for the ES changes and the build that follows, and the
A boxes those changes touch (#66's messages, #210's help row) say they are
re-checked on the next build. Current build `77e7e97515` stays on the QA
handheld for everything else.

## 00:25 UTC — No EmulationStation change is needed: the punch-list code shipped in the pin

Before changing anything, the ES worktree at the RC's pin (`fb6947fb4`) was
read: PL-07 is `5a0095455` (offline says so; a tri-state probe blames nobody
when it cannot read an answer) and PL-17 is `772d70035` (`helpRowPerc` takes
the help style from both callers). Both are in the candidate. What #151 still
lacked was evidence, and the 2026-09-13 comments already held it: offline
framed EN/FR at 640x480; the probe-timeout half not producible without TLS
interception (a black-holed API times out the *first* request, so the probe
never runs) and deferred to #156 by the index itself; the two `helpSize`
readings equal, 0.164167, at both panel sizes. PL-20 was an issue edit (#168).

So #151 and #186 are closed as delivered, their linters PASS,
`register-check` PASS, #150's first box ticked, and D-QA-032 applied finds
nothing to build: `77e7e97515` on the RG35XX SP is the candidate as it stands,
unless section A finds an ES defect. The page and #236 say so. The lesson
repeats blindspot 48's shape from the other side: read the pin before
planning a change to it — an hour of build pipeline was about to be spent on
code already shipped.

## 02:30 UTC — The device list re-cut to what the VM cannot prove (D-QA-033)

The maintainer, four boxes into section A: most of it the VM could verify, so
why is it theirs? Right, and the rule already said so. Twenty-three boxes
moved to B with their ids kept (so the four they had ticked -- the build id,
the SSID row, the startup card's first step, the mDNS name -- stay ticked);
A holds the radio and a hotspot, the tailnet, battery alerts and sleep, the
#211 crash path on the real GPU driver after the VM attempt, and hours on the
hardware. They also asked that the list's items become end-to-end and unit
tests run through the VM, and pointed at microsoft/winappcli -- which, read,
is a Windows app-development CLI (SDK, MSIX packaging, identity), not a
testing tool and not something that drives a Linux guest; noted back, with
tui-test (D-QA-024) as the one already on file. The test-conversion idea
itself is #120's epic; a child issue maps each moved box to a suite.

## 04:25 UTC — Section B on the VM: eleven boxes proven, one fixture wall, one night of walks

On the RC (`77e7e97515`), guest d at 640x480 and guest a at 1280x800: #190,
#193, #194 offline (link cut on the monitor, carrier 0), #189's row and scan
page, #68 online, #160 at 640x480, #157's startup card sequence (CHECKING THE
CONNECTION... → RECEIVING → COMPLETED), #195's times in both clock formats,
#210's help bar on the Screenshots and Tools lists, and the three backup
boxes (#45 closed under D-QA-033; the archive 3,114 bytes, the RA token
absent, no leak warning). Frames under `docs/qa-frames/2026-09-21/`, account
names boxed. The offline-achievements suite failed honestly: the QA account
has earned the only routed achievement (Tobu, 2026-09-14), so #211's flush
and journal boxes wait on a reset of the account's progress on
retroachievements.org (the maintainer's) or a newly routed title.

Left in B: #82 (a file dropped into `screenshots/` mid-session, then the
list), #196 (LAUNCH from a slot, quit, the auto save and the slot both
there), #181 (add the GBA fixture for the logo comparison), #183 (a boot
sampler for PLEASE WAIT), the scraper four (#64 #66 #67 #65, QA ScreenScraper
account), #153's cut S3 frame, #211 box 1. The walks' misfires and their
rules went into `generic-x64-vm-testing.md`.

## 05:35 UTC — The testing direction, registered (D-QA-034)

The maintainer: the QA work is the foundation of a CI/CD framework -- unit,
end-to-end, integration -- systematised into repeatable suites run regularly,
aligned with the architecture, judged qualitatively and quantitatively,
auditable and verifiable. Registered as D-QA-034; #120 is the epic, #237 the
first tranche (the round's boxes as suites), #171 the fork's own CI. They
also reset the QA account's Tobu progress on retroachievements.org, so the
offline-achievements suite runs again on guest d (05:36).

## 05:58 UTC — More B on the VM; the reset has not reached the API; a black screen after a self-quit

#183 (a per-second boot sample on guest a: carousel by ~20 s, no PLEASE WAIT;
the startup index had already given guest d's new ROMs their sets), #181
(NES, GB and GBA logos equally sharp on the VM -- the softness is the panel's
to show), #196 blocked by the fixture (junk state files make Probe quit at
once). The offline suite twice more: RetroAchievements' API still reports
Potato-tan Secret earned on 2026-09-14, so the account reset has not landed
there; asked again. New: after RetroArch quits on its own, guest d's
screendump stays a 2,127-byte frame with ES alive (#239); quits through ES
(`execute_kill`, `POST /emukill`) render fine. The ES HTTP API on :1234
offers `/launch`, `/emukill`, `/quit`, `/reloadgames`, `/notify`,
`/systems`, `/runningGame`, `/caps` -- the deterministic path for
launches in #237's suites.

## 15:17 UTC -- Kitesurf, the reset, and the fixture's budget (#240, D-QA-035)

The maintainer offered Cloudflare's Kitesurf (a remote CDP browser on
Workers that Playwright drives unchanged) for "browser-oriented tasks, like
the reset for different achievements", then: "If this isn't making our lives
easier, we definitely don't have to use it... try to see if there is no way
to do it programmatically through the API, or even a curl request". Checked
the same hour: the RA web API is read-only (api-docs lists reads only, keyed
by `y=`), so no curl path; the site's pages sit behind bot protection and a
plain headless Chromium (Playwright 1.60 from a sibling project, chromium
1223 already cached) did not reach the sign-in form -- and we do not build
around a site's protection, so that line of work stopped there. Kitesurf is
out twice over: the QA password would leave this host, and its own post says
it does not handle bot-challenge handshakes. Filed #240 under #120, row
D-QA-035.

The API also says the routed achievement (100359) is earned in softcore
only, and RA counts hardcore separately -- an hour went into "hardcore as a
second spend" before RAOfflineProxy's README settled it: hardcore is not
supported; RetroArch rejects every forwarded unlock with
`hardcore_not_supported`. So the budget is one softcore unlock per routed
achievement, and the way to grow it is routes. Tobu's 100358 ("listen to
the Heaven song for a bit on the jukebox") is menu-only like 100359, and a
discovery run in the suite's `--control` shape (online launch, link cut,
toggle off, RetroArch killed before the link returns) logs `Awarding
achievement` without spending it -- the same loss #162 describes, used on
purpose. Guest d still holds the QA account and the toggle from the earlier
runs; #82 is being re-proven there on the RC first.

## 15:33 UTC -- #82 on the RC; /emukill is a no-op; Tobu's jukebox is locked

#82 re-proven on guest d (640x480, the RC): the API launch, a PNG planted
mid-game, the exit through `execute_kill`, and SCREENSHOTS listing both files
without UPDATE GAMELISTS (frames filed, box ticked on the page and in #236).
Two things the walk taught. `GET /emukill` -- a GET, not the POST the earlier
notes assumed -- answers 200 and does nothing here: `ApiSystem::emuKill()`
runs `batocera-es-swissknife --emukill`, absent from this image, so the game
ran on for the forty seconds the walk waited and the "screenshots list" frames
were the game. The ES API launches; the exit hotkey's `execute_kill` (what
`ra-offline-test` writes to `/tmp/ra-offline-kill`) is the quit a walk may
use; #239's first box edited to say so. And the carousel keeps its position
across an API launch and exit: a walk that navigates by counting systems must
first read where it stands from a frame (this one stood on MUSIC PLAYER, then
on NES, and both times the count was made from the wrong origin).

Tobu 100358, the jukebox song, is not a fresh-save route: the suite's
`--control` shape (online launch, `achievements active` 27/28, link cut,
toggle off, RetroArch ended over serial before the link returned) framed the
MAIN MENU as PLAINS, LOCKED x5, SCORES. Nothing spent (`awarded=1` before and
after). Two STARTs a few seconds after the set loads land in the Plains level,
not on the menu; the pause menu's QUIT gets back. The candidates left are
gameplay routes -- Bobl 111764 (the Dive Ball), Ninoid 474237 (the double
jump) -- recorded on #240. The reset by the maintainer remains the cheap path
for a-211, and the API still shows 100359 earned on 2026-09-14.

## 15:52 UTC -- the scraper four on the RC (#64 #65 #66 #67), all on guest d

All four VM halves proven on `77e7e97515` at 640x480 and ticked on the page
and in #236, frames filed. #65: the strip is a focus stop with its own look;
an option row cycles in place. #67: GAMES TO SCRAPE FOR survives a tab switch
and a reopen. #64: with the QA account and the public JELOS pair
(`tools/qa-accounts 10026 screenscraper`, then a reboot) SCRAPE NOW scraped
the four fixture titles for real -- media landed under `images/`, `videos/`,
`manuals/` -- and the settings archive carried no password rows with the
leak scan silent (#64's archive box ticked). #66: the two refusal sentences,
each from a reshaped `es_settings.cfg`.

The shape of the failure configs: edit the file with EmulationStation
stopped, then reboot (a restarted ES lands on a game list with a stale
screendump). The helper is `/workspace/tmp/rocknix-session/scraper-config.sh`
(nopair, wrongpair, noaccount, restore). Its first `wrongpair` silently did
nothing: the `sed` substituted values in rows the previous `nopair` had
deleted, and the walk framed the no-pair sentence again under the wrong
label. Now each failure mode restores all four rows first and then breaks
the one it means to -- a config helper must build the state it names, not
mutate whatever the last mode left. A refusal dialog opens over the MAIN
MENU (the scraper page has already closed), so a walk frames after SCRAPE
NOW without navigating.

## 16:13 UTC -- #153's frame found the page wrong, and the candidate moves (ES 7afce37a6)

The last B frame: a 12 MiB settings archive cut at 75 % on the throttled QA
S3, from BACK UP TO THE CLOUD with SETTINGS alone chosen, EN and FR. The
script did what D-CLOUD-126/127 prescribe (ended 44 s after the cut, exit
69, "Couldn't finish: lost the network during the settings backup", the cut
PUT discarded, the marker unmoved). The page read SKIPPED - YOU'RE NOT
ONLINE over WHAT MADE IT IS IN YOUR CLOUD. THE REST IS STILL HERE. --
`GuiCloudTransfer::outcome` maps exit 69 to skipped unless a unit or tier
succeeded, and the note's `moved` was true once bytes flowed. Both wrong the
same way: skipped is nothing touched, and nothing made it. Fixed in ES
7afce37a6 (a cut run with any progress is COULDN'T FINISH; the note counts
files that completed; the SETTINGS/SAVES header goes through `_( )`, since
the French page showed SETTINGS), merged to `test/qa-integration`, pin
bumped here. Per the maintainer's rule for this round the candidate is
re-cut with it: GENERIC_X64 first (run 11), the #153 frame again on guest d,
then H700.

Two things learned on the way. The transfer page packs a fresh archive
before sending, so `cloud-round-trip`'s LINK5 trick (pad, `backuptool
backup`, remove the pad) does not reach a UI run -- the pad has to stay in
place while the page runs. And the chooser page remembers its toggles
across a reboot, so a walk that reads them from a frame before pressing is
the only safe one; an `A` on a switch row is a toggle, not a confirm.

## 16:28 UTC -- the #153 fix framed on a283f504a0, one word left, ES 988de6141

GENERIC_X64 run 11 built in 20 minutes on the warm root; guest d took it as
an in-place update (the tar into `/storage/.update`, sha256 matched on both
sides, BUILD_ID a283f504a0 after the reboot, the S3 fixture and the pad
intact -- an upgrade path exercised for free). The cut upload again, EN and
FR: COULDN'T FINISH / DON'T WORRY, NOTHING CHANGED. and N'A PAS PU SE
TERMINER / PAS D'INQUIETUDE, RIEN N'A CHANGE., the French header PARAMETRES.
One English word survived on the French page: the why line read SETTINGS -
VOUS N'ETES PAS EN LIGNE, because the failed item's label is the scripts'
word and only the header had gone through `unitName`. Fixed in ES 988de6141
(both sites that name a failed item), pin bumped; run 12 rebuilds the x64
image, and H700 follows it.

## 16:32 UTC -- run 12 died on my braces; tools/es-syntax-check (blindspot 49)

The second ES commit for #153 left a three-statement loop body unbraced;
run 12 found it at emulationstation twenty minutes in. Fixed (ES
`d842bbe16`, pin `d55169e59e`), and the check that should have run first
now exists: `tools/es-syntax-check` asks ninja in the x64 build root for the
exact compile command of the object, points it at the edited file with
`-fsyntax-only`, and runs it -- five seconds, the compiler's own errors.
Proven both ways before it was trusted (PASS on the fix, FAIL on a copy with
`int oops = ;`), registered in the guard, the tools list, the tool index and
`es-native-ui.md`. Run 13 is building the same pin; the H700 build follows it.

## 16:39 UTC -- #153 closed on d55169e59e; H700 run 4 building the second candidate

Run 13 (ES `d842bbe16`, pin `d55169e59e`) built clean; guest d updated in
place again (BUILD_ID d55169e59e, fixture intact) and the French cut upload
reads N'A PAS PU SE TERMINER / PARAMETRES - VOUS N'ETES PAS EN LIGNE / PAS
D'INQUIETUDE, RIEN N'A CHANGE. -- French throughout. The English words were
framed on a283f504a0 and the last commit changed nothing in English. #153's
last box ticked and the issue closed. H700 run 4 started at 16:38 UTC from
the devices worktree at d55169e59e (warm roots); its image is the second
candidate for the RG35XX SP. `tools/retroarch-wrapper-test` PASS against the
H700 root's shipped RetroArch recorded on #211.

## 16:44 UTC -- second candidate H700 d55169e59e built and recorded; vm-qa run 2

H700 run 4 took three minutes on the warm roots (EmulationStation and the
image). Artifacts under `/workspace/artifacts/rocknix-images/h700-all-20260921-d55169e59e/`
-- the name #150's row asks for -- with `SHA256SUMS` and a `RECORD.txt`
that says what changed and what proved it; the tar's SYSTEM reads
`BUILD_ID=d55169e59e`, `BUILD_BRANCH=build/devices`. The checklist page
carries the second candidate's note, one new box for the build id after the
update, and the RG SP / Nova rows now name `d55169e59e`. `tools/vm-qa` is
running over the x64 twin (run 2; the pair rebuilt from the image, S3 back
unthrottled, guest d's #153 fixture removed first). The RG35XX SP is off the
network (`192.168.1.81`, no route), so the tar waits; staging is inert and
the reboot is a question either way (D-QA-011).

## 17:12 UTC -- vm-qa run 2 PASSED on d55169e59e; the second candidate is proven on the VM

Eleven suites in 24 minutes (`qa-d55169e59e-webdav-a-20260921-1645`), the
time-to-play numbers unchanged (0.88 / 1.45 / 2.01 s). Row added to
`docs/vm-qa-log.md`; #150's H700 row ticked with the deviation named (built
through `make docker-H700` in the session script, not `devices/build-dev.sh`).
The RG35XX SP is still off the network; the tar waits under
`h700-all-20260921-d55169e59e/`.

## 17:31 UTC -- the RG35XX SP on d55169e59e

The maintainer brought the device back; idle check clean; the tar copied
(160 s over the LAN), hashed on the device with `DEVICE_ACT_TIMEOUT=900`
(the A53 takes minutes), equal to `SHA256SUMS`, moved into
`/storage/.update`. The reboot asked for by name and answered yes; issued
through `device-act` (boot id 79a7e5fc -> 8c343c14), the updater took about
five minutes, and the device came back on `d55169e59e`, `build/devices`,
queue empty. The RG SP receives nothing until A is done (D-QA-031).
