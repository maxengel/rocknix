## 00:05 UTC — ES changes before the other devices' RC (D-QA-032)

The maintainer, on the punch-list question: make the EmulationStation
changes now, cut a new build, and work the checklist on the RG35XX SP
meanwhile. So #151's PL-07 (an offline scrape told the server erred; an
inconclusive probe blaming the developer pair) and PL-17 (`helpRowPerc`
measured with an unthemed font) are code now, not deferrals to #156, and
#186's PL-20 is a line on #168's upstream list. The checklist page and
#236 gain a B group for the ES changes and the build that follows, and the
A boxes those changes touch (#66's messages, #210's help row) say they are
re-checked on the next build. Current build `77e7e97515` stays on the QA
handheld for everything else.

## 00:25 UTC — No EmulationStation change is needed: the punch-list code shipped in the pin

Before changing anything, the ES worktree at the RC's pin (`fb6947fb4`) was
read: PL-07 is `5a0095455` (offline says so; a tri-state probe blames nobody
when it cannot read an answer) and PL-17 is `772d70035` (`helpRowPerc` takes
the help style from both callers). Both are in the candidate. What #151 still
lacked was evidence, and the 2026-09-13 comments already held it: offline
framed EN/FR at 640x480; the probe-timeout half not producible without TLS
interception (a black-holed API times out the *first* request, so the probe
never runs) and deferred to #156 by the index itself; the two `helpSize`
readings equal, 0.164167, at both panel sizes. PL-20 was an issue edit (#168).

So #151 and #186 are closed as delivered, their linters PASS,
`register-check` PASS, #150's first box ticked, and D-QA-032 applied finds
nothing to build: `77e7e97515` on the RG35XX SP is the candidate as it stands,
unless section A finds an ES defect. The page and #236 say so. The lesson
repeats blindspot 48's shape from the other side: read the pin before
planning a change to it — an hour of build pipeline was about to be spent on
code already shipped.

## 02:30 UTC — The device list re-cut to what the VM cannot prove (D-QA-033)

The maintainer, four boxes into section A: most of it the VM could verify, so
why is it theirs? Right, and the rule already said so. Twenty-three boxes
moved to B with their ids kept (so the four they had ticked -- the build id,
the SSID row, the startup card's first step, the mDNS name -- stay ticked);
A holds the radio and a hotspot, the tailnet, battery alerts and sleep, the
#211 crash path on the real GPU driver after the VM attempt, and hours on the
hardware. They also asked that the list's items become end-to-end and unit
tests run through the VM, and pointed at microsoft/winappcli -- which, read,
is a Windows app-development CLI (SDK, MSIX packaging, identity), not a
testing tool and not something that drives a Linux guest; noted back, with
tui-test (D-QA-024) as the one already on file. The test-conversion idea
itself is #120's epic; a child issue maps each moved box to a suite.

## 04:25 UTC — Section B on the VM: eleven boxes proven, one fixture wall, one night of walks

On the RC (`77e7e97515`), guest d at 640x480 and guest a at 1280x800: #190,
#193, #194 offline (link cut on the monitor, carrier 0), #189's row and scan
page, #68 online, #160 at 640x480, #157's startup card sequence (CHECKING THE
CONNECTION... → RECEIVING → COMPLETED), #195's times in both clock formats,
#210's help bar on the Screenshots and Tools lists, and the three backup
boxes (#45 closed under D-QA-033; the archive 3,114 bytes, the RA token
absent, no leak warning). Frames under `docs/qa-frames/2026-09-21/`, account
names boxed. The offline-achievements suite failed honestly: the QA account
has earned the only routed achievement (Tobu, 2026-09-14), so #211's flush
and journal boxes wait on a reset of the account's progress on
retroachievements.org (the maintainer's) or a newly routed title.

Left in B: #82 (a file dropped into `screenshots/` mid-session, then the
list), #196 (LAUNCH from a slot, quit, the auto save and the slot both
there), #181 (add the GBA fixture for the logo comparison), #183 (a boot
sampler for PLEASE WAIT), the scraper four (#64 #66 #67 #65, QA ScreenScraper
account), #153's cut S3 frame, #211 box 1. The walks' misfires and their
rules went into `generic-x64-vm-testing.md`.

## 05:35 UTC — The testing direction, registered (D-QA-034)

The maintainer: the QA work is the foundation of a CI/CD framework -- unit,
end-to-end, integration -- systematised into repeatable suites run regularly,
aligned with the architecture, judged qualitatively and quantitatively,
auditable and verifiable. Registered as D-QA-034; #120 is the epic, #237 the
first tranche (the round's boxes as suites), #171 the fork's own CI. They
also reset the QA account's Tobu progress on retroachievements.org, so the
offline-achievements suite runs again on guest d (05:36).
