# 2026-09-12 work log

## 00:20 UTC -- one console at a time

The maintainer, after the council: sync never happens between two consoles
at once; the use case is a hand-off from the commute handheld to the home
one; concurrent use of one save folder is to be prevented, not reconciled
(D-CLOUD-102). A good part of the council's deliberation had gone to the
race between two publishers -- it was the argument that carried store-first
to its 3-2 win. With the race out of scope, the delta's original shape comes
back as the cheaper sufficient form: retain the displaced version from the
device's own stage (it restored it, so it has the bytes), then publish; two
uploads, no cloud-to-cloud copy, no escrow of every version, no `in_store`
flag. The council's engineering (transaction shape, deferral as a pair, the
protections, the audit, the migration) carries over untouched. Prevention
falls out of the manifests every pass already reads: a device behind another
device's publication restores before it publishes, and defers when it
cannot. Written up as `docs/save-history-serial-model-addendum.md` with the
choice: amend by hand, or a short council follow-up on the one question.

## 00:50 UTC -- no lock, and the wizard assumes serial play

The maintainer thought the lock through and set it aside: babysitting, and a
stale lock from a console that died mid-game is a worse problem than the one
it solves. The prevention rule in the first draft of the addendum went with
it -- the case it guarded is R4's both-changed case already. What the serial
assumption gives is presentation: the wizard can order the two versions by
publish sequence and say which console played later, and KEEP BOTH matters
more because both are real progress. One question left for the maintainer:
whether the cursor may rest on the later version. Amending by hand, chosen.

## 01:10 UTC -- the cursor rests on the newer one; the columns stay put

"Rest its cursor" needed explaining -- on a handheld the page opens with one
control highlighted and A acts on it -- and once explained the maintainer
decided it in a sentence: keep the source's columns, cloud left and console
right, and open with the cursor on the newer version (D-CLOUD-104). The
amendment's §3 now says so; the later/earlier line goes under each column
rather than swapping them.

## 02:30 UTC -- the council's text is in the tracker

Applied: #134 rewritten (title and body: the design on one screen, build
order, E1-E15 with venues, time to play as a gate, the decisions that bind,
the maintainer's open list P-1..P-13, ten acceptance items); #22 R1-R11 row
by row; #23, #25, #21, #135 amended; two children opened and attached --
#136 the guard image (allowlist first rules, the `--delete-excluded` and
`--backup-dir` audit, the rules-file upgrade path) and #137 the fold of
today's set-aside folders (copy-verify-retire, `legacy`, no question,
mixed-version boundary). #134 sits under #11 with the milestone. The
bodies were transcribed by an agent from the Step 5 draft with the
amendment applied and reviewed here before they went up; it caught one
error of mine, a register ID that did not exist (D-CLOUD-106 for the
device name -- D-NET-001 is the row). Docs: the delta and gap analysis
marked superseded; the wizard IA at rev 6; the menu map's pending nest;
#42 carries the public page's layout table. Bodies kept under
`research/council-runs/2026-09-11-save-history-one-home/applied/`.

## 03:40 UTC -- the maintainer's questions found a simpler architecture

Working the nine decisions one at a time, the maintainer stopped on "the
stage" and asked how a copy taken at exit could be safe if the emulator
might still be writing -- and caught a sentence of mine that was wrong as
worded. The precise answer (the emulator that exited is gone; the copy
guards against the *next* emulator during a long upload) led to the
better question: why allow a launch during the upload at all? Cancelling
a sync the player can see is counterintuitive. So: the launch waits, the
sync bounds itself to a few seconds and fails with a connectivity outcome
past that, never minutes, time-based bounds (D-CLOUD-109). With nothing
writing during the upload, the frozen copy of the new save has no job;
the stage is the last-agreed cache and nothing more (D-CLOUD-110).
Decisions 1-4 before it: D-CLOUD-105 (fleet setting; OFF keeps a
transaction copy -- the council's reading rejected), 106 (prune order,
the 90-day cap dropped), 107 (copy never move), 108 (the one fallback
fetch).

## 04:20 UTC -- three more rulings while walking the list

The offline benchmark (D-CLOUD-112): sync-on-exit stays on through the
subway and the plane, so a console with no connectivity must reach its
outcome in about the time a real sync would have taken -- the yardstick
the connect timeout and the probe are sized to, measured beside the
online exit in #135. Two transfer contracts (D-CLOUD-113): a deliberate
back up or restore may take time and says so on its page; an automatic
sync is quick, bounded and waited for -- Steam's behaviour is the
precedent to confirm. And the words (D-UI-040): the exit card said
BACKING UP SAVES TO THE CLOUD while the startup card said SYNCING SAVES
AT STARTUP; the exit card now says SYNCING SAVES TO THE CLOUD, "back up"
kept for the deliberate action. Changed in ES on
`feature/exit-card-sync-words`, to be pinned with the next build.

## 05:00 UTC -- decisions 5 and 6: the heal's words, and where restoring lives

The heal's card names the emulator and keeps the corrupted file as a real
version (D-CLOUD-114) -- the maintainer's question "could we ever be wrong"
has the honest answer *once, for a full-size file of one byte*, which is why
the file is kept and the second occurrence asks. Then a reframing of the
labels question: restoring is conflict resolution's business (#25, after
the wizard), the settings row is about keeping, and corrupted or legacy
entries are trash not yet emptied -- recoverable, listed last, never
promoted (D-UI-043). Least surprise (D-UI-042) was written down between
the two, at the maintainer's word.

## 05:40 UTC -- the nine decisions are done

Seven of the council's proposals answered as recommended, two against:
OFF keeps a transaction copy (the council's "off means off" rejected), and
the 90-day cap dropped. The walk itself produced more than the nine: the
launch waits for a bounded sync (109), the stage is one cache (110), the
bound watches progress (111), the offline benchmark (112), two transfer
contracts (113), the exit card says syncing (D-UI-040, #138), the offline
reminder (D-UI-041, #139), least surprise as a principle (D-UI-042), the
heal names the emulator and keeps the file (114), restoring belongs to
conflict resolution (D-UI-043), the cutover residual accepted (115), the
limits measured on the VM against WebDAV and SFTP with a realistic exit
(116), the four refinements confirmed plus a warning in the rules file
(117). Lesson for next time: "budget", "stage", "P-4" meant nothing to
the maintainer until put in terms of what they would see; lead with that.


## 06:50 UTC -- the exit card framed on the VM, and one more defect

`d94ca7b159` (ES `6b49a663aa`, #138) built; the SYSTEM squashfs carries the
two new strings and none of the old. The card was framed on guest c the way
a player meets it: the NES probe launched from EmulationStation (through
GO TO MANUFACTURER, then the save-state manager's START NEW GAME -- the
first attempts opened PICO-8's Splore, and `esc` does not quit RetroArch
on this image, so the game was ended through input_sense's own
execute_kill), then screenshots every half second. Against a live remote
the run took a second and the card read SYNC SAVES / COMPLETED; against
the dead port it stayed up five seconds and read SYNCING SAVES TO THE
CLOUD -- with rclone's two stats fields run together, "2.0sTransferred:",
now #140. The runner is on the image from a fresh pair; H700 building.

## 07:40 UTC -- d94ca7b159 passes the runner; H700 built, not staged

`tools/vm-qa` on `d94ca7b159` from a fresh pair: scripts, round-trip
100/100, exit, walks 13/13 -- the first image to pass all four on its first
run, the runner now seeding its own fixtures. The startup card framed
unchanged (SYNCING SAVES AT STARTUP); #138 closed. H700 `d94ca7b159` built
(tar `a9b762c770fb38be...`), the SYSTEM checked for the strings; it waits for
the maintainer's per-device yes. Nothing touched the RG35XX SP.
