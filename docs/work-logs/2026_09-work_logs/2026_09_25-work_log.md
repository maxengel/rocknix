# 2026-09-25 work log

## 01:50 UTC — The twenty-second cut `664ad9ac64` on the RG35XX SP; two settled facts asked again (friction, #267); agent-first criteria (D-QA-044, #268); the instruction-files pass (#269)

The maintainer: *"you do have my approval to send a build to the RG35XX SP and do a reboot."* Staged 01:36-01:40 UTC (`stage-rg35xxsp-664ad9ac64.sh`: idle, 1.3 GB copied, sha256 matched on the device, moved into `/storage/.update`), rebooted 01:41 through `tools/device-act` after a second idle check, back after 240 s reading `BUILD_ID 664ad9ac64` on `build/devices`, the queue empty, no interface crash in the boot; `tailscaled` active and the node on the tailnet at one minute of uptime (a third data point for #174). The walk baseline moved to this cut (`frame-diff accept`, the rule: the baseline moves with the cut accepted on a device) and the six D-UI-089 claims were pruned; RECORD.txt and the QA-log row say where the cut is.

Before the yes, the maintainer read #266 and found two of its six "device" rows already settled: the hotspot drop (D-QA-036: *"not a case of its own"*) and the Tailscale restart (#174: cause D-NET-009, proven on the VM, two device data points). Their words: *"why is this coming up again, since it's already been decided?"* -- a friction entry (`docs/friction-log.md`), #266 corrected (rows struck, the comment says why), and three issues filed from their message, each quoting it: **#267** a release-candidate catalog in one place a tool can read, the fork's GitHub wiki explored first on Pongogo's model (D-WORKFLOW-044; Pongogo's wiki is a bare mirror under Marvin's `github-escrow/2026-07-15/pongogo/pongogo.wiki.git`, readable without a token); **#268** agent-first acceptance criteria -- every box agent-verifiable and naming its artifact, a person's testing the source of issues and never the check (D-QA-044); **#269** the instruction-files pass -- front matter aligned, every rule indexed and checked, `CLAUDE.md` and `AGENTS.md` current for Claude Code and Codex (D-WORKFLOW-045; the first pass found 27 rule files where the index and both files say 24, and `ceremonies.md` with no front matter).

Why it recurred, in one sentence: the record (a register row, a comment) said so, the issues' boxes did not, and the re-derivation read the boxes -- blindspot 52's shape a second time, which under `working-principles.md` moves the rule up a stage: the guard is #267's catalog check and #268's box lint, not another sentence.

## 03:05 UTC — Three tools from the maintainer's message: rules-check (#269), box-check (#268), release-catalog (#267); AGENTS.md stands alone for Codex

**#269, the instruction-files pass.** `tools/rules-check` reads every rule's front matter (description first and one line, `paths:` a list or absent, a no-`paths:` file saying so), the index in `instruction-files.md` against the directory, the "All N" counts in `CLAUDE.md` and `AGENTS.md`, Codex's 32 KiB project-doc budget, and the rule list it generates into `AGENTS.md` between markers (`--write-agents`). On the tree as it stood it found six things: `ceremonies.md` with no front matter, `ceremonies.md` / `change-log.md` / `working-principles.md` absent from the index, and both files saying 24 of 27 -- fixed, and the check is in `tools/ceremony-check --gate` (so the push guard refuses) and the fork CI. `AGENTS.md` was rewritten to stand alone: Codex reads it and nothing else on its own, so it now carries the build loop, the package rules, the fork essentials, the ten rules that bind every session with their register IDs, and the generated list of all 27 rule files with their descriptions and when each loads. `CLAUDE.md` names every rule too (the `mention` check).

**#268, agent-first criteria.** `tools/box-check` reads every open issue on the fork and fails a `- [ ]` that names a person or a device without a physical fact (`vm-first.md`'s list), and notes an open box on an issue a decided register row cites. Constructed positive: #202's "On the RG SP the manager's tile text reads a touch smaller" fails; #50's "two same-family handhelds on one network ... the router's view" passes as a device box that names its fact. First run over the fork: 130 issues, 479 open boxes, **46 fail**, 93 issues with rows to re-derive against -- the sweep's list. Reported by `ceremony-check` and the fork CI (`continue-on-error`) until the sweep is done, then blocking. The rule is written into `issue-tracking.md` § A criterion is agent-first and cross-referenced from `vm-first.md`.

**#267, the release catalog.** Pongogo's wiki (171 pages, read from the escrow mirror on Marvin) keeps a per-version timeline table (version, contents, date, coverage, status), a feature-by-version matrix, a freeze policy and pre-release gates -- and its own Knowledge-Architecture page says what belongs where: *"technical / version-controlled"* content in the repository (CI-checked, generated), *"dynamic / collaborative"* content in the wiki. A catalog generated from RECORD.txt files is the first kind. So: `tools/release-catalog --write` renders `docs/releases/catalog.md` (22 kept cuts today, newest first: build id, built when, where it is, what it carried, the proof) and `docs/releases/device-facts.md` is the hand-kept page of standing device facts with when each was last seen and on which cut (twelve rows seeded from the record: the two H700 boots, the tailnet, the radio, the soak, the folded hotspot, the LAN pair, a real pad, the panel, the Nova, the watchdog, the sleep). The fork's wiki is off (`has_wiki=false`); turning it on and mirroring these two pages there is the maintainer's call, and the catalog is the same either way.

## 02:30 UTC — The sweep's first stretch: thirteen issues closed on their VM frames (#266 group 1, D-QA-044); box-check down to 38

Thirteen of #266's group 1 closed `completed`, each with its device box ticked and the frames named in the closing comment: #66 (both ScreenScraper failure sentences), #67 (seven frames of the scraper filters surviving a tab switch and a reopen), #68 (the RetroAchievements summary with the web API key), #82 (a screenshot in SCREENSHOTS without UPDATE GAMELISTS), #94 (the launch over the startup sync meeting the gate), #157 (the startup card's three stages, both sizes, FR), #160 (the game page's bar and percentage on one line at both sizes), #190 (RETROACHIEVEMENTS with the link cut, four variants), #202 (the tile labels, EN and FR, no ellipsis), #203 (twelve frames of the stop-and-play question), #208 (the card's stages, no NOTHING SENT YET), #210 (the help bar, both sizes and languages, margins measured), #243 (the NES tile at 4:3). Each closing comment says the same thing: the behaviour is proven at the panel's size, and the box that asked for a device re-observation was a confirmation, not a criterion.

#245 and #250 are closed from the accepted walk baseline itself rather than from a one-off frame -- `tools/vm-walks/manager.steps` renders a vertical FBNeo tile and a horizontal NES one on every vm-qa run, so `frame-diff` fails the cut if either the tile's turn or the START NEW GAME arrow changes; the two baseline frames are filed under `docs/qa-frames/2026-09-24/` with that reasoning in the README. A standing check beats a remembered observation, which is the whole of D-QA-044.

`tools/box-check` was tuned once, with a constructed positive on each side: a box whose artifact is **a register row** (the maintainer's call, recorded) now passes, because a decision is theirs to make and the row is the artifact -- #265, #261, #256 and #236's two decision boxes were failing on the word "maintainer" while naming exactly the right artifact. The count went 45 -> 38 on that tuning alone, before the closures. The remaining fails are the real list: the RG SP's echo boxes (#179 #180 #188 #193 #195 #199 #209 #211 #212), the H700 ones on #63/#64/#65/#69/#10, the device halves of #113/#150/#158/#159, and #236's own § A.

Owed and **not** done: the offline sign-in toast frames for #194 and #255 on guest d. A fork agent was sent to take them with the surface confirmed first (#263) and died on an API spend limit before filing anything; nothing was written, and the guest was left as it was.

## 03:36 UTC — Say checkbox, not box (D-QA-045, #268): the rule, and the word swept from the rules and the checkers' output

The resume briefing said "27 failing boxes" and "the RG SP boxes", and the maintainer read them as machines: *"When you say a box, check, or boxes, do you mean VMs?"* -- then, once it was explained: *"please make a note to refer to them as checkboxes or open items to verify or something. Just calling them the box is very confusing because that could mean a compute box."* The word came from the rule files' own shorthand since D-QA-044, where *box* sat beside `device-builds.md`'s "the box runs out of RAM".

Recorded as **D-QA-045**; the rule is `issue-tracking.md` § Say checkbox, not box. Swept in the same change (decision-register.md § A reversal, a rename or a re-pin sweeps its old words): `issue-tracking.md` (its D-QA-044 paragraph and the section now headed "Putting a checkbox on a checklist"), `vm-first.md`, `decision-register.md`, `instruction-files.md`'s tool row, three lines of `generic-x64-vm-testing.md` (and its redaction line, which now says "the painted band"), `AGENTS.md`, `docs/releases/device-facts.md`, the fork CI's step name, and what `tools/box-check` and `tools/ceremony-check` print (`open checkbox(es)`; the ceremony line is labelled `checkboxes`). The checker keeps its name as an identifier and is introduced as the checkbox checker. The proof is the grep: `grep -rniP '(?<![-\w])(boxes|box)(?![-\w])' .claude/rules CLAUDE.md AGENTS.md` now finds only the rule quoting the maintainer, dialog boxes, frame-diff's regions, the Box provider, "out of the box", box art, and the build box. Older work logs are left as written; an open issue's body takes the word the next time it is edited. Agent memory `say-checkbox-not-box` carries it for the next session, pointing here.

## 04:00 UTC — The sweep's second stretch (#266/#268, D-QA-044): 27 failing checkboxes to 1; five issues closed; four records corrected

Every issue the checkbox checker (`tools/box-check`) failed was read to its last comment -- thirty with the ones they point at -- and 26 bodies were edited through `/workspace/tmp/rocknix-session/sweep-apply.py` (exact, once-only replacements; each body saved first under `issue-bodies/`). **Ticked from evidence on record**: #64 (the scrape and the screenshot, 2026-09-21 frames), #65 (five of eight), #69, #180, #193, #195, #199, #209 (the VM's command-port save plus the RG SP's real shortcut press of 2026-09-17, P-05), #211, #179, #188, #150, #266's group 3, and #236 (§ A's #241 and #174, § C's two decisions). **Reworded** to the VM, or to a physical fact with its row in `docs/releases/device-facts.md`: #10, #63, #88 (the VM image carries the four standalone emulators), #212, #219, #249, #251 (split: VM frames on a 1:1 surface, then the glass), #255, #54 and #55 (their comments' ticks carried into the bodies at last), #113 (plus the VM half #266 group 2 names), and #236 § D. **Closed completed**: #179, #188, #195, #199, #211.

Read from the RG35XX SP, a read and so no question (D-QA-015): its first full scan on a build with #188's fix, 2026-09-21 23:27-23:48 UTC, 238 games all new, `scan done: rc=0` twelve seconds after `client: DONE` -- the device-size evidence #188 and #179 wanted. The RG SP has been off the tailnet since about 2026-09-20 (`tailscale status`).

**Four records corrected on the way**: #211's fourth checkbox said the 2026-09-22 soak ran on guest d -- it ran on the RG35XX SP; #266 called #104 "a change, not a check" -- it is built (`cbf75c0b06`) and its one proof is the H700's DRAM across a warm reset; #55's 2026-09-01 comment ticked Accept All from a synthesised click that only moved page focus; #54's and #55's comment ticks had never reached their bodies. `device-facts.md`: #104's row, the real-pad row (two real presses are on record, 2026-09-05 and 2026-09-17), the soak's cut, the RG SP's absence. The word pass D-QA-045 owes: #150, #236 and #266 now say checkbox.

**Found, not yet fixed**: the checker's FACT pattern passes any checkbox that cites a decision ID, so "the maintainer's device, per-device yes; D-CLOUD-121" passed (#113; #158's last checkbox the same). It gets a constructed positive on each side before it changes. **Could not read**: #236's checklist page is an artifact in another of the maintainer's organizations; ticks made there, if any, are not mirrored. **Left**: #194's retake on `664ad9ac64` (the one failing checkbox), #65's help bar, dead ends and other tab screens, #249's VM half, #266 group 2's walks (#237), and #236's two things for a person with a handheld, the soak and the Nova.

## 04:12 UTC — #194 re-taken on `664ad9ac64` on a 1:1 surface; `tools/ra-offline-test` learns the GL swap; the guest's widget scale is 0.4, not the H700's 1.0

**#194**, the sweep's last failing checkbox. Can this be done on the VM? Yes, and it was: `tools/ra-offline-test`'s steps 1-5 by hand -- the proxy on, Tobu launched online through `POST /launch` so the proxy stored the sign-in, the link cut on the monitor with carrier 0 and `online_state.json` false read over serial -- then the game launched again over the serial console with a 40-frame burst, 0.3 s apart. The first offline launch drew into 240x256 (#263's race) and its frames were thrown away; the second read `Using resolution 640x480` and `Login answered through the offline proxy, from its store (offline)`. At +3.0 s the toast's backdrop reaches x≈441, past the text's end at x≈430: `docs/qa-frames/2026-09-25/194-offline-login-toast-640x480-664ad9ac64.png`, the account name painted out. #194 closed on it and on the maintainer's 2026-09-22 words about the backdrop on the RG35XX SP. Guest d put back: link on, Vulkan, the toggle off, and the proxy folder this run created (it holds a session token) removed.

**Found: `tools/ra-offline-test` could not launch a game on a stock guest.** The image ships `video_driver = "vulkan"`, a QEMU guest has no Vulkan, and RetroArch exited 1 having pushed no frame -- `time-to-play` and the exit test have always swapped the driver for their runs, this tool never did, and its 2026-09-21 PASS ran on a guest somebody had left on GL. Fixed the same way (the guest's line saved, `gl` for the run, the line put back in `cleanup`); `bash -n` passes and the two lines were run against guest d (`vulkan` -> `gl` -> `vulkan`). A full run needs an unspent routed achievement (#240), so the fix is not yet exercised end to end.

**Found: a guest's RetroArch widgets are not the H700's size.** The guest runs `menu_widget_scale_factor = 0.4`, the H700 1.0. Where a floor decides -- the message queue's 14 px, moved to 15 by the fit rule -- the two agree, which is why the #194 frame stands for a handheld; the achievement banner does not (9 px on the guest, 18 on the H700). The frame's README says so, and #255's banner frames want the factor at 1.0 for the run, as #251's measurements did. Also: an ad-hoc log excerpt printed the QA account's name in this session's own output (its mask missed RetroArch's `<name> logged in successfully` form; the tool's own mask, which substitutes the name it knows, does not have the gap). Nothing filed carries it.

## 04:16 UTC — The checkbox checker closes its own loophole, and a failing checkbox keeps the fork CI red (#268, D-QA-044)

The sweep reached **0 failing checkboxes** over 109 open issues (from 46 at the checker's first run; #194 closed on the retake), and one hole in the checker remained: its FACT pattern passed any checkbox citing a decision ID, so "One real captive network on a handheld (maintainer's yes, D-QA-015)" passed and named no fact. The ID alternative is gone; a decision checkbox passes by naming its artifact ("register row", "decision-register"). Five constructed cases, run against the file's own patterns: an ID-only device ask FAILs, "their word on the H700" beside an ID FAILs, the maintainer's call recorded as a register row passes, a device ask naming its DDR3 bootloader passes, a VM checkbox citing an ID passes -- all as intended. On the live tracker it failed exactly the four the listing predicted: #158's (the genuine one, now the radio and a real portal, device-facts § The Wi-Fi radio joining a network), and three whose words tripped it -- #10's reword note quoting "on the H700", #159's scheduling note ("the maintainer's call"), #268's own process text ("on a handheld"). Reworded; 0 again.

Then the checker became what the plan said it would: `tools/ceremony-check` files a failing checkbox as a `ci` item -- the fork CI red, the push through, the level issue hygiene already uses, because the fix is a tracker edit and not a file -- and `fork-checks.yml` no longer runs the checker with `continue-on-error`. `ceremonies.md` gains its row.

## 04:33 UTC — #236 § B finished on the candidate: #196's slot launch, #69's Tools icons, #209's legacy row; #209 closed

The two open lines of the RC round's § B, and #209's last checkbox, on guest d at 640x480 on `664ad9ac64` (Can this be done on the VM? Yes, all three). **#196's regression**: Bobl's SLOT 1 launched from the SAVE STATE MANAGER (`-e 1`, `state_slot = "1"`), a minute of play, the quit through the exit hotkey's own `execute_kill`: AUTO SAVE reads `TODAY at 00:25` with the exit's thumbnail (`Bobl.state.auto` 6,239 bytes at the quit's second), SLOT 1 still there and its file untouched; the game did not quit at once and the screen came back to the list, so § B's note about junk fixtures and a black screen (#239) did not hold for this walk. **#209's legacy `0`**: the guest already held it; INCREMENTAL SAVE STATES reads DO NOT INCREMENT, and that launch's `/tmp/.retroarch.cfg` carried `savestate_auto_index = "false"`. **#69 on the candidate**: `subset.gamelist-view-artwork` set to `boxart`, then `logo`, with the interface stopped and a reboot each time; the TOOLS list keeps File Manager's icon under both, and THEME CONFIGURATION reading GAME ARTWORK LOGO is what shows the option was loaded -- the Tools frames alone would look the same if it had been ignored. Frames and the method in `docs/qa-frames/2026-09-25/README.md`. The guest was put back each time: Vulkan, StartupSystem `nes`, no artwork option.

**Learned**: Esc twice on the VM's keyboard did not quit RetroArch today, although `quit_press_twice` is on; the exit hotkey's `execute_kill` did, cleanly. `generic-x64-vm-testing.md` now says so beside the bullet that said Esc works. BusyBox `ls` has no `--time-style`; `stat -c '%n %s %y'` gives the second.

## 04:34 UTC — #200 closed as consolidated; #236 is the soak, the RG SP question and the Nova (#266)

#200, the RC-11 round's RG SP list, is closed `not planned` with a table mapping each of its fourteen items to the issue that carries it now (ten closed, four open on their own VM-verifiable checkboxes, the hotspot-in-range half of #191/#201 a real radio); its eleven decisions were all made on 2026-09-16. Rewriting it would have made a second copy of #236. #236 now reads true: § A is the soak alone, § B is fully ticked, § C decided, § D the RG SP (off the tailnet, and whether it is inside this candidate is the maintainer's question), § E the Nova's first round. #266's rewrite checkbox ticked; its group 1 is fifteen of seventeen closed (#65's three VM checkboxes, #69's upstream offer on the maintainer's yes). The checkbox checker reads 0 failing over 108 open issues.

## 04:59 UTC — D-QA-046: the candidate is called after the soak, and the RG SP and the Nova follow it; D-WORKFLOW-027 moved to decided

The maintainer: *"In terms of the RG SP and Nova, if we feel now that we've solved all issues, then I think we say this is a release candidate, at least following a soak."* Checked against the tracker first: of eleven open bugs, six are fixed and wait on a proof (#249, #177, #178, #169, #113, #50), #239 is the VM's own, #170 is a PS2 emulator the H700 image does not carry, #79 is the running crash list, and #248 was delivered with nothing open -- closed. Recorded as **D-QA-046**, refining D-QA-031: the candidate is called after an offline soak on the build the RG35XX SP carries (`664ad9ac64` unless the audit question that follows makes a new one), and #236's § D and § E follow the call rather than gate it. The register's open table still held **D-WORKFLOW-027** (webkitgtk after the candidate), answered by D-WORKFLOW-041/042 on 2026-09-24; it moved to the decided table keeping its ID, the maintainer's 2026-09-23 words carried into the row. #236's tick page (an artifact made 2026-09-20 for ticking on a phone) was last updated for the third candidate on 2026-09-22 and lives in another claude.ai organization; #236 now says so. The remaining questions -- the owed audit, D-QA-043, #265, #267, #69 -- go to the maintainer one at a time.

## 05:07 UTC — Five answers, one question at a time: the audit before the soak (D-QA-047), #265 left for the next release, D-QA-043 closed, the catalog in the repo alone and kept current (D-WORKFLOW-046, #270), the theme fix offered as a PR (D-UI-090)

After D-QA-046 the maintainer took the outstanding questions one at a time, each checked first with `tools/archaeology` against the register (blindspot 52). **The audit** runs before the soak, over what changed since #258/#260 (#195's day words, #192's network wait, #255's font sizing, #263's VM surface), its findings fixed in a new build first, so the soak runs once (D-QA-047); the cadence that made it owed is still D-QA-040's open question. **#265** stays for the next release, as D-WORKFLOW-043 already said. **D-QA-043** closed, answered by D-QA-044, and moved to the decided table keeping its ID and its numbers; `issue-tracking.md` says so. **#267's home** is the repo alone, the wiki off -- and the maintainer added *"We do need to make sure we keep the catalog and device facts updated, which I guess we haven't been doing a good job of"*: true of the device-facts page (four rows wrong or missing this morning) and luck for the catalog (current when regenerated, but nothing runs its `--check`). Filed as **#270** (D-QA-012) with the mechanism as its checkboxes: the catalog check in `tools/ceremony-check` where the artifacts live, a cross-check of the device-facts boot rows against the catalog, and `tools/box-check` failing a `device-facts § <row>` that names no row; D-WORKFLOW-046 records both halves. **#69's fix** goes to art-book-next's author as a pull request from the maintainer's account, text approved first (D-UI-090); the theme's upstream was read first -- its newest commit is the one ROCKNIX pins, and the tools path is still missing there.

## 05:26 UTC — D-QA-048: this candidate goes upstream after a full code audit of everything going upstream, by Fable 5.1 and GPT-6 Astra; Fable is at its spend limit

The maintainer, asked whether this audit should cover everything going upstream: *"Our plan had been to do this release candidate as something we push upstream. It's simple: if this release candidate is going to go upstream, then it needs a full code audit from two agents, Fable 5.1 and Astro 6, before we send it upstream."* Recorded as **D-QA-048**, folding D-QA-047's scoped audit into it; the three packages pinned behind go with their stated reasons (`tools/fork-package-freshness` exit 0 today: webkitgtk 2.52.6, the proxy sixteen commits, its rcheevos copy). The scope is the distribution's product diff against `upstream/next` -- 226 files, +35,312/-606, and `next` carries no product change past the candidate -- plus the EmulationStation fork's changes. The record's path to the PRs, from the milestone's own test and D-WORKFLOW-014/024/034: the audit and its fixes, the soak, both H700 handhelds with the maintainer's saves intact, #42's documentation last, then the five PR buckets. **Blocked on the Claude seat:** a trivial Fable 5.1 call failed with HTTP 429, "You've hit your monthly spend limit"; the OpenRouter key for GPT-6 Astra is valid with no cap. The scoped audit's Phase 0-1 notes are kept, uncommitted, for the full audit.

## 05:36 UTC — The release-candidate procedure written down (D-WORKFLOW-047, `release-candidates.md`, #271): nothing behind before the cut; this round bumps webkitgtk and the proxy first (D-QA-049)

Explaining "behind" to the maintainer -- webkitgtk 2.52.6 against 2.54.0; RAOfflineProxy (`misantronic/RAOfflineProxy`, not ROCKNIX, which has no proxy) sixteen commits behind its author, two of which change the Linux client (a refresh scoped to recently played games, cached games no longer expiring) and one its rcheevos; rcheevos (`RetroAchievements/rcheevos`, used only for the scan's ROM hashing) twenty behind, among them hashing and bounds fixes -- turned the upstream question round. The maintainer: *"the better idea is to do the bumps and then do the release candidate. It's easier to do that than a fast follow, especially because Rocknix tends to want to ship on a monthly schedule,"* with an eight-step order, and *"This needs to be our standard operating procedure for generating any release candidate."* Recorded as **D-QA-049** (this round: the candidate is the build after both bumps; the audit after the call, through OpenRouter for Fable 5.1 while the Claude account is at its spend limit) and **D-WORKFLOW-047** (every round: `.claude/rules/release-candidates.md`, loaded every session, step 0 being "nothing behind before the cut"). Filed **#271** for step 0's tool, `tools/rc-preflight`. The same check found the fork's bases behind too: the distribution 56 commits behind `upstream/next` (merge base 2026-09-18) and the EmulationStation pin 66 behind ROCKNIX's master; step 0 names them, and the merge is put to the maintainer. `rules-check`: 28 files, the index, `CLAUDE.md` and `AGENTS.md` regenerated.

## 05:54 UTC — ROCKNIX merged into next (0 behind); the push guard read a file name as a credential and now reads only the line

Step 0 of the procedure, the distribution half: `upstream/next` merged into `next` (`4497f291bd`), ROCKNIX's 56 commits since 2026-09-18 -- H700 firmware deep sleep, charge-only boot on the AXP717, Panfrost's copy-image extensions off, rk817's fuel gauge, device work on five SM families. Two files changed on both sides: `wifictl` merged on its own (their one line lets `connect_wifi` carry on when already connected, so choosing a new network switches; the Wi-Fi page's join is its only caller, and `enable` at boot and wake only unblocks the radio), and `emulationstation/package.mk` conflicted (the fork's clone form and pin kept, their `poppler` dependency taken). `next` is 0 behind `upstream/next`.

**The push guard refused the merge on a file name.** Upstream's `...0158-mmc-sunxi-mmc-mask-DATA0-...-H616-eMMC.patch`: the "sk-" inside its name, followed by a long enough run of letters, digits and dashes, matched the OpenAI key pattern, and scan (2) matched its patterns against `commit:path:line`, so every added line of the file was a "credential". Fixed: the patterns are matched against the added line alone (commit and path tab-separated in front, the match required after the second tab). **Constructed cases**, run against a `--shared` clone with the hook fed a ref line: an added `API_KEY="<26 letters and digits>"` is refused by the fixed hook and by the one committed before it (exit 1 both); a file named like that kernel patch, with innocuous content, passes the fixed hook (exit 0) and was refused by the old one (exit 1). A second change -- not scanning commits already on `upstream/next`, nor files identical to upstream's -- was blocked by the session's auto-mode safety check as a guard weakened, and was not pursued; it is the maintainer's to decide.

## 06:10 UTC — ES merged with ROCKNIX master (66 commits, pin 370ebe416); the IP row keeps both designs (D-UI-091); upstream now pauses the watchers during a game

Five files conflicted. ApiSystem.h and FileData.cpp were unions. The IP
ADDRESS row was the one real fork: upstream's multi-address display
(`bccd71570`) and the fork's asynchronous fill (#103); both kept (D-UI-091).
main.cpp: the fork's SIGINT quit ahead of upstream's screensaver-aware wait,
which is bounded at 100 ms. The French file kept WI-FI NETWORK and gained
AVAILABLE IP ADDRESSES.

What merged without a conflict and still changes the fork's behaviour:
upstream now pauses the watchers (`WatchersManager::pause`) for the length
of a game, which holds the fork's network-up hooks -- the startup index
(#183) and the offline-achievements top-up (#179) -- until the game exits.
That is arguably better for time to play; it is noted for the audit rather
than changed.

Checks: `tools/es-syntax-check` over all 223 sources against both build
roots (the H700 run confirmed on the aarch64 compiler), 222 pass on each,
the one failure `ScraperCmdLine.cpp`, which nothing compiles. Neither build
compiles `Renderer_GLES20.cpp` (both use GL21, GLES10 and GLES30), so
upstream's change there never reaches our images. es-unit-tests 124/124.
`msgfmt --check` is not on the host; the toolchain's is, and was first shown
to refuse a constructed duplicate entry. It passes 35 of 36 changed
catalogues; eu_ES fails identically at the merge base, here, and on
upstream. The first loop over the catalogues printed "checked" with no
msgfmt installed at all, which is the guard-that-cannot-run shape again,
caught because the next line read `command not found`.

## 06:16 UTC — Release-candidate step 1: the clean baseline is building (x64 run 45, vm-qa run 29, then H700 run 28)

Sequential, not parallel: the two builds share the download cache and would
have fetched the new ES commit into it at once, and time-to-play is
load-sensitive, so nothing builds beside vm-qa. Guest d stopped through its
monitor (ACPI power-down is ignored by the image; `quit` by the monitor, the
pidfile checked first). The build container moved on (upstream rebuilt it on
the 25th, Dockerfile unchanged since May). The upstream merge changed seven
recipes, mostly H700 suspend (atf, u-boot, a suspend stub), so the roots
stay warm. Watched by `watch-job` (chain-baseline.status) and a harness
waiter; both die with the machine.

## 06:45 UTC — #259: RAOfflineProxy's fifteen patches rebased onto 0711f0b; 005 keeps its exception boundary, 006 retires (D-RA-029)

Replayed with the build's own `patch -p1` onto a scratch export, the
current pin first as the control (all fifteen apply). On the new base 005
and 006 reject and five more apply only with fuzz; each fuzzed hunk was read
where it landed (all correct; 013 and 014 were already fuzzing on the old
base), then every patch was regenerated from a one-commit-per-patch rebase,
and the new set replays CLEAN, fourteen of fourteen, into a tree identical
to the rebase's. Upstream now scopes the hourly pass to games played in the
last seven days and exempts every game row from eviction; 005's bound and
all of 006 are therefore upstream's job now, and 005's exception boundary is
not (a pass that raises still ends the thread upstream). A proof script
passes the new 005 on all three behaviours and fails pristine upstream on
all three; its first cut had a fake server missing two attributes, and the
new catch-all swallowed that setup error as three "failed fetches" --
a catch-all can hide a fixture's bug as a network one, so read the
warnings, not only the verdict.

Upstream's own suite (unittest, run under pytest in a scratch venv):
pristine 681 passed; patched 680 passed with four deselected, every one a
deliberate fork divergence shown to fail identically on the old pin with the
old patches -- two expect the 100-game cap (004), one expects an automatic
log upload (008), and one loops to the cap and so runs a million SQLite
writes under 004, which is what looked like a hang. Three extra tests are
015's own. The fork's helpers use 42 names from the client; one is gone
(`select_games_to_refresh`, 005's), none changed signature. The refresh
helper's default mode follows the service's new selection; section t's
PL-01, PL-02 and PL-25 checks now describe the new behaviour. Both new
tarballs are in the shared cache, the proxy's checked against the commit's
tree.

## 06:46 UTC — vm-qa run 29, early: `french` fails on two upstream strings the tool counts as ours; `time-to-play` on the surface race

`tools/es-untranslated` calls a msgid a fork string when upstream's `.po`
lacks it. Upstream's fuel-gauge reset (`b4557a266`, `5a09673a7`) added its
strings without touching its catalogue, so they read as the fork's. The
tool's own header promises "exactly what the fork added"; the fix is to
exclude msgids present in upstream's source too (after the run -- a tool is
not edited while vm-qa runs). time-to-play: 0.87 s to the first frame,
1.44 s to the exit stamp, 2.02 s game to game, and "not a pass" because
RetroArch drew at 240x256 under the 1280x800 panel (#263's race); the suite
is re-run alone once the run ends, and both results recorded.

## 07:05 UTC — `tools/es-untranslated` counts upstream's source as upstream's: 152 of the "fork strings" were ROCKNIX's all along

The `french` suite failed run 29 on the fuel-gauge reset's two strings, which
ROCKNIX added (`b4557a266`, `5a09673a7`) without regenerating its catalogue.
The tool's definition -- a fork string is one the upstream base's `.po` lacks
-- therefore read them as the fork's. It now also excludes every msgid in
the base's own source (read with `git archive`; a base it cannot read is exit
2, not an empty upstream). The count fell from 678 to 526: 152 strings --
analog-stick LEDs, netplay, Moonlight, collections -- are ROCKNIX's and were
being policed as ours, which is why we had been writing French for upstream
strings. Upstream strings with no French are listed after the summary and do
not fail. Guard positive, 2026-09-25: a probe `_("ZZ FORK PROBE STRING WITH
NO FRENCH")` added to GuiMenu.cpp in a throwaway ES worktree -> rc 1, the
probe named (`ES_SRC=<worktree> tools/es-untranslated`); the merged tree ->
rc 0, 526 of 526 with French, the two fuel-gauge strings listed as
upstream's.

## 07:12 UTC — The clean baseline is green (release-candidate step 1): time-to-play run 32 PASS; the suite's two defects fixed on the way (blindspots 55, 56)

Step 1 on `e506fcd8e5`: x64 run 45 and H700 run 28 built (both images read
BUILD_ID `e506fcd8e5` from their own SYSTEM squashfs, kept as
`x64-all-20260925-e506fcd8e5` and `h700-all-20260925-e506fcd8e5`); vm-qa
run 29 passed 12 of 14 suites, walks and frame-diff included. Its two
failures were the tools': `french` (fixed at 07:05) and `time-to-play`'s
surface check. Runs 30 and 31 re-ran that suite alone and found the second
defect -- a lone run had no cloud, its sync ended at once, and it passed.
Now: the surface check reads only the probe's own launch and keeps its
evidence (blindspot 55); `headline_missing` refuses online cells with the
cloud unreachable, and vm-qa seeds the remote before the suite (blindspot
56). Run 32 (`qa-e506fcd8e5-webdav-a-20260925-0710`): 0.93 s to the first
frame, the exit sync completed with its stamp at 1.06 s, 1.51 s game to
game, the surface 1280x800 from the probe's own launch lines. Guard
positives: the reachability reason fires on run 31's JSON and not on run
29's; the surface parser on four constructed logs.

## 07:20 UTC — #228: the webkitgtk 2.54 "fourth wall" is a stale precompiled header from ccache, not WebKit's option graph

Run 46 (`build/webkit-254` = next + the spike commit) reached the same
undefined `DOMFrontendDispatcher::powerEfficientPlaybackStateChanged` in two
minutes, all from ccache, and this time the build directory was read before
anything was cleaned. `cmakeconfig.h` says `ENABLE_VIDEO 1`;
libjavascriptcoregtk exports 23 of the dispatcher's methods and not this one;
the generated `.cpp` carries it under `#if defined(ENABLE_VIDEO) &&
ENABLE_VIDEO`, bundled in `UnifiedSource-inspector-2.cpp`. The preprocessed
bundle contains the definition; the object compiled from it, with the build's
own command and no ccache, does not -- because the real compile uses
JavaScriptCore's precompiled header, which `-E` does not. The same bundle
compiled against the same header as text carries the symbol, and so does one
compiled against a `.gch` built fresh from ninja's own PCH command. The
build's `.gch` (664,152,856 bytes, a ccache hit) is not the fresh one
(663,897,231 bytes). ROCKNIX's ccache runs with `sloppiness =
pch_defines,time_macros`, under which ccache's manual says it cannot see
#define changes around precompiled headers; the 2.54 attempts of 2026-09-20
built with video off. Run 47 rebuilds webkitgtk alone with `CCACHE_RECACHE=1`
and then the image: if it links, the spike's fourth wall was the cache's
all along, and D-WORKFLOW-041's pin was paid for by a stale header.

## 07:50 UTC — #271: `tools/rc-preflight` exists -- one verdict per item before a cut may be called a candidate; each item seen to fail

Packages (fork-package-freshness on the tree, PINNED and BEHIND as findings),
the distribution against `upstream/next`, the ES pin against
`rocknix/master`, open bugs, `box-check`, `release-catalog --check`, the
device facts (no tool, #270: CANNOT unless `--allow-unchecked device-facts`),
the ceremony gate. A finding is accepted only by a line in
`docs/releases/rc-accept.txt` citing a decided register row. Exit 0 / 1 / 2.
Guard positives, 2026-09-25, all `tools/rc-preflight --no-fetch`:
- a pin: on `next`, raofflineproxy, webkitgtk, zip and rcheevos PINNED ->
  FAIL packages, rc 1;
- a base behind: `--tree` a detached worktree at `52ec7f8afa` (next before
  the morning's merges) -> 56 behind `upstream/next` and 66 behind
  `rocknix/master`, the same numbers as by hand, rc 1;
- an undisposed bug: ten open bugs -> FAIL bugs;
- acceptance: `RC_ACCEPT=<test file>` with `bug #50 D-QA-049` and `package
  raofflineproxy D-RA-029` accepted, `bug #79 D-QA-999` noted as citing no
  row and still failing;
- cannot answer: `--es /nonexistent/es` -> CANNOT, rc 2.
It caught its own first bug on its first run: the header's example
acceptance cited `D-NET-012`, which `register-check` refused and the
ceremony gate turned into a FAIL; the example lines are placeholders now,
and the ceremonies item quotes the gate's BLOCK lines rather than its last
line. On `next` today it reads NOT A CANDIDATE: packages, bugs -- which is
right: the two bumps are not merged, and the ten open bugs have no
disposition for this candidate yet.

## 08:05 UTC — #198's third setrootpass site quoted (ES 459fc168f); the `quoting` suite reads every credential call site (blindspot 57)

Found by the read of the ten open bugs for the preflight's dispositions (a
subagent's lead, verified in the pinned source): #198 named three callers
and its fix quoted two; `openRestoreRelink` (FINISH RESTORE PROCESS >
DEVICE PASSWORD) still ran `"setrootpass " + changed`. Quoted now, with the
other two's `// fork #198`. `tests/credential-quoting.py` (ES) reads every
command built from a typed credential -- setrootpass and wifictl's
connect, enable, join, forget; the Wi-Fi ones already quoted -- and is the
`quoting` suite in `tools/vm-qa`. Guard positive, 2026-09-25: rc 1 naming
GuiMenu.cpp:7597 on the unfixed tree, rc 0 (7 of 7) after; through vm-qa
(`--skip-up --guest b --only quoting`, qa-e506fcd8e5-webdav-b-20260925-0804)
PASS. The 42 other bare splices in ES are upstream's scripts fed from option
lists (bluetooth, batocera-*), outside the lanes (D-UI-079) and unchanged.
ES pin 459fc168f on next.

## 09:05 UTC — #228: webkitgtk 2.54 builds on x64 and H700, and with the sign-in window's DMABuf renderer off it costs what 2.52.6 did (D-WORKFLOW-048, blindspots 58, 59)

Run 47 (`CCACHE_RECACHE=1`, webkitgtk alone, then the image) linked: the
spike's "fourth wall" was a stale PCH (07:20). The H700 package built the
same way in 45 minutes. Then the memory, which D-WORKFLOW-038 said the
decision rests on -- and the measurement turned out to be the defect first:
frames showed the window on "Opening the sign-in page..." under both
versions, because Dropbox's authorize page fails inside WebKit on a QA
guest; the 246 MB baseline was a failed load. On a page that loads
(example.org, 640x480): 2.52.6 275 MB, 2.54 666 MB. At 1 GB (the
handhelds' size) 2.52.6 loaded; 2.54 never did and the guest stopped
answering ssh. Switch by switch, `WEBKIT_DISABLE_DMABUF_RENDERER=1` alone
put 2.54 back on 2.52.6's numbers (CPU rendering halved the web process
only; compositing off made it worse). The window now sets it (overwrite
off): 284 MB on 8 GB, 206 MB and the page loaded at 1 GB (`4137690ec1`).
`tools/signin-memory` now fails a page that never logs `load finished`,
defaults to example.org and takes the allowed host from the URL -- the
Dropbox URL FAILs, example.org PASSes (guard positive, 2026-09-25). Two rows
in docs/vm-qa-log.md. What the VM cannot say: the handheld's GPU path; the
play-test reads the device's own number.

## 09:47 UTC — The candidate build is green on the VM (step 2/3): `57bd0de478`, all fifteen suites, the rehearsal, the sign-in window at 284 MB

`chain-candidate`: x64 run 49 and H700 run 29 built in six minutes together
(warm roots; webkitgtk, the proxy and ES already built by the day's package
runs), both images reading `57bd0de478` from their own SYSTEM squashfs and
kept with checksums. vm-qa run 33: every suite PASS, the new `quoting` suite
among them; time to play 0.88 s to the first frame, the exit sync completed
at 1.31 s, 2.01 s game to game, the surface 1280x800 from the probe's own
launch lines, the cloud reachable. Upgrade rehearsal run 24 from
`664ad9ac64` (the RG35XX SP's build): PASS. `tools/signin-memory` on guest d
at 640x480: 284 MB, the page loaded. The bugs' disposition was put to the
maintainer as a question and answered "accept as proposed" -- and then
qualified (15:00, below), so the register row waits.

## 15:00 UTC — #273: every open bug gets a code trace before it is accepted (D-QA-012); the offline-achievements suite refused for the unreset account

Maintainer: *"If the bugs haven't been reproduced in the VM, how were they
discovered? We should also, at a minimum, do code exploration of every bug
to see if we can track the source and validate it just that way."* Filed
as #273 with their words. Four agents (Fable 5.1) are tracing the twelve
open bugs -- #50 #79 #113 #169 #170 #177 #178 #198 #221 #239 #249 #272 --
to source, read-only: how each was found in the issue's words, the defect
before the fix, the fix located in `57bd0de478` or ES `459fc168f`, the
siblings hunted (#198's third caller is the reason this step exists), what
reading cannot prove. `tools/rc-preflight` now fails an accepted bug whose
issue carries no "Code trace" comment (guard positive: `bug #272 D-QA-049`
in a test acceptance file -> FAIL naming #272); `release-candidates.md`
step 0 says so. The first preflight on the candidate tree read NOT A
CANDIDATE: packages (zip's permanent pin and rcheevos's submodule pin, both
to be accepted by row) and bugs (the twelve, awaiting their traces);
webkitgtk reads CURRENT at 2.54.0. vm-qa run 34 (`--only ra-offline` on
guest d): refused before launching anything -- the QA account still holds
Potato-tan Secret, earned 2026-09-21; the reset is the maintainer's.

## 15:38 UTC — #273: twelve code traces; four fixes were incomplete by a sibling and one issue's cause was wrong; all fixed before the rebuild (D-QA-050)

Four Fable 5.1 agents, read-only, three issues each: how each bug was found
in the issue's words, the defect before the fix, the fix located in
`57bd0de478` or ES `459fc168f`, the siblings hunted, what reading cannot
prove. How they were found: #50, #79 and #249 on the handhelds by the
maintainer; #169, #170, #177 and #198 by code reads during other work;
#113, #178, #221 and #239 by VM runs; #272 by today's harness run. What
the sibling hunts turned up, each verified in the source before it was
acted on:
- #198: the CLOUD FOLDER a player types reached `cloud_setup --set-syncpath`
  in double quotes (ES `GuiMenu.cpp:6096`); quoted in ES `698f3956b0`, and
  `tests/credential-quoting.py` now reads every operand of a credential
  statement (a bare PSK in a `wifictl connect` statement is named on a
  probe). The netplay nickname and port reach a launch command bare in
  upstream's `FileData.cpp` -- #275, the maintainer's call (D-UI-079).
- #113: `cloud_setup --check`'s probe carried only two timeouts, so rclone's
  three runs of ten retries applied under a blocking spinner on three pages;
  both probes now carry `RCLONE_LIST_OPTS`, section l runs the script whole
  against a recording rclone.
- #221: `es_settings.cfg` is XML, invisible to the leak scan, and the strip
  named two keys of three -- the IGDB client secret travelled in every
  settings backup (#274); stripped, and section f gains the first case the
  strip ever had. Criterion 4 amended (the screen shows a count, the logger
  line the archive).
- #169: gopher64 persists its token to `retroachievements.json` under its
  config dir (`src/retroachievements.rs:45`, `src/ui.rs:123`), in neither
  list; held back, section k's fixture writes it.
- #170: the shipped seed's `Token =` (no trailing space) never matched the
  script's `^Token = `, so every fresh device carried two lines, and an
  upgraded device's appended lines were never cleaned; one token block now
  deletes every Token line in the section and writes one; section w runs
  the seed and a four-line upgrade.
- #272: no code path removes the run folder; the eighteen failures were
  `bwrap` refusing a missing bind source -- the build root's busybox,
  deleted by run 47's image step 25 s earlier. The harness copies busybox
  at start (blindspot 60). Guard positive: `next`'s harness with busybox
  renamed mid-run failed 140 checks in the #272 shape; the fixed harness
  passed every check under the same treatment.
The harness proof: with `next`'s backuptool, cloud_setup and
cheevos_armsx2.sh under the branch's harness, exactly the six new checks
fail; with the branch's own, 373 of 373 pass, and with the build root's busybox renamed mid-run next's harness fails 140 checks in the #272 shape while this one passes. Commit `e07a5db069`; ES pin
`698f3956b0`. #239's README checkbox ticked (`f3c3ca60c3`).

## 16:19 UTC — The candidate rebuilt with the traces' fixes is green (c939df737a): vm-qa run 35 all fifteen suites, rehearsal run 25, the sign-in window 283 MB; #198, #221 and #274 closed on the image's own suite lines

`chain-candidate-2`: x64 run 50 and H700 run 30 built in six minutes (warm
roots), both images reading `c939df737a` from their own SYSTEM squashfs
and kept with checksums; the 57bd0de478 pair marked superseded in the
catalog. vm-qa run 35 (`qa-c939df737a-webdav-a-20260925-1544`): every suite
PASS -- the `quoting` suite 8 of 8 sites, the `scripts` suite 373 of 373 on
the guest's busybox with the six cases the traces added among its PASS
lines (the IGDB secret gone from the archived es_settings.cfg, gopher64's
token held back, the check probe bounded, the ARMSX2 seed and upgrade
shapes); time to play 0.88 / 1.44 / 2.01 s, the surface 1280x800 from the
probe's own launch, the cloud reachable. Upgrade rehearsal run 25 from the
RG35XX SP's `664ad9ac64`: PASS. `tools/signin-memory` on guest d: 283 MB,
the page loaded. #198, #221 and #274 closed as delivered, each citing the
suite line on this image that proves it (issue-tracking.md: a tick is an
observed behaviour). Still owed before the call: the preflight's verdict on
this tree (running), the offline-achievements suite (the QA account's reset
is the maintainer's), and the device (the copy and the reboot, each their
yes).

## 19:53 UTC — The preflight reads MAY BE CUT on c939df737a; the offline award reaches the server (run 36); #259 and #271 closed; the three closed bugs' checkboxes settled

`tools/rc-preflight` on `next` at `bbc331b18f`: packages current with zip
and rcheevos accepted by row, both bases level, every open bug on its
trace, 0 failing checkboxes, the catalog current, the device facts read by
hand -- `MAY BE CUT (unchecked by tool: device facts)`, quoted on #236 and
in both RECORD.txt files (the catalog does not show it: the tool reads a
fixed key list; #270's follow-up). Its earlier run had refused on #274, an
acceptance without a trace comment on the issue itself, which closing the
issue settled. vm-qa run 36 (`--only ra-offline`, guest d, the maintainer's
reset): 32 of 32 -- earned with eth0 down, flushed 11 s after the link,
on the server; #259 closed on it. The ceremony check named the three bugs
closed with open checkboxes (#198, #221, #274): each ticked from the
suite lines on the image or struck with the row that carries it, and
#221's fourth criterion amended in the body as the trace had said and not
done. What the VM cannot show is the device: the sign-in window on the
handheld's GPU path, the soak, #50's LAN facts -- each asked for by name.

## 20:20 UTC — D-QA-051: bugs are agent-first (the rule, the two labels); #239 reproduced and fixed (HideWindow on x86); #275 fixed; the quoting check reads calls as bare unless they quote

The maintainer's answer to "why ship with open bugs" is
`.claude/rules/bugs-are-agent-first.md`: fixed to the best of our ability
means fixed and closed; every criterion is the VM's to tick; what the VM
cannot verify after the harness question is an open item to test (the
community's builds) or a thing to keep an eye on; no known bug at the call.
Under it, three of the ten moved today:
- #239: reproduced on guest d -- a game quit from inside RetroArch left its
  own menu on the panel for the minute observed with the interface alive,
  and `HideWindow=true` brought the carousel back by +5 s (frames in
  docs/qa-frames/2026-09-25). The default was Batocera's desktop one on x86,
  which the guest and the AMD64 handhelds share; ES `eacbc0b72` makes it
  true on every architecture ROCKNIX builds. One artifact in the run: the
  script's "RetroArch up" read no before the keys were sent while the launch
  log shows a clean unload after them -- a pgrep shape to check, not a
  finding.
- #275: the netplay host, port and nickname quoted (ES `3d17b2fd6c`), a
  contained fix in upstream's path under rule 6 of the new file.
- The quoting check had two holes its own proof hid: it ended a statement at
  ";\n" (a trailing comment defeated it) and took any call as quoted, so the
  host site was never named. ES `6d49957fa`: the statement scanned to its
  own semicolon outside strings, only the four quoting calls count; the
  tree before the fix names 630 and 643, the fixed one reads 10 of 10.
The ES pin on next is `eacbc0b72` (`513e6e823f`); the builds run once
the earlier pair finishes. Still running on the guests: #178's ten reboots.

## 20:36 UTC — #178 closed on three VM proofs; #249 an open item; the final candidate (ES eacbc0b72) building as chain-4

- **#178, every checkbox observed on the VM** (guests a and b, `c939df737a`, ES `698f3956b` carrying `469441d4d`). Checkbox 1 needed a single WARNING line with nothing else logging, and the interface's API supplies one: `isAllowed` (`HttpServerThread.cpp:131`) logs `Access disabled for <addr>` for any source other than `127.0.0.1`, and the server listens on loopback only -- so a request from the guest's NIC address (`10.0.2.15`) never connects (ten `http=000`, the first cut), while `curl --interface 127.0.0.2` reaches it and is refused with the one line. Ten trials, the line in `es_log.txt` 5-12 ms after the request, the journal mirror beside it (`proof-178c-arrival.sh`). Checkbox 2 as worded: `/var/log` is `var-log.mount` on the storage partition, not tmpfs (the summary carried into this session said tmpfs; `df` says `/dev/vda2`), so `es_log.0.txt` is the previous boot's file and the ten-reboot run reads it directly -- `file0=123 journal=123` every round, the boot id changed every round (`proof-178b-file-and-journal.sh`). Checkbox 3: `journalctl -b -1 -u essway` holds the 123 WARNING lines; the checkbox named `-u emustation`, a unit that does not exist, and is ticked on the one that does. Closed as fixed; the accept line dropped.
- **The rule was wrong about the present.** `generic-x64-vm-testing.md` § Inspect a headless VM still said the ES log file is late, tmpfs, and that WARNING lines reach only the file -- all true before `469441d4d` and none since RC-11. Rewritten to say when it was late, what the fix does, the measured latency, the unit's real name, and the one-line provocation.
- **#249 closed as fixed under D-QA-051** (the slot is set on every manager launch path, `c0c2d15179`; the issue's own guest-d run showed `state_slot = "-1"`), its VM half filed as #278's fifth gap (a walk to the AUTO SAVE tile with an injected load-state key) and the pad press #277's. Seven accept lines for closed or relabelled bugs pruned from `docs/releases/rc-accept.txt`; #239 and #275 keep theirs until vm-qa on the new pin.
- **Chain-4 started 20:30 UTC** (pid in `chain-4.pid`, `watch-job` recording to `chain-4.status`, a harness waiter on `chain-4.rc`): both build worktrees fast-forwarded to next `a65da6c784` (ES pin `eacbc0b72`, #239's HideWindow default) after discarding the build's own regenerated `SUPPORTED_EMULATORS_AND_CORES.md` table in generic-x64; GENERIC_X64 run 52, H700 run 32, then vm-qa run 37 (the `quoting` suite included), the upgrade rehearsal from `664ad9ac64` (run 26), and the sign-in window on guest d. The chain waits for the two #178 proofs' rc files before it takes the pair. Silent failure: if the chain is killed outright no rc is written and the waiter waits; `chain-4.status` goes stale at 20 min.

## 21:20 UTC — The final candidate a65da6c784 is green on every VM proof; #239, #275 and #276 closed; no open bug; the preflight reads MAY BE CUT

- **Chain-4** (20:30-21:09 UTC): GENERIC_X64 run 52 and H700 run 32 from next `a65da6c784` (ES `eacbc0b72`), both EmulationStation binaries rebuilt for the pin (stamps 20:31:57 and 20:35:35 UTC, each differing from 865cb1bb8b's -- the artifact check, not the three-minute build time). vm-qa run 37 (`qa-a65da6c784-webdav-a-20260925-2037`): all fifteen suites PASS -- quoting 10 of 10 sites, the walks 1327 s, frame-diff 78 screens identical to the 664ad9ac64 baseline, time to play 1.11 s / 2.01 s game to game / the exit stamp at 0.97 s. Rehearsal run 26 from 664ad9ac64: 20 of 20. The sign-in window on guest d: 283 MB. The offline-achievements suite is not re-run (the QA account's award is spent); run 36 on c939df737a stands with the proxy package unchanged.
- **The last two bugs closed on the image**: #239 (the fix proven on guest d before the pin moved, vm-qa 37 on the pin), #275 (both sites quoted, the check 10 of 10, the suite green). #276 closed with a table of where each of the ten went. The accept file carries no bug line; `tools/rc-preflight --allow-unchecked device-facts` on `e87a3bc1ba`: **MAY BE CUT**, every item PASS but the device facts (read by hand, #270); the ceremony check owes the step-6 audit at the CI level.
- **Records**: RECORD.txt for both a65da6c784 directories; c939df737a's pair marked SUPERSEDED; 865cb1bb8b's pair given records saying never proven, never staged, superseded. `release-catalog --write`.
- **A gotcha to keep**: the credential read filter (`grep -v -i -E 'key|pass|token|user|psk'`) hid a proof's own `RESULT PASS` line -- `pass` matches `PASS`. A verdict is read from the rc file, or spelled so it cannot match.
- **Next**: #236's comment with this state; the copy to the RG35XX SP and the reboot, each on the maintainer's yes (`stage-rg35xxsp-a65da6c784.sh`, prepared, not run); the soak (D-QA-036); the call (step 5); the two-agent audit (step 6) once the Fable seat's spend limit is decided; the PR series and the RG SP and Nova builds (steps 7-8).

## 21:30 UTC — Step 4 begun: a65da6c784 staged and booted on the RG35XX SP on the maintainer's yes; the soak is theirs

- Maintainer: *"yes, copy it to the RG35XX SP and reboot"*. `stage-rg35xxsp-a65da6c784.sh` at 21:17 UTC: the device idle (no emulator, no cloud process, both locks free), the 1.3 GB tar copied in 2 min 43 s, its sha256 verified on the device (1 min 4 s on the A53), moved into `/storage/.update`. A second idle check with the queue holding one file, then `sync; reboot` through `tools/device-act` at 21:21:53 UTC (boot id `b16fc9f6` -> unreachable). Back at 21:26 UTC on `a65da6c784` (boot id `fde0ae20`, BUILD_BRANCH build/devices), the queue empty.
- Read 90 s in: essway active, one emulationstation process, tailscaled active with `tailscale.up=1` (the fourth data point for the Tailscale row), no failed unit, the error-priority lines the usual udev bfq-scheduler and pipewire-pulse ones, the interface log's WARNING lines the startup set. `docs/releases/device-facts.md`: the H700 boot and Tailscale rows moved to a65da6c784; both RECORD.txt Device lines written; the catalog regenerated.
- What the VM could not give and the device now can, once the soak has run: the sign-in window's memory on the handheld's own GPU path (#228, D-WORKFLOW-048) and the soak itself (D-QA-036) -- the maintainer plays; the journal is read afterwards, then the call (step 5).

## 23:40 UTC — The device round: four reports from the soak, three bugs fixed (#249, #279, #280), one turned into a decision (#282); the candidate rebuilt as 8db9042afa

- **#279, the IP row.** The maintainer saw `(+)` beside the Wi-Fi address and, with Wi-Fi off, the tailnet address alone. Cause: upstream's `bccd71570` ("Show all ip addresses") rewrote `queryIPAddresses` to keep every non-loopback interface and the ROCKNIX merge took it whole, dropping the older name list (eth/wlan/usb...) that the fork's offline-achievements check (`proxyOffline`, "a VPN's tunnel does not count"), the netplay gate and the carousel's cheevos action all relied on. Fix in ES `0cc13be61`: a link is up, running, not loopback, not point-to-point; the row's value is the first link's address, the list behind A names every interface, the tunnel's too. Proof: `proof-279-tunnel.sh`, a tun held open by a reader on guest b, frames of the page with eth0 up and down.
- **#280, the turned tiles.** Dr. Mario, then F-Zero: the exit capture a quarter turn clockwise. Read on the device: every capture PNG upright; four `.rotation` records all `turns=3` (Ms. Pac-Man's right, the others not); no `video_rotation`; `/var/log/exec.log` holding fourteen launches back to the 2026-09-11 exit test. `runemu.sh` truncated the log only when `system.loglevel` was not `none` (the device's value) while appending the emulator's output regardless, and `turnsFromLog` took the file's last `SET_ROTATION` line -- Ms. Pac-Man's -- as the next game's. Two layers: the launcher truncates at every launch; the parser reads only the last build banner's section (the banner, not the content line: fbneo and mame log "Content loading skipped"). Unit cases added (125 pass). A wrong record heals on that game's next exit. The first-pass trace had ruled the script out from its text; the device's file said the text does not run -- read the artifact.
- **#249, reopened.** The maintainer: menu + L1 from an auto save "failed to load the state". The launcher's append config carried `state_slot = "-1"` as `c0c2d15179` writes; RetroArch's `command_event_set_savestate_auto_index` resets the slot at every content load (to the last numbered slot, or 0), so the hotkey loaded a slot-0 file that does not exist (`Failed to load state from ".../mspacman.state"`). Patch 0018 leaves a configured negative slot alone. The earlier close rested on the launch's auto-load, not the slot -- the trace's own "what reading cannot prove". Proof: `proof-249-auto-slot.sh`, the load-state key through the guest's monitor (P-05's substitute, named).
- **#282, not a bug.** The saves sync after Wi-Fi returned read COULDN'T FINISH while a toast said the offline achievements were sent. The log: the game-exit sync with its automatic bounds (`--max-duration 20s`) carried a two-hour backlog (`recent: ... 7297s`), Dropbox moved a file a second, the bound cut it at 18 s, rclone committed what it had (exit 124, "THE CLOUD TOOK TOO LONG - IT'LL TRY AGAIN NEXT TIME", "dropbox: answers now"). The proxy's flush is not in the picture. The designed behaviour meeting a case the design did not name; three options put to the maintainer on #282 (leave it; a catch-up sync when the link returns; the deliberate budget when nothing waits).
- **The CPU spike (#281)**: an orphaned `python3 -` holding Copilot's July session store, 46 h at 100 %, no launch service behind it; killed by the maintainer. `build-preflight` gets a runaway line.
- **Rebuilt**: next `8db9042afa` (feature/rc-device-fixes: ES pin `0cc13be61`, RetroArch patch 0018, `runemu.sh`). The x64 image carries all three (stamps after the build's start; retroarch, runemu.sh and emulationstation differ from a65da6c784's). Chain-5: H700 run 33, vm-qa run 38, the three proofs, rehearsal 27 from a65da6c784, the sign-in window; the waiter on `chain-5.rc`.
- **A harness gotcha**: `tools/es-syntax-check` says `MISSING <file>` when a relative .cpp path is not under the pinned build tree; name the files by absolute path with `--tree <worktree>` and it compiles them.

## 00:05 UTC (2026-09-26) — The maintainer's two calls implemented (90 s ceiling, one surface at a time); the scripts suite refuses a shell background job

- **#282 (D-CLOUD-137)**: `--max-duration 90s` and `SYNC_CEILING_SECONDS=90` in `cloud_sync.conf`, its defaults and both scripts' fallbacks; `cloud_sync_helper` `migrate_default` moves a config still on the superseded default (20, and the old option string) to the new one and leaves a changed value alone -- the append loop adds only a missing key, so without it the maintainer's device would keep 20 for ever. Test case j2 (three checks). A launch mid-sync still asks STOP IT AND PLAY / KEEP WAITING (`FileData.cpp`), so the longer ceiling holds no launch. `--transfers` stays at one (D-CLOUD-121); per-backend options are #284.
- **#283 (D-UI-093)**: ES `c1c0d6ddc` -- `Window::processNotificationMessages` starts no toast while a card is up; `createAsyncNotificationComponent` stops a running toast and re-queues its words (with a real duration, through a shared helper). Rule text in `es-native-ui.md` § Spacing. Proof `proof-283-surfaces.sh` (frames every half second, the toast through `/notify`, the card from a hotkey-path exit).
- **A harness lesson, again**: `tools/last-good-scripts-test` launched as `setsid nohup bash -c '...' &` failed the two scan-cancel checks twice (rc 0, the scan completing) while the 16:00 foreground run and vm-qa's guest run of the same file passed. A shell background job starts with SIGINT ignored, every sandboxed script inherits it, and a trap on a signal ignored at entry never fires -- the 2026-09-22 lesson (memory) applied to the suite itself rather than a run inside it. Guard: the tool now reads its own `SigIgn` and refuses (rc 2, a sentence naming `setsid -f` and the foreground); proven: started as `bash -c '...' &` it exits 2 at once; started with `setsid -f` the third run passed 377 of 377. Learning-capture § 3: the procedure is a flag's refusal, not prose.
- **Next**: chain-5's proofs, then chain-6 on `355d4c540f` (the whole round: five fixes, four proofs), the record, the copy and the reboot on the maintainer's yes.

## 00:40 UTC (2026-09-26) — The four proofs made to run: five harness lessons, and #249's second half

- **The proofs' first runs failed on the harness, not the code** (chain-5, `8db9042afa`): the walk's `key down 5` (the driver takes one key per line); launches sent while the interface sat on the page vm-qa's walks had left open (the API answers 200 and starts nothing -- every guest-a proof now reboots to the carousel first); a fresh guest's Vulkan driver, with which RetroArch exits before its first frame in QEMU (the runner's cells set GL; the proofs now do, `vm_gl`); busybox's `pgrep -x` matching the whole command line, so `pgrep -x retroarch` read 0 while the launch log showed RetroArch running (`pgrep -f 'retroarc[h] -L'`; memory, and the VM rule's busybox section); and the exit sync's toggle read at the interface's start, so it goes on before the reboot, as `tools/time-to-play` reboots for the same reason. The one-surface proof's first frames showed the toast alone because no exit sync ran.
- **#279 proven** (four frames, `docs/qa-frames/2026-09-26/`): `10.0.2.15 (+)` with a tunnel beside eth0 and the list naming both; NOT CONNECTED with eth0 down and the tunnel alone in the list. **#280 proven**: one banner per launch, no record without a line, `turns=3` with the substitute, `turns=0` after the heal.
- **#249, the second half.** With the first hunk in, the launcher's `-1` survived the auto-index scan and the key still loaded slot 0: RetroArch's runtime log restores the slot a game was last left on at every content load (`runtime_file.c` -> `entry_state_slot` -> `runloop.c`). The patch's second hunk keeps a configured negative slot there too (`a8175c6193`). Chain-6 and chain-7 were stopped before their vm-qa (the first for the fix, the second because a rebase had not run and the build worktrees followed a next without it); their images are kept, superseded. Chain-8 builds `a8175c6193` with everything.
- **A proof's control**: `proof-283-surfaces.sh` on the image before the one-surface change is the positive that should show the toast over the card (running).

## 01:30 UTC (2026-09-26) — Chain-8: the final candidate a8175c6193 green on every VM proof; the device round's five bugs closed

- **Chain-8** (00:37-01:22 UTC): GENERIC_X64 run 56 and H700 run 36 from next `a8175c6193` (the ES pin `c1c0d6ddc`, RetroArch patch 0018 both halves, `runemu.sh`, the rclone ceiling and migration). The x64 image carries them all -- retroarch and emulationstation differ from 8db9042afa's, the helper has `migrate_default`, the defaults read `CEILING_SECONDS=90`; the H700 image the same. vm-qa run 41: all fifteen suites PASS (`qa-a8175c6193-webdav-a-20260926-0041`), frame-diff identical to the 664ad9ac64 baseline. Rehearsal run 30 from a65da6c784: PASS. Sign-in window on guest d: 284 MB.
- **The four proofs on the image**: #280 (one banner per launch, no record without a line, `turns=3` from the substitute, `turns=0` after the heal); #279 (four frames: the link's address with `(+)` beside the tunnel, NOT CONNECTED with the link down); #249 (the launcher's `-1` survives both the scan and the runtime log's restore -- no `found_last_state_slot` line -- and the injected load-state key loads `Probe.state.auto`; before the patch the same key loaded slot 0 and failed); #283 (frames taken; the control on the image before the change showed the toast lost under the card; the final image's reading follows).
- **Closed as fixed**: #249, #279, #280, #282 (the ceiling and its migration in the image, case j2). #283 closes on its after-frames. Records: the two intermediate pairs (8db9042afa, 82b91a4e3f) written as superseded and never staged; a65da6c784's pair marked superseded (the build on the device until the candidate is staged).
- **The proof scripts** (`proof-common.sh`, `proof-279-tunnel.sh` + `net-page.steps`, `proof-280-rotation-log.sh`, `proof-249-auto-slot.sh`, `proof-283-surfaces.sh`) live in the session directory; promoting them into `tools/` is the next harness item on #278 (the tunnel walk and the injected key are two of its gaps closed).

## 01:40 UTC (2026-09-26) — #283 proven on the final image and closed; the records, the catalog and the changelog for a8175c6193; the preflight

- **#283 after-frames** (guest d, 640x480, thirty seconds): with a toast queued before a game exit and a ~7 MiB exit sync, no toast on any of the 34 frames the card was up, the toast alone once the card had gone (frame 39), then the carousel; the control on 8db9042afa had lost the toast under the card. Closed as fixed; the six frames filed in `docs/qa-frames/2026-09-26/`.
- **Records**: RECORD.txt for both a8175c6193 directories; the catalog regenerated; the vm-qa-log row; the changelog's cut entry. `stage-rg35xxsp-a8175c6193.sh` prepared, not run.
- **Preflight** on next after the five closes: `rc-preflight: next at 248cf0dcb9 -- NOT A CANDIDATE: checkboxes`.
- **The QA backend under a large same-name replace** (#286): both one-surface runs on the final image ended their sync with COULDN'T FINISH (a 405, then YOUR CLOUD STOPPED ANSWERING) -- the harness's backend, not the scripts' change, since vm-qa's round trip and time-to-play pass on the same image with their smaller files; the card's outcome line is exactly what D-UI-028 says for it.
- **Preflight, again, after #285's checkbox was reworded** (it named the maintainer's call, which the checker refuses -- the row is the artifact): `rc-preflight: next at aec0ce9101 -- MAY BE CUT (unchecked by tool: device facts)`. The record's report name and the scripts count were wrong on the first write (the rehearsal's directory was taken for vm-qa's; `qa-a8175c6193-webdav-a-20260926-0042` is the run, 377 of 377) and are corrected in this commit.

## 02:00 UTC (2026-09-26) — D-WORKFLOW-049: the council and the code audit go through OpenRouter, every seat; both seats probed

- Maintainer: *"Let's route the council calls through OpenRouter. We can do the same thing for the code audit. We can continue to use the native harness for Fable, but other work, like our code audit work and our council runs, etc., can use OpenRouter."* Asked how Astra was being called: through OpenRouter all along (`openai/gpt-6-astra`, the provider pinned to OpenAI, no fallbacks); the Claude seat had gone to Anthropic's API by default and the audit's Fable passes had been Claude Code subagents on the Claude account, whose monthly spend limit stopped the audit on 2026-09-25. The key lives in `~/.config/council/env` (0600, one export line), sourced by hand before the Facilitator runs -- a session shell reads the variable as unset, which was mis-read as "no key" for a moment.
- Recorded: D-WORKFLOW-049; `release-candidates.md` step 6 and `adversarial-council.md` reworded; the memory rewritten (an audit pass is a Facilitator call with `--provider openrouter`, never an Agent-tool subagent); #287 filed in the maintainer's words.
- Probed: the Claude seat answered on `anthropic/claude-fable-5.1` (xhigh, 4.1 s), the GPT seat on `openai/gpt-6-astra` via OpenAI (max, 6.5 s), both `outcome=success` with the served model in the Facilitator's log line and the declared model in the provenance file. The paused audit (`docs/audits/2026_09_25-milestone-rc-round-since-258/`) resumes on this route after the soak and the call.

## 02:20 UTC (2026-09-26) — Step 4 again: a8175c6193 staged and booted on the RG35XX SP on the maintainer's yes; the ceiling migrated on the device

- Maintainer: *"we can transfer the latest build and reboot now."* `stage-rg35xxsp-a8175c6193.sh` at 02:05 UTC: the device idle, the 1.3 GB tar copied in 2 min 42 s, its sha256 verified on the device (1 min 13 s), moved into `/storage/.update`. A second idle check with the queue holding one file, then `sync; reboot` through `tools/device-act` at 02:10 UTC (boot id `fde0ae20`). Back at 02:15 UTC on `a8175c6193` (boot id `2ea54cb6`), the queue empty.
- Read at 80 s: essway active, tailscaled active with `tailscale.up=1` (the fifth data point), no failed unit, the error-priority journal lines the usual udev bfq-scheduler and pipewire-pulse ones only. **The device's own `cloud_sync.conf` now reads `--max-duration 90s` and `SYNC_CEILING_SECONDS=90`**: the helper's `migrate_default` ran on the first boot, as #282's close said it would -- the artifact, read from the device, not the log line (the helper's migration message did not turn up in the logs read; the config is the fact). `runemu.sh` on the device is the truncating one.
- Records: both RECORD.txt Device lines; the device-facts rows (H700 boot, Tailscale) moved to a8175c6193; the catalog. The soak (D-QA-036) is the maintainer's, on the build that carries the fixes for what the last soak found; its journal is read afterwards, then the call (step 5).

## 03:05 UTC (2026-09-26) — #288: the records #280 fixed stayed wrong until each game was replayed; a record now says where its turn came from (D-UI-094); every fix answers what was already written (D-WORKFLOW-050, #289, blindspot 62)

The maintainer, on the RG35XX SP twenty-five minutes into `a8175c6193`: *"the existing autosaves from before the upgrade for Doctor Mario and F-Zero remain rotated clockwise by 90°. In addition, the screenshots playlist shows the Dr. Mario achievement screenshots as also rotated 90° clockwise."* Then: *"Didn't we just solve for this? ... What did we miss?"* The device (reads only, 02:41 UTC): the three records still `turns=3` (Ms. Pac-Man's, written the 23rd and the 25th under the accumulating log), the launch log untouched since 23:19 UTC on the 25th, no launch in the boot's journal -- nothing had exited, so nothing had healed, and D-LAUNCH-004 had said "a wrong record heals on that game's next exit" as if that were a fix. The upgrade rule, broken with the rule loaded (blindspot 62).

**The fix (D-UI-094, ES `5644752aa`, pin `3f93dc4683`)**: the record carries `from=own-launch`; `CaptureRotation::read` uses the core's table for a record without the line (0 for the NES, the GBA and the Genesis; 3 for Ms. Pac-Man from `fbneo.txt`) and `recordAfterSession` rewrites such a record even when the turn agrees. Read both, write the new one: nothing migrated, nothing to play, the cloud's copies heal as the rewritten record is newer. `tools/vm-qa`'s manager fixture writes the marked record. Unit tests 126 of 126 (`es-unit-tests`); `tools/es-syntax-check` PASS. #288 filed in the maintainer's words with the reads, the code trace posted (its sixth line below).

**The proof so far**: `proof-288-stale-record.sh` (session dir; #278's promotion list) seeds the probe ROM as Bobl with the fixture picture as its auto save and an old `turns=3` record, walks `manager.steps`, measures the fixture's green mark against the picture (the one blue thing on the page), launches through the API, leaves through the exit hotkey, re-seeds the picture (RetroArch's own auto save replaces it at the exit) and walks again. On guest d (`a8175c6193`, 640x480) the first walk reproduced the defect: the tile turned, the mark on the **bottom** (`proof-288-d-run1/before-heal-3-manager.png`), and the exit healed the record to `turns=0` alone -- the old reader's heal by play. The first run's measurement had no result (a missing `importlib.machinery` import; the second walk's picture gone); rerun at 03:00 UTC. The fixed build's run is the chain's: `chain-9.sh` started 02:54 UTC (pid 852939; `watch-job` to `chain-9.status`, a harness waiter on `chain-9.rc`) -- x64 run 57, H700 run 37, vm-qa run 42, the five proofs with #288 first, the rehearsal from `a8175c6193`, guest d and the sign-in window. Nothing on the device; the copy and the reboot are the maintainer's yes.

**The process (D-WORKFLOW-050, #289)**: the maintainer -- *"shouldn't this be part of our general upgrade cycle and upgrade best practices? We should make sure that when we push a fix, we ensure parity for the existing system to leverage any new functionality ... This would have caught the need to rotate existing files if we'd followed this policy in place."* The rule existed and was broken, so it moved up a stage: every bug's code trace carries an `Already written:` line (what the old code left, and read both / migrated / nothing inherited, argued), `tools/rc-preflight`'s new `already written` item reads every open bug with a trace and every bug closed as completed since 02:45 UTC today, and `upgrade-and-install.md`, `release-candidates.md` step 0 and `issue-tracking.md` say so. **The guard proven**: `tools/rc-preflight --no-fetch --allow-unchecked device-facts` before #288's line -- `FAIL already written 1 code trace(s) with no "Already written" line: #288 (#289, D-WORKFLOW-050)` (`rc-preflight-289-before.txt`); after the code trace comment -- `PASS already written 1 code trace(s) since 2026-09-26 answer what the old code had already written` (`rc-preflight-289-after.txt`); the `bugs` item still reads #288 open without a disposition, as it should until the VM proof closes it.

## 04:00 UTC (2026-09-26) — The cut rebuilt with #288, `3f93dc4683`: proven on the VM, staged on the RG35XX SP on the maintainer's yes; the reboot asked for

`chain-9` (02:54-03:45 UTC): x64 run 57 and H700 run 37 in three minutes each from `3f93dc4683` (warm roots; both images' `emulationstation` carries `from=own-launch`, read from their SYSTEM), vm-qa run 42 all fifteen suites PASS (`qa-3f93dc4683-webdav-a-20260926-0300`; time to play 1.10 s, game to game 1.51 s), `proof-288` on guest a PASS (the old record ignored, `turns=0 / from=own-launch` after the exit), #279 four frames, #283 the toast after the card (case A f001-f038 the card, f042 the toast), rehearsal run 31 from `a8175c6193` 20 of 20, the sign-in window 286 MB on guest d. Two proofs failed on guest a: `proof-280` on its own one-line expectation (the record is two lines now; corrected) and `proof-249` on an injected load-state key that produced no load line at all -- the same RetroArch and launcher as `a8175c6193`, where it passed. Rerun on guest d, rebuilt from the image at 640x480 (`rerun-3f93.sh`, 03:45-03:51): `proof-288-d2` PASS with the frames at the panel's size (the mark on the right, 131x98; on `a8175c6193` the bottom, 83x111), `proof-280` three cases PASS, `proof-249` PASS (auto-file loads 1 -> 2). The guest-a `#249` failure reads as the injected key going astray on a guest that had just run three proofs; noted for #278, not a defect of the build. A stronger read of #288's table criterion, `proof-288-fbn.sh`: an old `turns=1` beside `mspacman` draws the arcade fixture with its mark on the top, `fbneo.txt`'s 3 over the record's 1.

The maintainer, 03:48 UTC: *"Once the new build is complete, we can load this build onto the device."* #288 closed on the proof; `rc-preflight` on next `2fbeb70425` -- MAY BE CUT (unchecked by tool: device facts), the new `already written` item PASS on #288's trace; the catalog regenerated (40 cuts); the records `x64-all-20260926-3f93dc4683` and `h700-all-20260926-3f93dc4683`; the QA-log row; the changelog's cut entry; `docs/qa-frames/2026-09-26/288-*` with the README. Staged on the RG35XX SP 03:52-03:56 UTC (idle, scp, sha256 matched on the device, moved into `/storage/.update`); the reboot is asked for, not taken -- `a8175c6193` runs until the maintainer's yes.

## 04:25 UTC (2026-09-26) — Step 4 again: `3f93dc4683` booted on the RG35XX SP on the maintainer's second yes; a8175c6193 superseded

*"You may reboot."* (04:11 UTC). A second idle check (no emulator, no cloud run, both locks free, the tar in the queue), the reboot through `tools/device-act` at 04:12 UTC, the device back on `3f93dc4683` at 04:17 UTC (boot id 2ea54cb6 -> 490ccd51): the update queue empty, no failed unit, no interface crash in the boot, the binary carrying `from=own-launch`. The first read at 0 min of uptime found the four rotation records "missing" -- `/storage/roms` was not yet mounted -- and the read a minute later found them unchanged (unmarked `turns=3`, mtimes 09-22 to 09-25), which is the fix's shape: the reader ignores them for the tables, nothing migrated. `a8175c6193`'s two records marked SUPERSEDED, the catalog 40 cuts, the device-facts H700 boot row moved to 04:17 UTC, #236's step-4 comment. The soak and the look at Dr. Mario's and F-Zero's captures are the maintainer's (D-QA-036); the call is step 5.

## 05:50 UTC (2026-09-26) — #290: leaving a game, the interface is back before the capture runs (D-LAUNCH-006); the cut `86dc949300` proven on the VM; the injected load-state key's flake named and the proof hardened

The maintainer, on `3f93dc4683`: *"can you check if we just had a crash when I exited Miss Pacman? It took a long time to exit from RetroArch"* -- no crash (the launch log's clean end, `emu-exit=0`, no backtrace, no core, no OOM); the device's timeline read RetroArch's teardown (3 s) and then six seconds with nothing on screen while the interface ran `cloud_capture` synchronously before `window->init`, then the sync card for 15 s (Dropbox's pace). Filed as #290 in their words; *"let's fix that and generate a new build."*

**The fix (ES `e563e0024`, pin `86dc949300`)**: the capture command is built where it was and run on a worker thread once the window is up; the worker posts the exit sync's start (or the offline achievements' sentence) to the interface thread, so the capture still finishes before the sync starts. A generation counter and `mRunningGame` guard the posted start: a game launched in the capture's seconds goes ahead without a question and its own exit's `--recent` run carries both sessions' saves; a launch over a sync that has started still asks STOP IT AND PLAY. Both images carry the worker (`strings` on their EmulationStation binaries).

**The proof**: `proof-290-exit-order.sh` shadows `/usr/bin/cloud_capture` with a three-second wrapper by a bind mount over the read-only image (P-05's named substitute for the A53's seconds) and reads frames against the emulator's going and the capture's start and end: on `3f93dc4683` (guest d) the screen stays black until the capture ends, the carousel at 4.4 s; on `86dc949300` (guest a) the carousel is back at 1.2 s while the capture runs to 3.6 s. `chain-10` (04:43-05:36 UTC): x64 run 58, H700 run 38, vm-qa run 43 all fifteen suites PASS (`qa-86dc949300-webdav-a-20260926-0449`; game to game 2.0 s, one sample, as run 41's), #288, #280, #279 PASS, #283 the card, its outcome, then the toast in both cases, rehearsal run 32 from `3f93dc4683` 20 of 20, the sign-in window 283 MB.

**#249's injected key**: the proof failed on guest a in chain-9 and chain-10 and on a fresh guest d at 05:38, and passed on guest a in chain-8, on guest d at 03:50 and at 05:43. `diag-249.sh` on the guest and build that had just missed: RetroArch launched the proof's way, F4 through the monitor -- the first two presses each loaded the state, the third (after a `swaymsg focus`) did not. So a key sent through the monitor reaches RetroArch's udev reader about half the time on a first press, on both builds; the launcher and RetroArch are unchanged since a8175c6193, where the maintainer's own menu+L1 is the real input (#277). The proof now presses up to three times and reads each back (#278); the criterion is that the key reloads the auto save, not that one synthetic press lands.

## 15:10 UTC (2026-09-26) — 86dc949300 on the RG35XX SP (step 4 again); the injected key measured (#278); the QA guests render with softpipe -- virgl and llvmpipe are the levers (#291)

*"You may both transfer the build and reboot it."* -- staged 14:51-14:55 UTC (idle, scp, sha256 matched on the device, moved into `/storage/.update`), a second idle check, the reboot through `tools/device-act` at 14:55, back on `86dc949300` at 15:00 UTC (boot id 490ccd51 -> 833ee3a5): the queue empty, no failed unit, no crash, the interface and tailscaled up (the seventh data point), the binary carrying the exit worker. `3f93dc4683`'s records marked SUPERSEDED; the catalog, the device facts, #236.

**#249's key, measured** (*"Are there ways for us to solve for this problem moving forward? How can we improve the harness?"*): `diag-249-hold.sh` on an idle guest d landed sixteen of sixteen presses at QEMU's default 100 ms hold and at 500 ms, so the miss (three of six first presses, always on a guest that had just been busy) is a stall, not the hold as such; the proofs now hold a key 500 ms (`proof-common.sh`'s `monkey`) and read the press back and repeat it (proof-249). The plan on #278: RetroArch's command channel where the key is not the claim, a readiness read before the press, the press count in the vm-qa report.

**Why RetroArch is slow on the VM** (*"why wouldn't part of the solution be how we can get RetroArch to run more quickly?"*): the guest's RetroArch log reads `[GL] Renderer: softpipe` -- Mesa's reference software rasterizer -- with 60 % of frames dropped; the image's Mesa carries no llvmpipe although the GENERIC_X64 options say it should (Mesa 26 names `llvmpipe` and `softpipe` separately), and it does carry the virgl driver. The host's QEMU 10.2 has `virtio-gpu-gl-pci`, `egl-headless` and virglrenderer 1.10, with the Intel iGPU (i915/xe, renderD128) and the RTX A1000 (nvidia, renderD129): hardware GL for the guests needs no change to QEMU, only to how the guests are launched, and llvmpipe is the fallback to fix in the image. #291 in the maintainer's words, with the numbers to move (the dropped-frame ratio, the walks' 1320 s, the first-press count).

## 15:30 UTC (2026-09-26) — #291: the QA guests rendered with softpipe; hardware GL through virgl and the frames over VNC (D-QA-052, blindspot 63); llvmpipe restored to the image

The maintainer, on the injected key: *"why wouldn't part of the solution be how we can get RetroArch to run more quickly?"*, *"This might require changes to QEMU"*, *"BuildBox has a NVIDIA A1000"*, then *"this would be a good thing to fix while I test the new build ... additive benefits for every subsequent run."*

**The cause**: RetroArch's launch log on a guest reads `[GL] Renderer: softpipe`. The x86 options have said `LLVM_SUPPORT="yes"` for llvmpipe since 2026-08-21; `config/graphic`'s `get_graphicdrivers` resets it to `no` after the device options are read (upstream's 2018 reset), so Mesa was built `-Dllvm=disabled` with `softpipe,svga,virgl` (the build's own meson log). The image did carry the virgl driver, never given a GL display. The host needs no QEMU change: 10.2 has `virtio-gpu-gl-pci`, `egl-headless`, virglrenderer 1.10, and two render nodes (`renderD128` i915/xe, `renderD129` nvidia).

**Done** (next `9faf3d5026`, `0ab7d5dab5`): `config/graphic` keeps a device's explicit yes (GENERIC_X64 now `llvmpipe softpipe svga virgl` with LLVM; H700 unchanged) -- x64 run 59 building LLVM for the target and Mesa (15:17 UTC, watched by pid); `generic-x64-vm --headless` launches virtio-gpu-gl with `egl-headless` on the first Mesa render node by default (`--gl auto|none|<node>`, `vm-pair` passes `VM_GL`); QEMU's `screendump` answers `Error: no surface` under a GL scanout, so `vm-visual-qa`'s `Monitor.screendump` takes the frame over the guest's VNC display (`info vnc`; an RFB client in the tool, raw encoding, ~0.1 s at 640x480) and `cloud-round-trip`'s monitor through it; `vm-qa`'s report says the guest's display and its Mesa's renderer. Guest d under virgl on the iGPU (`rebuild-d3.sh`, `diag-291-virgl.sh`): `virgl (Mesa Intel(R) Graphics (ARL))`, GL 4.3, `Frames pushed: 2391, Frames dropped: 1` (softpipe: 2470 / 1490), the injected F4 five of five at the 100 ms hold, `ui-settings-toggle` walked in eighteen seconds through the VNC path. The rule section in `generic-x64-vm-testing.md`; D-QA-052; blindspot 63 (the options comment taken as the behaviour, a month of `softpipe` lines unread; the guard is the report's renderer line).

**Queued** (`gl-run.sh`): the image kept as `x64-all-20260926-f483f215ad`, vm-qa run 44 with the pair on GL -- the walks' time against 1320 s, the time-to-play headlines beside the software numbers, the one-time `frame-diff accept` the renderer costs -- then the QA log row and #291's checkboxes.

## 15:35 UTC (2026-09-26) — #249's device half: the load-state hotkey from the pad, confirmed by the maintainer

*"I can confirm that last night when I was doing testing with the prior build, that the save state experience when autoloading via the RetroArch hotkey combination after having started the game from an autosave did work as expected with that build once the fix was in place."* The real input the VM's injected key stood in for (P-05), on the RG35XX SP running `3f93dc4683` (patch 0018 unchanged since `a8175c6193`). The device-facts pad row, #277's table row and a comment on #249 carry it; the VM proof stays the regression check.

## 16:20 UTC (2026-09-26) — #291 done: the first vm-qa on hardware GL, the baseline moved once, two harness rules the renderer exposed

x64 run 59 built in nine minutes (LLVM for the target was warm in ccache): the image carries llvmpipe and libLLVM. vm-qa run 44 (`qa-f483f215ad-webdav-a-20260926-1527`) with the pair on virgl through the Intel iGPU for the first time: thirteen suites PASS, the walks 992 s (1320 s on softpipe), RetroArch `virgl (Mesa Intel(R) Graphics (ARL))` with one dropped frame in 1864; the two FAILs were the renderer's, not the image's. **frame-diff**: every screen's pixels moved with the rasterizer and nothing a walk changed (`ui-settings-toggle/02-toggled` read side by side) -- accepted as the baseline, build `f483f215ad`, 16:02 UTC, the softpipe one kept as `walk-baseline-664ad9ac64-20260926-1602`. **time-to-play's surface check** read RetroArch's `Using resolution` log line, which under a GL context is written before the compositor's fullscreen resize lands (`240x256` on a 1280x800 guest whose game frame is drawn pixel-sharp at the panel -- `diag-291-fidelity.png`, a real NES title); it now reads RetroArch's own GPU screenshot (`video_gpu_screenshot` on for the run, three presses of F8 at a 500 ms hold, the file removed after), which is the viewport -- 1067x800 under 1280x800, the 4:3 game at the panel's height -- so a surface is "scaled" only when short on both dimensions. **The 200-CPU-tick readiness rule** of the exit test and time-to-play's `wait_drawing` is a software-rendered RetroArch's signature: on GL the process burns about five ticks a second after thirty of init (measured), so both tools waited their loops out (the exit suite 110 s, time-to-play 166 s); the rule also fires at thirty ticks four seconds after the process is seen (`--vm`; a device keeps the two seconds). Reruns on the same guest: exit PASS in 28 s, time-to-play PASS in 70 s (0.61 / 1.06 / 1.02 s; RetroArch drawing 4.46 s), the report's `renderer:` line reading virgl. proof-249 on guest d under GL: one press to a load. #291 closed; the QA log's row; the record `x64-all-20260926-f483f215ad`.

## 18:00 UTC (2026-09-26) — #292, #293: the offline achievements' own cards and a launch that asks over them; the first two proofs

**What was built** (ES `0ade9086b`, `92bc92a4e`, `f5a120262` on `test/qa-integration`; pins `88ee7de862` and
`8c31827c42` on `next`; GENERIC_X64 runs 60 and 61, H700 run 39 from `8c31827c42`). A new `ProxyCards` unit in
EmulationStation: on the link's return a **send card** follows the proxy's award queue (SENDING OFFLINE ACHIEVEMENTS...
/ N TO SEND) and ends in the sync card's shape -- COMPLETED with OFFLINE ACHIEVEMENTS HAVE BEEN SENT (TO
RETROACHIEVEMENTS only where the line has room, D-UI-096), or COULDN'T FINISH - RETROACHIEVEMENTS STOPPED ANSWERING
with IT'LL TRY AGAIN WHEN YOU'RE CONNECTED -- stamps `/storage/.cache/cloud_sync/last-sync-link` so "was it shown?"
is a read, and then runs the exit sync that was skipped for want of a network (`CloudText::exitSyncOwed`, a unit
case) with its own card: the sentence "next time you're connected" now has a mechanism behind it (D-RA-030). The
ctl's **top-up** gets a card once its progress file says it has work (UPDATING OFFLINE ACHIEVEMENTS... / N OF M;
COMPLETED / N GAMES ADDED FOR OFFLINE PLAY., or YOUR OFFLINE ACHIEVEMENTS ARE UP TO DATE.), and its outcome is said
even when the run was too quick for a card to attach (`92bc92a4e`, told by the stamp). A launch over either asks in
the sync question's shape: OFFLINE ACHIEVEMENTS ARE BEING SENT. IT'LL BE A MOMENT. PLAY NOW / KEEP WAITING (the
proxy's send cannot be stopped from the interface, so the question offers to play through it); YOUR OFFLINE
ACHIEVEMENTS ARE BEING UPDATED. IF YOU STOP IT, IT'LL TRY AGAIN NEXT TIME YOU'RE CONNECTED. STOP IT AND PLAY / KEEP
WAITING (the ctl is signalled through the pid in its lock; its trap writes the stamp). The exit card no longer
carries the achievements at all -- the awards block, the toast after a failure, the two-part "awards and saves"
sentence -- as the maintainer asked (*"I would actually prefer it's not shown when leaving a game"*). One floating
surface at a time (D-UI-093): the send card waits for a sync card, and an automatic sync's end asks for it. French for
every string (`tools/es-untranslated`: 541 of 541); `tools/vocabulary-check` 0 wrong; `tools/es-syntax-check` PASS on
every file; 127 unit cases. The two conventions the approval settled are in `es-player-text.md` (`fcf4439aaa`).

**Proof run 1** (`proof-292-cards.sh`, guest d on `88ee7de862`, frames over VNC by `vnc-grab.py` with a signature per
frame): the top-up card and its COMPLETED / 1 GAME ADDED FOR OFFLINE PLAY., and the STOP IT AND PLAY question, are on
screen exactly as designed (`docs/qa-frames/2026-09-26/293-*`). The other three phases graded the harness: `ret`/`esc`
pressed where the image maps `x`/`z`; the exit-sync toggle written after the interface had loaded its settings; and
Tobu's route driven to an achievement the QA account earned on 2026-09-25 16:22:54 (id 100359, read from the API
with the web key, never printed) -- all three already in `generic-x64-vm-testing.md`, none read first (friction log,
#278).

**Proof run 2** (`proof-292-cards-v2.sh`: the proxy's `pending` shimmed by a wrapper bind-mounted over the ctl, a
synthetic input named as such; the walks' keys; settings before a reboot) found a real defect in the design: the exit
card's end asked for the send card **after a run that had found no network**, so the card followed a queue that could
not empty, said COULDN'T FINISH over an exit meant to stay quiet, and the link's own card, finding one up, dropped the
sync it owed. Fixed in `f5a120262`: a sync that could not reach the network asks for nothing at its end. The stamp's
why is written with spaces, as every ES-written stamp is (`writeStamp` keeps the sentence; `parseLastRun` reads
either spelling); the proof's expectation, not the writer, was wrong there.

**Open**: the end-to-end with a real award (an achievement earned offline, flushed by the proxy at the link, the card
saying it went) needs the QA account's routed achievement reset on the site -- the ask goes to the maintainer; until
then the checkbox stays partial (vm-first, P-05). The proof reruns on the rebuilt image next.

## 18:30 UTC (2026-09-26) — #292, #293 proven on the VM: the third proof, two fixes, four diagnostics; the cut `64a0934a5d`

**The third proof** (`proof-292-cards-v3.sh` on guest d on `64a0934a5d`, ES `6e6643687`): 23 of 25, the two failures the
harness's (the shim's copy of the ctl could not find its helpers -- the ctl locates them by `$0` -- so its scan died
at game 1 and the launch went through). Frames filed as `docs/qa-frames/2026-09-26/292-*`: the exit card with the
link down says SKIPPED - YOU'RE NOT ONLINE / SAVES WILL BE SYNCED NEXT TIME YOU'RE CONNECTED. and nothing about
achievements; the link back with an award waiting: SENDING OFFLINE ACHIEVEMENTS... / 1 TO SEND; a launch over it:
OFFLINE ACHIEVEMENTS ARE BEING SENT. / IT'LL BE A MOMENT. / PLAY NOW, KEEP WAITING; 45 s on, COULDN'T FINISH -
RETROACHIEVEMENTS STOPPED ANSWERING / IT'LL TRY AGAIN WHEN YOU'RE CONNECTED., the stamp `5 not-sent`; then the
SYNC SAVES card that was owed (`last-sync-exit` 69 no-network -> 0 completed); PLAY NOW launches (RetroArch up in
2 s) and the send card ends behind the game; the link back with the proxy's flush stamp: SEND OFFLINE ACHIEVEMENTS /
COMPLETED / OFFLINE ACHIEVEMENTS HAVE BEEN SENT TO RETROACHIEVEMENTS. (the long candidate fits at 640x480), the stamp
`0 sent`, no sync card when none is owed.

**Two fixes the second and third runs earned** (ES `f5a120262`, `6e6643687`; pin `64a0934a5d`, x64 run 62, H700 run
40, both 18:05-18:09 UTC): a sync card that found no network no longer asks for the send card at its end -- the card
followed a queue that could not empty and said COULDN'T FINISH over an exit meant to stay quiet, and the link's
probe, finding a card up, dropped the sync it owed; and PLAY NOW's launch comes back through `launchGame`, where the
gate asked the same question on the next frame without end -- the answer is kept for that one launch
(`sPlayThroughSend`), as the sync gate's cancel and the transfer gate's wait already make theirs. Also: a top-up too
quick for a card still says its outcome (`92bc92a4e`, told by the stamp).

**One miss, not reproduced.** In the third run's 1b the game exited at 18:13:28 with the link up and no exit sync
ran (no `cloud_backup` in `cloud_sync.log`, `last-sync-exit` unchanged), while a send card said COMPLETED four
seconds later -- the link's own card, its "online" landing at the game's exit: the watcher polls the link every 5 s
and pauses while a game runs, and the link had been flipped 11 s before the launch. With the interface's Info log on
(`Debug=true`), five exits through the same shapes all ran the exit sync (`diag-292-1b` twice: the send card's
PLAY NOW, `launch: the player chose to play through the send`, `cloud_backup` 1 s after the exit, then the send card
`sent` from the sync card's end; `diag-292-race`: the link flipped 2, 6 and 12 s before a launch -- `1790447075 ->
1790447154 -> 1790447200 -> 1790447252`, each `0 completed`). The one run without the log stays an open item to
verify on #292, named as such; the proof's own `wait_change` check accepted the unchanged value (a check that cannot
fail, blindspot 8's shape) and is fixed in the next cut of the script.

**STOP IT AND PLAY over a run** (`diag-293-stop`, the real ctl's scan, 37 games): the question at 640x480, `x` ->
`launch: the player chose to stop the top-up for a game`, the running file gone, RetroArch up 2 s later. **The
top-up card at the link** (run 1's frames, `293-topup-card-*`): UPDATING OFFLINE ACHIEVEMENTS... / STARTING..., then
COMPLETED / 1 GAME ADDED FOR OFFLINE PLAY.; in the race diagnostic the first link's top-up had a job list and added
nothing, and its card said the achievements were up to date once -- the half-hour mark kept the two later links
silent.

**The candidate**: `64a0934a5d` (x64 run 62 at 18:05, H700 run 40 at 18:09 UTC, one synced head; records
`x64-all-20260926-64a0934a5d`, `h700-all-20260926-64a0934a5d`); `rc-preflight` MAY BE CUT at `ac51cc88d0` (the
device facts read by hand; RAOfflineProxy's same-day `c1bd3724d1` accepted by D-RA-031 -- spruce/Onion platform work,
and the proxy's own proof waits on the QA account's reset). vm-qa run 45 is in flight. The H700 image is not staged:
the copy and the reboot are the maintainer's two yeses.

## 19:20 UTC (2026-09-26) — the copy held: the capture-failure toast goes in, RAOfflineProxy bumped, a cut after `64a0934a5d`

The maintainer approved the copy and the reboot of `64a0934a5d`, then, before either ran: *"Let's deal with the
capture failure toasts. We shouldn't have left that out. It was a relatively quick fix, and I would have rather
included it in the last build."* So the copy is held (nothing has touched the device), and #293's item 3 goes in
now: after a game, and after the startup game once the window exists, a capture that could not record says COULDN'T
RECORD THIS SESSION'S SAVES. THEY'RE STILL ON THIS DEVICE. once, as a toast, before the sync card starts so it shows
after the card (ES `d3ba4edca`; French; the startup game runs before the window is created, so its failure is
remembered in a static and said where the startup sync starts). The lesson is a bullet in `player-language.md` and
a memory: an item blocked only by unapproved words ships with the proposed words -- a deferral costs a build round.

The same minute: *"the Tobu Deluxe achievements have all been reset"* (the API reads 0 of 28 earned), which is
D-RA-031's condition. RAOfflineProxy is bumped to `c1bd3724d1` (`423ad7d081`; the fourteen fork patches apply
unchanged in order -- a first dry run applied each alone against the pristine tree and reported five failing, which
was the missing predecessors, not upstream; the rcheevos and libchdr submodule commits are unchanged, the three
recipes' comments swept), the acceptance line leaves `rc-accept.txt`, D-RA-032 records it. The proof for both is
the next image: `proof-293-toast` (a wrapper over `cloud_capture` that exits 1, a bind mount, P-05) and
`proof-292-real-award` (`tools/ra-offline-test` on guest d with the VNC grabber running throughout, so the send card
at the link's return is on film with a real award and no shim), then vm-qa run 46. x64 run 63 from `d72084ccad`
started 19:17 UTC; H700 run 41 follows it.

## 19:35 UTC (2026-09-26) — #293 item 3 proven: the toast after the card; the `d72084ccad` cut built

`d72084ccad` (x64 run 63 at 19:22, H700 run 41 at 19:25 UTC, one synced head; ES `d3ba4edca`, RAOfflineProxy
`c1bd3724d1`). `proof-293-toast-v2` on guest d, 8 of 8: with `cloud_capture` shadowed by a wrapper that exits 1, a
game left through the hotkey shows SYNC SAVES / COMPLETED at 1.5 s and, once the card has gone, the toast COULDN'T
RECORD THIS SESSION'S SAVES. THEY'RE STILL ON THIS DEVICE. at 7.5 s (`docs/qa-frames/2026-09-26/293-capture-failed-*`);
the interface's log carries `cloud_capture exited 1`; the control exit with the real capture shows nothing. The first
run of the proof launched RetroArch on the image's Vulkan driver (a guest cannot draw with it; no session, nothing to
capture, no toast) -- the GL line every other proof carries was missing; `player-language.md` and `es-player-text.md`
carry the sentence and the lesson. #293's item 3 ticked with the frames. vm-qa run 46 (started with `setsid -f`, so
the scripts suite can trap its signal) and `proof-292-real-award` (`tools/ra-offline-test` with the frame grabber, a
real award, no shim) are in flight; the records for both images are drafted with their proof lines pending. The H700
image is not staged: the yeses given were for `64a0934a5d`, and the maintainer asked for this one instead.

## 19:45 UTC (2026-09-26) — the real award: the proxy proven on the bump, the card not shown for a harness reason

`proof-292-real-award` on guest d on `d72084ccad`: `tools/ra-offline-test` 32 of 32 on RAOfflineProxy `c1bd3724d1` --
the award queued with the link cut, flushed 17 s after the link returned, earned on the site at 19:33:56 UTC, one
fewer active on the relaunch (D-RA-032's proof). The grabber's frames show no send card at the link's return: the
tool turns the toggle on from the shell after the interface has loaded its settings, so the interface's copy read off
and the link's probe found nothing owed -- the same trap as the first proof's exit-sync toggle, in the interface's
read of `global.retroachievements.offlineproxy` (`OfflineAchievements::toggleOn`). `proof-292-real-award-v2` enables
the toggle before the reboot; it needs the Tobu achievement reset once more, since this run earned it again. #292's
first checkbox stays partial; the shimmed proof on `64a0934a5d` stands for the card's mechanics.

## 20:45 UTC (2026-09-26) — vm-qa on `d72084ccad`: one walk screen, explained and accepted; the walk baseline moves

Run 46 (fresh pair) fourteen of fifteen; frame-diff on one screen of 78, `run-transfer/08-transfer-done`. Run 47 (walks
alone with `--skip-up`) reused run 46's guests and met their leftover state on nineteen boxes -- a walk rerun needs a
fresh pair; `--only walks` on kept guests proves nothing (#278). Run 48 (a full run, fresh pair) fourteen of fifteen
again, the same screen plus the change-folder editor's blinking caret. The frames walk's own transfer completes
identically in the `f483f215ad` baseline and here (23 FILES, 640 KB, 3 s); the difference is the second back-up of the
same content that `run-transfer` then makes: the reference frame held it still WORKING on FBNEO, ITEM 2 OF 4, at the
90 s settle -- the QA WebDAV backend refusing a same-name replace and rclone retrying (#286) -- while this image's runs
end it in 2 s with COMPLETED / NOTHING NEW - YOUR CLOUD ALREADY HAD IT ALL. The backend was restarted (`cloud-test-backend
up` in the guest-d rebuild) between runs 45 and 46, which is the state that changed; the transfer page is untouched by
the cut. So the sane outcome is the reference from now on: run 48's walks accepted as the baseline (build `d72084ccad`,
20:40 UTC; the `f483f215ad` baseline kept as `walk-baseline-f483f215ad-20260926-2040`), the caret masked for the
change-folder walk (`66bd729fc1`), frame-diff PASS against it. Records and the QA log carry it; the catalog reads 47
cuts. The candidate stands: `rc-preflight` MAY BE CUT; the H700 image waits on the maintainer's copy and reboot.

## 21:25 UTC (2026-09-26) — `d72084ccad` on the RG35XX SP; the "stacked alerts"; the running change log brought up to date (#294)

The maintainer approved the copy and the reboot of the most recent build and reset the Tobu achievements. Staged
20:56-21:00 UTC (idle: emulators=0 cloud=0 lock=free scanlock=free; sha256 matched on the device), rebooted 21:00:17
through `tools/device-act`, back 21:05 on `d72084ccad` (boot `833ee3a5` -> `b65e53f6`, the update applied across the
reboot, queue empty, no crash backtrace); the device facts, both records, the catalog, the QA log and #236 carry it.

Minutes later, on the device: *"I just saw the new stacked alerts. Those were nice. I hadn't seen those before,
actually. That's an elegant solution to the problem. We should make sure we note that in our change log we've been
working on. We should make sure that the running change log is fully up to date as well."* Filed as #294 in those
words (D-QA-012), the alerts' bullet written under the `64a0934a5d` entry (D-UI-093: a toast waits for a card, a card
takes a toast's place and puts its words back on the queue, the send card waits for a sync card, an automatic sync's
end asks for it), and the log checked against every fork issue closed as completed since 2026-09-24: thirty not named
by number, twenty-four described in words or not player-visible, six with no entry -- #68, #82, #182, #188, #199,
#274 -- now a section of their own naming each cut and its evidence. #294 closed with the list.

The real-award proof's second cut (the toggle on before the reboot) missed the award on its first try -- Tobu's route
did not reach the hidden song in 240 s (the injected key's half-chance, #278); the achievement stayed unearned, so
the retry runs on the same reset.
