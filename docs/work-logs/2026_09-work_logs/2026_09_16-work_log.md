# 2026-09-16 work log

## 02:15 UTC — The RC-12 round's eleven decisions, one at a time

- The maintainer asked to settle the #200 decisions before any build goes to the device, one at a time. Recorded as D-RA-022/023, D-UI-065..071, D-WORKFLOW-023, D-QA-026: the toast stays `Logged in as "Name" (offline).`; times padded with literal AM/PM; the offline lines' French takes the short `HORS LIGNE : …` shape; MANAGE SAVED NETWORKS' words stand; slot numbers never change (no renumbering -- the maintainer asked what serves conflict resolution's long-term direction, and renumbering is a manufactured delete-plus-add to the sync); the hub row's outcome-until-seen approved; the speed criterion accepted with the tool's latency named; #185 its own round; the two QA-name commits stay; the row is WI-FI NETWORK.
- On the way: *"translation is totally separate from all of the work we're doing ... not on the critical path"* and *"we should go where the users are"* -> D-UI-065 and #204. The GitHub stargazers endpoint answers 404 for ROCKNIX/distribution (1,555 stars, 351 forks), so geography by stargazer location is not available from here; the translation-activity proxy in upstream EmulationStation puts French, Spanish, Portuguese (both) and German ahead all-time, German, Japanese, Chinese and Brazilian Portuguese ahead this year. Site or download geography is the ROCKNIX team's to share.
- The four code-changing decisions are ES `8d7213712` (`feature/round-decisions-rc12`, merged `f26668e7c`): WI-FI NETWORK, no renumbering (both call sites), the two French lines. Pinned as **RC-12 build 6 `bc26baa60d`**; x64 then H700 building in one chain from the same synced trees.
- The RG SP answered a read-only probe at 00:48 UTC awake and charging (boot 36 min old, no suspend in that boot's journal); the maintainer asked whether it had answered while asleep -- it had not: every read of mine was within minutes of a reboot.

## 02:55 UTC — RC-12 build 6 `bc26baa60d` proven on guest d; the device candidate

- Guest d upgraded in place from build 5 (the sixth in-place upgrade of the night, all clean): NETWORK SETTINGS reads `WI-FI NETWORK  Home Wi-Fi`; Tobu's manager with slot files 3 and 1 (a gap) shows them as they are; Y on slot 1, YES: `state3` keeps its name on disk (upstream renamed it to the first slot); a launch from slot 3 and a quit: `state3` untouched (mtime 12:00), the auto save rewritten -- D-UI-069 as built. Böbl's manager in French now titled GESTIONNAIRE DES SAUVEGARDES D'ÉTAT (#202's second half). The nine suites: see the QA row.
- The French offline lines (D-UI-067) were not framed: START (`sendkey ret`) was swallowed on every attempt within minutes of an interface restart tonight -- with the link on or off, held 300 ms or not -- while A and B registered; it did register twice after a long idle. The frame tool's `wait-for-change` retry makes it worse for a toggling key (a second START closes the menu it opened). Noted for the harness: after a restart, wait past a minute before START, or reach pages through A/B only; the lines themselves are one-line translations shorter than the English that fits.
- H700 `h700-all-20260916-bc26baa60d` filed; not staged -- the maintainer asked for the decisions before any build goes to the device, and they are in; build 6 is the one to offer.

## 03:08 UTC — Build 6 staging on the RG SP; French stays, low priority

- Maintainer: *"you may stage the buils"* -> `stage-rgsp-run-bc26baa60d.sh`: idle check clean (no game, no cloud process, lock free, queue empty; the device on build 2 with a boot of its own since ~00:12 UTC, charging 100 %), the H700 tar of build 6 going into `/storage/.cache/*.part` from 02:56 UTC; the sha check and the move into `/storage/.update` follow; the reboot is the next question (D-QA-015).
- Maintainer: *"if we want a second language, we can stick with French, but I don't think it is the top priority"* -> D-UI-072; #204 closed as parked (French continues as the implementer's layout check; no evidence hunt, no other language until translations matter).

## 03:20 UTC — Build 6 staged on the RG SP

- The H700 tar of RC-12 build 6 `bc26baa60d` is in the RG SP's `/storage/.update` (02:56-03:18 UTC; the device's own sha256 `db5bf7e1ed4e9551…` equal to the artifact's; 1,304,258,560 bytes; BEGIN/END in the device-actions log, boot id `471e13c8`). Inert until a reboot, which is asked for by name and not issued (D-QA-015).

## 03:35 UTC — Build 6 applied on the RG SP

- Maintainer: *"you may reboot"*. Idle check clean (no game, no cloud process, lock free, the build 6 tar alone in the queue, battery full, not charging). `tools/device-act rgsp "reboot-apply-h700-bc26baa60d (maintainer yes 2026-09-16: 'you may reboot')" -- 'sync; reboot'` at 03:33 UTC; back after ~75 s on `bc26baa60d`, boot `04a27ebb`, queue empty, interface / proxy / Tailscale up, offline toggle on, hardcore off, index-at-start on, `ready=247 pending=0`, 100 %. The RG SP now carries everything since RC-11 build 2: the Wi-Fi paradigm, the manager's labels and French title, the launch-over-sync question, and the round's eleven decisions.

## 04:45 UTC — The automatic syncs ask too (D-CLOUD-130); build 7

- Maintainer, on build 6's behaviour: *"The case I ran into hasn't been changed, correct? When I ran into it, it was an auto-save on exit, and it didn't present me with an option. If we want consistent behavior ... we should have a consistent behavior."* Right: build 4-6 replaced the exit sync's two-second modal with a spinner and kept #101's silent cancel. Now `FileData::launchGame` asks the one question over any sync on the card and cancels only on the answer (ES `e21841060`; the transfer page's question unchanged). D-CLOUD-130 reverses #101 / D-CLOUD-109 for launches.
- A broken commit slipped onto `test/qa-integration` for eleven minutes: a replacement in the gate's comment block left one line without its slashes, the syntax check printed the error, and the commit, merge and push ran anyway because the check only gated an echo. Fixed by `7f1c7a8e7`; the check now gates the commit (`|| exit 1`). Lesson filed as a memory.
- **RC-12 build 7 `586d2334fd`** pinned (ES `265540258`); x64 then H700 building in one chain. Proof to take on guest d: the exit sync (SYNC SAVES WHEN EXITING A GAME on, guest d pointed at its own QA folder `/GAMES-d` so the suites' folder is untouched, `--bwlimit 8k`), a game quit, then a launch during the card -> the question.

## 04:58 UTC — Build 7 `586d2334fd` proven on guest d: the exit sync asks

- Guest d on build 7, pointed at its own QA folder (`/GAMES-d`, so the suites' folder stays untouched -- the rule from last night), `--bwlimit 8k`, SYNC SAVES WHEN EXITING A GAME on: a game quit, and a launch during the card `SYNCING SAVES TO THE CLOUD` -> the question (`203-question-over-exit-sync-…`); STOP IT AND PLAY -> the group took about nine seconds to die, the spinner covered it (the first stop path that actually needed the spinner), the game up at 00:54:00 local; `last-sync-exit` reads `… 130 cancelled`. A first attempt answered the question after the sync had already ended by its own progress bound (D-CLOUD-111: with 8 KB/s the exit sync reads THE CLOUD TOOK TOO LONG after some twenty seconds), which is why the second run answered within the sync's first seconds. Two frames filed. Guest d's cloud config restored, `cloudsaves.gameexit` back to 0.
- H700 `h700-all-20260916-586d2334fd` built (tar sha 550b8863dad59f41…); not staged. The nine suites on the build 7 image: running.

## 05:20 UTC — Build 7 nine of nine; the benchmark answers the question

- The full run read time-to-play FAIL: the game-to-game cell fires a launch the moment the emulator is gone and waits for the next game's first frame; with D-CLOUD-130 that launch meets the question, and the cell saw no frame (`the next game's first frame missing`, the stamp `completed`). `tools/time-to-play` now presses STOP IT AND PLAY half a second after the relaunch, from the frame loop's thread, and reports the press as a row (D-CLOUD-131; a26 `a59007e398`, `4f6b42945b`). Alone: PASS in 59 s -- the relaunch 0.16 s, the question answered 0.99 s, the next game's first frame 2.01 s after the exit, the stamp `cancelled`. So the price of the question, on this hardware, is about a second of the player's own press; D-CLOUD-098's budget stands.
- Build 7 `586d2334fd` is the device candidate: H700 filed (tar sha `550b8863dad59f41…`), not staged; the RG SP is on build 6.

## 15:50 UTC — The manager's DELETE pause is the capture on the interface thread (#205); COPY TO FREE SLOT records nothing (#206)

- Maintainer: *"deleting the save state from the EmulationStation save state manager took longer than usual ... it seemed to hang for a second or so ... duplicating a save is incredibly quick."* Not a fault: since D-CLOUD-053 the Y handler runs `cloud_capture --retire` before the unlink and `cloud_capture --rescan` after it, both through `ApiSystem::executeScriptLegacy` (`popen`, blocking) on the interface thread, then refreshes the grid. X runs no script. Renumbering is gone since D-UI-069, so no files move.
- Measured on guest d (build 7, x86_64): retire 60-65 ms, rescan ~75 ms, the pair ~140 ms; the real manager deletion of the D-UI-069 proof (2026-09-15 22:35 UTC) logged retire at 150 ms. The RG SP was unreachable on both its addresses (two read-only attempts in the device-actions log), so the device's number -- the `(--retire, N ms)` line in `/var/log/cloud_sync.log`, this boot only -- is still to be read. Several times the host's cost on the A53 is the second the maintainer felt.
- Side finding, proven on guest d: a slot made by COPY TO FREE SLOT gets no manifest entry from any mode (exit skips it as pre-existing, rescan/full adopt nothing new, the copy is not a move while its source still holds the bytes): `entries` stayed 4 after `cp state3 state5` + `--rescan`. Harmless under today's rclone sync; invisible to the manifest-driven sync #185 will build. The X handler also has no running-sync gate.
- #205 and #206 filed as sub-issues of #11 with acceptance criteria and a proposed direction (worker thread + spinner, or retire on the worker and the rescan detached); the maintainer decides the feel. Blindspot 45 (a fixture's speed, not the device's). Guest d's manifest now carries two scratch `retired` rows (`Bobl.state5`, `Bobl.state6`); the scratch files are gone; nothing else on the guest changed.

## 16:00 UTC — #205 reproduced on guest d at the interface: YES takes 0.59 s to the first changed frame, a copy 0.35 s

- Maintainer: *"Let's first see if we can reproduce this on the VM and if we can notice the time differences, then we can continue to solve it."* Done through the manager itself on guest d (build 7), a press timer over the QEMU monitor (`press-time.py`, 83 ms per frame): Bobl -> X (GAME OPTIONS) -> SAVE STATES -> the manager; slot 3 -> COPY TO FREE SLOT made slot 4 (11:42) with no capture line in the journal and `last-capture` untouched; slot 4 -> Y -> ARE YOU SURE...? -> A.
- Press to first changed frame: open a menu or the dialog 0.26 s (this rig's floor), the copy 0.35 s, **YES on the delete 0.59 s**. The journal (`journalctl -u essway -o short-precise`, ES logs `executeScriptLegacy ->` to stdout) puts the retire's start at 11:43:49.052 and the rescan's at 11:43:49.217: 165 ms of interface thread for the retire alone, then the rescan (the script's INFO line only prints when something changed, so its end is not stamped finer than `last-capture`'s second). The retire itself logged 150 ms under ES against 60 ms when run from a shell with ES idle -- `popen` forks a 346 MB process (ES `VmRSS`) twice, which the A53 will feel more than the host does.
- So on x86_64 the delete costs about a third of a second more than a copy, below the eye's threshold; the same work at handheld clocks is the second the maintainer felt. Frames filed under `docs/qa-frames/2026-09-16/205-manager-*`; guest d's fixture is as it was (slot 4 made and deleted; the manifest carries its retired row).

## 18:45 UTC — #205 built: RC-12 build 8 `a4dfd97ca5`, the manager's deletion instant (D-UI-073, D-CLOUD-132)

- Maintainer, asked how the deletion should feel once the bookkeeping leaves the interface thread: *"Let's go with option 1 with the mitigation steps you've outlined."* Register D-UI-073 (instant; the three rules: paths not pointers, one job at a time, joined at exit) and D-CLOUD-132 (the post-delete `--rescan` dropped: nothing renumbers since D-UI-069, and the reproduction showed it changing nothing).
- ES `a11cde632` on `feature/savestate-delete-async`, merged to `test/qa-integration` as `2bf92e67f`: `SaveStateDeleteQueue` (pure: the order book, unit-tested, 7 cases / 52 assertions, 109 / 1201 in all) under `SaveStateDeleter` (one worker thread; retire, then unlink; `shutdown()` joins from main's tail and the atexit hook). `GuiSaveState` hides a pending tile in `loadGrid()`, enqueues two strings on YES, and polls `completed()` in `update()` to read the directory back with the cursor kept. The syntax check gated the commit.
- Distribution `a4dfd97ca5` (the pin `1c…`, the schema doc's renumber bullet corrected). x64 built 17:24-17:27, H700 17:27-17:30 UTC; both binaries carry the worker's log line and no manager `--rescan` (`strings`). Filed: `x64-all-20260916-a4dfd97ca5`, `h700-all-20260916-a4dfd97ca5` (tar sha `444cfc0c3c242d4f…`). Not staged.
- 18:40 UTC: the nine suites started on the build 8 image (`vm-qa-a4dfd97ca5.log`). Guest d: the update tar staged in `/storage/.update`, sha equal; the reboot that applies it waits -- the auto-mode classifier refused the VM reboot in the same command as the copy, so it is asked for.

## 23:15 UTC — Build 8 proven on guest d, and a ghost found: the reopened manager lists a file the worker has already deleted

- Nine suites PASSED on `a4dfd97ca5` (`qa-a4dfd97ca5-webdav-a-20260916-1840`, 18:40-19:05 UTC). Guest d updated in place from build 7 (maintainer: *"please reboot that guest d vm"*; the fixture survived: Bobl's slots, the manifest 4/8).
- The press timer on build 8: open the manager 0.266 s, COPY TO FREE SLOT 0.264 s, **YES on the delete 0.265 s** (0.588 s on build 7 this morning) -- the floor of this rig, the same as a copy. The retire ran on the worker (journal: `[2523]` the worker's thread, 160 ms), no `--rescan` line, the file gone, the retired row written, ES's pid unchanged. Two copies then two deletions back to back: both at the floor, two retire runs in order, both files gone, two rows, no capture failure. YES then B ten milliseconds later: the page died with the retire in flight (`queued` 19:08:28.788, `deleted` .963), ES's pid unchanged, the file gone, the row written.
- **Found by the teardown case:** the manager reopened afterwards showed a tile labelled `09/16/2026 19:08` with no thumbnail -- slot 4's `SaveState`, still in the system's `SaveStateRepository`, whose file the worker had unlinked. The page that would have called `refresh()` on completion was the one B had closed; the next manager's constructor calls `loadGrid()` on the repository as it stands. `hasSaveStates` had the same ghost. Fix: the repository never hands out a state whose file is gone (`onDisk`, an uncached stat, in `getSaveStates` and `hasSaveStates`); `refresh()` still frees the stale objects when a live page sees a completion. The restart-in-flight case that followed pressed YES on that ghost, so it tested nothing and is rerun on the fix. Note for the record: ES does not handle SIGTERM (`signal(SIGTERM, …)` is commented out in main.cpp), so `systemctl restart essway` kills it in 25 ms without main's tail or the atexit hook -- the join guards ES's own exit paths (QUIT, reboot from the menu), and a `systemctl stop` mid-retire ends the cgroup whole, manifest intact by its atomic write, the file still present: consistent, as today.
