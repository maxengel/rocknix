# 2026-09-07 — work log

## 03:55 UTC — Task 6: the fixtures for #11 (#35), each a failing commit first

The host had rebooted and cleared `/tmp`; `tools/vm-pair up` rebuilt the
pair on `088c2bd22e` in four minutes, with the fixed `backuptool` put back
in `/tmp/qa-bin` by hand. Before writing a fixture: what the QA WebDAV
compares by, measured in a fourteen-case matrix on guest `a` (rclone rule
§ "What the QA backends compare by") — the recency lever is the *local*
mtime, since the endpoint's modtime is the upload time.

The harness grew a second device (`--second-port`), prepare-and-restore per
device from a record captured before anything is written, one multiplexed
ssh connection (5 ms a call instead of 90), and `epath()` — which found
that on MinIO the seeding step had been writing and reading its note
through a doubled bucket path, agreeing with itself about a folder the
device never looked at (blindspot 31). Then the fixtures in order, each run
on the pair and committed with its output: A1 (fourteen red — every
writer, both timings, blindspot 28 in full), A2 (the retry already holds,
the equal-size change does not), A3, A8 (the torn pair installs as a mixed
set), A9 (a renumber keeps its mtime, `--update` skips it, the delete is
undone and S3 ends up nowhere), A11, A12 (the helper warns but keeps
`RESTOREPATH`, and every pass refuses), A14, A5, the retention kill, and
#9's ten items under both transports (`--transport`, `--bisync-flags`).

Two defects came out of setups rather than assertions. A3 pointed `a` at
the folder `b` had filled with `cloud_setup --set-saves-remote`, as CHANGE
CLOUD FOLDER does, and was refused: the probe read `rclone lsd`'s listing as
a rejection, so every existing folder with subfolders was — and ES ran the
command with its exit code discarded, so on a handheld the refusal was
silent. Both fixed (`f3b330b5e1`; ES `2d17709b2`, pinned `164f8f48d8`), the
ES half waiting for a build and a VM frame. And on MinIO `rclone cat` of a
missing key exits 0 with nothing, which passed A14's first check for a
manifest that was never there; `cloud-test-backend cat` checks existence
first.

The count on WebDAV: 48 single-device checks pass; 43 two-device checks
fail by design (37 fixtures, 6 contract). On MinIO: 38, then 39 with the
`cat` fix — MinIO keeps modtimes and offers hashes, so the equal-size
change is caught there, a killed upload leaves nothing, and four of
bisync's six WebDAV failures do not recur. Gate 11 (#9) reads those two
columns; the pair fixtures stay red until #21/#22.

## 17:10 UTC — Gate 11: the spike scored, and the verdict (#9)

Task 8 to 10 in one sitting, on the pair. First the runner learned to
show its work: `--dump` writes every transport run's raw output to the
host, `moved:` names what rclone copied or renamed, the first conflict
line's bytes are printed before any parser trusted them, and item 9 counts
every rclone the pass spawns rather than the transfer alone. The dump paid
at once: item 5's "3 transferred" under bisync on WebDAV was bisync
downloading the both-changed pair to compare it — and skipping it as
equal — so the item is a PASS, and the runner's count had been reading a
hash download as a copy.

Then the variants: `--compare size,modtime,checksum --download-hash` makes
the same-size change visible on WebDAV by downloading the whole tree on
every pass (item 9: four of four files, idle); `--compare` alone does not
see it; on MinIO everything but item 10 passes plain. Item 10 fails on
both backends: without its listings bisync demands `--resync`, and
`--resync` means the device's copy wins. That is architectural under
D-CLOUD-039's rule, so the verdict is the reconciler's own transport
(D-CLOUD-052) and D-CLOUD-044 is moot. Recorded on #9 with the grid and
the dumps, mirrored on #22, #9 closed, the epic's child ticked.

The maintainer's worry from D-CLOUD-039 — rebuilding what bisync offers —
got its answer from the runs rather than argument: the two things bisync
did for free here (a renumber, a resumed push) are the retired records and
the pending-publish record #21/#22 already need for the wizard's questions.
The copy stand-in's budget (eight spawns, four downloads on an idle pass)
is the stand-in fetching every remote file to hash it, not the design;
Gates 4 and 12 measure the real reconciler on the H700.
