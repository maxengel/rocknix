# Work log, 2026-09-23

## 00:15 UTC — #245's tenth cut turned nothing; a two-byte record read as empty (eleventh cut)

The tenth cut `bf53cea7e4` proved the recording path (a cookieclicker session with a `SET_ROTATION: "1"` line appended to the launch log wrote `/storage/roms/savestates/nes/cookieclicker.rotation`) and turned no picture: every mark stayed on the right. A diagnostic binary built with `PACKAGE=emulationstation make docker-package` and run on guest d in place of the service (no image rebuilt, so vm-qa's image was left alone) said `CaptureRotation: read .../Bobl.rotation exists yes turns 0`: `Utils::FileSystem::readAllText` reads three bytes first to skip a UTF-8 byte-order mark, a two-byte file fails that read, `seekg(0)` does not clear the failed stream, and `rdbuf()` yields nothing. The record is `turns=N` now (the reader takes a bare digit too), unit suite 118/1285, rule `es-code-traps.md` "A file under three bytes reads as empty". The tenth cut's vm-qa was stopped (the cut is superseded); the eleventh `ed61a18d5c` (pin `034b5d574`) builds with the manager, recording, list and viewer proofs in that order -- the list pass reboots the guest at its end, which is what had made the first recording test run against a guest that was down.

## 00:50 UTC — #245 proven on the eleventh cut; two chain mistakes on the way

`ed61a18d5c` on guest d: the recording path (cookieclicker's session with a `SET_ROTATION: "1"` line → `turns=1` at exit, `CaptureRotation: cookieclicker turns 1 (core 1)` in the log) and every surface upright with the right-edge mark above -- manager NES 73x73 and GB 89x89 tiles portrait, the SCREENSHOTS list 141x141 / 172x172, the full-screen viewer 305x304 (frames `docs/qa-frames/2026-09-23/245-*`). Staged on the RG35XX SP; vm-qa run 12 and the rehearsal running; the reboot asked after them.

Two mistakes of mine cost half an hour: a chain script written without `chmod +x` sat with an empty log until its process list was read, and the image file name carries the build date, so a chain that crossed midnight named a `-20260922` image that no longer existed (the memory `session-scripts-outside-the-scratchpad` already said so; the chains now glob the newest). And the proof scripts that end in a reboot -- both the manager pass and the list pass -- must not be followed by a test that needs the guest up; the recording test ran twice against a rebooting guest before it ran alone.
