# Work log, 2026-09-23

## 00:15 UTC — #245's tenth cut turned nothing; a two-byte record read as empty (eleventh cut)

The tenth cut `bf53cea7e4` proved the recording path (a cookieclicker session with a `SET_ROTATION: "1"` line appended to the launch log wrote `/storage/roms/savestates/nes/cookieclicker.rotation`) and turned no picture: every mark stayed on the right. A diagnostic binary built with `PACKAGE=emulationstation make docker-package` and run on guest d in place of the service (no image rebuilt, so vm-qa's image was left alone) said `CaptureRotation: read .../Bobl.rotation exists yes turns 0`: `Utils::FileSystem::readAllText` reads three bytes first to skip a UTF-8 byte-order mark, a two-byte file fails that read, `seekg(0)` does not clear the failed stream, and `rdbuf()` yields nothing. The record is `turns=N` now (the reader takes a bare digit too), unit suite 118/1285, rule `es-code-traps.md` "A file under three bytes reads as empty". The tenth cut's vm-qa was stopped (the cut is superseded); the eleventh `ed61a18d5c` (pin `034b5d574`) builds with the manager, recording, list and viewer proofs in that order -- the list pass reboots the guest at its end, which is what had made the first recording test run against a guest that was down.

## 00:50 UTC — #245 proven on the eleventh cut; two chain mistakes on the way

`ed61a18d5c` on guest d: the recording path (cookieclicker's session with a `SET_ROTATION: "1"` line → `turns=1` at exit, `CaptureRotation: cookieclicker turns 1 (core 1)` in the log) and every surface upright with the right-edge mark above -- manager NES 73x73 and GB 89x89 tiles portrait, the SCREENSHOTS list 141x141 / 172x172, the full-screen viewer 305x304 (frames `docs/qa-frames/2026-09-23/245-*`). Staged on the RG35XX SP; vm-qa run 12 and the rehearsal running; the reboot asked after them.

Two mistakes of mine cost half an hour: a chain script written without `chmod +x` sat with an empty log until its process list was read, and the image file name carries the build date, so a chain that crossed midnight named a `-20260922` image that no longer existed (the memory `session-scripts-outside-the-scratchpad` already said so; the chains now glob the newest). And the proof scripts that end in a reboot -- both the manager pass and the list pass -- must not be followed by a test that needs the guest up; the recording test ran twice against a rebooting guest before it ran alone.

## 01:30 UTC — The eleventh cut applied to the RG35XX SP

vm-qa run 12 eleven suites PASSED and the rehearsal 19/19 (`docs/vm-qa-log.md`); the maintainer: *"Yes, you can reboot."* Sent 01:24:11 UTC through `tools/device-act` after the idle check (no emulator, no cloud run, both locks free, the tar in `/storage/.update`); back after 280 s on `ed61a18d5c`, `build/devices`, queue empty, store 238 intact, settings intact, keeper armed, no rotation record yet (a session writes the first), no fault. The device now carries #241, #242, #243, #245 and #246 over the fourth cut it ran this afternoon. What is theirs: a vertical FBNeo game's session (#245), an offline session with unlocks (#246), an NES thumbnail's shape (#243), and the RG SP when the candidate is confident (D-QA-031).

## 01:45 UTC — #248: a capture made before the record existed stays wrong until the game is replayed; the core's table answers at once

The maintainer, on the eleventh cut: *"the existing thumbnail in the game save for Miss Pacman is still rotated the wrong way. If we're going to do it properly, we need to have it adjust itself during upgrade for the existing screenshots in place. This Miss Pacman screenshot needs to be rotated 90° clockwise."* Right: the record is written at the end of a session, so nothing played before this cut has one. FBNeo's driver table knows every game's orientation without a launch -- `BDF_ORIENTATION_VERTICAL` / `FLIPPED` per driver, mapped by its libretro layer (VERTICAL 1, FLIPPED 2, both 3, Vertical mode off); Ms. Pac-Man is VERTICAL | FLIPPED → 3 → three quarter turns counter-clockwise, the 90° clockwise the maintainer named. `fbneo-lr` now generates `/usr/config/emulationstation/rotation/fbneo.txt` at build time from the source it compiles (`scripts/rotation-table.py`: 2,544 games with a turn; 1,241 / 37 / 1,266 by value), and `CaptureRotation::read` falls back to the table for the game's core (`getCore()`: the `fbn` system's default is fbneo) when no record exists -- read at draw time, nothing migrated, the record still wins. Register D-UI-082; #248 filed with the quote. The FBNeo system on the image is `fbn` (`/storage/roms/fbneo`, states under `savestates/fbn`), not `arcade` (mame2003_plus). Twelfth cut `428d44af40` building; the proof seeds Ms. Pac-Man's ROM name with a left-marked fixture (the game's top on the raw frame's left edge, for rotation 3) and no record, and finds SCREENSHOTS on the carousel by its logo (`find-carousel.py`), since a new system moves the order.

## 02:40 UTC — Which cores rotate, and tables for the ones with a driver list (thirteenth cut)

The maintainer, before the reboot to the twelfth cut: *"I'm using fbneo and the arcade playlist, if I understand correctly. either way, shouldn't we have the apply for all arcade systems that might save rotated screenshots by default? is there a way to identify what cores do that?"* Read from the device (read-only): all 39 arcade games under `/storage/roms/fbneo` (the `fbn` system, default core fbneo; `arcade` and `mame` empty), Ms. Pac-Man's state under `savestates/fbn`, no core overrides -- the twelfth cut covers their library. The census, from the build root's core sources (`RETRO_ENVIRONMENT_SET_ROTATION` in `.c`/`.cpp`, not the header that defines it -- the first pass matched every core that way): beetle-lynx, beetle-wswan, fbalpha2012, fbalpha2019, fbneo, flycast, flycast2021, mame2003-plus, mame2010, mame, melonds-ds. MAME 2015 rotates inside itself (no call), so its captures are already upright. The handheld and console ones (Lynx, WonderSwan, DS) take the rotation from the ROM header or a player setting -- the session record covers them going forward and a table is not derivable the same way. Flycast's Naomi verticals wait.

Tables now: `rotation-table-mame.py` (a plain ROT270 is 1, ROT180 2, ROT90 3, as `mame2003-plus/src/mame2003/video.c` and `mame2010/src/osd/retro/retromain.c` map them; a driver whose orientation carries a flip is rotated by the core itself and gets no line) for mame2003-plus (1,643 games; mspacman 3, galaga 3, dkong 1) and mame2010 (2,183); `rotation-table-fba.py` (FBNeo's generator; the same VERTICAL 1 / FLIPPED 2 / both 3 mapping in both FB Alpha ports) for fbalpha2012 (853) and fbalpha2019 (1,924). Each package installs its own `<core>.txt` at build time from the source it compiles. dkong differs between FBNeo (3) and MAME 2003-plus (1): each core's raw frame is its own, which is why the table is per core. Current MAME keeps the same mapping and waits for a build window (its rebuild is hours on the H700). And the SCREENSHOTS lookup now reads RetroArch's achievement screenshot name, `<content>-cheevo-<id>` -- the maintainer's folder was mostly those. Thirteenth cut `9221b4528d` building; the proof adds galaga under `arcade` (mame2003_plus, no record) and a cheevo-named screenshot.

## 03:57 UTC — The thirteenth cut applied to the RG35XX SP

vm-qa run 14 eleven suites PASSED and the rehearsal 19/19; the maintainer: *"Yes, you may reboot."* Sent 03:51:02 UTC through `tools/device-act` after the idle check (no emulator, no cloud run, both locks free, the tar in `/storage/.update`); back after 280 s on `9221b4528d`, `build/devices`, queue empty, the five tables present (`mspacman 3` in FBNeo's), no record for Ms. Pac-Man yet -- exactly the state #248 exists for -- the store's 238 games and the settings intact, the keeper armed, no fault. Over the fourth cut the device ran at the start of 2026-09-22 it now carries #241, #242, #243, #245, #246 and #248. The maintainer's checks: Ms. Pac-Man's existing thumbnail upright with nothing replayed; a vertical FBNeo game's session; an offline session with unlocks; an NES thumbnail's shape. The RG SP (D-QA-031) waits for the word that the candidate is confident, with `h700-all-20260923-9221b4528d/ROCKNIX-H700.aarch64-20260923.tar`.

## 04:15 UTC — The rotation confirmed on the device; the banner question; #249 (the AUTO SAVE tile leaves RetroArch's slot at 0)

The maintainer on the thirteenth cut: *"I can confirm that the rotation worked."* (#248's device box ticked.) Then: *"the bottom-left RetroAchievements banner that says it's logged in looked a little blurry. Is there anything that could be causing that? ... Did something change with font scaling or something else."* From the tree: between the fourth cut the device ran yesterday morning and this one, the packages that changed are EmulationStation, the proxy (patch 015), fbneo-lr and the four arcade cores (install steps only); RetroArch is untouched since 2026-09-15 (patch 0014, the notification backdrop), and the H700 config keeps the widgets at a fixed scale of 1.0 with the xmb monochrome font. Nothing we shipped draws that banner or scales its font; if it looks softer in Ms. Pac-Man than in Bubble Bobble on the same build, that is RetroArch's own widget rendering under rotation and worth a separate look; if both look the same, that is how the font has always rendered at 640x480.

Then: *"when loading from the autosave slot in the Save State Manager, if I hit the menu button and then the load save hotkey, it doesn't reload the autosave ... RetroArch, when loading from the auto save, had the save state slot at slot 0 and not at slot auto."* Cause in `setsettings.sh`'s `set_autosave` (#196): the `.auto` file turns `savestate_auto_load` on and leaves `state_slot` alone, so RetroArch's current slot stays 0 and the load hotkey loads slot 0's file; a numbered tile gets `-e <slot>`, which makes that slot current. RetroArch's own Auto slot is `state_slot = -1` (`runloop.c`: a negative slot names `<content>.state.auto`; the quick menu says `Auto`). Fix: the `.auto` case sets `state_slot -1`; the save hotkey then writes the auto save too, which is what the Auto slot means and what the exit save writes anyway. #249 filed; checked on guest d with the launcher bind-mounted over the image's (`check-249.sh`).

`check-249.sh` on guest d (04:10 UTC), the launcher bind-mounted over `/usr/bin/setsettings.sh`: the AUTO SAVE tile launches with `-state_file .../Bobl.state.auto`, the launcher logs `state_slot = "-1"`, RetroArch auto-loads the file; SLOT 1 gets `-e 1`, `state_slot = "1"`, `Loading state .../Bobl.state1`; START NEW GAME leaves the slot alone. Note for the next walk: the manager opens on START NEW GAME, so AUTO SAVE is one step right and SLOT 1 two -- the first pass launched a new game by mistake. The launcher's settings go to its own temp config (`TMP_CONFIG`, appended to RetroArch's), not to `retroarch.cfg`, so the launcher's log line is the thing to read. Fourteenth cut `c0c2d15179` building.

## 05:05 UTC — #250: the manager's arrow turned with the thumbnails; the fifteenth cut

The maintainer, on the thirteenth cut: *"the arrow icon that appears in the save state manager in EmulationStation is now rotated as well, which it shouldn't be. It's also scaled to reflect how we're scaling the system ... the arrows should all remain exactly how they were. It's just the screenshot itself."* Right, and my own frames had shown it since the eleventh cut -- the START NEW GAME arrow pointing down beside Ms. Pac-Man's tile -- while the ring measure looked only at the ring. `GuiSaveState` set the transform on the whole grid (`setImageDisplayAspect` / `setImageDisplayRotation`), so the two arrow tiles (`:/freeslot.svg`, the placeholder states with no screenshot) took it too. Now the grid's tile decorator applies it only to a tile whose entry has a capture (`saveState->getScreenShot()` non-empty), and the grid-wide setters are gone. ES `<pin>`, fifteenth cut building; the proof compares the arrow tile's pixels (`compare-region.py`, x 8..156, y 262..436) against the fifth cut's frame -- 0 of 25,752 pixels may differ -- while the ring measure says the capture tiles still turn. The lesson for the fixture rule: a proof that measures the thing that changed must also assert the things that must not have -- the arrow tile was on every frame and nobody looked.

## 05:20 UTC — #250's reference frame was wrong: the fifth cut had already reshaped the arrow

The fifteenth cut's proof compared the arrow tile against the fifth cut's NES frame and reported 1,033 differing pixels; the arrow measured 54x43 against the fifth cut's 84x38, and for a while that read as a second regression (upright, but smaller). It is the other way round. The fifth cut (#243 alone) set the aspect on the whole grid too, so on an NES game -- a 4:3 system -- the arrow was already fitted at 4:3: 84x38, wide and flat; the same cut's Game Boy frame (no aspect entry) kept it at 54x43. Every manager frame on file before #243 -- 09-15, 09-16, 09-17 and the 09-21 frame `77e7e97515` -- has the arrow at 54x43, x 55..108, y 306..348, and the fifteenth cut's tile is pixel-identical to the 09-21 frame in all three systems (0 of 25,752 differ; `compare-region.py`, x 8..156, y 262..436). The SVG explains the numbers: `freeslot.svg` is a 612x792 box holding a 391x314 arrow, about 1.25 wide to high at its own shape, which 54x43 is. So the fix restores the tile to before either change -- "exactly how they were" -- and the acceptance criterion on #250, which named the fifth cut as the reference, is corrected in the body. Frames `docs/qa-frames/2026-09-23/250-*`. The lesson beside the earlier one: the "before" of a must-not-change assertion is the build before the *first* change of the series, not the previous cut, because the previous cut can already carry the defect.

## 05:45 UTC — The fifteenth cut green; the RG35XX SP off with the fourteenth's tar queued

vm-qa run 16 on `aa8d525a8a`: eleven suites PASSED (`qa-aa8d525a8a-webdav-a-20260923-0506`, 05:06-05:31 UTC); the rehearsal from `b245fd12ac` 19/19 (`qa-aa8d525a8a-upgrade-from-b245fd12ac-20260923-0531`). RECORD.txt written; the fourteenth's marked SUPERSEDED. `stage-rg35xxsp-aa8d525a8a.sh` waited three minutes for the RG35XX SP (05:35-05:40) and stopped: no route to host, ping silent -- the device is off (01:40 local). Said plainly, because it decides what the next boot does: the fourteenth's `ROCKNIX-H700.aarch64-20260923.tar` is still in its `/storage/.update`, so its next power-on applies `c0c2d15179` (#249, green), and the fifteenth (#250) then needs a stage and a reboot of its own; if the device comes back online before it is rebooted, the fifteenth's same-named tar replaces the fourteenth's in the queue. Both the transfer and the reboot are asked for (D-QA-011).

## 06:05 UTC — The maintainer's two questions after #250: the VM's eyes, and one arrow for every console

The maintainer: *"Are there any changes we want to make to our VM policies to check for some of these visual errors? Also, is there a reason that the arrow in the Start New Game tile shouldn't be the same for every console? Maybe I'm missing something, but is every system now using the same arrow in the same direction at the same size?"*

The arrow: yes, and by design -- it is one SVG (`freeslot.svg`) drawn at the tile's size with no per-system transform, on START NEW GAME and START NEW AUTO SAVE alike; only a tile showing a capture takes the game's aspect and turn. The fifteenth cut's arrow tile is pixel-identical across `fbn`, `nes` and `gb` and to the frames from before #243. Between the fifth and the fourteenth cuts it was not: fitted wide on 4:3 systems, turned on vertical games, unchanged on the Game Boy -- three different arrows depending on the game, which is what was wrong.

The VM: the walks suite keeps frames of fifteen cloud screens and compares them to nothing; the manager, the SCREENSHOTS list and the viewer have no walk; a per-issue proof asserts the changed thing only. Filed #252 with the proposal: a frame diff against the last accepted cut's frames with masks and a per-cut claims list, so an unclaimed box fails the suite; walks for the screens the recent work touches; and the rule that a proof names its must-not-change baseline from before the series' first change. Parked as an open register decision pending the maintainer's word.

## 16:35 UTC — #252 adopted (D-QA-038): the walk frames are diffed against the last accepted cut

The maintainer: *"Device is now powered back on. I think your recommendation for 252 is a strong recommendation, and so we should roll that out as well. Since that's just a change on our end, it shouldn't require us to cut a new build. Let me know what testing is required on this."* The device came up on the fourteenth (`c0c2d15179`, the queued tar applied at the power-on, queue empty); the fifteenth's transfer and reboot are still the question.

`tools/frame-diff` (stdlib PNG, boxes from 16-px cells, masks, claims keyed by the baseline build, `accept`), the `frame-diff` suite in `vm-qa` (rc 3 = SKIP, shown in the table and the footer, never folded into PASS), `tools/vm-walks/masks.txt` and `claims.txt`, three `manager-*` walks with `ensure_manager_fixture` (stub ROMs, the ring pictures, `turns=1` for Bobl, mtimes 2026-09-01 12:00), and `default-pre` clearing `StartupSystem` with essway stopped. Proofs on the host: the constructed violation -- the twelfth cut's `fbn` manager against the fifteenth's, clock and date masked -- reports one box, the arrow (x 53..110, y 294..360, 1,533 px), and nothing else; the negative control -- runs 15 and 16, 63 frames -- reports the clock on every frame and the running transfer's count line and bar on one, and nothing else outside the masks, in 0.6 s; against the real baseline (run 14, the thirteenth) the clock's hour digit reached x 1135, so the mask is 1110..1215. Baseline accepted: run 14's frames, build `9221b4528d`, the cut the maintainer confirmed on the device. Run 17 (`--only walks,frame-diff` on the fifteenth's image) is the end-to-end proof. Testing required of the maintainer: none on the device; it is all host and VM.

## 17:45 UTC — #252's first three runs: the suite fails closed, then holds still

Run 17 (`qa-aa8d525a8a-webdav-a-20260923-1627`, `--only walks,frame-diff`): the sixteen walks PASS in 1,332 s, the three manager walks reached the SAVE STATE MANAGER at 1280x800 (arrow upright and identical on all three, the capture tile portrait with the mark above on `nes` and `fbn`, dates `09/01/2026 12:00`), and frame-diff **FAIL (1)** with 156 unclaimed boxes against the thirteenth's baseline -- the suite doing its job on a change I had made myself: the manager fixture's ROM folders changed the carousel backdrop behind every dialog (the right half of the frame) and the systems page, and the hub's SYNC SAVES WHEN EXITING A GAME row read COMPLETED with the switch on in the baseline (a full run, after the exit suite) and NOT DONE with it off in a `--only walks` run. Two fixes: `default-pre` now turns both sync switches off and removes `/storage/.cache/cloud_sync/last-*`, and the walks reset the QA cloud before seeding it (run 18 had framed the systems page and the phase line counting what run 17 uploaded). The phase-line mask widened to the dialog's width (the text is centred and as wide as its numbers). Run 18 (1653): the same boxes minus the hub's, FAIL as expected; accepted as the first baseline under the new suite (`walk-baseline/BASELINE.txt`, build `aa8d525a8a`, the thirteenth's x64 image not being kept), the three whole-frame claims pruned. Run 19 (1717): walks PASS 1,336 s, then vm-qa died at the frame-diff suite with `---: command not found` -- I had edited `tools/vm-qa` while it ran, and bash reads a script by offset (memory `never-edit-a-running-bash-script`). Run by hand, run 19's 78 frames against run 18's: **PASS, zero boxes** -- the whole suite, manager walks included, is pixel-stable between two runs. Run 20 (17:40, the cloud reset in place) retakes the baseline; run 21 proves it. No edits to shell tools until then.

## 18:35 UTC — #252 proven end to end: runs 20 and 21 pixel-identical

Run 20 (`qa-aa8d525a8a-webdav-a-20260923-1741`, the QA cloud reset before the walks seed it) against run 18's baseline: eight boxes, every one a row on the systems page -- FBNEO and NES reading `4 KB NOT YET IN YOUR CLOUD · 1 FILE` on a fresh cloud where run 18 had read NOTHING NEW TO BACK UP on a populated one (frames `252-diff-systems-page-*`). Accepted as the baseline. **Run 21 (1804): walks PASS in 1,332 s, frame-diff PASS, 0 boxes over 78 frames.** So under the final suite two runs frame every walked screen identically outside four masked rectangles, and a change on any of them fails the next cut unless its claim line says so. The baseline is the fifteenth's own frames for now: the thirteenth's x64 image is not kept, and the rule's "moved when a cut is accepted on the device" starts with the next cut. Left for #252: walks for the SCREENSHOTS list and the viewer (the carousel does not land on `imageviewer` from `StartupSystem`; the proof scripts found it by its logo). Committed `f23e56b11b` and the docs after it. What the maintainer has to test: nothing on a device; the gate is host and VM only, and it ran five times today.

## 19:40 UTC — Four "pending decisions" that were settled: the archaeology, and what prevents the repeat (#253, blindspot 52)

The maintainer, on the list of decisions I had put to them: *"With 209, can you check? We dealt with this question already once before ... I'm worried that we're relitigating an issue we already did."* And: *"I think a lot of these are actually settled issues. We need to spend the time doing our own project archaeology around this and then figuring out how we prevent this from happening in the future."*

The archaeology, from the register, the issues' comments, the commits and the logs: **#209** -- settled by parity (D-UI-057, 09-15; D-UI-059/069); its one open item ("what INCREMENT PER SAVE should do about the launched slot") is what Batocera's configgen does, a new slot; the only defect is ROCKNIX's `set_savestates` mapping. Recorded as D-UI-083. No RetroPie comparison exists in the record. **#228** -- the RC decision was made 09-20 (`77e7e97515`, back to 2.52.6; the 08-30 GStreamer pass is what makes 2.52.6 work, `64907d0ab8` the lesson that VIDEO cannot be off); recorded as D-WORKFLOW-026; the open question is the path after the RC (D-WORKFLOW-027, pros and cons on the issue: the tree already carries a gst-plugins-bad recipe, 2.54 needs its `mpegts` library and GL in gst-plugins-base; "WebKit without GStreamer" is not a configuration the GTK port builds). **#225** -- the maintainer's scope cut of 09-19 (RetroArch's own process; patch 0015's gate covers all 22 sites and the font free; upstream closed libretro/RetroArch#19577 the same way); recorded as D-QA-039 and the issue closed as delivered. **#42** -- D-WORKFLOW-014 (09-13): the last step before the upstream PR, not an RC item. **#251** -- verified in RetroArch's source: the fraction of the scale is dropped by `FT_Set_Pixel_Sizes` before rasterising and nothing is resampled; on the H700 the message-queue font comes out at 10 px (20 x 0.363 x 1.0 x 1.5) beside the achievement banner's 17 px; the options now carry numbers.

Why it happened: #236's "decisions pending" section was a copy of four questions written on 09-20, three of them settled when written, and no row in the register carried any of them under those numbers; blindspot 51 (09-21) was the same failure on the device boxes. What lands (#253): `tools/archaeology <terms>` / `--issue N` (register, blindspots, every work-log entry, rules, `git log -G`, issues and comments, as dated lines); `tools/work-log-index --write` (`docs/work-logs/INDEX.md`: 492 entries over 51 days, days under ISO weeks under months; `--check`; the push guard warns when a log is pushed without it); three rule sentences (archaeology before a question; a comment decision gets its row the same day; a pending section names IDs); blindspot 52; #236 § C rewritten to two open items (D-WORKFLOW-027, #251).

## 21:05 UTC — The ceremonies become a state machine a tool turns (#254, D-WORKFLOW-028)

The maintainer: *"What are we doing to operationalize our blind spot prevention? ... Do we need to build this into our CI workflow itself so that we're running some of these processes as state machines that are mandatory, as opposed to things that are just optional?"* -- naming futro, mini retros, code auditing, the friction log and its issues, the work log's patterns, and Pongogo's multi-timescale logging.

The archaeology first: the skills exist (`futro`, `mini-retro`, `begin-delivery`, `code-auditor`, `council`) and ran at phase boundaries -- kickoff 08-18, retro 09-04, futro 09-05, audits 08-25/09-03/09-06/09-11/09-13/09-14 -- and not once in the nine days of RC cuts after 09-14, the window of blindspots 47-52. No friction log existed anywhere in the record. `working-principles.md` already had the frame (a rule is written, routed, evidenced, audited, self-examining; broken twice, it moves up a stage). Pongogo (`pongogo/pongogo`, `.pongogo/instructions/`) keeps daily trigger-based entries, a weekly summary when the week ends, a monthly summary from the weeklies, retrospective triggers with depths and anti-triggers, mandatory archaeology before an improvement, and a read-this-file compliance gate.

What lands: `tools/ceremony-check` (retro / friction / weekly / monthly / index / register / blindspot guards / audit / futro, each ok, due or overdue; two levels -- the cheap ones refuse a push of `next` through the guard, an audit or a futro owed keeps `fork-checks.yml` red); `docs/friction-log.md` (six entries from today, each with an issue or a guard); `docs/retros/`, `docs/futros/`; `.github/workflows/fork-checks.yml` (index, register, ceremonies on every push of next and daily); `.claude/rules/ceremonies.md` (the table of states, markers, cadences and costs; the timescales; what a blindspot entry owes). The guard's positives: a friction line past its grace, a retro past its grace and an unguarded blindspot entry each make `--gate` return 1 (`CEREMONY_TODAY` moves the clock for the proof); the hook's refusal is proven with a dry-run push below. Found on the way: the index warning I had put into the hook earlier today sat inside the `pr/*` section and could never run for `next` -- moved. Register: D-WORKFLOW-028 decided (the maintainer's direction), D-QA-040 open (the cadence numbers and which ones block). The clock starts 2026-09-21.
