# 2026-09-22 work log

## 01:25 UTC -- #241 built and proven on the VM; a row-layout defect found under it

The maintainer: "include these changes in whatever build we next send to
the device." All three surfaces in one EmulationStation change
(`acd7a6fee`, merged `0970b66e1`): the scan page and the transfer page ask
CANCEL on B and stop their run -- SIGINT to the scan's process group (the
job now starts under setsid with a `>>> pid` line, CloudTransferJob's
shape), the transfer's existing SIGTERM with a `player-cancelled` stamp
token so the hub rows say SKIPPED, YOU CANCELLED IT rather than "a game
was started"; the scraper moved from the corner card and the toast to
`GuiScraperRun`, a page of the scan page's shape over `ThreadedScraper`,
which now reports through `progress()` under a lock. The ctl traps INT as
the player's cancel (stamp with the counts reached, `why=CANCELLED`, exit
130), and `last-good-scripts-test` gained a `cancel` plan word for its
fake helper -- sent from the run's side, because a harness background job
has SIGINT ignored at entry and bash cannot trap it, which cost one run
(342/0 on the second). `es-syntax-check --with` had been appending `-I`
after the build tree's, so the first check graded the old headers; it adds
`-iquote` now and a new file borrows a sibling's compile command.

On guest d (`f0576e5f1a`, EN): every frame as designed -- the footer, the
dialog, `SKIPPED - YOU CANCELLED IT` with the counts, the row's SKIPPED,
the stamps. And one defect: after a 37-game scan the OFFLINE ACHIEVEMENTS
page beneath came back with the row's two texts on top of each other and a
selector bar a screen tall. `MultiLineMenuEntry::layoutRows` read the cell
sizes back and re-split by 0.9/1.1 on every `setDescription`; the previous
candidate has it too (a second guest from vm-pair's `d55169e59e` disk: a
0-game scan leaves the row merely tight). Every walk so far scanned a
handful of games -- the fixture never had the device's numbers (blindspot 44). Fixed in
ES `7c7ffab43` (heights from the fonts), pin `3305233d1`, `next`
`56fe1fb2cf`; x64 run 15 and H700 run 6 building.

## 01:40 UTC -- the second cut proven, a French line cut mid-word, a third cut building

On `56fe1fb2cf` (ES `3305233d1`) guest d, EN then FR, every frame as
designed and the OFFLINE ACHIEVEMENTS page beneath drawing correctly after
the cancelled scan closes (`13-b10-page-after-cancel`): the row fix holds.
The scan's stamp `130 scan cached=2 skipped=31 ready=3 why=CANCELLED`, the
backup's `130 player-cancelled`, the scraper's SKIPPED - YOU CANCELLED IT ·
GAMES SCRAPED: 11, and the French of all three. One thing the 640x480
French frame caught (blindspot 41's class): the scraper's done-page note
"METTEZ À JOUR LES LISTES DE JEUX POUR APPLIQUER LES MODIFICATIONS." ran
off the line and was cut mid-word. ES `46111b1e3`: the note takes the
longest form that fits (D-UI-035), with "UPDATE GAMELISTS TO APPLY IT." as
the short one; pin `54b5e7427`, `next` `75603308e4`. vm-qa run 3 on the
second cut was stopped at its first minute so the suite runs once, on the
final cut; the chain is x64 run 16, then H700 run 7 beside the full EN/FR
proofs, then vm-qa run 4 and the upgrade rehearsal from the RG SP's build
`b245fd12ac`. H700 run 6's artifacts are marked superseded.

## 02:25 UTC -- third candidate `75603308e4` proven; the round waits on the soak

x64 run 16 and H700 run 7 (`h700-all-20260922-75603308e4`, RECORD.txt;
run 6's directory marked superseded), the full EN/FR proofs on guest d
again (37 frames a pass, the French scraper note fitting its line now),
vm-qa run 4 eleven suites PASSED (`qa-75603308e4-webdav-a-20260922-0153`),
the upgrade rehearsal from the RG SP's build `b245fd12ac` to this tar 20/20
(`qa-75603308e4-upgrade-from-b245fd12ac-20260922-0218`). One more backup
cancelled on d and the QA cloud listed after it: 33 whole files, no
`.partial`/`.rclone*`/`.tmp`, the cut 150 MB file absent -- the "no partial
file in the cloud" box ticked on evidence, not on rclone's reputation. Key
frames filed under `docs/qa-frames/2026-09-22/` (the RETROACHIEVEMENTS
SETTINGS frame, which shows the QA account's name, is not filed). #241's VM
boxes ticked; open there: the maintainer's classification of the other
background cards, and `ra-offline` not re-run on this cut. #236 carries the
third candidate (body, comment, page v9 with `a-build-3`); D's rehearsal box
is green for this tar. Guest d and the pair are down; the H700 tar goes to
the RG35XX SP after the soak, and the reboot is asked for by name.

Two things for next time. A harness case that sends a signal must not run
from a `&` job: asynchronous lists in a non-interactive shell have SIGINT
ignored, and bash cannot trap a signal ignored at entry -- the fake helper
now sends it from inside the run. And `es-syntax-check --with` appended `-I`
after the build tree's, so the first pass graded stale headers as if they
were mine; `-iquote` now, and a new file borrows a sibling's command.

## 02:40 UTC -- the other background cards stay (D-UI-079); #241 closed

The maintainer, on #241's last box: the game index, the content installer,
the OS update and the Bluetooth scan are outside the core work and keep
their cards for now; the foreground-page rule is for the lanes the fork
works in -- cloud sync and cloud behaviours, game saves, Wi-Fi. D-UI-079,
a paragraph in `engineering-practices.md` ("Change stays inside the fork's
lanes") and a line in `es-native-ui.md`. #241 closed completed: the three
pages, the ctl trap, the row fix and the French line, on `75603308e4`.

## 04:50 UTC -- #242: the proxy's name lookup, unbounded, on every offline request

The maintainer, mid-soak: Wi-Fi off, Doctor Mario, VIEW THIS GAME'S
ACHIEVEMENTS, "An error occurred. Timeout was reached."; then the launch
showed no RetroAchievements. The device's logs, read once they brought it
back: the interface asked the proxy's store for `patch` (game 1469, whole in
the store: patch, unlocks, achievementsets, gameid under RetroArch's hash),
waited its fifteen seconds, fell through to the web, and got libcurl's
timeout ten seconds later. The proxy logged each `Request:` twenty seconds
after it arrived, because the handler probes RetroAchievements to write
`online=` in that line, the probe's `urlopen(timeout=5)` does not bound the
resolver, and with an address and a route but no DNS glibc waits its
twenty. The launch paid it twice (`login2` always tries upstream first)
before the store answered the sign-in. Three `Unknown game` launches
earlier in `exec.log` were `ExitProbe.gb`, the QA fixture with no set.

The VM never showed it: with the route gone the resolver fails at once
(`Network is unreachable`), which is the shape #190/#193 were proven on. A
resolver that waits is a black-holed nameserver (`repro-242.sh`, v2). Fix
in the fourth cut `f2f23da875`: proxy patch 015 (`resolve_host_bounded`,
three seconds on its own thread, before the probe's HEAD and every upstream
attempt; the log line reads the tracker; 23/23 tests), ES `c8b03e3d6` (pin
`a29db7111`: offline, a proxy that did not answer is a sentence, not a web
request). The chain: x64 run 17, guest d rebuilt, the fixture again, H700
run 8, vm-qa run 5, the rehearsal.

## 05:00 UTC -- #242 fixed and proven in the handheld's shape; a fixture lesson

Fourth cut `f2f23da875` on guest d with the address kept, the route gone
and a black-holed resolver (`options timeout:10 attempts:2`): the summary
at once, Böbl's page within seconds, the proxy logging `patch` and
`unlocks` the instant they arrived and its probe as "name lookup ... took
longer than 3 s"; no timeout in the interface's log. The first after-run
went to the web anyway, and that was the fixture: the toggle had gone on
through the ctl after the reboot, and a running interface never sees a
setting changed from the shell -- the reboot goes back in. Two lessons in
`generic-x64-vm-testing.md`: an offline proof runs in both shapes, the cut
link and the resolver that waits (#242 shipped in three candidates past
proofs that only cut the link), and a fixture that flips a setting from the
shell reboots before the walk. Both images carry patch 015 (the compiled
modules hold its strings). vm-qa run 5 and the rehearsal running.

## 05:25 UTC -- vm-qa run 5: ten suites green, the eleventh was the harness's own gap

`qa-f2f23da875-webdav-a-20260922-0452`: `scripts` FAIL (1) because
`last-good-scripts-test` unpacks only `*/linux/raofflineproxy/*` of the
pinned client before applying the package's patches in order, and patch
015 carries three tests under `linux/tests/` -- one hunk with no file to
land on, and `patch` refuses the whole patch: "does not apply to the pinned
client", while `scripts/unpack` had applied it to the full tree for the
build. The harness unpacks the tests too now; rerun on the same tree,
PASSED 342/0, 15 patches applied and compiled. The rehearsal from
`b245fd12ac` 20/20. Row in `docs/vm-qa-log.md`.

## 05:35 UTC -- #242 closed; a stdlib PNG blackout for frames that carry the account's name

The service-stopped proof needed two tries: stopping the service before
the monitor had marked the device offline left the state file at `online:
true`, and the page went the online way (the web's timeout, then the
device's copy refused) -- upstream's design for a service that dies while
believed online, noted on #242 and left. With the monitor's mark first, the
page says THE OFFLINE ACHIEVEMENTS SERVICE DIDN'T ANSWER within eight
seconds. Its frame carries the QA account's name in the summary's title, so
`tools/png-blackout` paints a rectangle out with the standard library (no
Pillow on this host or in the frame tool), and the frame is filed rather
than described. #242 closed as delivered in `f2f23da875`.

## 15:10 UTC -- the RG35XX SP on the fourth cut, with the maintainer's yes

The maintainer, ~14:55 UTC, bringing the device online: they had not run
the extended offline test after the timeout and wanted the latest build
first -- "you can transfer it over, and you also have permission to
reboot." Idle check clean at 14:57 (no emulator, no cloud transfer, the
sync and scan locks free), the tar copied to a staging directory, hashed
on the device (equal to `SHA256SUMS`), moved into `/storage/.update`, the
reboot at 15:01 through `tools/device-act` (its label says the yes came at
~05:45 UTC, which is wrong: the script was written at 05:45 and run at
14:57; the act's own record carries the true time), back at 15:06 on
`f2f23da875`, `build/devices`, the queue empty; the proxy active with the
toggle on, the 238-game store and the account intact, no crash status in
the new boot's journal. The soak restarts here.

## 15:20 UTC -- the maintainer confirms #242 on the device

On the RG35XX SP on `f2f23da875`: "RetroAchievements are now visible when
browsing within EmulationStation ... it correctly shows when I started the
game, that I am offline, that the text box extended to the correct point,
and that the right images were shown." The offline page, the offline
sign-in toast with its backdrop (#194), the badges -- on the hardware the
defect was found on. Recorded on #236 (the `a-build-3` line, still open for
the build id and CANCEL on the scan page) and #242. The soak continues.

## 16:45 UTC — The soak's two unlocks count on any core (D-QA-037)

The maintainer, mid-soak on the RG35XX SP: *"I've unlocked a number of offline achievements in Dr. Mario for Nintendo. I know we're looking for FB Neo, but is it really a difference if we use one core versus another?"* No: #211's crash was RetroArch's threaded video wrapper running a posted command twice (patch 0015, `e5d14d4220`), reached through the badge image that follows an award, and rcheevos, the proxy and that wrapper sit above every core. D-QA-036 named FBNeo because Bubble Bobble is where it happened, which makes that session the preferred repeat of the original conditions and not the gate. Register row D-QA-037; #236's soak line and a comment say so. The device is offline (no ping), so the journal's award lines and any `status=139` wait for its return.

## 16:50 UTC — #243: thumbnails and screenshots at the system's aspect; the fifth cut proves the manager, the sixth the list

**The change (D-UI-080, ES `3b7265f33` + `9f46264a8`, pins `820852ea7` then `cc2189f47`; distribution `5d8bc093c7` = fifth cut, `2e2773bacf` = sixth).** `ImageComponent::setDisplayAspect(ratio)` fits a picture at a width-to-height of its own instead of the file's; the SAVE STATE MANAGER's grid gives every tile its game's system's aspect (`ImageGridComponent::setImageDisplayAspect` → `GridTileComponent`), and the SCREENSHOTS list gives the picture bound to a screenshot the aspect of the system the screenshot was taken in, found by the content name RetroArch put in the file name (`DisplayAspect::forScreenshot`, cached per file). `DisplayAspectText` holds the table (4:3 for the consoles and computers whose pixels are not square; handhelds and arcade left at the file's) and the file-name rule; unit tests. `tools/es-syntax-check --tree <worktree>` builds a farm of the build tree's headers with only the changed ones pointed at the worktree, so an edit touching headers in es-core and es-app checks without `#pragma once` seeing a header twice.

**The fixture (`mkpng.py`)**: a picture whose circle is round only at the aspect the pixels were made for — a 256x240 NES one drawn round at 4:3 (in pixel space its ring measures 165x207, w/h 0.80) and a 160x144 Game Boy one (round as is). `measure-243.py` finds the ring in a frame and reports its box, so the proof is a number and not an impression. Seeded on guest d as Bobl's auto save and slot 1 (NES), Ninoid's auto save (GB), and two screenshots named the way RetroArch names them.

**Fifth cut on guest d, 640x480 (`5d8bc093c7`)**: SAVE STATE MANAGER, Bobl: both tiles' rings 87x87 (w/h 1.00) — a 256x240 file drawn 4:3; Ninoid: 98x99. SCREENSHOTS list: Bobl's picture 151x189 (0.80 — **the file's proportions, unfixed**), Ninoid's 191x191. The list check named a system "screenshots"; on the image the list is the system **`imageviewer`** (fullname Screenshots, platform imageviewer), which is what `FileData` already tests for a picture file's own image. Fixed with `hasPlatformId(PlatformIds::IMAGEVIEWER)` in both places (`9f46264a8`), pin `cc2189f47`, sixth cut `2e2773bacf` building (x64 run 19 waits for vm-qa run 6 to release the image; H700 run 10 done, rc 0).

**Driving the page, for the next walk**: on a game list, A launches; the manager is X = GAME OPTIONS (opens with SAVE STATES highlighted) then A (`key s`, `key x` on the guest's keyboard). `StartupSystem=<name>` with the interface stopped first (`systemctl stop essway`, then the edit, then reboot) lands the carousel on nes / gb / pico-8 — **not on imageviewer**, which comes up at PICO-8 although the API lists it visible with two games; from PICO-8 it is five to the right (nes, gb, gbc, gba, then SCREENSHOTS), and the walk now shoots a frame per step so the route is on record. `vm-visual-qa run --outdir` must be absolute — a relative one wrote no frames and said nothing. `LastSystem` counts only when `StartupSystem` is `lastsystem`.

**A slip to own**: `set -e; tools/es-syntax-check … | tail -3` printed FAIL and the chain committed, merged, pinned and started two builds anyway — a pipe's status is `tail`'s (blindspot: guards fail closed; memory `gate-commits-on-the-check` updated). The FAIL itself was relative paths against the wrong cwd; the re-run with real paths passed both files. `set -o pipefail` under `set -e`, always.

**#244**: the change log's ten missing days (2026-09-12 → 21) drafted by a survey agent from the work logs, register and closed issues, 49 claims each with a file:line on `next` or the ES tree, spot-checked by grep (eleven on-screen strings, the ceiling, the FTP absent-not-broken guard, the backup's identical-file pruning — all present); merged into `docs/cloud-sync-changelog.md` before the 2026-09-22 section, evidence comments stripped; two drift lines fixed (rclone 1.75.1; the relink row is FINISH RESTORE PROCESS). New rule `.claude/rules/change-log.md`: the section is written the day the change lands. Commit waits for the sixth cut's list proof, since the 09-22 section claims it.

## 17:10 UTC — #245: a vertical arcade game's thumbnail is a quarter turn off (the maintainer, mid-soak)

*"The thumbnails shown for the screenshots in the save state manager in EmulationStation are rotated 90° clockwise when using an arcade emulator. I found this to be true for FB Neo, and it might be true for others."* Same family as #243: `tasks/task_screenshot.c` in the RetroArch we build (`bdba046fa6`) has no rotation handling; `take_screenshot_raw` writes the frame as the core hands it over, and the core's `SET_ROTATION` (`runloop.c:1890`) is honoured by the video driver at display only. Save-state thumbnails are always the raw path and `video_gpu_screenshot=false` makes screenshots raw too, so both are turned back to the core's memory layout for every game that asks for 90/270. Filed as #245 with three options; the interface cannot know a game's rotation, so the capture is where it belongs -- open decision D-UI-081, the maintainer's. The VM has no arcade ROM to prove a fix with; the proof would be the device.

## 17:30 UTC — #243, the SCREENSHOTS list: the picture on screen is the theme's own extra, not md_image

The sixth cut (`2e2773bacf`, pin `cc2189f47`) named the right system and still drew Bobl's screenshot at 151x189 (0.80). Two diagnostic cuts (`0f916d52a4`, `8dc8233331`; log lines in the lookup, the details container and `ImageComponent::resize`) showed the lookup right all along -- content `Bobl`, matched `nes/nes`, aspect 1.33333, md_image present -- and then the component's numbers: **two** images show the file. `md_image`, target 307x192 max, took the aspect (256x192, 4:3) and is hidden by the theme (`<visible>false</visible>` in art-book-next's detailed view); the picture on screen is the theme's `game-artwork` extra, `<path>{game:image}</path>`, target 260x220, resized at aspect 0 to 234.667x220 -- the file's shape. That extra is an `ISimpleGameListView::mThemeExtras` entry bound by `updateThemeExtrasBindings`, not one of `DetailedContainer::mThemeExtras` (which numbered 0 there). Seventh cut (`4e4ab0647`, pin `4217ca838`, distribution `220585b56b`): `DisplayAspect::applyToBoundImages` walks a component tree and gives every ImageComponent showing the file's image the aspect; the list view applies it to its extras right after binding them, the container keeps doing so for md_image and its own; the diagnostics are gone. Builds, the two proofs, vm-qa run 8 and the rehearsal are queued as one chain (`chain-243-9.sh`).

Two lessons that cost the most time: an ES `LOG(LogInfo)` line shows nowhere on the image until `Debug=true` is in es_settings.cfg (essway stopped first), and `/var/log/es_log.txt` is tmpfs -- the proof's closing reboot had been wiping the evidence (memory `es-info-log-needs-debug`; the proof script grew a `KEEP` switch). And a chain's tail must not overlap the next chain's build: the fifth cut's upgrade rehearsal staged a tar the sixth cut's build was rewriting (scp failed, rc 143, voided), and vm-qa run 7 was stopped after ten green suites because the cut it tested was already superseded.

## 21:15 UTC — The soak read; the interface died once in it (#246); the seventh cut staged, reboot asked

The maintainer brought the RG35XX SP back at 21:08 UTC (17:08 EDT; *"I had to enable wifi after boot"* -- the Wi-Fi switch was the soak's offline path, so the open question about switch versus hotspot is answered by use). Read read-only through `tools/device-act`: the soak boot ran 11:05-13:18 EDT, switch off at 11:41 (`CONNECTED_LOCAL`), a clean power-off; eight Dr. Mario achievements `queued_offline`; on the new boot the proxy's `Flush complete: total=8 flushed=8 pending_remaining=0` within a minute of the link. No Wi-Fi drop but the deliberate one. **One crash**: EmulationStation SIGSEGV at 11:52:49 EDT, 2.4 s after a Dr. Mario exit's `cloud_capture`, offline, before the `pending` read the game-end flow makes next; the same sequence 285 s earlier went through; the supervisor restarted it and the maintainer did not mention it. No core: `rocknix-corekeep` was not armed (`/storage/.config/keep-core-dumps` absent). Filed #246 with the timeline and a direction (arm the keeper with the maintainer's yes; reproduce the offline game-end flow on the VM with awards queued and `Debug=true`; read the path between the capture and the `pending` read with #241's newest code in view); row 5 on #79; #236 comment. Note the device's exec.log lives at `/var/log/exec.log` and `/var/log` survives a reboot there (`es_log.N.txt` rotate), unlike the VM's tmpfs -- the crashed run's warnings are in `es_log.1.txt` and say nothing after 11:48.

The seventh cut's tar was staged the runbook's way (idle check, copy to `/storage/.update-staging/`, hash on the device, move into `/storage/.update` on a match) with no reboot in the script; the reboot and the keeper's arming are both questions to the maintainer.

## 21:58 UTC — #246 root cause: the after-exit folder rescan reloads a view whose cursor it has just deleted

The maintainer, on the crash in the soak: *"let's dump the core and examine it, if we can learn anything about what happened with the ES segfault. if we can do any root cause analysis now, it might be worth doing before we transfer this build."* There was no core (the keeper was not armed) and no debugger on the host (`gdb` absent, `sudo` needs a password), so three things in parallel: `rocknix-corekeep --on` on the RG35XX SP with their yes (implicit in the ask, said so), the eighth cut `8b8bc1c964` (ES `50a505192`: the SIGSEGV handler writes glibc's backtrace to stderr and dies of the signal instead of `exit()` -- the old handler's `exit()` ran the static destructors on the faulting thread, so any core described the teardown, and systemd saw an exit code), and a reproduction on guest d in the soak's shape (offline achievements on with the six-game store, exit sync on, resolver black-holed, RetroArch on `gl`, Bobl launched through the interface's API and ended with the shipped `execute_kill`, a screenshot dropped into `/storage/roms/screenshots` each session). **The interface died on the sixteenth session** (`repro-246.sh`, 21:48 UTC); the keeper kept a core and the Debug-level `es_log` of the run.

**The cause, from the run's log and the code.** The last line before the fault: `SystemData::rescanIfFolderChanged: imageviewer changed on disk, re-reading /storage/roms/screenshots` -- #82's after-exit rescan. It did `mRootFolder->clear()` (deleting every screenshot FileData) and then `ViewController::reloadGameListView(this)`, which starts by reading the old view's cursor -- a FileData just freed. Freed memory held plausible bytes seventeen times on the VM; on the device (every Dr. Mario award a screenshot, the SCREENSHOTS list browsed earlier, the view alive) the second exit was enough. Fix, the ninth cut `520e1c92ca` (ES `5aea8a314`): `dropGameListView` reads the cursor while alive and takes the view down before the files go; `remakeGameListView` makes a fresh view after the repopulate and finds the cursor by path. The check: forty sessions on the ninth cut (`chain-246-9.sh`), then vm-qa run 10 and the rehearsal. #243's code is not involved.

**What the core could not say.** RIP in a texture-map lookup with si_addr `0x408` -- the teardown's fault, as predicted -- and the file is exactly the keeper's cap (201326592 bytes) with the main thread's stack beyond it, while the note says `whole`: a keeper bug, filed. `unwind.py` (NT_PRSTATUS registers and a stack scan) is in the session dir for the next core. Two mistakes of mine cost the first attempt: busybox `pgrep -x retroarch` never matches `/usr/bin/retroarch`, and RetroArch on the VM runs blind without `video_driver = "gl"` (the first session ran eleven minutes with a black screen and every later cycle saw "no emulator"). Rule: `es-code-traps.md` "A rescan that deletes FileData drops the view first".

## 22:25 UTC — The fix holds: forty sessions on the ninth cut

`520e1c92ca` on guest d, the same loop and shape that killed the seventh cut on session 16 and the eighth on session 13: 40 sessions, 41 rescans of the SCREENSHOTS folder, the interface the same process throughout, no core. The eighth cut's second reproduction had given the shape of the death -- the faulting PC in the heap (`[0x18a63370]` under the signal trampoline), a virtual call through the freed FileData's vtable -- which is `cursor->isPlaceHolder()` in `reloadGameListView` exactly. The tar is staged on the RG35XX SP (checksum matched, 22:07 UTC); the reboot waits for vm-qa run 10 and the rehearsal, per the round page's rule, and for the maintainer's yes.

## 23:00 UTC — The ninth cut applied to the RG35XX SP; are the earlier crashes the same bug?

The maintainer: *"you may reboot. also, is this issue the cause of any prior issues? or, are those crashes separate?"* Reboot sent 22:57:06 UTC through `tools/device-act` after the idle check (no emulator, no cloud run, both locks free, the tar in `/storage/.update`).

Separate. Every earlier row on #79 is RetroArch's own process (`coredump: NNN(retroarch)`): rows 1, 3 and 4 on 2026-09-07/08 (row 1 before the rescan even existed -- `b09ca9409` landed 2026-09-08; rows 3-4 root-caused to the gl3 texture command read from a returned stack frame), row 2 the video thread wrapper running a posted command twice (#211, patch 0015). #246 is EmulationStation's process and a different mechanism (a freed FileData's vtable). What they share is the trigger: a RetroAchievements unlock writes a screenshot and fetches a badge, so rows 2-4 followed an award and #246 followed a game exit after one -- which is why they looked alike from the couch and why the soak was the place to see it.

## 23:50 UTC — #245 built on our side (D-UI-081): a game's captures turned by the rotation the display gave its frame

The maintainer: *"I think we do it on our side. If we want to file a bug upstream against RetroArch around it, we can do that, but I imagine RetroArch moves much more slowly than we will."* D-UI-081 settled (was open). The interface can turn a picture as easily as it fits it; what it lacked was the angle, a per-game fact the core states at launch (`[Environ] SET_ROTATION: "n"` in `/var/log/exec.log`). So it learns it: `CaptureRotation::recordAfterSession` runs where play count and last played are updated, reads the log's last line, folds `video_allow_rotate` and the player's `video_rotation` from RetroArch's config (`CaptureRotationText`, unit-tested: log, config, record text), and writes `<savestates>/<system>/<rom>.rotation` -- beside the states, so it travels with them; a game never played here has no record and no captures. `DisplayAspect::Transform` (aspect + turns) replaces the bare ratio and is read by every surface that draws a capture: the SAVE STATE MANAGER's tiles (grid-wide), the SCREENSHOTS list's picture (the theme's bound extra and md_image), the full-screen viewer (`ZoomableImageComponent`) and its grid, and the SCREENSHOTS system in grid style (`ImageGridComponent::setTileDecorator`, a per-tile hook, since each tile is another game's). `ImageComponent::setDisplayRotation` turns the picture inside its layout box -- the texture corners move round, the quad stays; an odd turn swaps the fitted width and height and the min-target crop's axis. The direction: RetroArch's rotation 1 is 90 degrees counter-clockwise, so the raw frame's right edge is the game's top; the fixture carries a green mark on that edge and the proof asks where the mark lands. ES `12de21757`, pin `a4c06e89a`, tenth cut `bf53cea7e4` building; the VM proof seeds `Bobl.rotation`/`Ninoid.rotation` = 1 and a session of cookieclicker with a SET_ROTATION line appended to the launch log for the recording path (`seed-245.sh`, `proof-245s.sh`, `exit-record-245.sh`). Unit suite 118/1281. Left as they are and said so on #245: the screensaver slideshow and a game image set by hand to a screenshot.
