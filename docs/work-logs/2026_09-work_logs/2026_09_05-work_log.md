# Work log — 2026-09-05

## 00:01 UTC — Skraper is the interim scraping route (D-QA-004)

The maintainer asked why fork builds cannot use screenscraper.fr, since the
scrapers that remain (IGDB, ArcadeDB) are poor. The answer is a credential:
ES sends ScreenScraper a developer id and password on every request
(`SCREENSCRAPER_DEV_LOGIN`, compiled in), upstream's lives in CI secrets, and
borrowing it would bill our scraping to ROCKNIX's quota under their name. Our
own id is a registration with ScreenScraper, in the maintainer's name.

Skraper sidesteps that: it carries its own developer id and uses the player's
account. Checked skraper.net — free, closed source, GUI only, Windows/Linux
(mono)/macOS (WINE), writes RecalBox-layout gamelists our ES reads. Checked
our side — ROCKNIX shares `/storage/roms` over SMB, and
`cloud_content_backup` carries gamelist.xml and media (only saves are
excluded), so one scrape propagates through the content tier. Maintainer:
"a good interim route." Recorded as D-QA-004 refining D-QA-003; the stash's
"scraper route" open question is closed.

Practical notes for the first run: RecalBox output profile; scrape with ES
idle and reload gamelists afterwards; expect a slow first pass on a free
ScreenScraper tier; it does not run on serval (no desktop).

**Does Skraper have a CLI? (asked 00:00 UTC) — No, not any more.** Verified
against the release itself rather than the docs: downloaded
`Skraper-1.4.1.7z` (25 MB, built 2026-08-04) and listed it — 104 files, one
executable, `SkraperUI.exe`, plus `SkraperLibrary2.dll`. No `Skraper.exe`. The
bundled release notes from 1.2 through 1.4.1 never mention a command-line,
silent or batch mode. The Linux installer's generated `run.sh` still carries an
`elif [ -f ./Skraper.exe ]` branch — a vestige of the 2018 command-line tool
(`Skraper.exe <romdir> "<system>" -output=… -ssu= -ssp=`) that the GUI grew out
of; the Recalbox forum thread that documented it now redirects to their wiki.
So the interim route is a GUI on a machine with a desktop. If unattended
scraping ever matters, Skyscraper (open source, CLI, carries its own
ScreenScraper developer credential, writes ES gamelists) is the tool to
evaluate, and it would run on serval.

## 00:44 UTC — The exit sync: 18 seconds to move nothing, and what it is now (D-CLOUD-028)

The maintainer exited a game and watched the sync "try every game". The
device log (`/var/log/cloud_sync.log`, still there because the H700 had not
rebooted) said otherwise: `Checks: 70 / 70, Listed 313`, `There was nothing to
transfer`, `Elapsed time: 0.9s` — inside an 18-second run. The rest, timed step
by step on the handheld: **starting rclone is ~1 s on its own** (`rclone
version`: 997 ms), `rclone listremotes` 874 ms, `rclone mkdir` 2.1 s,
`cloud_sync_helper` 0.7 s, plus `sleep 2`, `sleep 2`, `sleep 3` in `main()`,
plus the system-archive phase the exit hook had no reason to run. And the game
had written no save at all — the maintainer had launched from an existing slot
because the ribbon's labels were cropped (#27, spec posted).

Asked for a per-game filter; chose a time window instead (`--recent`:
`--max-age` since the last successful stamp + 600 s, `--no-traverse`, copy
forced, no `mkdir`, no `rmdirs`). Names would miss PPSSPP and Dreamcast saves.
Exit 4 = no default route, decided with `ip route` in 140 ms, stamps nothing;
ES shows it as SKIPPED. Under `--yes` the pauses are no-ops; the remote name
comes from `rclone.conf`. ES: exit hook runs `--yes --saves-only --recent`;
the card renders `Checks:` as "COMPARING SAVE FILES WITH THE CLOUD n / m" and
no longer drives the bar with it. ES `bc199045a`, merged `6b0d34c0f`, pinned.

Tested on the H700 with the patched script from `/storage/tmp`: nothing
changed **5.3 s** (was 18); one new save pushed and verified on Dropbox in
10 s, probe removed both sides; `ip` shim → rc 4 in 0.14 s, stamp untouched;
full pass still probes and tidies (11 s). Three steps added to
`tools/cloud-round-trip`, which still has never run end to end (#35).

Two patches failed on their anchors before writing anything; the second
time the device re-timed the *previous* script and I nearly read it as the
new one — the log line "Ensuring remote sync path" was the tell. Check the
diff landed before trusting a measurement of it.

## 00:49 UTC — H700 image with the fast exit sync, built and verified

`build-dev.sh H700` from the devices worktree at `3643188f9d` (image stamp
cleared first): **BUILD OK 00:47:38 UTC**, about 35 minutes on the warm root.
Verified the artifact, not the report (device-builds.md, *Iterating on
EmulationStation*): `strings` on `image/system/usr/bin/emulationstation`
finds `--saves-only --recent`, `COMPARING SAVE FILES WITH THE CLOUD` and
`SKIPPED - NO NETWORK CONNECTION`; the installed `cloud_backup` under
`install_pkg/rclone-1.75.0` carries `RECENT`, `check_network_link` and
`first_remote`.

```
target/ROCKNIX-H700.aarch64-20260905.tar   1.30 GB
sha256 71b3d0f5cb87358c26e7d039e42b2e6e476ce22dd6ce7d0fabceefe6d25e2cbc
copy:  /workspace/artifacts/rocknix-images/
```

The H700 had gone off the network by the time the build finished ("No route
to host" — asleep, most likely), so the transfer to `/storage/.update/` is
queued behind a ping loop. Reboot into it is the maintainer's action. After
the reboot: exit a game with the sync on and expect the card to say
"COMPARING SAVE FILES WITH THE CLOUD 0 / 0" and finish in about five seconds;
turn Wi-Fi off and exit a game and expect "SKIPPED - NO NETWORK CONNECTION"
at once.

## 01:09 UTC — D-UI-018 decided: both save surfaces stay

Maintainer, on the hub tick versus the GAME SETTINGS rows: "it is fine having
those options in two places because they're contextual to the two workflows."
Recorded as decided under the same ID. The one thing I flagged and they did
not rule on — the remembered SAVE DATA tick silently skipping saves after one
ROMs-only backup — is noted in the row as theirs to take or leave. With this,
the four open questions the 2026-09-04 stash carried are all closed
(D-INFRA-003/004/005, D-QA-003/004, D-CLOUD-027, D-UI-018).

## 01:14 UTC — Image on the device; the BIOS screen's provenance; the tab strips (#63)

**Transfer verified.** `ROCKNIX-H700.aarch64-20260905.tar` is in
`/storage/.update/`, device sha256 `71b3d0f5…` equals the host's. The H700
had been powered off, not asleep; the queued job caught it at 01:08 UTC. Reboot
is the maintainer's.

**Missing BIOS check — ours or upstream?** Upstream. `GuiBios` is batocera's
(2020, tabs 2024-01-16); `ROCKNIX/emulationstation-next` `master` has zero
commits past our 2026-08-18 base; our only change is `2c72d9e42` (the
tab-aware empty-list message). The *list* comes from `rocknix-systems`
(python, OS side), last changed upstream 2026-05-30 (`9a0617be5b`, "update
bios check": −781/+40 lines, dropped hundreds of MAME-style `.zip` entries)
and 2026-06-02 (Dreamcast/NAOMI paths under `bios/dc/`). Both predate our
base, so they were already in every image we have built since August — not a
change between the last two images. On the device now: 3603 files under
`bios/`; `rocknix-systems` reports nds (4 DSi files missing), psx
(`psxonpsp660.bin` missing), and UNTESTED hashes for firmware.bin,
scph101/1001 and bios_CD_U.

**The tab strips.** The maintainer: "Installed systems" / "All" and the
scraper's tabs "don't match any other screen." One component,
`ComponentTab` (batocera 2022), six screens. The selected-tab underline is
0.25 % of screen width — 1.6 px on the H700 — over a 6 %-black tab background;
labels are sentence case on two screens and uppercase on the rest. Filed
#63 with the six screens, the constants, and acceptance criteria; the
fix is in the component, once, with labels uppercased at display time so the
`.po` msgids stay.
