# Work log — 2026-09-05

## 00:01 UTC — Skraper is the interim scraping route (D-QA-004)

The maintainer asked why fork builds cannot use screenscraper.fr, since the
scrapers that remain (IGDB, ArcadeDB) are poor. The answer is a credential:
ES sends ScreenScraper a developer id and password on every request
(`SCREENSCRAPER_DEV_LOGIN`, compiled in), upstream's lives in CI secrets, and
borrowing it would bill our scraping to ROCKNIX's quota under their name. Our
own id is a registration with ScreenScraper, in the maintainer's name.

Skraper sidesteps that: it carries its own developer id and uses the player's
account. Checked skraper.net — free, closed source, GUI only, Windows/Linux
(mono)/macOS (WINE), writes RecalBox-layout gamelists our ES reads. Checked
our side — ROCKNIX shares `/storage/roms` over SMB, and
`cloud_content_backup` carries gamelist.xml and media (only saves are
excluded), so one scrape propagates through the content tier. Maintainer:
"a good interim route." Recorded as D-QA-004 refining D-QA-003; the stash's
"scraper route" open question is closed.

Practical notes for the first run: RecalBox output profile; scrape with ES
idle and reload gamelists afterwards; expect a slow first pass on a free
ScreenScraper tier; it does not run on serval (no desktop).

**Does Skraper have a CLI? (asked 00:00 UTC) — No, not any more.** Verified
against the release itself rather than the docs: downloaded
`Skraper-1.4.1.7z` (25 MB, built 2026-08-04) and listed it — 104 files, one
executable, `SkraperUI.exe`, plus `SkraperLibrary2.dll`. No `Skraper.exe`. The
bundled release notes from 1.2 through 1.4.1 never mention a command-line,
silent or batch mode. The Linux installer's generated `run.sh` still carries an
`elif [ -f ./Skraper.exe ]` branch — a vestige of the 2018 command-line tool
(`Skraper.exe <romdir> "<system>" -output=… -ssu= -ssp=`) that the GUI grew out
of; the Recalbox forum thread that documented it now redirects to their wiki.
So the interim route is a GUI on a machine with a desktop. If unattended
scraping ever matters, Skyscraper (open source, CLI, carries its own
ScreenScraper developer credential, writes ES gamelists) is the tool to
evaluate, and it would run on serval.

## 00:44 UTC — The exit sync: 18 seconds to move nothing, and what it is now (D-CLOUD-028)

The maintainer exited a game and watched the sync "try every game". The
device log (`/var/log/cloud_sync.log`, still there because the H700 had not
rebooted) said otherwise: `Checks: 70 / 70, Listed 313`, `There was nothing to
transfer`, `Elapsed time: 0.9s` — inside an 18-second run. The rest, timed step
by step on the handheld: **starting rclone is ~1 s on its own** (`rclone
version`: 997 ms), `rclone listremotes` 874 ms, `rclone mkdir` 2.1 s,
`cloud_sync_helper` 0.7 s, plus `sleep 2`, `sleep 2`, `sleep 3` in `main()`,
plus the system-archive phase the exit hook had no reason to run. And the game
had written no save at all — the maintainer had launched from an existing slot
because the ribbon's labels were cropped (#27, spec posted).

Asked for a per-game filter; chose a time window instead (`--recent`:
`--max-age` since the last successful stamp + 600 s, `--no-traverse`, copy
forced, no `mkdir`, no `rmdirs`). Names would miss PPSSPP and Dreamcast saves.
Exit 4 = no default route, decided with `ip route` in 140 ms, stamps nothing;
ES shows it as SKIPPED. Under `--yes` the pauses are no-ops; the remote name
comes from `rclone.conf`. ES: exit hook runs `--yes --saves-only --recent`;
the card renders `Checks:` as "COMPARING SAVE FILES WITH THE CLOUD n / m" and
no longer drives the bar with it. ES `bc199045a`, merged `6b0d34c0f`, pinned.

Tested on the H700 with the patched script from `/storage/tmp`: nothing
changed **5.3 s** (was 18); one new save pushed and verified on Dropbox in
10 s, probe removed both sides; `ip` shim → rc 4 in 0.14 s, stamp untouched;
full pass still probes and tidies (11 s). Three steps added to
`tools/cloud-round-trip`, which still has never run end to end (#35).

Two patches failed on their anchors before writing anything; the second
time the device re-timed the *previous* script and I nearly read it as the
new one — the log line "Ensuring remote sync path" was the tell. Check the
diff landed before trusting a measurement of it.

## 00:49 UTC — H700 image with the fast exit sync, built and verified

`build-dev.sh H700` from the devices worktree at `3643188f9d` (image stamp
cleared first): **BUILD OK 00:47:38 UTC**, about 35 minutes on the warm root.
Verified the artifact, not the report (device-builds.md, *Iterating on
EmulationStation*): `strings` on `image/system/usr/bin/emulationstation`
finds `--saves-only --recent`, `COMPARING SAVE FILES WITH THE CLOUD` and
`SKIPPED - NO NETWORK CONNECTION`; the installed `cloud_backup` under
`install_pkg/rclone-1.75.0` carries `RECENT`, `check_network_link` and
`first_remote`.

```
target/ROCKNIX-H700.aarch64-20260905.tar   1.30 GB
sha256 71b3d0f5cb87358c26e7d039e42b2e6e476ce22dd6ce7d0fabceefe6d25e2cbc
copy:  /workspace/artifacts/rocknix-images/
```

The H700 had gone off the network by the time the build finished ("No route
to host" — asleep, most likely), so the transfer to `/storage/.update/` is
queued behind a ping loop. Reboot into it is the maintainer's action. After
the reboot: exit a game with the sync on and expect the card to say
"COMPARING SAVE FILES WITH THE CLOUD 0 / 0" and finish in about five seconds;
turn Wi-Fi off and exit a game and expect "SKIPPED - NO NETWORK CONNECTION"
at once.

## 01:09 UTC — D-UI-018 decided: both save surfaces stay

Maintainer, on the hub tick versus the GAME SETTINGS rows: "it is fine having
those options in two places because they're contextual to the two workflows."
Recorded as decided under the same ID. The one thing I flagged and they did
not rule on — the remembered SAVE DATA tick silently skipping saves after one
ROMs-only backup — is noted in the row as theirs to take or leave. With this,
the four open questions the 2026-09-04 stash carried are all closed
(D-INFRA-003/004/005, D-QA-003/004, D-CLOUD-027, D-UI-018).

## 01:14 UTC — Image on the device; the BIOS screen's provenance; the tab strips (#63)

**Transfer verified.** `ROCKNIX-H700.aarch64-20260905.tar` is in
`/storage/.update/`, device sha256 `71b3d0f5…` equals the host's. The H700
had been powered off, not asleep; the queued job caught it at 01:08 UTC. Reboot
is the maintainer's.

**Missing BIOS check — ours or upstream?** Upstream. `GuiBios` is batocera's
(2020, tabs 2024-01-16); `ROCKNIX/emulationstation-next` `master` has zero
commits past our 2026-08-18 base; our only change is `2c72d9e42` (the
tab-aware empty-list message). The *list* comes from `rocknix-systems`
(python, OS side), last changed upstream 2026-05-30 (`9a0617be5b`, "update
bios check": −781/+40 lines, dropped hundreds of MAME-style `.zip` entries)
and 2026-06-02 (Dreamcast/NAOMI paths under `bios/dc/`). Both predate our
base, so they were already in every image we have built since August — not a
change between the last two images. On the device now: 3603 files under
`bios/`; `rocknix-systems` reports nds (4 DSi files missing), psx
(`psxonpsp660.bin` missing), and UNTESTED hashes for firmware.bin,
scph101/1001 and bios_CD_U.

**The tab strips.** The maintainer: "Installed systems" / "All" and the
scraper's tabs "don't match any other screen." One component,
`ComponentTab` (batocera 2022), six screens. The selected-tab underline is
0.25 % of screen width — 1.6 px on the H700 — over a 6 %-black tab background;
labels are sentence case on two screens and uppercase on the rest. Filed
#63 with the six screens, the constants, and acceptance criteria; the
fix is in the component, once, with labels uppercased at display time so the
`.po` msgids stay.

## 01:21 UTC — BIOS check: what "Installed systems" meant, and the line that ran off the screen

Maintainer: not sure what "Installed systems" shows; and the all-caps text
under it clips off the side instead of wrapping. Both in `GuiBios`. The tab
filters on `SystemData::sSystemVector` — systems ES has loaded, which it does
only when they have games — so it meant "systems with games" and said
"installed". Renamed: **SYSTEMS WITH GAMES** / **ALL SYSTEMS** (new msgids,
uppercase per convention). The clipped line was ours from yesterday
(`2c72d9e42`): a `TextComponent` measures itself as one line when built,
`ComponentList::updateElementSize` then calls `setSize(width, oldHeight)`,
so `mAutoCalcExtent` goes (false,false) and it never re-wraps. Fix:
`setMultiLine(MULTILINE)` + `setSize(listWidth * 0.94, 0)` so the height is
computed by wrapping and the row grows; fallback to the window width rule when
the list is not yet sized. Message shortened to a sentence pair. ES
`5dc8552cf`, merged `d831eecbb`, pinned. Not yet built — the device still
holds the un-rebooted 00:47 tar, and replacing a tar in `/storage/.update/`
while somebody may be rebooting into it is a race not worth running; the
rebuild goes ahead and the copy waits for the maintainer's word.

## 01:39 UTC — BIOS CHECK rebuilt as one ordered list (D-UI-019)

Maintainer's second look: "Installed systems" should mean BIOS files present,
and offered four shapes. Chose the fourth (one view, icons), refined: per
system, not per file, or the five missing files vanish among hundreds of
green ones. Facts that shaped it: `rocknix-systems` emitted only MISSING and
UNTESTED — nothing for a good file — so "present" needed a script mode;
`ViewController`'s launch warning filters on `status == "MISSING"` and calls
the default, so `--all` is opt-in and the warning is untouched; UNTESTED had
been drawn with the *missing* icon (the code tested for an "INVALID" the
script never emits).

Built: `rocknix-systems --all` (default byte-identical on the H700; `--all`
10 systems / 21 present / 5 missing / 4 unverified, 0.5 s);
`ApiSystem::getBiosInformations(system, all)`; `GuiBios` rewritten — rows
with icon + `MultiLineMenuEntry(name, summary)`, `stable_sort` on
(problem, hasGames, missing, untested, name), `makeAcceptInputHandler` → a
`GuiSettings` page per system with `addWithDescription(icon + path, status +
MD5)`; menu entries and title say BIOS CHECK. Syntax-checked all four TUs
with the H700 cross g++ and the flags from `compile_commands.json` of the
last build — which caught `MultiLineType` unqualified (it nests in
`TextComponent`), an error the *pinned* tab-rename commit `d831eecbb` also
carried and would have failed the build with. Superseded before it was ever
built; the `-fsyntax-only` pass is cheap enough to be a habit before any ES
bump.

## 01:44 UTC — Second H700 image (BIOS CHECK), and the scraper is upstream's, unchanged

**Build OK 01:42:52 UTC** from `4ff06e737d`, about 20 minutes on the warm
root. Verified the artifact: `strings` on the image's ES binary finds
`BIOS CHECK` (×2), `ALL PRESENT`, `NOT A KNOWN VERSION`, `THE BIOS CHECK
RETURNED NOTHING`, `PRESENT, BUT NOT A KNOWN VERSION`, and no `MISSING BIOS
CHECK`; the image's `rocknix-systems` carries `PRESENT` and `--all`.

```
ROCKNIX-H700.aarch64-20260905.tar   1.30 GB
sha256 777dd10962276d93b605345f55cfb48d72bd220aabd4b7d9dfae30e1be81f416
copy:  /workspace/artifacts/rocknix-images/  (overwrites the 00:47 tar of the same name)
```

Delivery: the device was still on `3624e2e329` with the 00:47 tar waiting
in `/storage/.update/`. Copied the new one as a dot-prefixed `.part` file,
verified the hash on the device, then `mv` over the old name — atomic on one
filesystem, and a `.part` never matches the updater's `*.tar`, so a reboot at
any moment applies a complete image. Reboot is the maintainer's.

**Scraper UX — upstream, and not changed between our images.** No commit of
ours touches `GuiScraper*` or `scrapers/`. The tabbed SCRAPE / OPTIONS
rewrite is batocera's (`e14538b5c`, 2025-06-26, with the IGDB scraper),
followed by batocera's per-game manual-scrape rework (2025-12-09),
interruptible search (2026-01-06) and fan-art label normalisation
(2026-08-08), each pulled into ROCKNIX ES by spycat88's merges (2025-11-30,
2025-12-13, 2026-01-14, 2026-08-18). Between the image the device ran from
2026-08-19 (pin `222dc15bd`) and today's (`ba5cbe9fe`) there are **zero**
scraper-screen commits and the ROCKNIX base is the same `9d664e2ff`. So the
"changed since prior versions" is real only relative to an official release
older than late 2025. Maintainer's call: upstream decision, leave it. The
one piece we do own is the tab strip's rendering (#63).

## 02:26 UTC — D-UI-020: the hub ticks stay remembered, with defaults

Maintainer, on the remembered SAVE DATA tick: "everything should remember,
and we also should have defaults." That is the page as built — three ticks,
each remembered per direction, first-visit defaults SAVE DATA on, ROMS AND
BIOS off, SYSTEM SETTINGS off — so the decision is recorded, the D-UI-018 note
closed, nothing changes in code. The last open question from the 21:03Z stash
is gone from the state file too.

## 02:39 UTC — Locking down the two exits: the image and git (D-INFRA-006, blindspot 26)

Maintainer: personal ScreenScraper and RetroAchievements credentials for local
builds are fine; "be sure we do lock everything down if there are keys that
could leak" — then sharper: logs on the build box do not matter, what matters
is what escapes through a build or through git.

- **Through a build.** The four keys are compiled into the ES binary, so a
  personal key in `~/.ROCKNIX/options` ships inside every image built while it
  is set — fine on the maintainer's devices, a leak in a public prerelease.
  `tools/fork-publish-release` now refuses a binary containing `devpassword=`
  or `&y=KEY` (tested with a fake binary and a dummy target: rc 1 with the
  message; a clean binary passes). Cheap fixes made while there even though
  logs are tolerated: `scripts/get_env` forwarded the *entire* shell
  environment into a world-readable `.env` and every container — the
  maintainer's Fireflies and Sentry tokens were in it — now filtered by name
  (the four keys pass; `*TOKEN*`, `*_KEY*`, `*SECRET*`, `*PASSWORD*`… drop),
  `.env` is 0600 and removed after the run, and the ES recipe prints
  `USING: <key> (set)` instead of the value. The leaked `devices/.env` is gone.
- **Through git.** Both public repos already had GitHub secret scanning and
  push protection on, and neither tree holds a secret-shaped value. Push
  protection knows GitHub's token catalogue, not a ScreenScraper dev password
  or an RA key, so `.githooks/pre-push` now scans **every** pushed branch for
  credential-shaped values at the pushed tip, redacts them in the message and
  refuses; angle-bracket placeholders pass. Proven: real value → rc 1,
  placeholder → rc 0, incremental range with an RA key → rc 1, zero false
  positives across the tracked tree.
- **The guard that was not running.** `core.hooksPath` still pointed at
  `/home/max/Development/rocknix/.githooks`, gone since the 2026-09-04 move.
  Git ran no hooks and said nothing; a dozen pushes of `next` tonight were
  unguarded. Repointed to `/workspace/repos/rocknix/.githooks`;
  `tools/fork-worktree list|sync` warn when the path is unset, relative, or
  missing (tested with an env-injected bad path). Blindspot 26.

`~/.ROCKNIX/options` created 0600 with commented placeholders and the warning
about where the values end up. No secret has been typed anywhere.

A slip while proving the hook: the test made a throwaway branch and used
`git commit -a --amend`, which swept the five *uncommitted* guard files into
the temporary commit that was then deleted with the branch. The working tree
came back without them; the reflog still had the commit (`254e87dafd`) and
`git checkout <sha> -- <files>` restored them. Never test with `-a` while
real work sits unstaged — stage and commit the work first, then test on top.

## 02:50 UTC — D-INFRA-007: the key rule is about who can download

Maintainer clarified twice: a leaked key is fine in a personal, private
pre-release and not in a public one; more precisely, a build generated here
and played only by them may carry the key, and beyond that it is stripped out.
The publish guard now keys on the target repo's visibility (`gh api
repos/<repo> --jq .visibility`): private → publish with a note, public or
unknown → refuse and say to rebuild without the keys. Tested: public repo
refused with the rule in the message; unknown repo refused; a shimmed `gh`
returning `private` took the note branch. The 404 body had leaked into the
message on the unknown path — normalised to `unknown`.

## 02:53 UTC — Runtime ScreenScraper developer credentials, proposed (#64)

Maintainer: could the panel take developer id/secret beside the account
id/secret, with a flag so upstream builds omit the option? Yes. Six
compile-time sites in ES; the account pair is already a runtime setting read
per request; `backuptool` already strips `ScreenScraperPass` by name and
verifies it. Design filed as #64: a `SCREENSCRAPER_RUNTIME_DEV_LOGIN`
build flag set only by the fork's package.mk, two masked settings rows under
it, a credential source that prefers the compiled string when present, and
`ScreenScraperDevPass` added to backuptool's strip and its check. Result:
nothing secret in any fork image, so images can be shared and D-INFRA-007's
guard has nothing to refuse. Not built tonight — the maintainer is moving to
the conflict-resolution milestone; it is ready to pick up.

## 03:06 UTC — #64 built: ScreenScraper developer pair on the device (D-QA-005)

Six ES sites, as the issue said. `SCREENSCRAPER_RUNTIME_DEV_LOGIN` (CMake
option or env; the fork's package.mk passes `-D...=1`), the guards in
`ScreenScraper.h/.cpp` and `Scraper.cpp` widened to either define,
`screenScraperDevLogin()` preferring the compiled string, three URL builders
call it, two `addInputTextConfigRow`s under `runtime && !compiled` in
`GuiScraperStart::loadAccountsPage`, a pre-flight in `pressedStart` when the
chosen scraper is ScreenScraper and the pair is empty, and a DEVELOPER
PASSWORD row on the restore credential page in `GuiMenu`. Syntax-checked with
the H700 cross g++ in all three configurations — runtime, compiled-in,
neither — all clean. Distribution side: `backuptool` strips
`ScreenScraperDevPass` (tested on a fake `es_settings.cfg`); the publish
guard now needs a value after `devpassword=` because the bare fragment lives
in every fork binary (tested: bare passes, real trips). `SCREENSCRAPER_SOFTNAME`
already defaults to `ROCKNIX-Emulationstation` in `EmulationStation.h`, so
nothing else is needed for the API's softname field.

## 03:25 UTC — Third H700 image (#64) built, verified, delivered; state refreshed for the move

**BUILD OK 03:09:08 UTC** from `1f1ac12d3c`. `strings` on the image's ES
binary: `DEVELOPER ID` ×2, `DEVELOPER PASSWORD`, `SCREENSCRAPER NEEDS A
DEVELOPER ID…`, `jeuInfos.php` (the scraper is in a fork image for the first
time), `ScreenScraperDevPass`; **zero** `devpassword=<value>` or `&y=<key>`,
so the publish guard passes it; BIOS CHECK and `--saves-only --recent` still
present. The image's `backuptool` strips `ScreenScraperDevPass`.

```
ROCKNIX-H700.aarch64-20260905.tar   1.30 GB
sha256 c8da9a8cc9d626e97cd6541c7014975b84cfdbca54382fb752dddb54d2cb1d2d
```

The maintainer had rebooted into the BIOS CHECK image meanwhile (`BUILD_ID
4ff06e737d`, `/storage/.update/` empty), so the swap put the new tar into an
empty directory; device-side hash equals the host's. Reboot is theirs; the
three device-side acceptance items for #64 are on the issue. Session state
refreshed with the lockdown, #64, the new image and the device's actual
build — the next session opens in the conflict-resolution worktree.
