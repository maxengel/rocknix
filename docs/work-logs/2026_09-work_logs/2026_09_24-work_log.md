# 2026-09-24 work log

## 00:15 UTC — The sixteenth cut green end to end: #209, #198, #247 (`6205420b3d`)

vm-qa run 22 twelve suites PASSED -- the `frame-diff` gate's first run on a real cut, 0 boxes against the fifteenth's frames, since nothing this cut changes is walked -- and the rehearsal from `b245fd12ac` 19/19. On guest d: DO NOT INCREMENT from SLOT 1 rewrote `Bobl.state1` (4096 -> 6235 bytes, a new mtime, no new slot); INCREMENT PER SAVE from SLOT 1 wrote `Bobl.state4` and left slot 1's bytes and mtime alone; a real SIGSEGV of `sleep` through the keeper's pipe noted `whole: 311296 raw bytes, 28839 compressed`. The save-mode proof took three runs: the walk counted presses to SLOT 1, and the manager sorts by slot under DO NOT INCREMENT and by date under INCREMENT PER SAVE (`GuiSaveState.cpp:239`), so the same presses landed on AUTO SAVE once a save had changed the dates -- now a rule (`generic-x64-vm-testing.md`: arrange the files so the tile is last and press past the end, and let the proof read back `-e <slot>`), and a friction line. RetroArch's auto-index keeps a running index across a session: after a removed `state3` the new slot is `state4`. #198's interface side is one token at two sites, syntax-checked and in the image; its end-to-end press is the maintainer's first box. RECORD.txt written, the fifteenth's marked superseded once the sixteenth is applied; the copy and the reboot asked for (D-QA-011). #251 waits on a pick. The RG35XX SP still runs the fifteenth.

## 00:50 UTC — #251 measured: stroke width against the grid, and the hinting lever that did nothing

The maintainer: *"is the issue strictly the font size, or is the font size rendering cleanly related to the resolution of the screen and/or its DPI?"* and *"will different fonts render more sharply because they're mathematically more viable on certain screen resolutions ... Are we just saying we'll make it bigger and hope it makes it clear?"* The code first: the KMS context reports no DPI (`drm_ctx.c`, `get_metrics` NULL), so RetroArch scales widgets from the pixel diagonal against 1080p (0.363 at 640x480), times the config's widget factor (1.0), times 1.5 for ozone; FreeType gets the size as an integer (`FT_Set_Pixel_Sizes`), rasterises with the font's own hinting under the modern interpreter (vertical snapping only), and the glcore driver draws each glyph at an integer origin, so nothing is resampled. Then the experiment (`exp-251.sh`, guest d, config and environment only): the toast at factor 1.0 (10 px) and 1.4 (15 px), each with and without `FREETYPE_PROPERTIES=truetype:interpreter-version=35` on RetroArch's process. Findings, measured on the x-height row (`solidity.py`): 10 px -- 41 strokes, none with a fully-lit core, mean stem 0.76 px; 15 px -- 46 strokes, 10 solid, 1.40 px; the achievement banner at 17 px -- 56 strokes, 21 solid, 1.37 px (the one the maintainer calls sharp); at 24 px -- 51 of 56 solid, 1.87 px, and the banner runs off the 640 px panel's right edge, which rules out the wider widget scale (option 3). The hinting variants were pixel-identical to the defaults: the lever does nothing without a font or a load-flag change. So: strictly size, chosen from resolution because DPI is unknown; sharpness is stem width against the grid -- Inter UI's stems cross one pixel near 13 px and two near 25 px here -- and "bigger" works for that reason, not by hope. Frames `docs/qa-frames/2026-09-24/251-*`. The maintainer's follow-on -- pick sizes per panel that land on the grid -- filed as its own issue.

## 01:10 UTC — #251 decided: option 2, a 14 px floor for the message queue (D-UI-084); the seventeenth cut building

The maintainer, after the pros and cons and the panel table: *"Sounds good. Let's go with option two."* The table decided it: with a widget factor of 0.88 on every family but the H700, the queue sits at the 9 px floor on 480x320 panels and at 9-10 px on 640x480 ones; option 1 (a larger base) would have left the 480x320 devices at that floor and put a 1080p queue at 42 px; option 3 ran the achievement banner off a 640 px panel. Option 2 lifts every panel up to 960x544 to 14 px and changes nothing above. Patch `0016-widgets-message-queue-floor.patch` (`443028ff7a`): `gfx_widgets_font_init` takes the floor as a parameter -- 9 for the regular and bold faces, `MSG_QUEUE_MIN_SIZE` 14 for the message queue -- with the measurements in its header; dry-run applied clean on the post-0015 source. Chain 17 started 01:08 (x64 run 32, H700 run 21: RetroArch rebuilt on both arches, then guest d, vm-qa run 23, the rehearsal); `proof-251.sh` frames the toast at the H700's factor (expect 14 px, stems near 1.3 px) and at the VM's own 0.4 (the same floor). The maintainer's second question -- anything else for the RC beyond documentation? -- answered from the triage: nothing else changes the image; what remains is their device checks, the closures, the audit the ceremony gate will demand within days, and the docs.

## 01:20 UTC — Two more asks: the rigorous audit before the candidate, and several upstream PRs

The maintainer: *"The other thing we should do before a release candidate is a full code audit using the code auditor's skill and being very rigorous to make sure comment quality, etc., are all well defined and executed."* Agreed and already owed by the ceremony gate; it runs with the `code-auditor` skill over the work since #186 once the seventeenth cut's proofs land, with comment quality and definition in its scope. And: *"if we can break this into a couple of different PRs ... an interest expressed by the development team, given the sheer number of changes."* The record already says one self-contained change per PR, built by content (fork-workflow, D-WORKFLOW-006); the open part is the map, filed as #256 with the coupling measured: the distribution side splits by package, the interface side is coupled through `GuiMenu.cpp` (+5,774 lines) and `ApiSystem.cpp` (+1,458), so interface PRs are either sequenced on each other or one per repo beside several distribution PRs. `zip_file.hpp` (+13,902, vendored) and `Renderer_GLES30.cpp` (+2,291) are PRs of their own. The seventeenth cut: both builds done by 01:11 (RetroArch re-patched and rebuilt on both arches, ccache-warm), guest d rebuilding.

## 01:25 UTC — The seventeenth cut's toast measured at 14 px; one false alarm on the way

Guest d on `443028ff7a` at the H700's widget factor (`proof-251.sh`): the toast draws at 14 px. Over the same text span as the earlier sizes, on the x-height row: 44 strokes, 16 with a fully-lit core (36%), mean stem 1.19 px -- against 10 px's 0.76 px and none solid, and the achievement banner's 1.37 px and 38%. A first measurement over a shorter span (cut mid-word) reported 0 solid strokes and 1.07 px, and on that number I stopped the chain's vm-qa to change the floor to 15; the full span corrected it before the patch was touched, the floor stays 14, and vm-qa run 23 plus the rehearsal were restarted (`chain-17b.sh`). Friction line filed against #255: the measuring tool takes the span as an argument. Frame `docs/qa-frames/2026-09-24/251-toast-10px-vs-14px-4x`. The VM-factor variant produced no launch frame this time (RetroArch did not come up for the second launch); the H700-factor frame is the one that matters and the floor applies to both by construction.

## 01:50 UTC — The seventeenth cut green end to end; the copy and the reboot asked

vm-qa run 23 twelve suites PASSED with `frame-diff` at 0 boxes (`qa-443028ff7a-webdav-a-20260924-0116`); the rehearsal from `b245fd12ac` 19/19 (`qa-443028ff7a-upgrade-from-b245fd12ac-20260924-0145`, RESULT PASS 01:47). RECORD.txt finished, the sixteenth's marked superseded (never staged). `stage-rg35xxsp-443028ff7a.sh` ready; the copy and the reboot asked for (D-QA-011). The RG35XX SP runs the fifteenth. Next: the rigorous code audit the maintainer asked for, over the work since #186, with the `code-auditor` skill, comment quality in scope, in parallel with their playtesting. The maintainer reported a networking change on their side and an API connection error in their client around 01:45; nothing on this host was affected -- the run's monitor expired on its own timer with the suites already green.

## 02:05 UTC — The milestone audit of the RC round started (the maintainer's ask)

The maintainer: *"The other thing we should do before a release candidate is a full code audit using the code auditor's skill and being very rigorous to make sure comment quality, etc., are all well defined and executed."* Folder `docs/audits/2026_09_24-milestone-rc-round-since-186/`, running log written (Phase 0: skill and phases identical to `next`; scope frozen at `443028ff7a`; distribution `95e4961fb3..443028ff7a`, the fork's 134 commits over 96 files; EmulationStation `ae9c7d56b..75ca1dac2`, 73 commits). As on 2026-09-13, the forward phases run in a fresh Fable 5.1 agent that did not implement the work, read-only on code, issues and the guests, with the mechanical checks to run and comment quality as a named face of the retrospective; the implementing session verifies its leads before Phase 6 and closes 6-7. Second opinions through the council Facilitator follow once its Phase 2 exists (the key lives in `~/.config/council/env`; the environment does not export it). The RG35XX SP runs the fifteenth; the seventeenth waits on the maintainer's yes.

## 02:30 UTC — The seventeenth cut on the RG35XX SP

The maintainer: *"If the build is complete, we can transfer it to the RG35XX SP and restart it."* The first staging attempt copied nothing: the per-cut script carried the sixteenth's tar date in the file name and `scp` failed on a missing file (friction line; #257 makes the staging a tool that finds the tar by pattern). Rerun 02:17-02:20 (idle, copy, sha256 matched, queued), rebooted 02:21 through `tools/device-act` after a second idle check, up on `443028ff7a` at 02:27, queue empty. RECORD.txt and the QA row say so; the fifteenth's record is superseded; the walk baseline moved to run 23's frames (the rule: the last cut accepted on a device). The maintainer's boxes now: #251 (the toast reads), #209 (the save shortcut from a slot with DO NOT INCREMENT), #198 (a password with a space), #250, #249, #245, #246, #243. The milestone audit runs in the background on the frozen scope.

## 03:10 UTC — The audit resumed after the spend limit; the worktree's rules were eleven files stale

The auditor died at the account's monthly spend limit at Phase 1.6 and was resumed by message after the maintainer's `/login` (*"Let's pick back up our work."*), with two corrections: its running-log stamps had been estimates, not readings (a correction entry says so), and no more subagents. Its own Phase 0 had noticed what this session had not: the `conflict-resolution` worktree it runs from lags `next` by eleven rule files (ceremonies, change-log, es-code-traps, es-player-text, es-ui-style-guide, handheld-evidence, least-surprise, player-language, time-to-play, vm-first, working-principles). Merged up (`2fccc57077`; the five docs conflicts resolve to `next`, every row on this side already there); friction line under #254; a memory says to merge first thing.

## 03:40 UTC — Audit #258: thirty findings, none critical or high; the punch list filed

The auditor's report: PASS WITH FINDINGS -- 347 boxes (184 PASS, 54 PARTIAL, 1 FAIL, 76 SKIP, 32 UNTESTABLE) *[corrected at 15:55: 352 boxes, 195 PASS, 53 PARTIAL, 1 FAIL, 76 SKIP, 27 UNTESTABLE -- the headline was not the sum of the scorecard's rows, the seat's G-11]*, 8 Medium and 22 Low, nothing that loses progress, leaks a credential or bricks a device. Committed as `db91592776`; #258 filed with one box per item and the lint's contract (`tools/lint-audit-artifacts --issue 258` shows only the thirty Phase 7 outcome lines pending). Every lead re-read from the artifact before a fix: the first-boot copy (`userconfig-setup:13` excludes two files, links three), the retire unlink over every argument, the handler's LOG under a plain mutex, `EX_USAGE` undefined, `at=<epoch>images`, the 16-commit proxy gap (`gh api compare`: a scoped refresh, no expiry, rcheevos moved -- not dependency noise, so pinned with #259 as the bump's home, D-WORKFLOW-029), the two proxy pins' schema byte-identical (`storage.py` from both tarballs in the sources cache).

## 04:20 UTC — The punch list's code items, one cut; three guards proven to fire first

`efb8aec861` (distribution) and ES `217872e32`: PL-001/002/003/005/006/009/010/011/012/013/014/016/017/018/019/026/027/028. Three new harness cases in section t FAIL against `443028ff7a` (`BASE_REF=443028ff7a --old`: `rc 0` on `refresh --bogus`, `route=run at=<epoch>images`, the helper ran to `done`) and PASS now. `register-check` fires on a planted `D-ZZ-099` in the proxy sources and, with a new placeholder pattern, on `D-ZZ-0xx` -- which the citation pattern's `[0-9]+\b` had walked past for six days. `es-syntax-check` passes the three false FAILs (the worktree's include roots ahead of the build's, quoted and angled: `InputManager.h` writes `<utils/Delegate.h>`) and fails a constructed `int oops = ;`. Two traps met on the way: Python text-mode edits turned `StringUtil.cpp`'s CRLF into LF (a 3,137-line diff for a three-line comment; redone in binary mode, memory written), and `exit()` from the SIGINT handler died of SIGSEGV in the teardown on guest d -- so the nineteenth cut's handler sets a flag the main loop reads (`4fd019f04`), and the same commit stops the screenshot cache holding a turn (it holds the game by system and path, and asks the record's mtime on every look).

## 04:55 UTC — The eighteenth cut green; the record and the tools follow

vm-qa run 24 on `a84fce38a6`: thirteen suites PASS -- the new `fresh` suite (a first boot's OS-managed files are links) having FAILED on `443028ff7a` first (`qa-443028ff7a-webdav-a-20260924-0415`, `es_savestates.cfg -> (not a link)`) -- `frame-diff` 0 boxes, rehearsal 19/19. Guest d: `kill -SEGV` prints the name and the frames, `status=139`, essway back in 2 s; a password `a b$c;"x` set through the interface's command shape verifies against the stored hash and not a near miss. The five guard positives PL-020 asked for: meson refuses a wrap under `--wrap-mode=nodownload` (`Automatic wrap-based subproject downloading is disabled`) while without the flag it tries `example.invalid`; the push guard's INDEX warning printed on a constructed range (`043e743316`); `fork-checks.yml` run 35956388686 red on `ceremony_today=2026-11-15` beside a green push run; `tools/retroarch-wrapper-test` 2000/2000 and wired into vm-qa as `wrapper`; #253 box 1's terms list the four rows. Fourteen issue bodies edited under the tick-discipline rule; #186's index says resolved per item and the lint compares it with the table (it FAILED on the disagreement first). The menu map still described the leavable transfer page -- rewritten. The five instruction recommendations are rules and one check (`ceremony-check` hygiene, fired on #225 with a constructed box, restored byte for byte). The nineteenth cut (`c8609558d4`) is building.

## 05:40 UTC — The nineteenth cut green; one 2 px box, the editor's caret, masked; the audit's Phase 7 written

`c8609558d4` (ES `4fd019f04`): vm-qa run 25's fourteen suites, thirteen PASS and `frame-diff` FAIL on two frames of `confirm-cloud-folder` -- one 2 px column, x 387..388, y 226..267. Read pixel by pixel against the `443028ff7a` baseline: a white bar in the baseline where the run has the field's background, exactly after `/ROCKNIX/Saves` in the CLOUD FOLDER editor -- the text caret, caught on in one run and off in the other. A mask for the caret's column (`confirm-cloud-folder/* 380 222 396 272`), the field's text still compared; 0 boxes on re-comparison. That is D-QA-038's process working as written: a box nobody claimed stops the run, and the answer is either a claim (a change we meant) or a mask measured from the pixels (a thing that blinks) -- never a wider tolerance. Rehearsal run 20 PASS 19/19 with the reworked tool (PL-030). Guest d on this cut: `kill -INT` ends in `Deactivated successfully` where the eighteenth's exit() ended in SIGSEGV; a vertical game's screenshot turns after `turns=1` is written by hand, same process (PL-019's frames). #182's 1280x800 frame from a fresh guest b. The Phase 7 gate is in `05-punch-list.md`, the YAML index agrees, `tools/lint-audit-artifacts --issue 258` passes; the H700 artifacts are under `h700-all-20260924-c8609558d4` with their RECORD, the eighteenth's marked superseded. Not staged: the copy and the reboot onto the RG35XX SP are the maintainer's yes.

## 15:55 UTC — #260: the code auditor's second opinion is a phase; run on #258, nine items fixed as the twentieth cut; the audit's totals corrected (G-11)

The maintainer asked whether the code-auditor skill has an adversarial phase by default. It did not: Phase 4.5 is the auditing model refuting itself, and #186's GPT-seat second opinion was a choice (11 of 33 items came from it) that #258 did not repeat. So (D-WORKFLOW-031) the skill is v1.11.0 with **Phase 4.6**: the council's GPT seat (`openai/gpt-6-astra`, effort `max`, through `tools/council/run`) reads the four artifacts and the punch list and returns new findings, a refutation row per item and what it could not judge; the orchestrator grades every row against the tree (confirmed / agree re-graded / agree narrowed / disagree with the artifact / could not verify) before any reaches the punch list; the phase fails closed without the seat; `tools/lint-audit-artifacts` requires the section and the provenance for Epic and Milestone folders dated from today. The two questions the maintainer asked to have worked out are D-WORKFLOW-032: **ours first** (the model with the tools writes the record the seat reads), **a different model** (a second hat shares its priors), two calls at Milestone tier.

Its first exercise was #258's own artifacts (one call, 1,125 s, 44,498 completion tokens of which 37,808 reasoning; `second-opinions/gpt-6-astra-audit.provenance.json`): eleven G-findings, thirty refutation rows, thirteen could-not-judge rows. Graded: nine confirmed and fixed as PL-031..PL-039 in the twentieth cut `c041be7e98` (ES `d30cbd282`) -- the core keeper's floor did not hold (Medium; the orchestrator had read the check and passed it), the PNG validator stopped at the header, the store-only header was case-sensitive, a dead run's progress file lived on, CUT fired on a core of exactly the cap, the screenshot lookup walked the library per miss, the rehearsal counted cairo files without reading the link, a comment, and the audit's own totals; six re-graded; two disagreements recorded with the artifact (G-05: `GuiWifi.cpp:197-208` writes the key only after `ok`; F-29: the schema was read at fix time). **G-11 was real and worse than the seat saw**: three printed totals disagreed (headline 347 / 184 / 32; per-section subtotals 346 / 188 / 27; the scorecard's rows 352 / 195 / 27). The rows are the count from now; the lint sums every `**Total**` row, and its constructed positive also found the 2026-09-03 audit's UNTESTABLE column printed 5 for 6. The lesson for the record: the one claim in an audit a tool can settle outright is arithmetic, and none of ours checked it.

Learned about the seat itself: it grades severity higher than the orchestrator on latent defects (three of its four Mediums came down to Low because the only client or the only writer already behaves), and its could-not-judge list is a ready-made Coverage Boundary -- every one of its thirteen rows was a real gap in the packet, and the section now carries them. Chain: `chain-20.sh`; vm-qa run 26 and rehearsal run 21 in flight.

## 16:20 UTC — The twentieth cut green: vm-qa run 26 fourteen of fourteen, rehearsal run 21 20/20

`c041be7e98` (ES `d30cbd282`; x64 run 35 and H700 run 24, 15:42-15:46 UTC on warm roots) carries the second opinion's nine items. vm-qa run 26 (`qa-c041be7e98-webdav-a-20260924-1547`): fourteen suites PASS, `frame-diff` 0 boxes over 78 screens against the `443028ff7a` baseline; the `scripts` suite runs the seat's cases (G-01 validator, G-02 capitalisation, G-03 floor plus cap, G-06 exactly the cap, G-08 the dead run's file). Rehearsal run 21 from `b245fd12ac`: PASS 20/20, the twentieth check the libcairo link (G-10), first run. Time to play: 1.09 s to a game's first frame, 2.01 s game to game, the exit sync's stamp at 0.97 s. H700 artifacts under `h700-all-20260924-c041be7e98` with SHA256SUMS and RECORD.txt; not staged -- the copy and the reboot are the maintainer's yes (D-QA-011), asked in the report.

## 16:55 UTC — #262: every open item read to its last comment and bucketed before any twenty-first cut

Maintainer: *"let's go through every open item before we do a new build."* All 141 open issues read (bodies, open boxes, last comments) and placed in eight buckets on #262: 11 to close now with their evidence, 35 waiting on the maintainer's word on a device (one session on the twentieth cut, not 35), 12 decisions of theirs, 19 changes a build would be *for* (none a defect), 25 VM/host items of mine, 32 program and upstream items, 5 folds, 2 kept as logs. `git log c041be7e98..next -- projects packages scripts config` is empty, so the twentieth cut is the code: no build is owed by the record. Two counts in the proposal's prose were typed before the table was summed and corrected within the minute -- blindspot 53's shape, in the hour it was written.

## 17:20 UTC — The maintainer's twelve answers to #262 recorded: eight register rows, fifteen closures, the PR map on #256, Groundhog equipped

Every answer went where it is read: D-UI-086 (#255's font snapping goes ahead; the 14 px floor D-UI-084 stays under it), D-UI-087 (#195: today the time, yesterday YESTERDAY/HIER and the time, older the date and the time), D-WORKFLOW-033 (#44 after the RC, the documentation route first), D-QA-041 (#133: no provider accounts, open backends in containers, #232's shape), D-WORKFLOW-034 (#256: the five PR buckets in the maintainer's words), D-WORKFLOW-035 (#261: the process lane after the RC if only loose ends remain, before if not -- turning on whether #159 is inside the release), D-WORKFLOW-036 (#260: Groundhog equipped, not ported -- awecelot/groundhog#924, since no Groundhog session was running to message), D-WORKFLOW-037 (#238 shellper outside the project; closed). #192's reachable wait is shaped (wait only while a link is coming up; at once 69 with the radio off; on the startup card, off the launch path; `tools/time-to-play` before and after) and gets its row with the change. Bucket A closed: #16 #43 #154 #155 #165 #166 #182 #189 #246 #247 #258; bucket G: #17 #25 #29 #87 (#7 left open, not confirmed). #256 carries the path map from the merge base `6a7213eab3` (2026-09-18): the five buckets plus six lanes they miss (save states and captures, ES platform fixes, ScreenScraper on the device, device evidence and networking basics, GENERIC_X64, recipe hygiene) and two questions -- is #159 inside the release, and one ES bump PR or one per lane. #228's memory question is unmeasured on the record; the measurement is written on the issue for after the RC. The next cut's contents so far: #255, #192, #195.

## 17:50 UTC — The twenty-first cut's first two items in and tested: #195 YESTERDAY (ES `5067f7a1b`), #192 the reachable wait (harness section i +3)

`dayRelation` (calendar days in local time, a later day is OLDER) is pure in `TimeText` and pinned by es-unit-tests (122 cases, 1313 assertions PASS): midnight, a month end, a year end, February 29th, a stamp from the future. The manager's three tile branches call `toRelativeLocalTimeString(_("YESTERDAY"))`; HIER in the French catalogue; `es-syntax-check --tree` PASS on the three sources; the menu map notes the rule (D-UI-087). `cloud_net_ready` waits, up to its `--wait`, while NetworkManager says `connecting` (or a carrier is up with no NetworkManager) and there is no route yet, announcing `>>> doing network` so the card's WAITING FOR A NETWORK line is reached for the first time since RC-11; disconnected still exits 69 at once (D-CLOUD-136). Harness run `last-good-192.log`: 367 PASS, 0 FAIL -- the three new cases: announced-then-69 at the bound (4 s), disconnected 69 at once (0 s), a route arriving mid-wait settles after the grace (6 s). `tools/font-stems` (#255) written on the host's FreeType; its first table says the rasteriser hints Inter UI's stems to whole pixels from 11 px up (80-100 % solid), while the device's frames showed 22-37 % at 14-17 px and stems tracking the *unhinted* geometric width -- so the softness is upstream of the size, and the draw path (gl2/gl3/vulkan all round the text origin to whole pixels) is not it either; the rasteriser the image's RetroArch actually uses is the next thing to read.

## 18:30 UTC — Three more calls recorded (D-WORKFLOW-038..040); #159 was built all along; the webkitgtk 2.54 spike cut

The maintainer's second round: #228 becomes a bounded spike for 2.54 with the GStreamer pieces, memory measured either way (D-WORKFLOW-038); #233 no runner from this box, the team gets the actions and a runbook after the RC (D-WORKFLOW-039); and a correction -- *"I thought this was built. I can go in now and remove saved Wi-Fi networks."* #159's remembering and forgetting shipped in RC-11 under #191/#201 (`wifictl saved|forget|join`, MANAGE SAVED NETWORKS with FORGET, profiles kept with autoconnect priorities), and this session had read #159's open boxes as the state of the tree -- blindspot 51's shape, committed on the same day #262 warned about it. Corrected in the register (D-WORKFLOW-040, which also resolves D-WORKFLOW-035: the release is loose ends, the process lane comes after), on #159 (three boxes ticked from the frames and the maintainer's word; the one real remainder is the restore page listing several remembered networks), and on #256, #261, #262 in their own words. The spike: branch `build/webkit-254` in the x64 worktree -- webkitgtk 2.54.0 (sha from webkitgtk.org), gst-plugins-base with GL (EGL, wayland), a gst-plugins-bad override that flips the mpegts options and keeps `libgstmpegts` -- one build, the third fix the bound.

## 19:45 UTC — #263: the guest's RetroArch drew into a 240x256 surface; #251's numbers were of the scaling; the fit rule for #255 comes from FreeType's own table

While building #255's stem table (`tools/font-stems`, FreeType through ctypes, the image's own 2.14.3 gives the same numbers as the host's 2.14.2) the rasteriser came out sharp from 11 px -- and the #251 frames had said soft to 17 px. RetroArch's own log on guest d settled it: `Detecting screen resolution: 640x480` then `Using resolution 240x256`; the GENERIC_X64 cfg shipped `video_fullscreen_x/y = 0`, so under wayland the fullscreen surface was the game's size and sway scaled it to the panel. `video_windowed_fullscreen = true` changed nothing; `video_fullscreen_x/y = 640/480` (the H700's values) gave `Using resolution 640x480`, and the saved-state notification's stems went from two grey columns to one lit column in the same font at the same size (`docs/qa-frames/2026-09-24/263-*`). Filed #263; blindspot 54; D-QA-042. The fixes for the twenty-first cut: the cfg ships 640x480, `quirks/platforms/GENERIC_X64/092-retroarch-surface` follows the guest's mode from `/sys/class/drm/*/modes`, and `tools/time-to-play` fails a run whose RetroArch log names a surface under the panel. The fit rule (D-UI-088) is RetroArch patch 0017: a computed size moves up to the nearest size in its face's sharp table within 1.3, never down -- 17 -> 18 and 14 -> 15 on the H700's panel. Learned the hard way, twice today: `pkill -f` with a pattern that also appears in the ssh command's own argument list (`/storage/.config/retroarch/retroarch.cfg` carries "retroarch") kills the shell running it; `ps -eo comm | awk '$1 ~ /^retro/'` and `/tmp/ttp-kill` are the safe forms.

The webkitgtk 2.54 spike: the GStreamer pieces built first time; webkitgtk stopped twice inside WebKit's own option graph -- WebDriver's log channel (`ENABLE_WEBDRIVER=OFF`, fix one), then the GBM/GStreamer-GL mismatch (`USE_GSTREAMER_GL=ON`, fix two) -- and a third run ended at 19:37 for a reason read next.

## 19:55 UTC — #263's guard seen to fire; the small surface is RetroArch's own known race, and the cfg only changes the odds

`tools/time-to-play --only launch` on guest d with RetroArch windowed: `Not a pass: RetroArch drew into a 480x432 surface under a 640x480 panel: the frames are scaled (#263)` -- the constructed positive (`ttp-263c`); two runs at 640x480 read `scaled: False`. What the launches showed about the cause: with `video_fullscreen_x/y = 0`, three launches by hand logged 240x256 and two through the tool logged 640x480; with 640/480 set, two of three logged 640x480. RetroArch names it itself -- `gfx/common/wayland_common.c:87-104` detects "a fullscreen sizing bug on some hardware and/or compositors" by the surface coming up at its 240x256 splash size and falls back to a legacy configure path -- so the size is a race between the compositor's fullscreen configure and the first frame; the cfg and the quirk (`eb131d112f`) change the odds, the guard makes the measurement honest, and the deterministic fix is upstream's. `generic-x64-vm-testing.md` carries the rule: a RetroArch frame is evidence only when its log says the surface was the panel. The webkitgtk 2.54 spike's third run was not a compile failure: `make ... Terminated` (143) at 19:37, thirty minutes after its start, cause unknown; the fourth started 19:43 under `tools/watch-job --detach` so a repeat is a fact and not a guess.

## 20:20 UTC — The twenty-first cut's first H700 build stopped in retroarch: patch 0017 missed two callers; fixed, rebuilding; #264 for the check that was missing

`gfx_widgets.c:1063: too few arguments to function 'gfx_widgets_font_init'; expected 9, have 7` -- the fallback (non-ozone) regular and bold calls. The patch was committed on `patch --dry-run`, which proves the hunks apply and nothing about the result; blindspot 49's shape in the RetroArch lane, where no `es-syntax-check` sibling exists. Fixed in `24badef3cb` (all six callers carry the table arguments, the fallbacks `NULL, 0`), H700 run 26 started 20:15 under `tools/watch-job`; the friction line names #264 (`tools/retroarch-syntax-check`). The 2.54 spike's sixth build is at webkitgtk object 8344 of 8585 as this is written.

## 20:30 UTC — The 2.54 spike ends at its final link (D-WORKFLOW-041: pinned for this candidate); the twenty-first cut's H700 half is built, the x64 half starts

Run 43 compiled every object and failed linking `libwebkit2gtk-4.1`: `undefined reference to Inspector::DOMFrontendDispatcher::powerEfficientPlaybackStateChanged` -- the generated Inspector protocol lacks a method the DOM agent calls in this option set. That is the fourth wall and the bound was three, so the maintainer's own fallback applies: 2.52.6 stays for this candidate, 2.54 returns with the conflict-resolution one from branch `build/webkit-254` (three fixes and the GStreamer recipes, all of which built). Six builds between 19:03 and 20:19; the recipe's freshness line now says this rather than the 09-20 story. The x64 root is back on `build/generic-x64` with webkitgtk, gst-plugins-base, gst-plugins-bad and cloud-signin-window cleaned, and `chain-21` starts. H700 run 26 built the twenty-first cut's device image (`h700-all-20260924-24badef3cb`) with the completed patch 0017; chain-21 rebuilds it at the synced head so both images carry one BUILD_ID.

## 20:45 UTC — #228 becomes the next candidate's first work (D-WORKFLOW-042); versions for the fork's releases from the one after this (D-WORKFLOW-043, #265)

Asked what 2.54 would gain us: nothing for what the sign-in window does (a dozen basic WebKit calls; 2.54's Skia compositor, favicon and magnification APIs and media fixes touch none of it), and a clock for how long it stays safe -- the 2.52 branch got six point releases carrying 32 CVEs in its last two advisories, we are current at 2.52.6, and once upstream stops the branch the fixes land only in 2.54.x; our build runs WebKit without the bubblewrap sandbox, so a hole is not contained. The maintainer: *"I don't think we should wait to do the bump ... once we cut this release candidate, ship it with the current version pinned, and then immediately, for our next release candidate, work to get it to the more recent version."* Recorded; the recipe's pin line says so. And a second call: SemVer-shaped versions for the fork's releases, from the release after this one, so a release is targeted by its features -- #265 carries the scheme's questions (components, the pre-release form, the relation to BUILD_ID, where the tag and the image carry it).

