# 2026-09-24 work log

## 00:15 UTC — The sixteenth cut green end to end: #209, #198, #247 (`6205420b3d`)

vm-qa run 22 twelve suites PASSED -- the `frame-diff` gate's first run on a real cut, 0 boxes against the fifteenth's frames, since nothing this cut changes is walked -- and the rehearsal from `b245fd12ac` 19/19. On guest d: DO NOT INCREMENT from SLOT 1 rewrote `Bobl.state1` (4096 -> 6235 bytes, a new mtime, no new slot); INCREMENT PER SAVE from SLOT 1 wrote `Bobl.state4` and left slot 1's bytes and mtime alone; a real SIGSEGV of `sleep` through the keeper's pipe noted `whole: 311296 raw bytes, 28839 compressed`. The save-mode proof took three runs: the walk counted presses to SLOT 1, and the manager sorts by slot under DO NOT INCREMENT and by date under INCREMENT PER SAVE (`GuiSaveState.cpp:239`), so the same presses landed on AUTO SAVE once a save had changed the dates -- now a rule (`generic-x64-vm-testing.md`: arrange the files so the tile is last and press past the end, and let the proof read back `-e <slot>`), and a friction line. RetroArch's auto-index keeps a running index across a session: after a removed `state3` the new slot is `state4`. #198's interface side is one token at two sites, syntax-checked and in the image; its end-to-end press is the maintainer's first box. RECORD.txt written, the fifteenth's marked superseded once the sixteenth is applied; the copy and the reboot asked for (D-QA-011). #251 waits on a pick. The RG35XX SP still runs the fifteenth.

## 00:50 UTC — #251 measured: stroke width against the grid, and the hinting lever that did nothing

The maintainer: *"is the issue strictly the font size, or is the font size rendering cleanly related to the resolution of the screen and/or its DPI?"* and *"will different fonts render more sharply because they're mathematically more viable on certain screen resolutions ... Are we just saying we'll make it bigger and hope it makes it clear?"* The code first: the KMS context reports no DPI (`drm_ctx.c`, `get_metrics` NULL), so RetroArch scales widgets from the pixel diagonal against 1080p (0.363 at 640x480), times the config's widget factor (1.0), times 1.5 for ozone; FreeType gets the size as an integer (`FT_Set_Pixel_Sizes`), rasterises with the font's own hinting under the modern interpreter (vertical snapping only), and the glcore driver draws each glyph at an integer origin, so nothing is resampled. Then the experiment (`exp-251.sh`, guest d, config and environment only): the toast at factor 1.0 (10 px) and 1.4 (15 px), each with and without `FREETYPE_PROPERTIES=truetype:interpreter-version=35` on RetroArch's process. Findings, measured on the x-height row (`solidity.py`): 10 px -- 41 strokes, none with a fully-lit core, mean stem 0.76 px; 15 px -- 46 strokes, 10 solid, 1.40 px; the achievement banner at 17 px -- 56 strokes, 21 solid, 1.37 px (the one the maintainer calls sharp); at 24 px -- 51 of 56 solid, 1.87 px, and the banner runs off the 640 px panel's right edge, which rules out the wider widget scale (option 3). The hinting variants were pixel-identical to the defaults: the lever does nothing without a font or a load-flag change. So: strictly size, chosen from resolution because DPI is unknown; sharpness is stem width against the grid -- Inter UI's stems cross one pixel near 13 px and two near 25 px here -- and "bigger" works for that reason, not by hope. Frames `docs/qa-frames/2026-09-24/251-*`. The maintainer's follow-on -- pick sizes per panel that land on the grid -- filed as its own issue.

## 01:10 UTC — #251 decided: option 2, a 14 px floor for the message queue (D-UI-084); the seventeenth cut building

The maintainer, after the pros and cons and the panel table: *"Sounds good. Let's go with option two."* The table decided it: with a widget factor of 0.88 on every family but the H700, the queue sits at the 9 px floor on 480x320 panels and at 9-10 px on 640x480 ones; option 1 (a larger base) would have left the 480x320 devices at that floor and put a 1080p queue at 42 px; option 3 ran the achievement banner off a 640 px panel. Option 2 lifts every panel up to 960x544 to 14 px and changes nothing above. Patch `0016-widgets-message-queue-floor.patch` (`443028ff7a`): `gfx_widgets_font_init` takes the floor as a parameter -- 9 for the regular and bold faces, `MSG_QUEUE_MIN_SIZE` 14 for the message queue -- with the measurements in its header; dry-run applied clean on the post-0015 source. Chain 17 started 01:08 (x64 run 32, H700 run 21: RetroArch rebuilt on both arches, then guest d, vm-qa run 23, the rehearsal); `proof-251.sh` frames the toast at the H700's factor (expect 14 px, stems near 1.3 px) and at the VM's own 0.4 (the same floor). The maintainer's second question -- anything else for the RC beyond documentation? -- answered from the triage: nothing else changes the image; what remains is their device checks, the closures, the audit the ceremony gate will demand within days, and the docs.

## 01:20 UTC — Two more asks: the rigorous audit before the candidate, and several upstream PRs

The maintainer: *"The other thing we should do before a release candidate is a full code audit using the code auditor's skill and being very rigorous to make sure comment quality, etc., are all well defined and executed."* Agreed and already owed by the ceremony gate; it runs with the `code-auditor` skill over the work since #186 once the seventeenth cut's proofs land, with comment quality and definition in its scope. And: *"if we can break this into a couple of different PRs ... an interest expressed by the development team, given the sheer number of changes."* The record already says one self-contained change per PR, built by content (fork-workflow, D-WORKFLOW-006); the open part is the map, filed as #256 with the coupling measured: the distribution side splits by package, the interface side is coupled through `GuiMenu.cpp` (+5,774 lines) and `ApiSystem.cpp` (+1,458), so interface PRs are either sequenced on each other or one per repo beside several distribution PRs. `zip_file.hpp` (+13,902, vendored) and `Renderer_GLES30.cpp` (+2,291) are PRs of their own. The seventeenth cut: both builds done by 01:11 (RetroArch re-patched and rebuilt on both arches, ccache-warm), guest d rebuilding.

## 01:25 UTC — The seventeenth cut's toast measured at 14 px; one false alarm on the way

Guest d on `443028ff7a` at the H700's widget factor (`proof-251.sh`): the toast draws at 14 px. Over the same text span as the earlier sizes, on the x-height row: 44 strokes, 16 with a fully-lit core (36%), mean stem 1.19 px -- against 10 px's 0.76 px and none solid, and the achievement banner's 1.37 px and 38%. A first measurement over a shorter span (cut mid-word) reported 0 solid strokes and 1.07 px, and on that number I stopped the chain's vm-qa to change the floor to 15; the full span corrected it before the patch was touched, the floor stays 14, and vm-qa run 23 plus the rehearsal were restarted (`chain-17b.sh`). Friction line filed against #255: the measuring tool takes the span as an argument. Frame `docs/qa-frames/2026-09-24/251-toast-10px-vs-14px-4x`. The VM-factor variant produced no launch frame this time (RetroArch did not come up for the second launch); the H700-factor frame is the one that matters and the floor applies to both by construction.

## 01:50 UTC — The seventeenth cut green end to end; the copy and the reboot asked

vm-qa run 23 twelve suites PASSED with `frame-diff` at 0 boxes (`qa-443028ff7a-webdav-a-20260924-0116`); the rehearsal from `b245fd12ac` 19/19 (`qa-443028ff7a-upgrade-from-b245fd12ac-20260924-0145`, RESULT PASS 01:47). RECORD.txt finished, the sixteenth's marked superseded (never staged). `stage-rg35xxsp-443028ff7a.sh` ready; the copy and the reboot asked for (D-QA-011). The RG35XX SP runs the fifteenth. Next: the rigorous code audit the maintainer asked for, over the work since #186, with the `code-auditor` skill, comment quality in scope, in parallel with their playtesting. The maintainer reported a networking change on their side and an API connection error in their client around 01:45; nothing on this host was affected -- the run's monitor expired on its own timer with the suites already green.

## 02:05 UTC — The milestone audit of the RC round started (the maintainer's ask)

The maintainer: *"The other thing we should do before a release candidate is a full code audit using the code auditor's skill and being very rigorous to make sure comment quality, etc., are all well defined and executed."* Folder `docs/audits/2026_09_24-milestone-rc-round-since-186/`, running log written (Phase 0: skill and phases identical to `next`; scope frozen at `443028ff7a`; distribution `95e4961fb3..443028ff7a`, the fork's 134 commits over 96 files; EmulationStation `ae9c7d56b..75ca1dac2`, 73 commits). As on 2026-09-13, the forward phases run in a fresh Fable 5.1 agent that did not implement the work, read-only on code, issues and the guests, with the mechanical checks to run and comment quality as a named face of the retrospective; the implementing session verifies its leads before Phase 6 and closes 6-7. Second opinions through the council Facilitator follow once its Phase 2 exists (the key lives in `~/.config/council/env`; the environment does not export it). The RG35XX SP runs the fifteenth; the seventeenth waits on the maintainer's yes.

## 02:30 UTC — The seventeenth cut on the RG35XX SP

The maintainer: *"If the build is complete, we can transfer it to the RG35XX SP and restart it."* The first staging attempt copied nothing: the per-cut script carried the sixteenth's tar date in the file name and `scp` failed on a missing file (friction line; #257 makes the staging a tool that finds the tar by pattern). Rerun 02:17-02:20 (idle, copy, sha256 matched, queued), rebooted 02:21 through `tools/device-act` after a second idle check, up on `443028ff7a` at 02:27, queue empty. RECORD.txt and the QA row say so; the fifteenth's record is superseded; the walk baseline moved to run 23's frames (the rule: the last cut accepted on a device). The maintainer's boxes now: #251 (the toast reads), #209 (the save shortcut from a slot with DO NOT INCREMENT), #198 (a password with a space), #250, #249, #245, #246, #243. The milestone audit runs in the background on the frozen scope.
