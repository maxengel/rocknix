# GENERIC_X64 Boot Issues Tracking (2025-08-13)

## 1. Missing systemd unit/mount configuration files
- Many systemd units and mount points fail to load: "No such file or directory"
- **Action:** Audit rootfs build, ensure all required systemd unit files and mount targets are present in /usr/lib/systemd/system/, /etc/systemd/system/, /etc/fstab, and /etc.

## 2. Systemd service options disabled at compile time
- Warnings: "Support for option SystemCallArchitectures=|SystemCallErrorNumber=|SystemCallFilter= has been disabled at compile time and it is ignored"
- **Action:** Ensure kernel config enables CONFIG_SECCOMP, CONFIG_SECCOMP_FILTER, CONFIG_CHECKPOINT_RESTORE, CONFIG_HAVE_ARCH_SECCOMP_FILTER. Rebuild kernel if needed.

## 3. Plymouth connection refused
- "Failed to communicate with plymouth: Connection refused"
- **Action:** Mask plymouth services if not needed, or ensure plymouth is installed and enabled.

## 4. systemd-modules-load.service failures
- "ConditionKernelCommandLine=|modules_load failed" and related errors
- **Action:** Check kernel command line and /etc/modules-load.d/, /usr/lib/modules-load.d/ for required files. Ignore if not needed.

## 5. Device busy: SYSTEMD_READY property from device is false (nbd5)
- **Action:** Check udev rules and device node creation. Ignore if nbd devices are not used.

## 6. System is tainted: cgroupsv1
- **Action:** Enable cgroups v2 in kernel and boot parameters if full systemd features are needed.

## 7. Failed to mount ... (type n/a): No such file or directory
- **Action:** Audit /etc/fstab and systemd mount units for invalid or missing filesystems.

## 8. Control group is empty (systemd-update-utmp, systemd-hwdb-update)
- **Action:** Generally harmless, can be ignored unless functional issues are seen.

---

## Next Steps
- [ ] 1. Audit and fix missing systemd unit/mount files in rootfs
- [ ] 2. Double-check and patch kernel config for seccomp/syscall filter support
- [ ] 3. Mask or fix plymouth if not needed
- [ ] 4. Review modules-load.d and kernel command line
- [ ] 5. Review udev/device setup for nbd5 (if relevant)
- [ ] 6. Enable cgroups v2 (kernel + boot param)
- [ ] 7. Audit /etc/fstab and mount units
- [ ] 8. Ignore harmless cgroup-empty warnings

---

_This file is auto-generated by GitHub Copilot for tracking and resolution of GENERIC_X64 boot issues._
