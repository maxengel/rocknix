# Start: the CLOUD FOLDER editor, holding the current folder. START is OK:
# the script probes the provider (a few seconds) and either changes the
# setting or refuses; a refusal is a dialog naming the reason.
key ret
wait 8
shot folder-result
key x
wait 2
shot after-dismiss
