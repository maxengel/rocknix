# Pixels tools/frame-diff does not compare, `<screen-glob> x0 y0 x1 y1  # why`
# per line: half-open rectangles in the frame's own pixels (the pair's guests
# are 1280x800). Every line is a place the check is blind, so each says why,
# and each was measured from a diff of two runs (2026-09-23, runs 15 and 16:
# 63 frames, and outside these rectangles not one pixel differed).
* 1110 36 1215 64                  # the clock, top right: HH:MM is the run's minute; the hour digit reaches x 1135 (runs 14 vs 16)
run-transfer*/* 240 370 1040 396   # COMPARING N OF M FILES / UPLOADING N OF M: the phase line, centred, as wide as its numbers (x 333..948 seen)
run-transfer*/* 250 448 1030 474   # the progress bar
run-transfer*/* 560 508 720 536    # ELAPSED M:SS
