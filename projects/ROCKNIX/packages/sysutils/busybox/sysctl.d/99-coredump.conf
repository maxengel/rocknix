# Keep a bounded ring of core dumps under /storage/.cache/log/cores, so a
# crash can be read rather than argued about (fork #79, #211). The handler
# caps and prunes; see rocknix-corekeep for what it keeps and what it refuses.
kernel.core_pattern=|/usr/bin/rocknix-corekeep %e %p %t %s %E
