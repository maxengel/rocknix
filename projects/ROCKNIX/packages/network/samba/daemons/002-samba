STATE=$(get_setting samba.enabled)
SVC="samba"
CONF="smb.conf"
DAEMONS=("nmbd" "smbd")

mkdir -p /run/${SVC}
# Ensure /run/samba/smb.conf points to the active config if no generated file exists yet.
if [ ! -e "/run/${SVC}/${CONF}" ]; then
	ln -sf "/etc/${SVC}/${CONF}" "/run/${SVC}/${CONF}"
fi
