Depends on the schema (`docs/save-manifest-schema.md` rev 1, D-CLOUD-030/031) and adds rev 2 to it. Preceded by **Gate 12** (the unit table) and by Gate 0.

**What changed.** Capture no longer rides "the same `--recent` pass" (that pass is replaced by #22's reconciler); it is a step of its own, on every exit, with no rclone spawn. Sidecar-beside-the-save is dead (nothing beside an `.srm` passes the allowlist -- fixture on the RG35XX SP, `docs/save-manifest-alignment-review.md`). The manifest's cloud path is unchanged (D-CLOUD-031); its local working copy leaves the saves tree. Since 2026-09-12 the stage is load-bearing for #134 as well as for capture: under retain-only (D-CLOUD-102) the version a publish displaces is uploaded into the history store **from this device's stage**, so what the stage holds decides whether a save can be published at all.

### Requirements

- **R3 -- schema rev 2, additive to D-CLOUD-031.** Per entry: `unit` (which game-save the file belongs to, from the unit table), `producer` (when the file was imported from another device, that device's id), `published_at`, `pub` (the publication this version arrived in: `<device-id>:<counter>`). Top level: `units` (the members each unit declares -- what makes a torn publication detectable), a bounded `retired` list (deletions this device made: hash, path, the `pub` it retires, when), and **`history_keep {on, count, rev}`** (the one fleet-wide history setting, additive, D-CLOUD-045; read and adopted by #22 R9, written by #23's rows). Agreement is written only on verified equality and records `verified_by` (`remote-hash` / `size+mtime` / `download-sha256`). Manifests are read as a **claim set**: any device's claim about a path is a claim, and the cloud head is what the listing says (I5). The own manifest lives at `/storage/.cache/cloud_sync/manifest-<id>.json` and is published to `savestates/.rocknix/manifest-<id>.json` as a decided transfer; foreign manifests are cached under `/storage/.cache/cloud_sync/manifests/`, never written into the tree. The `schema` integer stays `1`: no cloud has received rev 1, and rev 2 only adds. **Never rewrite an unchanged manifest** (I8) -- its mtime is what would otherwise churn every exit.
- **R5 (capture half).** Capture runs on **every** exit -- toggle off, no network, lock held, emulator crashed -- because a file written and not recorded is exactly what the classifier cannot explain later. The emulator and core recorded are the ones **frozen at command construction** (`SaveState::setupSaveState` may rewrite `-emulator`/`-core` via `_changeCommandlineArgument`; `FileData::getCore(true)`/`getEmulator(true)` re-resolve and are not the answer). ES passes `--system --rom --emulator --core` and the unit context on the command line; capture infers nothing.
- **Sealing is an independent copy (B1).** A changed member is copied into `/storage/.cache/cloud_sync/stage/` and hashed there; never hard-linked (a hard link shares the inode; an emulator flushing in place would change bytes already hashed). Cost: a few megabytes of SD writes per changed save per exit; paid because the alternative pushes bytes that are not the bytes that were hashed (D-CLOUD-034). Gate 12 records the actual bytes.
- **The stage keeps the last agreed version of every save until a newer version is agreed** (D-CLOUD-078's one last-known-good, applied to the stage). It is read by two paths in #22: the heal of a suspect save (R4, D-CLOUD-100) installs the agreed version from it, and the **retain** (R5) uploads the displaced version from it into `<SAVES_REMOTE>/.history/` before the new version is published (D-CLOUD-095, D-CLOUD-102). A unit the stage cannot supply is never published over: it goes to the wizard, or it defers.
- **The walker distinguishes live saves from control metadata.** Capture **never claims `.history/`, `README.md` or the manifests folder** as this device's saves, whether it finds them locally after a download or in the cloud. Nothing under them enters the stage or the manifest.
- **Changed set at exit** = size or mtime differs from the last captured entry; an equal-size, equal-mtime byte change is caught by a full pass, which hashes (A2).
- **The core-pins file (I15; `docs/save-manifest-schema.md` section 9).** `/usr/share/rocknix/core-pins`, one line per core package `<package> <PKG_VERSION>`, emitted at image build from `LIBRETRO_CORES` x `get_pkg_version`; capture maps core name to package with a small exception table and records `"unknown"` when it has no answer. `/usr/lib/libretro/*.info` carries libretro-super's `display_version` and is recorded as `core_display_version`, never compared.
- Written whole to a temporary name, renamed into place; a reader never sees a torn file.
- **No rclone spawn.** Capture does not touch the network. The push is #22's. Capture takes **no lock**: ES holds the session lock around the whole launch, including capture (#22 R6).
- Nothing stamps a file it did not write; `unknown` is a value the wizard renders (`.claude/rules/upgrade-and-install.md`).
- Device fields come from `cloud_device_id` / `--label` (D-CLOUD-009). That label is the name the wizard shows under each version when it says which session was played later (#23, D-CLOUD-104).

### Acceptance

- [x] Exit a game with the toggle off, again with the network down, again while a boot pass holds the lock: the manifest entry for the changed save exists each time, with `sha256` equal to `sha256sum` of the file on disk. (VM: CAP fixtures green 2026-09-08; RG SP on `b6bd7dc3b7`, 2026-09-09: GBA exit through RetroArch/mgba wrote three entries whose `sha256` equal `sha256sum` of the `.srm`, `.state`, `.state.auto` on the card, and three sealed stage copies named by their own hash.)
- [x] The recorded emulator and core are the `--emulator=`/`--core=` tokens of the command EmulationStation ran, read after setupSaveState, never getEmulator()/getCore() re-resolved at exit (reworded 2026-09-08; CAP2 on GENERIC_X64).
- [x] A save flushed by the emulator during the seal window: the pushed bytes' hash equals the recorded hash (the independent copy); a hard-linked variant is shown to fail this. (CAP3, 2026-09-08)
- [x] Exit with nothing changed: the manifest's mtime does not change. (CAP4: mtime, inode, sha, stage count, 2026-09-08)
- [x] `core_build` equals the `PKG_VERSION` pin for the core's package on the same image. (RG SP 2026-09-09: `core_build` `e31759b24e7a...` = the image's `/usr/share/rocknix/core-pins` line for `mgba-lr` = the repo's `PKG_VERSION`.)
- [ ] A core absent from the map records `"unknown"`, and the wizard shows it as such (the wizard is #22's; the `unknown` half is a VM fixture check).
- [ ] A14: the manifest reaches the remote under `savestates/.rocknix/` with `remote_hash` null on WebDAV and non-null on MinIO, and is absent from the ROMs-and-BIOS tier after `cloud_content_backup` (#35).
- [x] An N64 game's `.eep` and `.mpk` appear as one `unit` with both members declared (CAP6, 2026-09-08); the remaining standalone layouts wait for round two (#88).
- [x] Rev 2 is written into `docs/save-manifest-schema.md` (section 10, `a96b29fd06`; section 9 corrected `7a6f9ccfae`) and noted on #20, with the SQLite index closed as *not built*.
- [ ] Agree a version, exit with a changed save, kill before agreement advances: the stage still holds the agreed version, and a following exit can retain it into `.history/` without a download. *(VM guest)*
- [ ] Seed a device with a downloaded `.history/` and both READMEs; run `cloud_capture --full`: nothing under them enters the stage or the manifest. *(VM guest)*

**Does not build**: no sidecars beside saves, no play-time fields, no second exit-path spawn, no lock in capture, no stamping of `unknown` files, no ancestry beyond `replaces`.

### Changed 2026-09-12

- **R3** -- `history_keep {on, count, rev}` added at top level, additive (D-CLOUD-045, P-1). The `store_seq` field the council's store-first plan proposed per entry is **withdrawn** with P-5: retain-from-stage needs no in-store flag (D-CLOUD-102).
- **New requirements** -- the stage keeps the last agreed version of every save until a newer one is agreed (D-CLOUD-078), because #22's heal and its retain both read it; and the walker never claims `.history/`, `README.md` or the manifests folder as this device's saves (D-CLOUD-095).
- **Acceptance** -- two items added for those two requirements, each on the VM guest (D-QA-007, D-QA-015). Existing ticks are unchanged.

---

*Body written from the council's handoff, `research/council-runs/2026-09-05-conflict-resolution-foundation/final-issue-draft.md` (consensus plan `revised_approaches/consensus_plan.md`), applied 2026-09-06 per the maintainer.*
*Amended 2026-09-12 from `research/council-runs/2026-09-11-save-history-one-home/final-issue-draft.md` as overridden by `docs/save-history-consensus-amendment.md` (D-CLOUD-095..104).*
