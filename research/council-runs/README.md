# Council runs

Every council deliberation writes its artifacts here, one dated directory per
topic (`YYYY-MM-DD-<slug>/`), laid out per
`.claude/skills/council/references/output-conventions.md`. The provenance JSON
beside each member output is what makes a run auditable; commit the whole
directory. This is a personal path on `next` and never part of an upstream PR.
