# Saved Session State

> **Saved**: 2026-09-14T13:18:00Z
> **Branch**: `feature/conflict-resolution` (worktree `/workspace/repos/rocknix.worktrees/conflict-resolution`; the work itself lands on `next`)
> **Repo**: maxengel/rocknix (fork of ROCKNIX/distribution) + EmulationStation at `~/Development/emulationstation-next` (build branch `test/qa-integration`, worktree `~/Development/emulationstation-next.worktrees/qa-integration`; ES upstream branch is `master`)

## Current Focus

Epic #11 (cloud saves), milestone 3 "Stable before upstream". Today (2026-09-13, UTC night): #45 VM half, #142 (closed), #93, #47, #27, #149 (closed), #129 (closed), #50 delivered (router/LAN boxes open), #82 VM half, #113 VM half, #66/#67/#68/#69 VM boxes all ticked. Decisions D-CLOUD-008, D-CLOUD-123, D-NET-002/003, D-UI-050/051, D-WORKFLOW-010/013/014, D-QA-016 written; register 232 IDs, `tools/register-check` clean; blindspots 40, 41.

## In Progress
- **RC-4 `c5c50a2d5f` is the release candidate** (distribution `next` `c5c50a2d5f` = ES `d9d6083de`, the bounded network-up token retry for #175: 10 s apart, nine attempts while the link is up; refusal/success reset). x64 image `ROCKNIX-GENERIC_X64.x86_64-20260914.img.gz` (sha `4a061306e0…`): **nine suites PASS** (`qa-c5c50a2d5f-webdav-a-20260914-0442`; 0.87 / 1.30 / 1.51 s). `ra-offline` not re-run on RC-4 (proxy path unchanged since RC-3; a run costs a reset, D-RA-006).
- **#175 box 2 ticked on RC-4** (comment 5659300487): guest d, deterministic resolver lag via `/storage/.config/resolv.conf` → `nameserver 127.0.0.1`; offline boot one failure/no retry; link on → re-check fails +3 s, retries 1-4 of 9, token 44 s after activation, one `eth0 -> activated`; refusal boot one ERROR, no retry. Evidence `/workspace/tmp/rocknix-session/175-rc4-evidence/`. #175/#176 stay OPEN with their siblings for the device round.
- **H700 RC built and verified** (05:07:51–05:10:48 UTC): `/workspace/artifacts/rocknix-images/h700-all-20260914-c5c50a2d5f/` — tar `ROCKNIX-H700.aarch64-20260914.tar` sha256 `766c56f206b669b8eccb6dfd66f849929dd25183ae2c956c89eb98764c849fa6`, DDR3/DDR4 img.gz + sha, `SHA256SUMS.tar`, `BUILD_INFO.txt`; the tar's SYSTEM squashfs checked (BUILD_ID, retry string, proxy ctl+unit, redact_credentials). `stage-h700.sh <host> [dir]` (HOSTOPT for `-o Hostname=…`) does the idle check, uploads `.part`, verifies the sha on the device, moves into `/storage/.update`; the reboot is asked by name.
- **Upgrade path proven on guest d** (05:15-05:18 UTC): kept `x64-all-20260912-ec12767b26` image → RC-4 x64 tar through `/storage/.update`; hostname, hardcore, RetroArch driver, ROM + six states intact, queue emptied, ES up, no parse error, no crash, proxy `enabled=0`. Script `/workspace/tmp/rocknix-session/upgrade-d.sh <old img.gz> <new tar> <id>` (candidate fork tool; the rule's `vm-visual-qa` upgrade claim is stale). Guest d now on `c5c50a2d5f` via the upgrade (hostname reset to default).
- **RC-5 `b3189ba85f` VM-COMPLETE** (image kept at `/workspace/artifacts/rocknix-images/x64-all-20260914-b3189ba85f/`): nine suites PASS (`qa-b3189ba85f-webdav-a-20260914-0821`); fully-offline start 0 FAIL; #179 scan from the page + Böbl offline after the scan 0 FAIL; #180 summary + game pages offline (frames; via the summary's A-on-a-game route -- the game-options entry gate is #183); #181 640x480 crisp; #174 switch -> 1 and active after reboot. QA row + comments + ticks done. **H700 RC-5 built and verified** (`/workspace/artifacts/rocknix-images/h700-all-20260914-b3189ba85f/`, tar sha256 `48e934a7d35f78eaaf2b03e9d0027dd1f9bb60398b76ed5f995e9c8fb595092f`; the tar's SYSTEM checked: BUILD_ID, four strings, ctl verbs, aarch64 rchash lib). NEXT, the RG SP (offline, last seen ~06:00 UTC): ask permission to transfer again (D-QA-011; the earlier yes was for RC-4), `stage-h700.sh rgsp <dir>` with HOSTOPT, the reboot as a question via `tools/device-act`; then the device round (the maintainer's two offline tests, #174 battery restart, #181 look, #157/#160 looks, #45/#50/#82/#113/#121, H700 boxes, PL-04).
- **Causes found today:** #181 = shared SVG texture clamped to the first consumer's size (game-list header) and GPU-upscaled in the system view; victims = navigation footprint (quick-select >2 systems away / PreloadUI); theme unchanged since March. #174 = the TAILSCALE switch writes `IsTailscaleUp()`'s answer (`!loggedOut`) to `tailscale.up`, so an enable that needed a sign-in persisted 0 and the boot starter stopped the daemon (D-NET-010); boot path proven fine on guest e (pre-fix image). #182 filed: gated rows never render dimmed.
- **Guests:** a/b pair on c5c50a2d5f (b may carry the mixed image 6b77c2914a+ES 20824cc29 from the #181 stream); d on c5c50a2d5f clean (the #180 agent may use it for reads/captures); e (`:10027`, `-e.sock`) on ec12767b26, scratch.
- **RG SP is on RC-5 `b3189ba85f`** (staged 13:09:06 UTC under the maintainer's transfer permission; rebooted 13:09:09 via `tools/device-act` under "reboot once you're ready and the transfer is complete"; back 13:13:14 on boot `a96c23bb…`; runbook checks pass incl. ES sha = tar's, the four strings, ctl verbs, rchash lib; proxy toggle off as the player left it; Tailscale reconnected on its own). **Device round on RC-5 next, item by item, each a yes:** the maintainer's two offline tests (turn OFFLINE ACHIEVEMENTS on, SCAN, then Wi-Fi off before the game / drop mid-game), #174 battery restart with the switch on, #181 look after quick-select jumps, #157/#160 looks, #45/#50/#82/#113/#121, #66-#69, PL-04. Then SM8550 for the Nova.
- **#178 filed** (ES `AsyncLogger` flushes every 8 batches; WARNING lines only in the file) — D-WORKFLOW-022: not a gate for this RC. Harness lessons in `generic-x64-vm-testing.md` (ES log at `/var/log/es_log.txt` and its lag; `/run/rocknix/resolv.conf` is a symlink into resolved's stub, the override is `/storage/.config/resolv.conf`; plain `reboot` over serial with `--timeout 8`, never backgrounded; mask credential values, do not drop lines — the refusal sentence contains "password").
- Guests: a/b on `c5c50a2d5f` (pair), d on `c5c50a2d5f` clean (account cleared, switch 0, override gone). Session scripts in `/workspace/tmp/rocknix-session/` (`build-x64.sh`, `rebuild-d.sh`, `prove-175-retry.sh`, `stage-h700.sh`, `current-x64-id`=`c5c50a2d5f`).
- Docs committed on `next`: `e6f9999324` (QA row RC-4, rule, D-WORKFLOW-022, work log 05:12 UTC).

## Next Steps

1. Device round on the RG SP (RC-4 applied and verified): #45, #50 LAN, #82, #113 Dropbox, #121, #66-#69, PL-04, #157/#160 looks, #167 phase 4 with a hotspot, #175 against real late DNS) → close #175/#176/#157/#160 and the rest per the round.
2. SM8550 build for the Pocket Nova from the same `c5c50a2d5f` after the RG SP round (D-WORKFLOW-020); RG35XX SP and Nova stay off the tailnet until then.
3. Maintainer's calls still open: #151 Phase 7 close; #47 look; D-RA-006 (a reset per `ra-offline` run vs a second account); #173 item (e); #161 journal on a device; #177 ES setrootpass logging; #178 (whether to fix before the RC — D-WORKFLOW-022 says after).
4. Milestone 4 phases 4-5 (#167, #168); then #159 Wi-Fi memory, #158 captive portal; #11 conflict resolution after (D-WORKFLOW-018/019).

## Standing rules that bind this work

- Nothing runs on a person's device or cloud without a per-action yes; QA guests a/b/d may be rebooted freely; guest/device reads filtered `grep -v -i -E 'key|pass|token|user|psk'`.
- Credentials live only in `~/.ROCKNIX/qa-accounts` (0600) and reach guests only via `tools/qa-accounts` (prints names only); ScreenScraper developer pair is the public `jelos`/`jelos`; never print values or ask for them in chat.
- Every out-of-band request becomes a fork issue quoting the maintainer (D-QA-012); issues only on `maxengel/rocknix`.
- Never `cd` into the primary checkout (`git -C`); commits `-c user.name="Max Engel" -c user.email="max@awecelot.com"` ending with the Co-Authored-By and Claude-Session lines; docs on `next` in the primary; register append-only (clerical re-key exception D-WORKFLOW-013).
- x64 build: both Docker mounts (`.git` and `/workspace/cache/rocknix-sources`) and `rm -f build.ROCKNIX-GENERIC_X64.x86_64/.stamps/image/build_target` first; H700 via `devices/build-dev.sh H700` after `tools/fork-worktree sync` (never during a build). Long runs `setsid nohup ... & disown`, polled from the foreground; watchers anchor on the interpreter, never the command's literal. One image conversion at a time on `/tmp` (31 GB tmpfs); guest d's disk lives in `/workspace/tmp` because `vm-pair up` wipes `vm-*` in its own dir.
- Subagent briefs say "wait with synchronous sleep/poll loops, return only when done"; **audit/review subagents run on Fable 5.1 (`model: "fable"`), never Opus**; adversarial or second-opinion work goes through the council Facilitator (`tools/council/run invoke`), never a direct provider call.
- Fork-only tools go in `PERSONAL_PATTERNS` (`.githooks/pre-push`) and `fork-workflow.md`'s list (`register-check`, `qa-accounts` added today).

## Key Files Touched Today

- `projects/ROCKNIX/packages/rocknix/sources/scripts/backuptool` (prune + `-X` restore skip); `packages/network/rclone/sources/{cloud_content_restore,cloud_setup,cloud_content_backup}` (absent_not_broken, mkdir-first); `packages/sysutils/systemd/scripts/network-base-setup` + `system.d/network-base.service`; `packages/network/avahi/{package.mk,system.d/avahi-daemon.service}`; `projects/ROCKNIX/packages/ui/emulationstation/package.mk` (pin `3466b36af7`).
- `tools/{last-good-scripts-test,vm-qa,cloud-round-trip,register-check,qa-accounts}`, `.githooks/pre-push`.
- ES: `es-app/src/guis/GuiSaveState.{cpp,h}`, `es-core/src/components/GridTileComponent.{cpp,h}`, `es-app/src/guis/GuiMenu.cpp`, `es-app/src/scrapers/ScreenScraper.cpp`, `locale/lang/fr/LC_MESSAGES/emulationstation2.po`, `CLAUDE.md`, `.githooks/pre-push`.
- Docs: decision/blindspot registers, `docs/vm-qa-log.md`, `docs/qa-frames/2026-09-13/`, `docs/work-logs/2026_09-work_logs/2026_09_13-work_log.md`; rules `es-code-traps.md`, `generic-x64-vm-testing.md`, `es-player-text.md`, `decision-register.md`, `fork-workflow.md`.
