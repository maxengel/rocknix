# Saved Session State

> **Saved**: 2026-09-08T01:15:00Z (host UTC; the earlier stamp was device-local)
> **Branch**: feature/conflict-resolution (worktree; the work itself landed on `next`)
> **Repo**: maxengel/rocknix (+ ES at ~/Development/emulationstation-next, branch feature/cloud-vocabulary → test/qa-integration)

## Current Focus

#11 batch 1: Gates 0 (#35), 11 (#9, D-CLOUD-052) and **12 round one** done. The census ran on the RG35XX SP (`tools/cloud-census`, read-only; D-QA-010 splits it by hardware — standalone layouts wait for the Retroid Pocket Nova): evidence on #22 (full) and #21 (unit table), the report at `/workspace/artifacts/rocknix-images/census-rg35xxsp-round1-report.md`. Every reconciler case appeared in the wild (renumber keeps mtime → `--recent` never uploads it; a path overwritten between pushes; deleted states living on in the cloud, due back at the next boot pair; a content duplicate). D-CLOUD-053 (the save-state manager is a gated writer; a menu deletion writes `retired`) on #22 R6/A6b and #21 R3. #79 is the running crash list (a RetroArch coredump at 15:13:50, not ours). **Next: #21 capture** (libretro units now; standalone rows after round two) — the first code of the epic.

Side tranches, all on `next` (`5f00ab7c26`): the match-flow notes (VM-framed twice, nits fixed, ES `a245e81b26`); #78's refusal dialog (framed, line breaks fixed); **#80 RetroAchievements keys** — three ES switches never reached RetroArch (upstream bug; fixed in `setsettings.sh` + two standalone scripts + the seeded `system.cfg`, proven on the VM; `pr/retroachievements-keys` `841a046fb4` built by content against `upstream/next` `1940c5cee8`, **not yet opened** — waits for the maintainer's word). **H700 `647bab04a7` is on both handhelds** (22:09/22:12; transfers and reboots approved in advance for this round). It carries everything: the match-flow notes, #78, #80 (three RetroAchievements switches), #81 (PROGRESS TRACKER switch). Upstream PRs open: ROCKNIX/distribution#3279 (#80 + #81's mapping), ROCKNIX/emulationstation-next#33 (#81's row, via the new fork `maxengel/emulationstation-next-rocknix`). The boot pair on the RG35XX SP had brought the census's deleted states back (A9 observed live; on #22). #81 closed (the ring counter stayed away). **The RetroArch crash is root-caused (#79, crash 3 with a core dump):** RetroArch 1.22.2's threaded video wrapper has a single-slot command mailbox that wakes any waiter of the same command type, so the achievement badge upload (main thread) and the screenshot task (task thread) posting at the same instant hand the video thread a pointer into a returned stack frame — `video_texture_load_wrap_gl3_mipmap` segfaults. Artefacts under `/workspace/artifacts/rocknix-images/crash-2026-09-08-sonic3-core/`. Options offered: upstream report to libretro/RetroArch; `video_threaded=false` or AUTOMATIC SCREENSHOT off as device workarounds. `core_pattern` on the RG35XX SP still points at `/storage/.cache/cores` until reboot. **#82 (image viewer re-read after a game exit / restore)** coded (ES `3eb511be8b`, pinned `5b8e6b45a3` on `next`), GENERIC_X64 booted and stable through two launches; the SCREENSHOTS list is not shown by the VM's theme, so the visible proof is the maintainer's on the device. **H700 `5b8e6b45a3` built** (`/workspace/artifacts/rocknix-images/h700-imageviewer-rescan-20260908-5b8e6b45a3`), **not deployed — asked**. #83 filed: the RG SP's internal card holds a second saves tree (audit, then clean up, on the maintainer's word). D-CLOUD-054 (open): the saves root's filesystem identity in the sync context. Earlier today: **H700 `63d7e7e0a2` went onto both handhelds** (23:54/23:57Z, transfer and reboot authorised in advance by the maintainer for this build). The maintainer's earlier PROGRESS TRACKER off went to `es_settings.cfg` and is lost; they must flip it again on the new row. **On-device boxes for #80/#81 wait for the maintainer's testing** (#80: CHALLENGE INDICATORS on, launch a game, I read `/tmp/.retroarch.cfg`; #81: after the next deploy, PROGRESS TRACKER off, play Sonic). AUTOMATIC SCREENSHOT was already off before the upgrades (read at 15:20); the maintainer turned it on at 19:45 and it persisted. Open question to the maintainer: what the bottom-right achievement widget looked like (likely RetroArch's progress tracker, not the challenge indicator). #79 is the crash list.

**2026-09-08 01:15Z — the maintainer's three items are done; a build is running.** (1) RetroArch: `1c4fa3b308` on `next` (the 1.22.2 patch); upstream report libretro/RetroArch#19517 and fix PR #19518 from `maxengel/RetroArch` (master variant: poster slot with owner/depth, pumps the cocoa trampoline; compile-tested release/DEBUG/c89, no runtime yet). AUTOMATIC SCREENSHOT can stay on once the build is on the devices. (2) #82 built earlier (`5b8e6b45a3`), rides this build. (3) #83: cleanup done (101 files), prevention `cloud_saves_root` + refusal in both saves phases + `cloud_setup --accept-saves-root` (`89f1cb15b7`), proven on GENERIC_X64 guest a (MinIO on :9010; guest a's remote re-pointed with `CLOUD_QA_BACKEND=s3 tools/cloud-test-backend rclone-conf`); D-CLOUD-054 decided; #83 box 4 ticked, box 3 (ctime after an RG SP reboot with both cards) waits for an authorised reboot. Docs `8a28349e0f`. **Then the maintainer asked how two cards that were never removed made two trees: a boot race** (udev-started automount unmounts `/storage/roms` and rebinds a second later while the interface and its boot restore start; RG SP journal 23:48:57–58). Guard refined `9618b63d7c` (D-CLOUD-055: `check --wait 15`, `record IDENTITY`; VM T1–T4 on #83), ordering defect filed #84, docs `08fe6c76dc`. **H700 `9618b63d7c` built** (05:22–05:24Z; image verified: wait + post-check + patched retroarch `71dcab62…`), `/workspace/artifacts/rocknix-images/h700-guard-wait-20260908-9618b63d7c/`, sha256 `eec0418745d175b9…`. **Staged and rebooted on both handhelds 05:32Z with the maintainer's authorisation for this build** ("you're all clear to deploy it to the RG SP and RG35XX SP, and then reboot"); **Both back on `9618b63d7c` at 05:38Z**: boot pairs SUCCESS on both, records written (RG SP `uuid:81083868-…` external card, RG35XX SP `uuid:bc8d18b8-…`), RG SP internal tree untouched (0 ctimes since boot) → **#83 closed completed**; ordering defect and content-tier guard on **#84**. Crash test (#79) is the maintainer's: an unlock with AUTOMATIC SCREENSHOT on. **Then #84 (root cause of #83): the wrong-card writes were a boot card-enumeration race, not unit ordering** — my first #84 diagnosis was wrong (the wall-clock journal jumps when NTP corrects the clock mid-boot; in monotonic time automount finished ~10 s before the interface on both handhelds). Fix `accaadc3c0` (on `next` via cherry-pick, next HEAD `53a6bdfd51`): `automount`'s `find_games` retries up to 15 s for an expected external card (`system.merged.device=external` or a pinned `system.gamesdevice`) before binding internal; a one-card boot is unchanged. **Boot cost (maintainer asked): a normal boot waits 0 s — the wait engages only when a card node is present but its fs is not readable yet (`external_node_present` over /sys/block); a removed card falls to internal after a 2 s grace; ceiling 10 s.** D-CLOUD-056/057, blindspot 32, memory `order-boot-events-in-monotonic-time`. The scenario harness found a real `$#`-expansion bug in the detector's sed (fixed). VM-tested against the real functions (detector + seven decision scenarios). RG SP `system.merged.device=external` confirmed (fix engages there); RG35XX SP `internal` (no wait). **H700 `88e5190d65` built and verified in the image** (`/workspace/artifacts/rocknix-images/h700-84-20260908-88e5190d65/ROCKNIX-H700.aarch64-20260908.tar`, sha256 `f2d917910df6ba5c…`; FIND_MAX=10, FIND_GRACE=2, external_node_present, single-quoted sed, cloud_saves_root, retroarch `71dcab62…`). **Deployed to both handhelds and rebooted 16:10/16:12Z (authorised); both on `88e5190d65`.** RG SP verified: external card bound, boot restore+backup SUCCESS into it, internal tree 0 files. **#84 closed completed.** Upstream PR of the automount fix to ROCKNIX/distribution is a follow-up. feature/automount-card-wait worktree removed (cherry-picked to next). **#21 (capture) started** — worktree `feature/capture`, on `next`. **Schema rev 2 written** (`docs/save-manifest-schema.md` §10, `a96b29fd06`, additive: per-entry `unit`/`producer`/`published_at`/`pub`, top-level `units`+bounded `retired`, agreement `verified_by`; schema integer stays 1; noted on #20; SQLite index stays not-built D-CLOUD-027). **Core-pins done** (`27eb3264a2` on `feature/capture`): `virtual/emulators` `makeinstall_target` emits `/usr/share/rocknix/core-pins` (`<package> <PKG_VERSION>` per libretro core, from `LIBRETRO_CORES`×`get_pkg_version`; held on the branch until the producer reads it; needs image-build verification). **#21 remaining**: (a) the capture producer script — every exit, no rclone, no lock, seal changed members into `/storage/.cache/cloud_sync/stage/` (independent copy, not hard-link), hash, write manifest to `/storage/.cache/cloud_sync/manifest-<id>.json`, core name→package map + exception table, `core_display_version` from `*.info`, `unit`/`units`/`retired`; (b) ES: invoke capture on **every** exit with `--system --rom --emulator --core` frozen at command construction (not `getCore(true)`/`getEmulator(true)` re-resolve), around `FileData.cpp:836`; (c) round-trip fixtures + the 8 acceptance checks (toggle-off/no-net/lock-held capture; command-line core; independent-copy seal; unchanged-exit no mtime churn; core_build pin; A14 manifest under savestates/.rocknix; N64 unit; rev 2 doc). Gate 12 round one = libretro units only (standalone waits for the Nova). **Next: #21 capture** (the epic's first code). Earlier: **H700 `89f1cb15b7` built** (04:43–04:45Z, ccache; retroarch and rclone stamps refreshed, `video_thread_wrapper.o` recompiled, retroarch binary sha `71dcab62…` vs `5fc4a715…` in the previous image; `cloud_saves_root` present in SYSTEM): `/workspace/artifacts/rocknix-images/h700-ra-guard-20260908-89f1cb15b7/ROCKNIX-H700.aarch64-20260908.tar`, sha256 `0a672f0ffa296b1b…`. **Nothing staged on either handheld; ask before staging (D-QA-011) and before each reboot (D-QA-008).** Then: #21 capture.

**2026-09-08 ~16:40Z — parallel tranche while #21 design runs (workflow wmkb37am8):** the maintainer's live look at the RG SP produced four transfer-flow items → **#85** (picker shows the upload delta, not total + IN YOUR CLOUD; progress bar lost on check-only runs — root cause: the page reads % only from the byte `Transferred:` line and ignores the count/`Checks:` lines; page padding; archive names carry the device label, with per-device retention and restore preference — closes #49). Implementation + adversarial review running as workflow **w3ble7lr1** in worktrees `rocknix.worktrees/transfer-ux` (feature/transfer-ux) and `emulationstation-next.worktrees/transfer-ux` (feature/transfer-ux from test/qa-integration 3eb511be8b). **#86**: both handhelds share the device-id hash `ee5013fc56` (cloned id; investigation in the same workflow). **D-QA-012** (maintainer: every out-of-band request gets an issue the same session, verbatim): rule in `issue-tracking.md` `c0ae681fb7`, memory `file-out-of-band-requests-as-issues`; back-filed **#87** (race concern → D-CLOUD-053, lands in #22/#21) and **#88** (Gate 12 round two on the Nova). ES worktree for #21's hook: `emulationstation-next.worktrees/capture` (feature/capture-hook). **#21 design done** (workflow wmkb37am8, 11 agents: 4 readers, 3 designs, 3 judges, synthesis; test-first won 22/21/20; spec `scratchpad/capture-final-spec.json`, decisions posted on #21). Overrides: allowlist not widened (**#89** filed: standalone N64 saves never synced); boot `--full` gated + background. **Implementation running: workflow wujtkor1m** (producer `cloud_capture` + package/autostart/core-pins ext; ES hook in `emulationstation-next.worktrees/capture` feature/capture-hook; CAP1–CAP10 + A8/A14 in tools/cloud-round-trip; runemu.sh cloud.backup hook removal; docs drafts) → run on guest a → adversarial review → fix. Next after both workflows: review diffs, commit (rocknix `feature/capture` + `feature/transfer-ux`; ES `feature/capture-hook` + `feature/transfer-ux` → test/qa-integration), bump ES pin, GENERIC_X64 build + `tools/vm-walks` screendumps (picker + transfer page) + capture VM procedures, H700 build, ask before deploy.

**2026-09-08 ~19:20Z status.** #85 items 1–3 done and reviewed: ES `ef14ec40a` (picker rows) + `396ff1bac` (transfer page bar/CHECKING/padding, D-UI-025) merged into test/qa-integration `dcf7135463`, pushed to origin (maxengel/emulationstation-next, the URL the build fetches); scan change `0fff618bef` and ES pin bump `83f3a2fc52` on `next`. **GENERIC_X64 build of `83f3a2fc52` running** (`scratchpad/build-x64.sh`, log `build-x64-85-83f3a2fc52.log`); then boot in the VM and screendump the picker (plant a local-only ROM for a delta) and a check-only content backup (bar from Checks + CHECKING line) via `tools/vm-walks`. A first GENERIC_X64 build was started from the wrong tree (a `git cherry-pick -q` typo broke the chain) and killed; poison sweep found nothing. **#85 item 4 (archive names)**: implementer done; its reviewer died on an API timeout — findings recovered from the transcript (old finder glob fails on labelled names; same-label retention) → D1 name `<stamp>-<label>-ROCKNIX_SETTINGS.tar.gz`, D2 retention stated exactly; fix→review workflow **w6kdqglia running** in `rocknix.worktrees/transfer-ux` (also edited cloud_device_id, conf comments, harness, changelog, register — commit after it ends; rebase branch onto next, ff-merge). **#86 root cause found** (investigation): fixed seed — `sit0` sorts before `wlan0`, `ethtool -P sit0`→`notset` passes the denylist, md5(`notset|unknown`)[0:10]=`ee5013fc56` on every wlan-only device with CONFIG_IPV6_SIT=y (H700/RK3566/RK3576); DEVICE unset on-device so family is always `unknown`; proposed fix: validate MAC (regex + type==1 + /device link), heal the poisoned stored id keeping the label, fix the comment, correct A11; posted on #86. **Implement #86 after w6kdqglia ends** (it touches cloud_device_id). **#21 implementation workflow wujtkor1m still running** (1 agent left). Memory `recover-timed-out-agent-from-transcript`.

**2026-09-08 ~19:25Z — #21 initial build landed.** Workflow wujtkor1m (9 agents, 0 errors): producer `cloud_capture` (1,247 lines), boot `--full` (gated+detached), core-pins ext, runemu.sh hook removal, CAP1–CAP11 (140 PASS/0 FAIL/2 SKIP on guest a), 22 review findings (5 serious) applied. Landed on `next` `7a6f9ccfae` (7 commits after rebase; append-only doc conflicts resolved as union — register now D-CLOUD-058..066 from capture; **the transfer-ux worktree's uncommitted D-CLOUD-058/D-UI-025 rows must be renumbered before commit**). ES `feature/capture-hook` → test/qa-integration `33a398083a` (pinned on `next`). #21 comment + 5 boxes ticked; remaining: (1)'s device-side confirmation, (5) shipped pins, (6) #22. **GENERIC_X64 build of `7a6f9ccfae` running** (`build-x64-21.out`; artifacts of the #85-only image kept at `x64-85-20260908-83f3a2fc52/`). Two wrong-tree x64 builds were killed (a `cherry-pick -q` typo; a rebase that stopped on conflicts my grep missed — check `rebase-merge` exists, not the last lines). **Visual QA of #85 running on guest b** (workflow wmu4ir0v7: in-place upgrade with the x64-85 tar, two walks, screendumps → `scratchpad/shots-85/`, two judges). **Archive-names fix workflow w6kdqglia still running on guest a** (do not reboot a). After it: commit transfer-ux (renumber register rows), rebase onto next, merge; then #86 fix (cloud_device_id: MAC regex + type==1 + /device, heal poisoned id, comment; A11). Then H700 build of everything; ask before deploy.

**2026-09-08 ~19:45Z.** #85 item 4 landed: archive fix workflow w6kdqglia (fix→review, 3 minors recorded as follow-ups on #85: backuptool restore applies the newest LOCAL archive, not the one cloud_restore chose; oracle laxer than regex; D2 example wording). transfer-ux committed (`623d0ab02c` scripts+conf, `a9322f6a60` harness, `89ec2ada87` docs; register row renumbered D-CLOUD-058→**067**, D-UI-025), rebased onto next (register conflict → union) and ff-merged: **next = `208c6f9273`** (all of #85, #21, #84…). GENERIC_X64 capture image built and secured: `x64-21-20260908-7a6f9ccfae/` (127 core pins incl. mgba-lr e31759…, cloud_capture, ES calls it, runemu hook gone). **#86 fix workflow launched** in `rocknix.worktrees/device-id-seed` (feature/device-id-seed from 208c6f9273): MAC regex + type==1 + /device; heal poisoned id (suffix == md5(notset|unknown)[0:10] = ee5013fc56) keeping the label, `.previous` file, `--previous` mode; cloud_restore chain own → legacy → previous → root; A15 fixture; docs; uses guest a. **Visual QA #85 on guest b still running** (wmu4ir0v7). Then: capture ES-side VM procedure on guest a (upgrade to x64-21 tar; launch a ROM with toggle off → last-capture stamp + log line; delete a planted state via the manager → retired row); H700 build of next; ask before deploy.

**2026-09-08 ~20:10Z.** #85 items 1–3 **seen on the VM** (workflow wmu4ir0v7: guest b upgraded in place to x64-85 `83f3a2fc52`; frames under `scratchpad/shots-85/`; two judges pass-with-nits; I viewed the four key frames). Boxes 1–4 ticked on #85 (640×480 remains the device check). **Polish workflow w3rkz039f running** (ES transfer-ux worktree: honest size formatter in the picker; done page carries a whole-run summary — N FILES · size BACKED UP / NOTHING NEW TO SEND…; render snapshots percent before drawing; single ` · ` separators; rocknix transfer-ux worktree: `tools/vm-walks/run-transfer.steps` needs two `right`s for the three-button bar, README order wake→reset). After it: commit ES on feature/transfer-ux → merge test/qa-integration → push → bump pin on next → x64 build → re-screendump done page + picker on guest b → H700. **#86 workflow wlwdl5iq4 still running on guest a.** Upstream `pr/automount-card-wait` (`aaa4797d6b`, 67-char title) pushed to the fork, **PR not opened — ask the maintainer**. Guest b holds QA scratch: /storage/roms/{snes/qa-one.sfc, gba/qa-two.gba, nes/qa-nes-001..300.nes, pico-8/sdl_controllers.txt}; CONTENT_REMOTE=/rocknix-qa/CONTENT.

**2026-09-08 ~20:50Z.** First build chain of 50fe7fb9c8 FAILED in ES's i18n step: xgettext "Non-ASCII comment" (eight comment lines with `·`/dashes in GuiCloudTransfer.cpp from the polish; syntax-only had passed). Fixed ES `b44a715a9` → test/qa-integration `dbb93c567f`, pin bump → **next `748ffd41b8`** (+ rule `330a0ae261` es-native-ui: comments near _( ) must be ASCII; memory `es-comments-ascii-xgettext`). **Build chain restarted from 748ffd41b8** (`build-chain.sh`; artifacts `x64-all-20260908-748ffd41b8/`, `h700-all-20260908-748ffd41b8/`; waiter b9dg3l0cd). VM final-checks workflow relaunched against the new artifact (guest b: sizes + done page; guest a: capture via the real exit path, shipped pins, manager delete → retired row). Then verify the H700 image and **ask before deploying**.

## Completed This Session

- #76 scripts: `--with-media`, `MEDIA_DIRS`/`MEDIA_EXCLUDES` on both `rclone copy` calls, `cloud_content_restore --scan` (union of both sides, six fields, awk `not_in` instead of `comm`). Commits on `next`: `b8773f2224`, `93621cfdbc`, the excludes fix, and ES bumps `744a19e3af`/later to ES `5dd7f7f04`.
- #76 interface (ES `6ce655b32`, `5dd7f7f04`): direction-aware picker, SCRAPED GAME CONTENT switch (`cloudsync.content.media`), entry rows on both transfer pages, hub row removed, `cloudMediaFlag()` on both transfer commands.
- VM run (four GENERIC_X64 images) found and fixed: busybox has no `comm`; `MEDIA_EXCLUDES` unused (blindspot 30); three-line switch row. Criteria 2–6 ticked on #76 from screendumps and the backend's directory.
- Docs on `next` (`d07fe2951a`): changelog "Scraped game content is a switch", blindspot 30, rclone rule paragraph (content flags; what busybox lacks), work log 06:56.
- H700 image `2b2a8d385f` kept at `/workspace/artifacts/rocknix-images/h700-content76-20260906-2b2a8d385f…/` (tar sha256 `7d56feb4…`); staged in the RG35XX SP's `~/.update` after on-device checksum. RG SP: nothing staged — it was running Mario Tennis.

## In Progress

- Task 6 done (see Current Focus). WebDAV: 48 single-device PASS, 43 two-device FAIL by design (37 fixtures + 6 contract); MinIO: 39. The run files of this session are gone with the scratchpad; each fixture's commit message carries its output.
- The VM pair is up (`tools/vm-pair info`; it vanishes with `/tmp` on a host reboot — `vm-pair up <img>`, then put `backuptool` and `cloud_setup` from `next` into `/tmp/qa-bin` on both guests); the backend is back on WebDAV :9010.
- A GENERIC_X64 build of `next` (`5330da7ee5`) is owed: ES `164f8f48d8` (the folder refusal dialog, #78's open box), `backuptool`, `cloud_setup`, the `--scan` tidy-up. VM first (D-QA-007) with a frame of CHANGE CLOUD FOLDER refusing a bad bucket name on MinIO; then H700, staged, reboots asked for (D-QA-008).

## Next Steps (the batch-1 task list, in execution order)

**Gate 0 — #35, the harness (VM, `feature/round-trip-harness` from `next`)**
1. Worktree + session script: boot `088c2bd22e` headless, the QA key over serial, WebDAV up; the harness's own fixtures, not `seed-*`.
2. First checks (futro §2): MinIO reachable from the guest under `CLOUD_QA_BACKEND=s3` (`rclone lsd` from the guest); the allowlist under the scratch root (`savestates/x.state`, `snes/game.srm` pass; `snes/game.srm.json` does not).
3. Baseline run of the harness as it is, on WebDAV: the `PASSED`/`N CHECK(S) FAILED` line and every failing check recorded on #35 — the failures are the repair list.
4. Repairs, each observed: `rclone.conf` restored after a run; old keys read on an old-shaped conf; archive assertions vs `cloud_backup`/`cloud_restore`'s dated names; content fixture under `CONTENT_REMOTE` with an ES-declared system, BIOS travelling; saves steps `--saves-only`, settings step as the tier runs it; D-UI-022 step names; the lock step seen to exit 3; PL-10's unsupported-system branch seen; exit-path steps asserting `--max-age`/`--no-traverse`.
5. Full runs on WebDAV and on MinIO; the final line and listings quoted on #35; Gate 0 boxes ticked on those.
6. ~~Fixtures, each a failing commit first~~ (done 2026-09-07): A1 (two roots against one remote, both changed **after a first pass**), the equal-size case, A2/A3, A8 (torn N64 pair), A9 (delete/renumber/absence/unmount/`rm`), A11 (cloned device id), A12 (legacy `RESTOREPATH`), A14 (manifest step, WebDAV null / MinIO non-null), A5's reader from the second root, the retention ordering kill, #9's contract fixtures runnable with bisync and with `copy --files-from`.
7. ~~Land on `next`; `docs/vm-qa-log.md` row; a micro-retro comment on #35.~~ (done)

**Gate 11 — #9, the bisync spike** — ~~8, 9, 10~~ done 2026-09-07: runner with `--dump`/`moved:`/shape/every-spawn budget; variants `--compare size,modtime,checksum [--download-hash]`; verdict D-CLOUD-052; #9 closed.

**Gate 12 — the census (RG35XX SP, the maintainer's hands)** — round one done 2026-09-07 (D-QA-010)
11. ~~Ask for a window;~~ the reading script (`find -newer` over the saves tree; PPSSPP/Flycast/Mupen/DuckStation unit table; retained bytes at count 3; sealed bytes per exit) runs over SSH afterwards; feeds #21's unit table and #22's ceiling.

Carried debt: rocknix.org docs PR; #73's on-screen review; the `--scan` BIOS tidy-up rides the next build.

## Key Files Modified

| File | Change | Notes |
| --- | --- | --- |
| `projects/ROCKNIX/packages/network/rclone/sources/cloud_content_restore` | Modified | `--with-media`, `--scan`, `not_in`, excludes on the copy |
| `projects/ROCKNIX/packages/network/rclone/sources/cloud_content_backup` | Modified | `--with-media`, excludes on the copy |
| `projects/ROCKNIX/packages/ui/emulationstation/package.mk` | Modified | pinned to ES `5dd7f7f048d9…` |
| ES `es-app/src/guis/GuiMenu.cpp` | Modified | picker per direction, switch, rows, media flag |
| `docs/cloud-sync-changelog.md`, `docs/blindspot-register.md` (30), `.claude/rules/rclone-cloud-sync.md`, work log 2026_09_06 | Modified | see above |

## Related Context

- **Tracker**: #76 (journey milestone, `cloud-saves`), D-CLOUD-048/043, D-UI-023, D-QA-007/008; epic #11 handed off (Step 6) earlier this session.
- **Council run**: `research/council-runs/2026-09-05-conflict-resolution-foundation/` (complete).
- **VM**: headless recipe in `generic-x64-vm-testing.md` + CLAUDE.md; helpers in the session scratchpad (`serialsh.py`, `ppm2png.py`, step files); QEMU pidfile `qemu76.pid`; sockets `/tmp/claude-1000/{m76,s76}.sock`; backend `tools/cloud-test-backend` (WebDAV, `~/.cache/rocknix-cloud-qa/data`).

## Notes for Next Session

- Driving ES over the QEMU monitor: `up` from a page's first row lands on the BACK button (three `up`s from the top of GAME SETTINGS to MANAGE CLOUD STORAGE); START on MAIN MENU closes it; after five idle minutes the first key only wakes the screensaver (send `shift`); keys: start=ret, A=x, B=z.
- `pkill -f` with a bracketed pattern still kills the shell whose command text holds the literal elsewhere — kill by pidfile.
- Busybox on the image: no `comm`, `pgrep -c`, `find -printf`, `ls --time-style`.
- After a compaction, grep for any code-state claim in the summary before building on it (blindspot 30; memory `verify-summary-claims-by-grep`).

## Open Questions

- A window for the Gate 12 census (the maintainer's emulator sessions on the RG35XX SP).
- Close #76/#77 after the maintainer's look at the review build?
- The folder-probe defect issue (filed and closed 2026-09-07) keeps one open box for the ES dialog until a VM frame shows it.
- D-CLOUD-044 (bisync-gap posture) stays parked until the #9 spike.
